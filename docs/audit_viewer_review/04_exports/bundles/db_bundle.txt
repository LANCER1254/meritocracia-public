bundle: db
generated: 2026-04-20 12:26:22

==================================================
FILE: ./viewer/db/academy/index.html
--------------------------------------------------
<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>士官学校制度 | Meritocracia DB</title>
  <link rel="stylesheet" href="style.css">
</head>
<body>
  <main class="container">
    <p class="sub">ACADEMY PORTAL</p>
    <h1>🎓 士官学校制度</h1>
    <p class="lead">
      士官学校関連制度の概要ハブ。
      詳細Wikiと、士官学校に併設される使用人養成学校への導線をまとめる。
    </p>

    <section class="grid">
      <a class="card" href="./overview.html">
        <span class="badge">OVERVIEW</span>
        <h2>🎓 士官学校概要</h2>
        <p>理念、学年、入学条件、パンフレット的な学校案内ページ。</p>
      </a>

      <a class="card" href="../../story/academy/">
        <span class="badge">WIKI</span>
        <h2>📘 士官学校詳細Wiki</h2>
        <p>学年構造、教官、制度、事件導線などの詳細設定資料。</p>
      </a>

      <a class="card" href="./servant-academy.html">
        <span class="badge">SUB SCHOOL</span>
        <h2>🫖 使用人養成学校</h2>
        <p>Diskens〜Primas の使用人階級と育成制度を扱うページ。</p>
      </a>
    </section>
  </main>
</body>
==================================================
FILE: ./viewer/db/academy/overview.html
--------------------------------------------------
<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Magic Dashboard | Meritocracia</title>
  <style>
    :root {
      --bg: #0b1020;
      --panel: #121a30;
      --line: #263252;
      --text: #e8eefc;
      --muted: #9db0d8;
      --accent: #7aa2ff;
    }
    * { box-sizing: border-box; }
    body {
      margin: 0;
      font-family: "Noto Sans JP", system-ui, sans-serif;
      background: linear-gradient(180deg, #0b1020, #111831);
      color: var(--text);
    }
    .wrap {
      max-width: 1200px;
      margin: 0 auto;
      padding: 32px;
    }
    .hero {
      padding: 24px;
      border: 1px solid var(--line);
      border-radius: 20px;
      background: rgba(18, 26, 48, 0.9);
      margin-bottom: 24px;
    }
    .hero h1 {
      margin: 0 0 8px;
      font-size: 2rem;
    }
    .hero p {
      margin: 0;
      color: var(--muted);
      line-height: 1.7;
    }
    .grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(260px, 1fr));
      gap: 18px;
    }
    .card {
      display: block;
      text-decoration: none;
      color: inherit;
      background: var(--panel);
      border: 1px solid var(--line);
      border-radius: 18px;
      padding: 20px;
      transition: transform 0.2s ease, border-color 0.2s ease;
      min-height: 180px;
    }
    .card:hover {
      transform: translateY(-4px);
      border-color: var(--accent);
    }
    .badge {
      display: inline-block;
      font-size: 0.75rem;
      color: var(--accent);
      margin-bottom: 8px;
    }
    .card h2 {
      margin: 0 0 10px;
      font-size: 1.1rem;
    }
    .card p {
      margin: 0;
      color: var(--muted);
      line-height: 1.6;
      font-size: 0.95rem;
    }
    .memo {
      margin-top: 28px;
      padding: 20px;
      border-radius: 16px;
      background: rgba(255,255,255,0.04);
      border: 1px dashed var(--line);
      color: var(--muted);
      line-height: 1.8;
    }
    code {
      color: #c8d8ff;
    }
  </style>
</head>
<body>
  <main class="wrap">
    <section class="hero">
      <h1>📘 MAGIC / INCANTATION LINK HUB</h1>
      <p>
        詠唱・ルーン・魔法体系・儀式語を集約するダッシュボード。<br>
        M-GSL、士官学校魔導科、禁制術式DBの素材収集用ハブとして利用。
      </p>
    </section>

    <section class="grid">
      <a class="card" href="https://en.wikipedia.org/wiki/Incantation" target="_blank">
        <span class="badge">INCANTATION</span>
        <h2>📜 詠唱辞典</h2>
        <p>詠唱・呪文・言霊・韻律の基礎。M-GSLの構文演出に最適。</p>
      </a>

      <a class="card" href="https://en.wikipedia.org/wiki/Grimoire" target="_blank">
        <span class="badge">GRIMOIRE</span>
        <h2>📘 魔導書辞典</h2>
        <p>儀式、召喚、素材、禁制書。魔導書DBと禁書庫設定向け。</p>
      </a>

      <a class="card" href="https://finalfantasy.fandom.com/wiki/Magic_(term)" target="_blank">
        <span class="badge">RPG SPELLS</span>
        <h2>🔥 属性魔法辞典</h2>
        <p>Fire / Blizzard / Holy など属性設計のインスピレーション用。</p>
      </a>

      <a class="card" href="https://en.wikipedia.org/wiki/Magic_in_Dungeons_%26_Dragons" target="_blank">
        <span class="badge">SYSTEM</span>
        <h2>🜏 D&D Spell System</h2>
        <p>詠唱時間、触媒、射程、範囲、持続時間。授業カリキュラム化しやすい。</p>
      </a>

      <a class="card" href="https://en.wikipedia.org/wiki/Rune" target="_blank">
        <span class="badge">RUNES</span>
        <h2>🔷 ルーン文字</h2>
        <p>魔法陣・封印術・刻印武装・士官学校の記号魔術科向け。</p>
      </a>

      <a class="card" href="https://en.wikipedia.org/wiki/List_of_Latin_phrases" target="_blank">
        <span class="badge">CHANT SOURCE</span>
        <h2>🕯 ラテン語句辞典</h2>
        <p>詠唱文の高位呪文、教会系祝詞、禁制魔法の語感強化に。</p>
      </a>
    </section>

    <section class="memo">
      <strong>おすすめ次段階:</strong><br>
      このページを <code>docs/viewer/db/magic/index.html</code> に配置。<br>
      次に <code>magic-data.json</code> を作り、<br>
      「属性 / 儀式 / ルーン / 禁制 / M-GSL」ごとに分離すると運用しやすいです。
    </section>
  </main>
</body>
</html>

==================================================
FILE: ./viewer/db/academy/servant-academy.html
--------------------------------------------------
<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>使用人養成学校 | Meritocracia DB</title>
  <link rel="stylesheet" href="style.css">
</head>
<body>
  <main class="container">
    <h1>🫖 使用人養成学校</h1>
    <p>Diskens から Primas までを育成する家門直属教育機関。</p>
    <p>士官学校に併設され、秘書・礼法・家門運営・護衛補佐を学ぶ。</p>
  </main>
</body>
</html>

==================================================
FILE: ./viewer/db/academy/servant-academy/servant-rank.html
--------------------------------------------------
<!DOCTYPE html>
<html lang="ja">
<head>
    <meta charset="UTF-8">
    <title>統合議事録｜使用人階級制度（ドイツ語正式運用版）</title>
    <style>
        body { font-family: 'Helvetica Neue', Arial, sans-serif; line-height: 1.6; color: #333; max-width: 800px; margin: 20px auto; padding: 20px; border: 1px solid #ddd; background-color: #f9f9f9; }
        h1 { color: #2c3e50; border-bottom: 2px solid #2c3e50; padding-bottom: 10px; }
        h2 { color: #e67e22; border-left: 5px solid #e67e22; padding-left: 10px; margin-top: 30px; }
        h3 { color: #2980b9; margin-top: 20px; }
        table { width: 100%; border-collapse: collapse; margin: 20px 0; background-color: #fff; }
        th, td { border: 1px solid #ccc; padding: 12px; text-align: left; }
        th { background-color: #ecf0f1; }
        .conclusion { background-color: #fff3e0; border: 1px solid #ffe0b2; padding: 15px; border-radius: 5px; }
        .benefits { background-color: #e8f8f5; border-left: 5px solid #1abc9c; padding: 10px 20px; }
        .footer { margin-top: 40px; font-size: 0.9em; color: #7f8c8d; text-align: right; }
    </style>
</head>
<body>

    <h1>統合議事録｜使用人階級制度（ドイツ語正式運用版）</h1>
    <p><strong>作成日：</strong>2026-04-10<br>
    <strong>決定権者：</strong>Lancer</p>

    <section class="conclusion">
        <h2>本日の結論（正式ロック）</h2>
        <p>『メリトクラシア -Reboot-』における使用人養成学校および家門直属使用人制度の正式階級名称を、以下のドイツ語ベース5段階で固定する。</p>
        <ul>
            <li>アイリスの成長階段</li>
            <li>使用人学校カリキュラム</li>
            <li>家門運営能力</li>
            <li>主人評価連動</li>
            <li>士官学校編の制度美</li>
        </ul>
    </section>

    <h2>使用人階級一覧（正式版）</h2>
    <table>
        <thead>
            <tr>
                <th>階級区分</th>
                <th>正式名称</th>
                <th>カタカナ</th>
                <th>用途</th>
            </tr>
        </thead>
        <tbody>
            <tr>
                <td>見習い使用人</td>
                <td>Diskens</td>
                <td>ディスケンス</td>
                <td>使用人養成学校生 / 配属初期</td>
            </tr>
            <tr>
                <td>下級使用人</td>
                <td>Diener</td>
                <td>ディーナー</td>
                <td>基本実務担当</td>
            </tr>
            <tr>
                <td>中級使用人</td>
                <td>Fähig</td>
                <td>フェーイヒ</td>
                <td>専門技能保有 / 補佐主任</td>
            </tr>
            <tr>
                <td>上級使用人</td>
                <td>Senior</td>
                <td>ゼニオール</td>
                <td>現場統率 / 指導役</td>
            </tr>
            <tr>
                <td>筆頭使用人</td>
                <td>Primas</td>
                <td>プリマス</td>
                <td>家門筆頭 / メイド長 / 執事長</td>
            </tr>
        </tbody>
    </table>

    <h2>各階級の役割詳細</h2>
    <h3>3-1. Diskens（見習い）</h3>
    <p>アイリスの初期ランク。使用人養成学校所属。Scene45の使用人服購入回にて制服ランクとして正式採用。</p>

    <h3>3-2. Diener（下級）</h3>
    <p>正式採用後の基本ランク。配膳、案内、清掃、主人日常補佐など。「ディーナー、こちらへ」といった会話での運用を想定。</p>

    <h3>3-3. Fähig（中級）</h3>
    <p>専門技能（会議記録、秘書補佐）を持つ段階。“能力ある者”という作品思想を象徴するランク。</p>

    <h3>3-4. Senior（上級）</h3>
    <p>現場責任者・指導役。アイリスがここに到達することで読者が成長を一目で理解できるポイントとなる。</p>

    <h3>3-5. Primas（筆頭）</h3>
    <p>制度頂点。家門筆頭や学園管理主任。アイリスの終盤ランク候補。</p>

    <div class="benefits">
        <h2>物語的メリット</h2>
        <ul>
            <li>アイリスの成長を数値化せず見せられる</li>
            <li>使用人服の色変化演出が可能</li>
            <li>ジェイドの統率力評価に直結</li>
            <li>読者がランクアップに熱狂しやすい</li>
        </ul>
    </div>

    <div class="footer">
        <p>次フェーズ：本編 Scene45 反映、Viewer DB 更新予定</p>
    </div>

</body>
</html>
==================================================
FILE: ./viewer/db/academy/style.css
--------------------------------------------------
body {
  margin: 0;
  min-height: 100vh;
  color: #e8e8e8;
  font-family: sans-serif;
  line-height: 1.8;
  background:
    radial-gradient(circle at top right, rgba(25, 60, 120, 0.25), transparent 40%),
    radial-gradient(circle at bottom left, rgba(10, 40, 90, 0.35), transparent 45%),
    linear-gradient(135deg, #07111f 0%, #09182a 40%, #0b1f36 100%);
}

.container {
  max-width: 960px;
  margin: 56px auto;
  padding: 40px 24px;
}

.sub {
  font-size: 12px;
  letter-spacing: 0.2em;
  color: #8fa6c2;
  margin-bottom: 8px;
}

h1 {
  color: #d6b36a;
  margin: 0 0 16px;
}

.lead {
  color: #d7dbe4;
  max-width: 760px;
  margin-bottom: 32px;
}

.grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
  gap: 20px;
}

.card {
  display: block;
  text-decoration: none;
  color: inherit;
  background: rgba(8, 18, 36, 0.58);
  border: 1px solid rgba(212, 175, 55, 0.16);
  border-radius: 16px;
  padding: 24px;
  box-shadow: 0 10px 30px rgba(0, 0, 0, 0.22);
  transition: transform 0.2s ease, border-color 0.2s ease, box-shadow 0.2s ease;
}

.card:hover {
  transform: translateY(-3px);
  border-color: rgba(212, 175, 55, 0.32);
  box-shadow: 0 12px 34px rgba(0, 0, 0, 0.28);
}

.card h2 {
  margin: 8px 0 10px;
  color: #d6b36a;
  font-size: 1.1rem;
}

.card p {
  margin: 0;
  color: #d7dbe4;
}

.badge {
  display: inline-block;
  font-size: 11px;
  letter-spacing: 0.12em;
  color: #9fb4ce;
}

==================================================
FILE: ./viewer/db/city/_local_staging_backup/academy_interactive_v1/index.html
--------------------------------------------------
<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>士官学校詳細図 | Meritocracia Academy Detail</title>
  <style>
    body {
      margin: 0;
      background: #08101c;
      color: #e6d2a2;
      font-family: "Noto Sans JP", sans-serif;
    }
    .topbar {
      padding: 14px 20px;
      border-bottom: 1px solid rgba(230,210,162,0.25);
      letter-spacing: 0.08em;
    }
    .wrap {
      position: relative;
      max-width: 1400px;
      margin: 0 auto;
    }
    .map {
      width: 100%;
      display: block;
    }
    .poi {
      position: absolute;
      width: 18px;
      height: 18px;
      border-radius: 50%;
      background: #d7b56d;
      border: 2px solid #08101c;
      cursor: pointer;
      box-shadow: 0 0 0 2px rgba(215,181,109,0.25);
      transform: translate(-50%, -50%);
    }
    .panel {
      position: fixed;
      top: 70px;
      right: 20px;
      width: 280px;
      background: rgba(8,16,28,0.92);
      border: 1px solid rgba(230,210,162,0.25);
      border-radius: 12px;
      padding: 18px;
    }
    .panel h2 { margin: 0 0 10px; font-size: 20px; }
    .panel p { margin: 0; line-height: 1.7; color: #d4c3a0; }
  </style>
</head>
<body>
  <div class="topbar">🎓 士官学校詳細図 / Academy Detail Interactive v1</div>
  <div class="wrap">
    <img class="map" src="../academy-map.png" alt="士官学校詳細図" />

    <div class="poi" style="left:45%;top:18%" onclick="showInfo('本校舎','ジェイドたちが講義・事件・対立を経験する中心施設。士官学校編のメイン舞台。')"></div>
    <div class="poi" style="left:34%;top:33%" onclick="showInfo('学生寮','キール、フィーネ、フランなど日常導線の起点。夜会話や密談シーン向き。')"></div>
    <div class="poi" style="left:73%;top:39%" onclick="showInfo('士官学校正門','登下校・襲撃・来訪イベントの主要導線。外部勢力侵入にも使える。')"></div>
    <div class="poi" style="left:47%;top:46%" onclick="showInfo('中央広場','入学式、公開試験、群衆イベント、演説に最適な舞台。')"></div>
    <div class="poi" style="left:41%;top:73%" onclick="showInfo('訓練校','実技試験・模擬戦・成長イベント専用。バトル導線の主戦場。')"></div>
    <div class="poi" style="left:61%;top:77%" onclick="showInfo('地下礼拝路入口','ZERO編や教会旧市街との地下導線接続ポイント。裏ルート向け。')"></div>
  </div>

  <aside class="panel" id="panel">
    <h2>士官学校詳細図</h2>
    <p>POIをクリックすると施設情報を表示します。次フェーズで academy → 本編POI → ZERO replay に接続可能。</p>
  </aside>

  <script>
    function showInfo(title, desc) {
      document.getElementById('panel').innerHTML = `<h2>${title}</h2><p>${desc}</p>`;
    }
  </script>
</body>
</html>
==================================================
FILE: ./viewer/db/city/_local_staging_backup/grand-central_interactive_v1/city-map.js
--------------------------------------------------
'use strict';

/* ============================================================
   CITY MAP — Grand Central Interactive Navigator
   Vanilla JS, no dependencies.
   ============================================================ */

/* ----------------------------------------------------------
   STATE
   ---------------------------------------------------------- */
const state = {
  data:         null,
  activeFilter: null,   // null = all
  activePoi:    null,   // poi id
  zoom:         1,
  panX:         0,
  panY:         0,
  dragging:     false,
  dragStartX:   0,
  dragStartY:   0,
  dragStartPanX:0,
  dragStartPanY:0,
  imgNaturalW:  0,
  imgNaturalH:  0,
};

const ZOOM_MIN  = 0.4;
const ZOOM_MAX  = 4.0;
const ZOOM_STEP = 0.18;

/* ----------------------------------------------------------
   DOM REFS
   ---------------------------------------------------------- */
const $ = id => document.getElementById(id);

let DOM = {};

/* ----------------------------------------------------------
   BOOT
   ---------------------------------------------------------- */
async function init() {
  DOM = {
    filterBar:    $('filter-bar'),
    poiList:      $('poi-list'),
    viewport:     $('map-viewport'),
    zoomRoot:     $('map-zoom-root'),
    imgWrap:      $('map-img-wrap'),
    mapImage:     $('map-image'),
    pinLayer:     $('poi-pin-layer'),
    detailPanel:  $('detail-panel'),
    detailBar:    $('detail-bar'),
    detailClose:  $('detail-close'),
    coords:       $('map-coords'),
    poiCount:     $('footer-poi-count'),
    sidebarToggle:$('sidebar-toggle'),
    sidebar:      $('sidebar'),
    backdrop:     $('sidebar-backdrop'),
  };

  try {
    const res  = await fetch('./map-data.json');
    if (!res.ok) throw new Error('map-data.json not found');
    state.data = await res.json();
  } catch (e) {
    console.error('[city-map]', e);
    return;
  }

  buildFilters();
  buildPoiList();

  // wait for image to load before placing pins
  if (DOM.mapImage.complete && DOM.mapImage.naturalWidth) {
    onImageLoaded();
  } else {
    DOM.mapImage.addEventListener('load', onImageLoaded, { once: true });
  }

  bindViewport();
  bindDetail();
  bindMobileSidebar();
  updateFooterCount();
}

/* ----------------------------------------------------------
   IMAGE LOADED → place pins & fit
   ---------------------------------------------------------- */
function onImageLoaded() {
  state.imgNaturalW = DOM.mapImage.naturalWidth  || DOM.mapImage.width;
  state.imgNaturalH = DOM.mapImage.naturalHeight || DOM.mapImage.height;

  // Set image rendered size to natural size (zoom handles scaling)
  DOM.mapImage.style.width  = state.imgNaturalW + 'px';
  DOM.mapImage.style.height = state.imgNaturalH + 'px';

  buildPins();
  fitToViewport();
}

/* fit image into viewport with some padding */
function fitToViewport() {
  const vw = DOM.viewport.clientWidth;
  const vh = DOM.viewport.clientHeight;
  const iw = state.imgNaturalW;
  const ih = state.imgNaturalH;
  if (!iw || !ih) return;

  const scaleX = (vw * 0.92) / iw;
  const scaleY = (vh * 0.92) / ih;
  const scale  = Math.min(scaleX, scaleY, 1);

  state.zoom = scale;
  // centre
  state.panX = (vw - iw * scale) / 2;
  state.panY = (vh - ih * scale) / 2;
  applyTransform();
}

/* ----------------------------------------------------------
   FILTER BAR
   ---------------------------------------------------------- */
function buildFilters() {
  const bar = DOM.filterBar;
  const filters = state.data.filters;

  // "ALL" button
  const allBtn = createFilterBtn('ALL', 'all', true);
  bar.appendChild(allBtn);

  filters.forEach(f => {
    const btn = createFilterBtn(f.label_ja, f.id, false);
    bar.appendChild(btn);
  });
}

function createFilterBtn(label, id, active) {
  const btn = document.createElement('button');
  btn.className = 'filter-btn' + (active ? ' active' : '');
  btn.textContent = label;
  btn.dataset.filter = id;
  btn.addEventListener('click', () => onFilterClick(id));
  return btn;
}

function onFilterClick(id) {
  state.activeFilter = (id === 'all') ? null : id;

  // update button states
  DOM.filterBar.querySelectorAll('.filter-btn').forEach(btn => {
    const isActive = (state.activeFilter === null && btn.dataset.filter === 'all')
                  || btn.dataset.filter === state.activeFilter;
    btn.classList.toggle('active', isActive);
  });

  updatePinVisibility();
  updatePoiListVisibility();
  updateFooterCount();
}

/* ----------------------------------------------------------

==================================================
FILE: ./viewer/db/city/_local_staging_backup/grand-central_interactive_v1/index.html
--------------------------------------------------
<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0, user-scalable=no">
  <title>グランセントラル インタラクティブ地図 | GRAND CENTRAL NAVIGATOR</title>
  <meta name="description" content="ZERO編〜本編 グランセントラル都市区画インタラクティブナビゲータ。POIクリック・ズーム・パン・フィルター対応。">
  <link rel="stylesheet" href="./interactive.css">
</head>
<body>

<div class="map-app">

  <!-- ============================================================
       TOP BAR
       ============================================================ -->
  <header class="topbar">
    <!-- モバイルサイドバートグル -->
    <button class="sidebar-toggle-btn" id="sidebar-toggle" aria-label="サイドバーを開く">☰</button>

    <a class="topbar-back" href="../index.html">← STATIC VIEW</a>

    <div class="topbar-title">
      グランセントラル
      <span>INTERACTIVE NAVIGATOR</span>
    </div>

    <div class="topbar-spacer"></div>

    <!-- フィルターバー -->
    <div class="filter-bar" id="filter-bar">
      <span class="filter-label">FILTER</span>
      <!-- JS が buildFilters() でボタンを生成 -->
    </div>
  </header>

  <!-- ============================================================
       SIDEBAR
       ============================================================ -->
  <aside class="sidebar" id="sidebar">
    <div class="sidebar-header">
      <div class="sidebar-archive-tag">City District Archive</div>
      <div class="sidebar-title">グランセントラル</div>
      <div class="sidebar-subtitle">Grand Central Navigator</div>
    </div>

    <!-- POIリスト -->
    <div class="poi-list-wrap">
      <div id="poi-list">
        <!-- JS が buildPoiList() で生成 -->
      </div>
    </div>
  </aside>

  <!-- ============================================================
       MAP VIEWPORT
       ============================================================ -->
  <div class="map-viewport" id="map-viewport">

    <!-- 座標表示 -->
    <div class="map-coords" id="map-coords">x:— y:—</div>

    <!-- ズームコントロール -->
    <div class="zoom-controls">
      <button class="zoom-btn" id="btn-zoom-in"  title="ズームイン (+)">+</button>
      <button class="zoom-btn" id="btn-zoom-out" title="ズームアウト (−)">−</button>
      <button class="zoom-btn" id="btn-zoom-reset" title="リセット (0)">⟳</button>
    </div>

    <!-- ズームルート（transform はここに適用） -->
    <div id="map-zoom-root" class="map-zoom-root">
      <div id="map-img-wrap" class="map-img-wrap">

        <!-- 地図画像 — 親フォルダの画像を参照 -->
        <img
          id="map-image"
          class="map-image"
          src="../grand-central-map.png"
          alt="グランセントラル都市区画図"
          draggable="false"
        >

        <!-- POIピンレイヤー (JS が pins を生成) -->
        <div id="poi-pin-layer" class="poi-pin-layer"></div>

      </div>
    </div>

    <!-- 詳細パネル -->
    <div class="detail-panel hidden" id="detail-panel">
      <div class="detail-bar" id="detail-bar"></div>
      <button class="detail-close" id="detail-close" aria-label="閉じる">✕</button>
      <div class="detail-body">
        <div class="detail-district" id="detail-district"></div>
        <div class="detail-name"    id="detail-name"></div>
        <div class="detail-name-en" id="detail-name-en"></div>
        <div class="detail-desc"    id="detail-desc"></div>
        <div id="detail-lore"></div>
      </div>
    </div>

  </div><!-- /.map-viewport -->

  <!-- ============================================================
       FOOTER
       ============================================================ -->
  <footer class="map-footer">
    <span class="footer-tag">MERITOCRACIA WORLD ARCHIVE</span>
    <span class="footer-poi-count" id="footer-poi-count">— POI</span>
    <span class="footer-spacer"></span>
    <span class="footer-hint">ホイール / ピンチでズーム　ドラッグでパン　[0] リセット</span>
  </footer>

</div><!-- /.map-app -->

<!-- モバイルサイドバー背景 -->
<div class="sidebar-backdrop" id="sidebar-backdrop"></div>

<script src="./city-map.js"></script>
</body>
</html>

==================================================
FILE: ./viewer/db/city/_local_staging_backup/grand-central_interactive_v1/interactive.css
--------------------------------------------------
@import url('https://fonts.googleapis.com/css2?family=Cormorant+Garamond:ital,wght@0,300;0,400;0,600;1,400&family=Noto+Serif+JP:wght@300;400;500&display=swap');

/* ============================================================
   CSS VARIABLES  （city.css / terror.css 継承）
   ============================================================ */
:root {
  --bg:           #060a11;
  --bg2:          #0a1020;
  --bg3:          #0e1628;
  --panel:        #0d1525;
  --panel2:       #111d35;
  --panel-glass:  rgba(13,21,37,0.92);

  --gold:         #c9a961;
  --gold2:        #e8c97a;
  --gold-dim:     #7a6035;
  --gold-glow:    rgba(201,169,97,0.15);
  --gold-glow2:   rgba(201,169,97,0.08);

  --text:         #d8ccb4;
  --text2:        #7a6e58;
  --text3:        #4a4030;

  --border:       rgba(201,169,97,0.14);
  --border2:      rgba(201,169,97,0.30);
  --border3:      rgba(201,169,97,0.50);

  --c-dead:       #8b1a1a;
  --c-dead-text:  #e05050;
  --c-fo:         #4a7fc1;
  --c-escape:     #b080f0;

  --sidebar-w:    320px;
  --topbar-h:     52px;
  --footer-h:     44px;
}

/* ============================================================
   RESET & BASE
   ============================================================ */
*, *::before, *::after { margin: 0; padding: 0; box-sizing: border-box; }
html, body { height: 100%; overflow: hidden; }

body {
  background: var(--bg);
  color: var(--text);
  font-family: 'Noto Serif JP', serif;
  font-weight: 300;
  user-select: none;
}

/* ============================================================
   APP SHELL
   ============================================================ */
.map-app {
  display: grid;
  grid-template-rows: var(--topbar-h) 1fr var(--footer-h);
  grid-template-columns: var(--sidebar-w) 1fr;
  grid-template-areas:
    "topbar  topbar"
    "sidebar viewport"
    "sidebar footer";
  height: 100vh;
  width: 100vw;
  overflow: hidden;
}

/* ============================================================
   TOP BAR
   ============================================================ */
.topbar {
  grid-area: topbar;
  display: flex;
  align-items: center;
  gap: 0;
  border-bottom: 1px solid var(--border);
  background: var(--panel);
  position: relative;
  z-index: 20;
  padding: 0 16px;
}

.topbar-back {
  font-family: 'Cormorant Garamond', serif;
  font-size: 10px;
  letter-spacing: 3px;
  color: var(--text2);
  text-decoration: none;
  padding: 0 16px 0 0;
  border-right: 1px solid var(--border);
  margin-right: 16px;
  transition: color 0.2s;
  white-space: nowrap;
}
.topbar-back:hover { color: var(--gold); }

.topbar-title {
  font-family: 'Cormorant Garamond', serif;
  font-size: 13px;
  letter-spacing: 4px;
  color: var(--gold);
  font-weight: 400;
  white-space: nowrap;
  flex-shrink: 0;
}
.topbar-title span {
  color: var(--text2);
  font-size: 10px;
  letter-spacing: 2px;
  margin-left: 10px;
}

.topbar-spacer { flex: 1; }

/* フィルターバー */
.filter-bar {
  display: flex;
  align-items: center;
  gap: 4px;
  flex-shrink: 0;
}

.filter-label {
  font-family: 'Cormorant Garamond', serif;
  font-size: 9px;
  letter-spacing: 3px;
  color: var(--text3);
  text-transform: uppercase;
  margin-right: 6px;
  white-space: nowrap;
}

.filter-btn {
  font-family: 'Cormorant Garamond', serif;
  font-size: 9px;
  letter-spacing: 2px;
  color: var(--text2);
  background: transparent;
  border: 1px solid var(--border);
  padding: 4px 10px;
  cursor: pointer;
  transition: all 0.18s;
  text-transform: uppercase;
  white-space: nowrap;
}
.filter-btn:hover {
  border-color: var(--border2);
  color: var(--text);
}
.filter-btn.active {
  background: var(--gold-glow2);
  border-color: var(--gold-dim);
  color: var(--gold);
}

/* モバイルトグル */
.sidebar-toggle-btn {
  display: none;
  background: none;
  border: 1px solid var(--border);

==================================================
FILE: ./viewer/db/city/_local_staging_backup/grand-central_interactive_v1/map-data.json
--------------------------------------------------
{
  "meta": {
    "map_id": "grand_central_interactive_v1",
    "title": "グランセントラル都市区画図",
    "title_en": "Grand Central City District Map",
    "image": "../grand-central-map.png",
    "version": "1.0"
  },
  "filters": [
    { "id": "zero",    "label": "ZERO編",    "label_ja": "ZERO編" },
    { "id": "main",    "label": "本編",      "label_ja": "本編" },
    { "id": "officer", "label": "士官学校",  "label_ja": "士官学校" },
    { "id": "church",  "label": "教会バイト","label_ja": "教会バイト" },
    { "id": "harbor",  "label": "港湾物流",  "label_ja": "港湾物流" }
  ],
  "pois": [
    {
      "id": "poi_central_market",
      "name": "中央市場",
      "name_en": "Central Market",
      "district": "中央市場区",
      "x_pct": 47.5,
      "y_pct": 24.0,
      "icon": "◈",
      "color": "#b8860b",
      "filters": ["zero", "main"],
      "description": "都市最大の商業・流通拠点。祝祭広場と隣接し、平時は市民の集う活気ある場所。ZERO編「中央市場炎槍事件」の主舞台となり、炎翼セルによる大規模テロ攻撃が発生した。",
      "lore_zero": "炎翼セル配置→初爆発→召喚獣出現→炎槍発射→カーネル死亡という一連のテロ事件が展開。ジェイドにとって「国家の嘘」と「心音覚醒」の起点となる転換点。",
      "lore_main": "本編ではテロ後の復興が進み、警備体制が強化された状態で登場。事件の痕跡が一部残る。",
      "lore_officer": "士官学校の演習ルート上に位置。市場警備との連携訓練が行われる。",
      "lore_church": null,
      "lore_harbor": null
    },
    {
      "id": "poi_festival_square",
      "name": "祝祭広場",
      "name_en": "Festival Square",
      "district": "中央市場区",
      "x_pct": 47.5,
      "y_pct": 34.5,
      "icon": "◉",
      "color": "#c9a961",
      "filters": ["zero", "main"],
      "description": "中央市場に隣接する市民広場。祭事・公式行事の場として機能する。ZERO編では炎翼セルの爆発波及エリアとなり、多数の市民が避難した。",
      "lore_zero": "初爆地点の延焼エリア。群衆避難ルートの起点。FOによる封鎖ラインが引かれた。",
      "lore_main": "定期市場と祭りが復活。テロ以前の賑わいを取り戻しつつある。",
      "lore_officer": null,
      "lore_church": null,
      "lore_harbor": null
    },
    {
      "id": "poi_star_church",
      "name": "星導教会",
      "name_en": "Star Guide Church",
      "district": "教会旧市街",
      "x_pct": 62.8,
      "y_pct": 55.0,
      "icon": "✦",
      "color": "#9a6aaa",
      "filters": ["zero", "church"],
      "description": "教会旧市街の中心的宗教施設。地下に礼拝堂・儀式広間・秘密通路を持つ。神父エリオスが管理し、炎翼セル上位組織との接続拠点として機能している疑いがある。",
      "lore_zero": "炎翼セル《地下儀式広間》が星導教会地下に存在。Scene30の締め「観測対象、反応あり」「監視継続」の示唆。エリオスが黒幕候補。",
      "lore_main": "表向きは通常運営。ルカのペンダントと七芒星の魔法石が回収伏線として継続する。",
      "lore_officer": null,
      "lore_church": "ルカが教会バイトとして勤務。地下礼拝路の存在を知る数少ない人物となる。",
      "lore_harbor": null
    },
    {
      "id": "poi_underground_entrance",
      "name": "地下礼拝路入口",
      "name_en": "Underground Chapel Entrance",
      "district": "教会旧市街",
      "x_pct": 65.5,
      "y_pct": 67.0,
      "icon": "▼",
      "color": "#7a3a6a",
      "filters": ["zero", "church"],
      "description": "教会旧市街の地下礼拝路への入口。表向きは廃れた礼拝施設への通路だが、炎翼セルの集合・展開拠点として利用されていた。",
      "lore_zero": "炎翼セル侵入軸の出発点。《地下儀式広間》→《ノクス潜伏路地》→《中央市場》という侵入ルートの起点。",
      "lore_main": null,
      "lore_officer": null,
      "lore_church": "ルカが誤って深部まで踏み込んでしまう。ペンダントの遺棄場所に近い。",
      "lore_harbor": null
    },
    {
      "id": "poi_jade_residence",
      "name": "ジェイド居住圏",
      "name_en": "Jade's Residential Area",
      "district": "東住宅区",
      "x_pct": 77.0,
      "y_pct": 41.0,
      "icon": "◆",
      "color": "#3a8e6a",
      "filters": ["zero", "main", "officer"],
      "description": "東住宅区内にあるジェイドの生活拠点。パン屋通りに近く、市場や士官学校への通学路が交差する。ZERO編からZERO編後を通じてジェイドの日常の軸となる場所。",
      "lore_zero": "テロ事件前後のジェイドの帰還地点。事件後の心理的変化・心音覚醒の余波を一人で処理する場。",
      "lore_main": "本編ジェイドの生活ベース。士官学校編の通学起点となる。",
      "lore_officer": "士官学校への最短ルートの出発点。朝の通学シーンの舞台。",
      "lore_church": null,
      "lore_harbor": null
    },
    {
      "id": "poi_bakery_street",
      "name": "パン屋通り",
      "name_en": "Baker's Street",
      "district": "東住宅区",
      "x_pct": 74.0,
      "y_pct": 31.0,
      "icon": "○",
      "color": "#c9a961",
      "filters": ["main", "officer"],
      "description": "東住宅区の商店街。ジェイドが日常的に通る生活導線。地元の人情と市井の空気が色濃く残る場所。",
      "lore_zero": null,
      "lore_main": "本編の日常描写の主舞台のひとつ。クララとの接点が生まれる場所。",
      "lore_officer": "士官学校通学ルートに含まれる。",
      "lore_church": null,
      "lore_harbor": null
    },
    {
      "id": "poi_east_gate",
      "name": "東門街道",
      "name_en": "East Gate Road",
      "district": "東住宅区",
      "x_pct": 80.5,
      "y_pct": 22.5,
      "icon": "→",
      "color": "#5a7a9a",
      "filters": ["main", "officer"],
      "description": "東住宅区の幹線街道。都市外部との主要アクセスルートであり、馬車停留宿が設置されている。",
      "lore_zero": null,
      "lore_main": "都市間移動の出入口として機能。",
      "lore_officer": "士官学校編で外部からの来訪者や訪問イベントに使用される導線。",
      "lore_church": null,
      "lore_harbor": null
    },
    {
      "id": "poi_central_office",
      "name": "中央庁舎",
      "name_en": "Central Government Hall",
      "district": "行政中枢区",
      "x_pct": 28.5,
      "y_pct": 41.0,
      "icon": "▣",
      "color": "#2a5a9a",
      "filters": ["zero", "main"],
      "description": "グランセントラル行政の本丸。三兵団本部と連携し、都市の法執行・政策決定を担う。ZERO編では炎槍事件の「迅速な解決」を演出した国家の意思決定拠点。",
      "lore_zero": "事件を早期収束させた国家の政治的意思がここから発令された。D-04《グラム》を実行犯として処刑予定とした決定もここで行われた疑いがある。",
      "lore_main": "本編では改革派と保守派の政治的対立の場として機能。",
      "lore_officer": null,
      "lore_church": null,
      "lore_harbor": null
    },
    {
      "id": "poi_three_corps_hq",
      "name": "三兵団本部",
      "name_en": "Three Corps HQ",
      "district": "行政中枢区",
      "x_pct": 27.0,
      "y_pct": 30.5,
      "icon": "◈",

==================================================
FILE: ./viewer/db/city/_local_staging_backup/grand-central_v3_restore/index.html
--------------------------------------------------
<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0, user-scalable=no">
  <title>グランセントラル 連続地図 v3 | GRAND CENTRAL SEAMLESS MAP</title>
  <meta name="description" content="グランセントラル都市全体図＋士官学校詳細の連続インタラクティブマップ v3。">
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Noto+Serif+JP:wght@400;600;700&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="./v3.css">
</head>
<body>

<!-- ══════════════════════════════════════════════════════════
     TOP BAR
     ══════════════════════════════════════════════════════════ -->
<header class="topbar">
  <a class="topbar-back" href="../index.html">← VIEWER HUB</a>
  <div class="topbar-sep"></div>

  <div class="topbar-title">
    グランセントラル
    <span>SEAMLESS NAVIGATOR v3</span>
  </div>

  <div class="topbar-spacer"></div>

  <!-- フィルターバー -->
  <div class="filter-bar" id="filter-bar">
    <span class="filter-label">FILTER</span>
    <!-- JS buildFilters() -->
  </div>

  <div class="topbar-sep"></div>

  <!-- Map mode tabs -->
  <div class="mode-tabs">
    <button class="mode-tab active" data-mode="combined">連続</button>
    <button class="mode-tab" data-mode="gc">都市</button>
    <button class="mode-tab" data-mode="ac">学校</button>
  </div>

  <div class="topbar-sep"></div>

  <!-- Minimap toggle -->
  <button class="minimap-toggle" id="minimap-toggle">ミニマップ</button>

  <!-- Zoom controls -->
  <div class="zoom-controls">
    <button class="zoom-btn" id="btn-zoom-in"    title="ズームイン (+)">+</button>
    <button class="zoom-btn" id="btn-zoom-out"   title="ズームアウト (−)">−</button>
    <button class="zoom-btn" id="btn-zoom-reset" title="全体表示 (0)">⟳</button>
  </div>
</header>


<!-- ══════════════════════════════════════════════════════════
     MAP VIEWPORT
     ══════════════════════════════════════════════════════════ -->
<div class="map-viewport" id="map-viewport">

  <!-- World root — all map content lives here, transform applied here -->
  <div id="world">

    <!-- GC map image -->
    <img id="layer-gc" class="map-layer"
         src="../grand-central-map.png"
         alt="グランセントラル都市区画図"
         draggable="false">

    <!-- Academy map image -->
    <img id="layer-ac" class="map-layer"
         src="../academy-map.png"
         alt="士官学校詳細図"
         draggable="false">

    <!-- Seam blending overlay (JS paints gradient here) -->
    <div id="seam-overlay"></div>

    <!-- POI pins -->
    <div id="poi-pin-layer" class="poi-pin-layer"></div>

  </div><!-- /#world -->

  <!-- Coordinate display -->
  <div class="map-coords" id="map-coords">x:— y:—</div>

</div><!-- /.map-viewport -->


<!-- ══════════════════════════════════════════════════════════
     DETAIL PANEL
     ══════════════════════════════════════════════════════════ -->
<div class="detail-panel hidden" id="detail-panel">
  <div class="detail-bar" id="detail-bar"></div>
  <button class="detail-close" id="detail-close" aria-label="閉じる">✕</button>
  <div class="detail-body">
    <div class="detail-district" id="detail-district"></div>
    <div class="detail-name"     id="detail-name"></div>
    <div class="detail-name-en"  id="detail-name-en"></div>
    <div class="detail-desc"     id="detail-desc"></div>
    <div id="detail-lore"></div>
  </div>
</div>


<!-- ══════════════════════════════════════════════════════════
     MINIMAP
     ══════════════════════════════════════════════════════════ -->
<div id="minimap-wrap">
  <canvas id="minimap-canvas"></canvas>
  <div id="minimap-viewport-rect"></div>
</div>


<!-- ══════════════════════════════════════════════════════════
     FOOTER
     ══════════════════════════════════════════════════════════ -->
<footer class="map-footer">
  <span class="footer-tag">MERITOCRACIA WORLD ARCHIVE</span>
  <span class="footer-poi-count" id="footer-poi-count">— POI</span>
  <span class="footer-spacer"></span>
  <span class="footer-hint">ホイール / ピンチでズーム　ドラッグでパン　[0]全体表示　[←→↑↓]移動</span>
</footer>

<!-- Scroll hint (auto-fades) -->
<div class="scroll-hint">← ドラッグして士官学校エリアへ →</div>

<script src="./v3.js"></script>
</body>
</html>

==================================================
FILE: ./viewer/db/city/_local_staging_backup/grand-central_v3_restore/v3.css
--------------------------------------------------
/* ============================================================
   GRAND CENTRAL NAVIGATOR v3 — Seamless Dual-Map Viewer
   Aesthetic: Dark Cartographic / Fantasy Atlas
   ============================================================ */

:root {
  --bg:          #0b0e16;
  --chrome:      #12161f;
  --border:      #2a3040;
  --gold:        #c9a961;
  --gold-dim:    #8a7040;
  --gold-bright: #f0d080;
  --text:        #d4c9a8;
  --text-dim:    #7a7060;
  --red:         #9a3a3a;
  --blue:        #2a5a9a;
  --green:       #3a7a5a;
  --panel-bg:    rgba(11,14,22,0.92);
  --seam-w:      60px;   /* blending seam width */
  --topbar-h:    48px;
  --footer-h:    36px;
}

*, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }

html, body {
  width: 100%; height: 100%; overflow: hidden;
  background: var(--bg);
  color: var(--text);
  font-family: 'Noto Serif JP', 'Georgia', serif;
  -webkit-font-smoothing: antialiased;
}

/* ── TOPBAR ─────────────────────────────────────────────── */
.topbar {
  position: fixed; top: 0; left: 0; right: 0;
  height: var(--topbar-h);
  background: var(--chrome);
  border-bottom: 1px solid var(--border);
  display: flex; align-items: center; gap: 12px;
  padding: 0 16px;
  z-index: 100;
  box-shadow: 0 2px 16px rgba(0,0,0,0.5);
}

.topbar-back {
  color: var(--gold-dim);
  text-decoration: none;
  font-size: 11px;
  letter-spacing: .08em;
  white-space: nowrap;
  transition: color .2s;
}
.topbar-back:hover { color: var(--gold); }

.topbar-sep {
  width: 1px; height: 20px;
  background: var(--border);
  flex-shrink: 0;
}

.topbar-title {
  font-size: 13px;
  font-weight: 600;
  letter-spacing: .12em;
  color: var(--gold);
  white-space: nowrap;
}
.topbar-title span {
  font-size: 9px;
  color: var(--text-dim);
  letter-spacing: .18em;
  display: block;
  margin-top: 1px;
}

.topbar-spacer { flex: 1; }

/* map-mode toggle */
.mode-tabs {
  display: flex; gap: 2px;
  background: rgba(0,0,0,0.3);
  border: 1px solid var(--border);
  border-radius: 4px;
  padding: 2px;
}
.mode-tab {
  background: none; border: none;
  color: var(--text-dim);
  font-size: 10px; letter-spacing: .1em;
  padding: 4px 10px;
  border-radius: 3px;
  cursor: pointer;
  transition: all .15s;
  white-space: nowrap;
}
.mode-tab.active {
  background: var(--gold-dim);
  color: var(--bg);
}
.mode-tab:hover:not(.active) { color: var(--text); }

/* minimap toggle */
.minimap-toggle {
  background: none; border: 1px solid var(--border);
  color: var(--text-dim); font-size: 10px;
  padding: 4px 8px; border-radius: 3px;
  cursor: pointer; transition: all .15s;
  letter-spacing: .08em;
}
.minimap-toggle:hover { border-color: var(--gold-dim); color: var(--gold-dim); }

/* zoom controls */
.zoom-controls {
  display: flex; gap: 2px;
}
.zoom-btn {
  background: none; border: 1px solid var(--border);
  color: var(--text-dim); width: 28px; height: 28px;
  border-radius: 3px; cursor: pointer;
  font-size: 14px; line-height: 1;
  transition: all .15s;
  display: flex; align-items: center; justify-content: center;
}
.zoom-btn:hover { border-color: var(--gold-dim); color: var(--gold); }

/* ── VIEWPORT ────────────────────────────────────────────── */
.map-viewport {
  position: fixed;
  top: var(--topbar-h);
  bottom: var(--footer-h);
  left: 0; right: 0;
  overflow: hidden;
  cursor: grab;
  background: var(--bg);
  /* Subtle noise texture */
  background-image:
    radial-gradient(ellipse at 20% 80%, rgba(42,90,154,0.06) 0%, transparent 60%),
    radial-gradient(ellipse at 80% 20%, rgba(201,169,97,0.04) 0%, transparent 50%);
}
.map-viewport.dragging { cursor: grabbing; }

/* ── CANVAS WORLD ────────────────────────────────────────── */
#world {
  position: absolute;
  /* size set by JS */
  transform-origin: 0 0;
  will-change: transform;
}

/* ── MAP LAYERS ──────────────────────────────────────────── */
.map-layer {
  position: absolute;
  top: 0;
  image-rendering: crisp-edges;
  display: block;
}

/* Grand Central — left map */
#layer-gc {

==================================================
FILE: ./viewer/db/city/_local_staging_backup/grand-central_v3_restore/v3.js
--------------------------------------------------
/**
 * GRAND CENTRAL NAVIGATOR v3
 * Seamless dual-map viewer
 * grand-central-map.png (1408×768) + academy-map.png (1380×752)
 */

'use strict';

// ─── CONFIG ────────────────────────────────────────────────────────────────

const CFG = {
  gc: { src: '../grand-central-map.png', w: 1408, h: 768 },
  ac: { src: '../academy-map.png',        w: 1380, h: 752 },

  // Where academy left-edge anchors on the combined world X axis
  // We overlap slightly so the seam at the 士官予備区 feels continuous.
  // Visually the 士官予備区 is roughly x=40–55% on GC map.
  // We want academy to "grow out" of that zone toward the right.
  // Overlap amount: 120px (the two images share ~120px of visual territory)
  overlap: 130,

  // Vertical offset: academy map is slightly taller/shorter than GC.
  // Center academy vertically relative to GC height.
  get acOffsetY() {
    return Math.round((CFG.gc.h - CFG.ac.h) / 2);
  },

  // World dimensions
  get worldW() { return CFG.gc.w + CFG.ac.w - CFG.overlap; },
  get worldH() { return Math.max(CFG.gc.h, CFG.ac.h); },

  // Seam X position (right edge of GC / left edge of AC in world coords)
  get seamX()   { return CFG.gc.w - CFG.overlap; },

  // Zoom
  minZoom: 0.35,
  maxZoom: 2.5,
  zoomStep: 0.18,
  initialZoom: null, // computed on init
};

// Hotspot data — all coords in WORLD-PIXEL space
// GC POIs use the x_pct/y_pct from map-data.json → converted to px
// Academy POIs are defined separately in world coords
const ACADEMY_POIS = [
  {
    id: 'ac_main_building',
    name: '本校舎',
    name_en: 'Main Academy Building',
    district: '士官学校',
    icon: '◈', color: '#9a3a3a',
    wx_pct: null, wy_pct: null,   // set below
    ac_x_pct: 49, ac_y_pct: 14,  // relative to academy image
    filters: ['officer'],
    description: '士官学校の中心棟。入学式・全校集会・重要行事が行われる。高い尖塔と石造りの外観が特徴。',
    lore_officer: 'ジェイドの入学式の舞台。グローリアテスト結果の発表もここで行われる。',
  },
  {
    id: 'ac_central_plaza',
    name: '中央広場',
    name_en: 'Central Plaza',
    district: '士官学校',
    icon: '◉', color: '#c9a961',
    ac_x_pct: 44, ac_y_pct: 46,
    filters: ['officer'],
    description: '本校舎正面の大広場。朝礼・訓練集合・自由時間の場として機能する。',
    lore_officer: '生徒同士の交流の起点。イベントフラグが多数存在する場所。',
  },
  {
    id: 'ac_faculty_building',
    name: '教官棟',
    name_en: 'Faculty Building',
    district: '士官学校',
    icon: '▣', color: '#2a5a9a',
    ac_x_pct: 20, ac_y_pct: 22,
    filters: ['officer'],
    description: 'ユミナら教官の執務室・会議室が集まる棟。許可なき立入禁止。',
    lore_officer: 'ユミナとの面談シーン・成績通知の舞台候補。',
  },
  {
    id: 'ac_dormitory',
    name: '学生寮',
    name_en: 'Student Dormitory',
    district: '士官学校',
    icon: '□', color: '#4a6a5a',
    ac_x_pct: 14, ac_y_pct: 38,
    filters: ['officer'],
    description: '学生の宿舎区画。複数棟から成り、性別・学年で分かれている。',
    lore_officer: '夜の自由時間・秘密の交流・夜化因子発覚前夜の描写の主舞台候補。',
  },
  {
    id: 'ac_front_gate',
    name: '士官学校正門',
    name_en: 'Academy Main Gate',
    district: '士官学校',
    icon: '→', color: '#8a7040',
    ac_x_pct: 64, ac_y_pct: 46,
    filters: ['officer', 'main'],
    description: 'グランセントラル都市部から東門街道経由でアクセスできる正門。通学生・来訪者の主要入口。',
    lore_officer: 'ジェイドが初登校する際に通過する門。都市側との接続点。',
    lore_main: '本編でのFO訓練許可証の受取場所候補。',
  },
  {
    id: 'ac_training_ground_inner',
    name: '訓練校（詳細）',
    name_en: 'Inner Training Ground',
    district: '士官予備区',
    icon: '△', color: '#7a2a2a',
    ac_x_pct: 34, ac_y_pct: 78,
    filters: ['officer'],
    description: '実戦演習・魔法戦闘訓練が行われる内部演習場。アカデミーマップ上の詳細位置。',
    lore_officer: 'グローリアテストの試験広場。心音能力の意図せぬ発動が起こりうる現場。',
  },
  {
    id: 'ac_star_church_near',
    name: '星導教会（近傍）',
    name_en: 'Star Guide Church (nearby)',
    district: '教会旧市街',
    icon: '✦', color: '#9a6aaa',
    ac_x_pct: 62, ac_y_pct: 68,
    filters: ['church'],
    description: '士官学校エリアから見た星導教会の方角。裏門からアクセス可能な旧市街側。',
    lore_church: 'ルカが勤務する教会への近道ルート。士官学校生が密かに抜ける裏路地。',
  },
];

// ─── STATE ─────────────────────────────────────────────────────────────────

const S = {
  zoom: 1,
  panX: 0,
  panY: 0,
  dragging: false,
  lastMX: 0, lastMY: 0,
  activeFilters: new Set(['zero','main','officer','church','harbor']),
  mapMode: 'combined', // 'combined' | 'gc' | 'ac'
  minimapVisible: true,
  allPois: [],   // merged GC + AC pois in world-px coords
};

// ─── DOM REFS ───────────────────────────────────────────────────────────────

const $ = id => document.getElementById(id);
let vp, world, layerGC, layerAC,
    pinLayer, coordsEl, footerCount,
    detailPanel, detailBar, detailClose,
    detailDistrict, detailName, detailNameEn, detailDesc, detailLore,
    filterBar, minimapWrap, minimapCanvas, minimapCtx, minimapRect;

// ─── INIT ───────────────────────────────────────────────────────────────────

window.addEventListener('DOMContentLoaded', () => {
  // cache DOM
  vp            = $('map-viewport');
  world         = $('world');
  layerGC       = $('layer-gc');
  layerAC       = $('layer-ac');
  pinLayer      = $('poi-pin-layer');
  coordsEl      = $('map-coords');
  footerCount   = $('footer-poi-count');

==================================================
FILE: ./viewer/db/city/_local_staging_backup/grand-central_wip_2026-04-16_1811/academy/index.html
--------------------------------------------------
<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width,initial-scale=1.0">
  <title>Grand Central Map v2</title>
  <link rel="stylesheet" href="./v2.css">
</head>
<body>
  <header class="header">
    <h1>グランセントラル都市マップ v2</h1>
    <p>ドラッグで都市全体 → 士官学校詳細へ移動</p>
  </header>

  <main class="map-scroll">
    <section class="map-panel">
      <h2>都市全体</h2>
      <img src="../grand-central-map.png" alt="都市全体図">
    </section>

    <section class="map-panel">
      <h2>士官学校詳細</h2>
      <img src="../academy/academy-map.png" alt="士官学校詳細">
    </section>
  </main>
</body>
</html>
==================================================
FILE: ./viewer/db/city/_local_staging_backup/grand-central_wip_2026-04-16_1811/archive/map_v2_pre_zero_overlay_2026-04-16/city.css
--------------------------------------------------
@import url('https://fonts.googleapis.com/css2?family=Cormorant+Garamond:ital,wght@0,300;0,400;0,600;1,400&family=Noto+Serif+JP:wght@300;400;500&display=swap');

/* ============================================================
   CSS VARIABLES
   ============================================================ */
:root {
  --bg:           #060a11;
  --bg2:          #0a1020;
  --bg3:          #0e1628;
  --panel:        #0d1525;
  --panel2:       #111d35;

  --gold:         #c9a961;
  --gold2:        #e8c97a;
  --gold-dim:     #7a6035;
  --gold-glow:    rgba(201,169,97,0.15);

  --text:         #d8ccb4;
  --text2:        #7a6e58;
  --text3:        #4a4030;

  --border:       rgba(201,169,97,0.14);
  --border2:      rgba(201,169,97,0.3);

  /* ステータスカラー */
  --c-dead:       #8b1a1a;
  --c-dead-glow:  rgba(139,26,26,0.2);
  --c-dead-text:  #e05050;

  --c-captured:   #b8901a;
  --c-captured-glow: rgba(184,144,26,0.2);
  --c-captured-text: #e8c050;

  --c-escape:     #5a2d8a;
  --c-escape-glow: rgba(90,45,138,0.2);
  --c-escape-text: #b080f0;

  --c-civilian:   #1a4a7a;
  --c-civilian-glow: rgba(26,74,122,0.2);
  --c-civilian-text: #60a8e0;

  --c-unknown:    #3a3a4a;
  --c-unknown-glow: rgba(58,58,74,0.3);
  --c-unknown-text: #9090b0;

  --c-shadow:     #0a0a0a;
  --c-shadow-glow: rgba(201,169,97,0.08);
  --c-shadow-text: #c9a961;

  /* ハイライト種別 */
  --hl-death:     rgba(139,26,26,0.12);
  --hl-pivot:     rgba(60,100,180,0.1);
  --hl-deception: rgba(90,45,138,0.12);
  --hl-awakening: rgba(80,160,120,0.1);
  --hl-oath:      rgba(201,169,97,0.1);
}

/* ============================================================
   RESET & BASE
   ============================================================ */
*, *::before, *::after { margin: 0; padding: 0; box-sizing: border-box; }

html { scroll-behavior: smooth; }

body {
  background: var(--bg);
  color: var(--text);
  font-family: 'Noto Serif JP', serif;
  font-weight: 300;
  line-height: 1.8;
  min-height: 100vh;
  overflow-x: hidden;
}

body::before {
  content: '';
  position: fixed; inset: 0;
  background:
    radial-gradient(ellipse 80% 40% at 50% 0%, rgba(139,26,26,0.06) 0%, transparent 60%),
    radial-gradient(ellipse 60% 50% at 80% 100%, rgba(20,40,90,0.12) 0%, transparent 60%),
    repeating-linear-gradient(
      45deg, transparent, transparent 100px,
      rgba(201,169,97,0.012) 100px, rgba(201,169,97,0.012) 101px
    );
  pointer-events: none; z-index: 0;
}

/* ============================================================
   LAYOUT
   ============================================================ */
.terror-shell {
  position: relative; z-index: 1;
  max-width: 1040px;
  margin: 0 auto;
  padding: 0 20px 100px;
}

/* ============================================================
   BACK LINK
   ============================================================ */
.back-link {
  display: inline-flex;
  align-items: center;
  gap: 8px;
  font-family: 'Cormorant Garamond', serif;
  font-size: 11px;
  letter-spacing: 3px;
  color: var(--text2);
  text-decoration: none;
  padding: 20px 0;
  transition: color 0.2s;
}
.back-link:hover { color: var(--gold); }

/* ============================================================
   ① INCIDENT HEADER
   ============================================================ */
.incident-header {
  padding: 56px 0 48px;
  border-bottom: 1px solid var(--border);
  margin-bottom: 64px;
  position: relative;
}

.incident-header::after {
  content: '';
  position: absolute;
  bottom: -1px; left: 0;
  width: 120px; height: 1px;
  background: var(--gold);
}

.incident-tag {
  font-family: 'Cormorant Garamond', serif;
  font-size: 10px;
  letter-spacing: 5px;
  color: var(--c-dead-text);
  opacity: 0.7;
  margin-bottom: 14px;
  text-transform: uppercase;
}

.incident-title {
  font-family: 'Cormorant Garamond', serif;
  font-size: clamp(2.4rem, 7vw, 4.2rem);
  font-weight: 300;
  color: var(--text);
  letter-spacing: 0.08em;
  line-height: 1.2;
  margin-bottom: 6px;
}

.incident-sub {
  font-family: 'Cormorant Garamond', serif;
  font-size: 13px;
  letter-spacing: 4px;
  color: var(--gold);
  opacity: 0.6;
  margin-bottom: 36px;
}

==================================================
FILE: ./viewer/db/city/_local_staging_backup/grand-central_wip_2026-04-16_1811/archive/map_v2_pre_zero_overlay_2026-04-16/city.js
--------------------------------------------------
'use strict';

/* ============================================================
   CONSTANTS
   ============================================================ */
const STATUS_CONFIG = {
  dead:     { label: '死亡',           dot: '#e05050' },
  captured: { label: '逮捕（影武者）', dot: '#e8c050' },
  escape:   { label: '逃亡・生存',     dot: '#b080f0' },
  civilian: { label: '生存（民間）',   dot: '#60a8e0' },
  unknown:  { label: '生死不明',       dot: '#9090b0' },
  shadow:   { label: '要監視・黒幕候補', dot: '#c9a961' },
};

const PHASE_ORDER = [
  '静寂と予兆',
  '英雄的選択と悲劇',
  '決別と覚醒の兆し',
  'FOによる制圧',
  '残酷な現実の確定',
  '政治的幕引き',
  '誓いへ',
];

const HL_BADGE_LABEL = {
  death:     '★ カーネル死亡',
  pivot:     '◆ 転換点',
  deception: '▲ 国家の欺瞞',
  awakening: '◉ 心音覚醒',
  oath:      '✦ 誓い',
};

const FORESHADOW_DATA = [
  {
    icon: '🔮',
    label: 'ABILITY',
    title: '心音システム初覚醒',
    desc: 'Scene14の異常な心音、Scene25の公開尋問での無自覚発火。「こいつじゃない」という確信がジェイドに生まれる起点。グローリアテストおよび夜化因子判定への直接の伏線。',
    connect: '→ グローリアテスト / 夜化因子 / 能力判定試験',
  },
  {
    icon: '📿',
    label: 'EVIDENCE',
    title: 'ペンダント証拠',
    desc: 'Scene27でカーネルの遺品として発見される七芒星の魔法石ペンダント。触れた瞬間に炎槍の記憶がフラッシュバック。教会バイト編でルカが持っていた物と同一。',
    connect: '→ 教会バイト編回収 / ルカ接点 / 炎翼セル証拠',
  },
  {
    icon: '👦',
    label: 'GUILT',
    title: 'ルカの罪悪感ライン',
    desc: 'Scene28でルカが黒煙を見つめ「ボクは悪くない」と自分に言い聞かせる。ペンダントが消えていることで全てを悟る。クララとは別の「生き残った罪」として作品を厚くする。',
    connect: '→ セラとの再接触 / 証言者候補 / 贖罪ライン',
  },
  {
    icon: '🏛',
    label: 'CONSPIRACY',
    title: '国家による真実隠蔽',
    desc: 'Scene24-26で国家は迅速な「解決」をアピール。D-04《グラム》が実行犯として処刑予定。本当の幹部《アーク》《ヴェル》は逃走、《ノクス》は自害を偽装している可能性がある。',
    connect: '→ グラム公開尋問 / ジェイドの孤独な違和感 / ZERO編エンジン',
  },
  {
    icon: '⛪',
    label: 'CHURCH',
    title: '教会地下黒幕継続',
    desc: 'Scene30の締めで「観測対象、反応あり」「監視継続」が示される。エリオスは神父として平常運用を続けるが、炎翼セル上位セルとの接続役を担う黒幕候補。',
    connect: '→ 炎翼セル上位組織 / エリオス / グローリアテスト編接続',
  },
  {
    icon: '🛡',
    label: 'FO',
    title: 'FOへの憧憬と不信の同居',
    desc: '聖剣で炎槍を止めたガーネットへの憧れ（FOへの憧憬）と、事件を早々に片付けた国家システムへの不信が、ジェイドの中に同時に生まれる。この矛盾がZERO編全体を駆動する。',
    connect: '→ FO入団動機 / 士官学校編 / 国家不信ライン',
  },
];

/* ============================================================
   UTILS
   ============================================================ */
function el(tag, cls, text) {
  const e = document.createElement(tag);
  if (cls) e.className = cls;
  if (text != null) e.textContent = text;
  return e;
}

function stagger(elements, base = 0, step = 40) {
  elements.forEach((e, i) => {
    e.style.animationDelay = `${base + i * step}ms`;
  });
}

/* ============================================================
   FETCH BOTH JSON
   ============================================================ */
async function loadAll() {
  const [storyRes, statusRes] = await Promise.all([
    fetch('./terror_story.json'),
    fetch('./terror_status.json'),
  ]);

  if (!storyRes.ok) throw new Error('terror_story.json の読み込みに失敗しました');
  if (!statusRes.ok) throw new Error('terror_status.json の読み込みに失敗しました');

  return {
    story:  await storyRes.json(),
    status: await statusRes.json(),
  };
}

/* ============================================================
   ① INCIDENT HEADER
   ============================================================ */
function renderHeader(story, status) {
  document.getElementById('incident-title').textContent = story.incident;
  document.getElementById('incident-sub').textContent   = story.subtitle;

  const members  = status.members;
  const dead     = members.filter(m => m.status === 'dead').length;
  const escape   = members.filter(m => m.status === 'escape' || m.status === 'unknown').length;
  const captured = members.filter(m => m.status === 'captured').length;

  document.getElementById('stat-dead').textContent     = dead;
  document.getElementById('stat-escape').textContent   = escape;
  document.getElementById('stat-captured').textContent = captured;
}

/* ============================================================
   ② STORY TIMELINE
   ============================================================ */
function renderTimeline(story) {
  const wrap = document.getElementById('timeline-wrap');
  const scenes = story.scenes;

  // フェーズでグループ化
  const phases = {};
  for (const sc of scenes) {
    if (!phases[sc.phase]) phases[sc.phase] = [];
    phases[sc.phase].push(sc);
  }

  const orderedPhases = PHASE_ORDER.filter(p => phases[p]);

  for (const phaseName of orderedPhases) {
    const group = phases[phaseName];

    // フェーズ区切り
    const divider = document.createElement('div');
    divider.className = 'phase-divider';
    divider.innerHTML = `
      <div class="phase-label">
        <span>${group[0].phase_en}</span>
        <span class="phase-label-ja">${phaseName}</span>
      </div>`;
    wrap.appendChild(divider);

    // シーンカード
    const cards = [];
    for (const sc of group) {

==================================================
FILE: ./viewer/db/city/_local_staging_backup/grand-central_wip_2026-04-16_1811/archive/map_v2_pre_zero_overlay_2026-04-16/city_map.json
--------------------------------------------------
{
  "map_id": "grand_central_city_v1",

  "districts": [
    {
      "district_id": "market_core",
      "name": "中央市場区",
      "x": 520,
      "y": 220
    },
    {
      "district_id": "admin_core",
      "name": "行政中枢区",
      "x": 250,
      "y": 300
    },
    {
      "district_id": "east_residential",
      "name": "東住宅区",
      "x": 820,
      "y": 300
    },
    {
      "district_id": "port_logistics",
      "name": "港湾物流区",
      "x": 130,
      "y": 540
    },
    {
      "district_id": "church_oldtown",
      "name": "教会旧市街",
      "x": 760,
      "y": 650
    },
    {
      "district_id": "academy_outer",
      "name": "士官予備区",
      "x": 520,
      "y": 560
    }
  ],

  "locations": [
    {
      "location_id": "loc_central_market",
      "name": "中央市場",
      "district_id": "market_core",
      "x": 520,
      "y": 180
    },
    {
      "location_id": "loc_civic_square",
      "name": "祝祭広場",
      "district_id": "market_core",
      "x": 520,
      "y": 260
    },
    {
      "location_id": "loc_central_hall",
      "name": "中央庁舎",
      "district_id": "admin_core",
      "x": 250,
      "y": 360
    },
    {
      "location_id": "loc_fo_hq",
      "name": "三兵団本部",
      "district_id": "admin_core",
      "x": 220,
      "y": 260
    },
    {
      "location_id": "loc_jade_home",
      "name": "ジェイド居住圏",
      "district_id": "east_residential",
      "x": 860,
      "y": 420
    },
    {
      "location_id": "loc_east_street",
      "name": "東門街道",
      "district_id": "east_residential",
      "x": 820,
      "y": 220
    },
    {
      "location_id": "loc_trade_port",
      "name": "交易港",
      "district_id": "port_logistics",
      "x": 80,
      "y": 470
    },
    {
      "location_id": "loc_magic_flow",
      "name": "魔石流通センター",
      "district_id": "port_logistics",
      "x": 250,
      "y": 700
    },
    {
      "location_id": "loc_star_church",
      "name": "星導教会",
      "district_id": "church_oldtown",
      "x": 720,
      "y": 560
    },
    {
      "location_id": "loc_bell_slope",
      "name": "鐘楼坂",
      "district_id": "church_oldtown",
      "x": 900,
      "y": 560
    },
    {
      "location_id": "loc_underground_entry",
      "name": "地下礼拝路入口",
      "district_id": "church_oldtown",
      "x": 800,
      "y": 760
    },
    {
      "location_id": "loc_training_school",
      "name": "訓練校",
      "district_id": "academy_outer",
      "x": 460,
      "y": 720
    },
    {
      "location_id": "loc_academy_facility",
      "name": "士官学校関連施設",
      "district_id": "academy_outer",
      "x": 560,
      "y": 520
    }
  ],

  "routes": [
    {
      "route_id": "rt_market_to_east",
      "name": "中央市場街道",
      "points": [
        { "x": 520, "y": 320 },
        { "x": 650, "y": 330 },
        { "x": 760, "y": 300 }
      ]
    },
    {
      "route_id": "rt_market_to_admin",
      "name": "北礼拝路",
      "points": [
        { "x": 440, "y": 300 },
        { "x": 360, "y": 260 },
        { "x": 290, "y": 220 }
      ]
    },
    {
      "route_id": "rt_market_to_academy",
      "name": "士官街路",
      "points": [
        { "x": 540, "y": 380 },

==================================================
FILE: ./viewer/db/city/_local_staging_backup/grand-central_wip_2026-04-16_1811/archive/map_v2_pre_zero_overlay_2026-04-16/city_status.json
--------------------------------------------------
{
  "incident": "中央市場炎槍事件",
  "cell": "炎翼セル",
  "members": [
    {
      "id": "C-01",
      "name": "アーク",
      "role": "セルリーダー",
      "role_en": "Cell Leader",
      "status": "escape",
      "status_label": "逃亡・生存",
      "public_info": "国家未把握",
      "future_hook": "上位セル接続担当。ZERO終盤黒幕導線。",
      "threat": "high"
    },
    {
      "id": "R-02",
      "name": "ノクス",
      "role": "儀式中枢",
      "role_en": "Ritual Core",
      "status": "unknown",
      "status_label": "生死不明（生存寄り）",
      "public_info": "死亡扱い",
      "future_hook": "影武者作戦の主導者候補。自害は偽装の可能性。",
      "threat": "high"
    },
    {
      "id": "R-03",
      "name": "セラ",
      "role": "儀式補助",
      "role_en": "Ritual Support",
      "status": "escape",
      "status_label": "逃亡・生存",
      "public_info": "行方不明",
      "future_hook": "ルカとの接点あり。後の証言者候補。迷いを持つ唯一の存在。",
      "threat": "medium"
    },
    {
      "id": "D-04",
      "name": "グラム",
      "role": "爆破担当",
      "role_en": "Demo Unit",
      "status": "captured",
      "status_label": "逮捕（影武者）",
      "public_info": "実行犯として報道・処刑予定",
      "future_hook": "公開尋問の替え玉本命。心音解析で「こいつじゃない」が確定する対象。",
      "threat": "low"
    },
    {
      "id": "D-05",
      "name": "イグナ",
      "role": "火炎拡大",
      "role_en": "Flame Unit",
      "status": "dead",
      "status_label": "死亡",
      "public_info": "戦死扱い",
      "future_hook": "炎翼セルの狂信性を象徴する存在。",
      "threat": "none"
    },
    {
      "id": "S-06",
      "name": "リド",
      "role": "市場監視",
      "role_en": "Market Scout",
      "status": "escape",
      "status_label": "逃亡・生存",
      "public_info": "未特定",
      "future_hook": "元一般人。再登場時に思想崩壊描写が可能な人物。",
      "threat": "medium"
    },
    {
      "id": "S-07",
      "name": "ヴェル",
      "role": "外周警戒",
      "role_en": "Perimeter Guard",
      "status": "escape",
      "status_label": "逃亡・生存",
      "public_info": "未特定",
      "future_hook": "残党ネットワーク維持役。逃走ルートを知る数少ない存在。",
      "threat": "medium"
    },
    {
      "id": "SUPPORT",
      "name": "ルカ",
      "role": "教会側被害者",
      "role_en": "Church Proxy",
      "status": "civilian",
      "status_label": "生存（民間）",
      "public_info": "無関係市民",
      "future_hook": "ペンダント伏線・贖罪ライン。知らずに利用された補助導線。",
      "threat": "none"
    },
    {
      "id": "CHURCH",
      "name": "エリオス",
      "role": "思想協力者",
      "role_en": "Church Collaborator",
      "status": "shadow",
      "status_label": "要監視・黒幕候補",
      "public_info": "神父として平常運用",
      "future_hook": "黒幕候補の中継点。教会地下への接続役。",
      "threat": "high"
    }
  ],
  "fo_involved": [
    {
      "id": "FO-LEAD",
      "name": "ガーネット・ヴァルグレイ",
      "role": "FO隊長",
      "role_en": "FO Commander",
      "action": "聖剣で炎槍を直接防御。路地裏でノクスの魔法陣を一撃破壊。",
      "note": "ジェイドに「人が神の槍を止めた」光景を刻んだ人物。"
    }
  ]
}

==================================================
FILE: ./viewer/db/city/_local_staging_backup/grand-central_wip_2026-04-16_1811/archive/map_v2_pre_zero_overlay_2026-04-16/city_story.json
--------------------------------------------------
{
  "incident": "中央市場炎槍事件",
  "subtitle": "ZERO編プロローグ事件",
  "arc": "中編・後編",
  "scenes": [
    {
      "id": 8,
      "title": "上空の異変",
      "phase": "静寂と予兆",
      "phase_en": "SILENCE",
      "tags": ["異変", "天使の示唆", "伏線"],
      "highlight": false,
      "summary": "炎の粉が逆流し、煙が上へ吸い込まれる。ジェイドだけが違和感を覚える。空の裂け目に、翼に見える巨大な炎影——正体は明示しない。"
    },
    {
      "id": 9,
      "title": "第二波",
      "phase": "静寂と予兆",
      "phase_en": "SILENCE",
      "tags": ["召喚獣", "戦場化", "ZG限界"],
      "highlight": false,
      "summary": "炎翼セル第二手。召喚獣が屋台を薙ぎ倒し市場が完全に戦場化。中央警備隊だけでは抑えきれない。カーネルが子供の泣き声で足を止める。"
    },
    {
      "id": 10,
      "title": "カーネルの選択",
      "phase": "英雄的選択と悲劇",
      "phase_en": "SACRIFICE",
      "tags": ["人格的必然", "姉の決断", "利他"],
      "highlight": false,
      "summary": "「……先にクララを頼む」——倒壊しかけた屋台の陰に迷子の子供を見つけ、迷わず単身向かうカーネル。これが死亡の人格的必然性となる。"
    },
    {
      "id": 11,
      "title": "炎槍の予兆",
      "phase": "英雄的選択と悲劇",
      "phase_en": "SACRIFICE",
      "tags": ["音の消滅", "赤染め", "スローモーション"],
      "highlight": false,
      "summary": "空の裂け目が脈打ち、音が消える。市場全体の色が一瞬だけ赤に染まる。カーネルが子供を抱き上げた瞬間、空から一本の細く鋭い光——。"
    },
    {
      "id": 12,
      "title": "貫通",
      "phase": "英雄的選択と悲劇",
      "phase_en": "SACRIFICE",
      "tags": ["炎槍貫通", "カーネル重傷", "記憶の断片化"],
      "highlight": true,
      "highlight_type": "death",
      "summary": "炎槍がカーネルの胸部を貫く。光が胸を抜け、一拍遅れて血が滲む。子供だけは無傷。倒れる音——ジェイドの記憶はここで断片化する。"
    },
    {
      "id": 13,
      "title": "断末魔",
      "phase": "決別と覚醒の兆し",
      "phase_en": "AWAKENING",
      "tags": ["駆け寄る", "遺言候補", "クララの絶望"],
      "highlight": false,
      "summary": "「ジェイド……大丈夫」「強くなれ」——倒れたカーネルへ駆け寄るジェイド。呼吸がまだ浅く残っている。クララは自分を庇って倒れた現実に言葉を失う。"
    },
    {
      "id": 14,
      "title": "号泣",
      "phase": "決別と覚醒の兆し",
      "phase_en": "AWAKENING",
      "tags": ["音の遮断", "心音異常", "感情フリーズの前段"],
      "highlight": false,
      "summary": "市場の悲鳴は遠く、ジェイドの耳にはほぼ入らない。視界に映るのは姉の顔だけ。「なんで」「いやだ」が言葉にならない。心音が異常に跳ねる——能力覚醒の伏線。"
    },
    {
      "id": 15,
      "title": "黒鎧の介入",
      "phase": "決別と覚醒の兆し",
      "phase_en": "AWAKENING",
      "tags": ["FO登場", "聖剣防御", "神話感"],
      "highlight": true,
      "highlight_type": "pivot",
      "summary": "クララを狙う第二の炎槍——その瞬間、黒い影が割り込む。金属音。聖剣が炎槍を真正面から受け止める。振り返らず一言「下がれ」。ジェイドの目に「人が神の槍を止めた」光景が刻まれる。"
    },
    {
      "id": 16,
      "title": "制圧線",
      "phase": "FOによる制圧",
      "phase_en": "SUPPRESSION",
      "tags": ["FO展開", "避難誘導", "統率"],
      "highlight": false,
      "summary": "FO黒鎧部隊がジェイド周辺を防衛円で確保。隊長が即座に命令——北通路を避難路に指定、火炎術師を召集、残党索敵。ジェイドは初めて国家機関の統率を目撃する。"
    },
    {
      "id": 17,
      "title": "索敵と避難誘導",
      "phase": "FOによる制圧",
      "phase_en": "SUPPRESSION",
      "tags": ["部隊三分割", "儀式核追跡", "担架到着"],
      "highlight": false,
      "summary": "槍の落下角度から「儀式核は地上」と即断。部隊を索敵・避難・救護の三班に分割。後方医療班がようやく到着し、担架が見え始める。ジェイドだけ姉から離れられない。"
    },
    {
      "id": 18,
      "title": "路地裏の発見",
      "phase": "FOによる制圧",
      "phase_en": "SUPPRESSION",
      "tags": ["七芒星儀式痕", "炎翼セル発見", "ノクス"],
      "highlight": false,
      "summary": "市場南側の路地に七芒星の儀式痕。奥にノクスを中心とした黒ローブ複数が魔法陣を維持。セラが魔力供給で苦しみ、リドが外周警戒。「発見。炎翼セル残党」——FO突入。"
    },
    {
      "id": 19,
      "title": "路地裏戦",
      "phase": "FOによる制圧",
      "phase_en": "SUPPRESSION",
      "tags": ["近接戦", "聖剣vs神性炎", "魔法陣破壊"],
      "highlight": false,
      "summary": "イグナの火炎魔法で路地が燃える。ノクスが儀式を維持しながら後退、セラが一瞬迷いを見せる。隊長ガーネットがノクスへ一直線——一撃で魔法陣の核を破壊。上空の裂け目が崩壊し炎槍が停止。"
    },
    {
      "id": 20,
      "title": "制圧完了",
      "phase": "FOによる制圧",
      "phase_en": "SUPPRESSION",
      "tags": ["ノクス自害", "セラ逃走", "熱が冷える"],
      "highlight": false,
      "summary": "逃げ場なし——「秩序は戻る」ノクス自害、イグナ炎で自決。セラだけが生き残る。「中央市場、制圧完了」——事件の熱が一気に冷える。"
    },
    {
      "id": 21,
      "title": "救護区画",
      "phase": "残酷な現実の確定",
      "phase_en": "REALITY",
      "tags": ["死亡宣告", "まだ温かい", "現実の固定"],
      "highlight": true,
      "highlight_type": "death",
      "summary": "医療兵「……脈、なし。死亡確認」——第三者による宣告が現実を固定する。ジェイドの世界の音が、再び遠のく。"
    },
    {
      "id": 22,
      "title": "家族の再合流",
      "phase": "残酷な現実の確定",
      "phase_en": "REALITY",
      "tags": ["母の崩壊", "父の沈黙", "泣けないジェイド"],
      "highlight": false,
      "summary": "クララを抱きしめ、ジェイドを探し、布を掛けられたカーネルを見つける——探す順番が命。母の精神支柱がここで折れる。父は無言で立ち尽くす。ジェイドは泣けない。"
    },
    {
      "id": 23,
      "title": "残党掃討",
      "phase": "政治的幕引き",
      "phase_en": "DECEPTION",
      "tags": ["ヴェル逃走", "セラ行方不明", "替え玉残存"],
      "highlight": false,
      "summary": "FOが路地裏・地下通路で残党を狩る。S-07《ヴェル》逃走、R-03《セラ》行方不明——現場には替え玉だけ残る。事件が終わったと思うジェイド vs 終わっていないと知る読者、このズレ。"
    },
    {
      "id": 24,
      "title": "影武者逮捕",
      "phase": "政治的幕引き",
      "phase_en": "DECEPTION",
      "tags": ["国家アピール", "D-04グラム", "替え玉"],
      "highlight": true,
      "highlight_type": "deception",

==================================================
FILE: ./viewer/db/city/_local_staging_backup/grand-central_wip_2026-04-16_1811/archive/map_v2_pre_zero_overlay_2026-04-16/index.html
--------------------------------------------------
<!DOCTYPE html>
<html lang="ja">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>中央市場炎槍事件 | Meritocracia Terror DB</title>
<link rel="stylesheet" href="./city.css">
</head>
<body>

<div class="terror-shell">

  <!-- ナビ -->
  <a class="back-link" href="../../index.html">← NATION ARCHIVE</a>
  <a class="back-link" href="./map.html" style="margin-left:24px; color:var(--gold-dim);">🗺 TACTICAL MAP →</a>

  <!-- ======================================================
       ① INCIDENT HEADER
       ====================================================== -->
  <section class="incident-header">
    <div class="incident-tag">ZERO編 ／ MERITOCRACIA INCIDENT RECORD</div>
    <h1 class="incident-title" id="incident-title">読み込み中...</h1>
    <p class="incident-sub" id="incident-sub"></p>

    <div class="incident-stats">
      <div class="stat-chip red">
        <span class="stat-label">死者</span>
        <span class="stat-val" id="stat-dead">—</span>
      </div>
      <div class="stat-chip purple">
        <span class="stat-label">逃亡・行方不明</span>
        <span class="stat-val" id="stat-escape">—</span>
      </div>
      <div class="stat-chip amber">
        <span class="stat-label">公式犯人（影武者）</span>
        <span class="stat-val" id="stat-captured">—</span>
      </div>
      <div class="stat-chip">
        <span class="stat-label">Scene</span>
        <span class="stat-val">08 – 30</span>
      </div>
    </div>
  </section>

  <!-- エラー表示 -->
  <div id="error-msg" style="display:none; color:#e05050; font-size:13px; margin-bottom:32px; padding:16px; border:1px solid rgba(139,26,26,0.3); background:rgba(139,26,26,0.08);"></div>

  <!-- ======================================================
       ② STORY TIMELINE VIEW
       ====================================================== -->
  <section class="section-block">
    <div class="section-eyebrow">02 — STORY TIMELINE</div>
    <h2 class="section-title">ストーリータイムライン</h2>
    <div class="timeline" id="timeline-wrap">
      <!-- JS が生成 -->
    </div>
  </section>

  <!-- ======================================================
       ③ CELL STATUS DB
       ====================================================== -->
  <section class="section-block">
    <div class="section-eyebrow">03 — CELL STATUS DATABASE</div>
    <h2 class="section-title">炎翼セル メンバーステータス</h2>

    <!-- 凡例 -->
    <div class="legend" id="status-legend"></div>

    <div class="status-grid" id="status-grid">
      <!-- JS が生成 -->
    </div>
  </section>

  <!-- ======================================================
       ④ LONG FORESHADOW PANEL
       ====================================================== -->
  <section class="section-block">
    <div class="section-eyebrow">04 — LONG FORESHADOW</div>
    <h2 class="section-title">長期伏線 考察DB</h2>

    <div class="foreshadow-grid" id="foreshadow-grid">
      <!-- JS が生成 -->
    </div>
  </section>

</div><!-- /.terror-shell -->

<script src="./city.js"></script>
</body>
</html>

==================================================
FILE: ./viewer/db/city/_local_staging_backup/grand-central_wip_2026-04-16_1811/archive/map_v2_pre_zero_overlay_2026-04-16/map.css
--------------------------------------------------
@import url('https://fonts.googleapis.com/css2?family=Cormorant+Garamond:ital,wght@0,300;0,400;0,600;1,400&family=Noto+Serif+JP:wght@300;400;500&family=JetBrains+Mono:wght@300;400&display=swap');

/* ============================================================
   CSS VARIABLES — dark fantasy + London tube aesthetic
   ============================================================ */
:root {
  --bg:           #060a11;
  --bg2:          #0a1020;
  --bg3:          #0e1628;
  --panel:        #0d1525;
  --panel2:       #111d35;
  --panel-glass:  rgba(13,21,37,0.92);

  --gold:         #c9a961;
  --gold2:        #e8c97a;
  --gold-dim:     #7a6035;
  --gold-glow:    rgba(201,169,97,0.15);

  --text:         #d8ccb4;
  --text2:        #7a6e58;
  --text3:        #4a4030;

  --border:       rgba(201,169,97,0.14);
  --border2:      rgba(201,169,97,0.3);

  /* District palette — muted London-map tones */
  --dist-church:     rgba(120,80,160,0.18);
  --dist-church-s:   rgba(120,80,160,0.35);
  --dist-market:     rgba(200,120,60,0.18);
  --dist-market-s:   rgba(200,120,60,0.35);
  --dist-south:      rgba(80,140,100,0.15);
  --dist-south-s:    rgba(80,140,100,0.30);
  --dist-alley:      rgba(100,100,140,0.16);
  --dist-alley-s:    rgba(100,100,140,0.32);
  --dist-fo:         rgba(60,120,200,0.16);
  --dist-fo-s:       rgba(60,120,200,0.32);
  --dist-civic:      rgba(180,140,80,0.14);
  --dist-civic-s:    rgba(180,140,80,0.30);

  /* Route colors — Tube-line inspired */
  --rt-kernel:       #e05050;
  --rt-luca:         #b080f0;
  --rt-flamewing:    #ff6a33;
  --rt-crowd:        #60d0a0;
  --rt-jade:         #60a8e0;
  --rt-fo:           #4a7fc1;

  /* Pin severity */
  --pin-normal:      #60a8e0;
  --pin-deploy:      #ff6a33;
  --pin-explosion:   #e05050;
  --pin-death:       #8b1a1a;
  --pin-fo:          #4a7fc1;
  --pin-end:         #60d0a0;
  --pin-oath:        #c9a961;

  --sidebar-w:       360px;
  --timeline-h:      72px;
}

/* ============================================================
   RESET & BASE
   ============================================================ */
*, *::before, *::after { margin: 0; padding: 0; box-sizing: border-box; }
html { scroll-behavior: smooth; }

body {
  background: var(--bg);
  color: var(--text);
  font-family: 'Noto Serif JP', serif;
  font-weight: 300;
  line-height: 1.6;
  height: 100vh;
  overflow: hidden;
}

/* ============================================================
   MAIN LAYOUT — sidebar + map + bottom timeline
   ============================================================ */
.map-app {
  display: grid;
  grid-template-columns: var(--sidebar-w) 1fr;
  grid-template-rows: 1fr var(--timeline-h);
  height: 100vh;
  width: 100vw;
}

/* ============================================================
   SIDEBAR (LEFT PANE)
   ============================================================ */
.sidebar {
  grid-row: 1 / 3;
  background: var(--panel);
  border-right: 1px solid var(--border);
  display: flex;
  flex-direction: column;
  overflow: hidden;
  z-index: 10;
}

.sidebar-header {
  padding: 20px 20px 16px;
  border-bottom: 1px solid var(--border);
  flex-shrink: 0;
}

.sidebar-back {
  display: inline-flex;
  align-items: center;
  gap: 6px;
  font-family: 'Cormorant Garamond', serif;
  font-size: 10px;
  letter-spacing: 3px;
  color: var(--text2);
  text-decoration: none;
  transition: color 0.2s;
  margin-bottom: 14px;
}
.sidebar-back:hover { color: var(--gold); }

.sidebar-tag {
  font-family: 'Cormorant Garamond', serif;
  font-size: 9px;
  letter-spacing: 5px;
  color: rgba(224,80,80,0.7);
  text-transform: uppercase;
  margin-bottom: 6px;
}

.sidebar-title {
  font-family: 'Cormorant Garamond', serif;
  font-size: 1.4rem;
  font-weight: 300;
  color: var(--text);
  letter-spacing: 0.06em;
  line-height: 1.3;
  margin-bottom: 4px;
}

.sidebar-subtitle {
  font-family: 'Cormorant Garamond', serif;
  font-size: 11px;
  letter-spacing: 3px;
  color: var(--gold-dim);
}

/* Route toggles */
.sidebar-section {
  padding: 16px 20px;
  border-bottom: 1px solid var(--border);
  flex-shrink: 0;
}

.sidebar-section-label {
  font-family: 'Cormorant Garamond', serif;
  font-size: 9px;
  letter-spacing: 4px;
  color: var(--text2);
  text-transform: uppercase;
  margin-bottom: 10px;

==================================================
FILE: ./viewer/db/city/_local_staging_backup/grand-central_wip_2026-04-16_1811/archive/map_v2_pre_zero_overlay_2026-04-16/map.html
--------------------------------------------------
<!DOCTYPE html>
<html lang="ja">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>中央市場テロ事件 地図 | Meritocracia Terror Map</title>
<meta name="description" content="ZERO編 中央市場炎槍事件の地図ビューア。ロンドン地下鉄路線図の読みやすさ + Googleマップの事件ピンUI + ダークファンタジー世界観を融合。">
<link rel="stylesheet" href="./map.css">
</head>
<body>

<!-- Mobile sidebar toggle -->
<button class="sidebar-toggle" id="sidebar-toggle" aria-label="メニュー">☰</button>

<!-- Mobile backdrop -->
<div class="sidebar-backdrop" id="sidebar-backdrop"></div>

<div class="map-app">

  <!-- ============================================================
       SIDEBAR (LEFT PANE)
       ============================================================ -->
  <aside class="sidebar" id="sidebar">

    <div class="sidebar-header">
      <a class="sidebar-back" href="./index.html">← INCIDENT RECORD</a>
      <div class="sidebar-tag">ZERO編 ／ TACTICAL MAP</div>
      <h1 class="sidebar-title">中央市場炎槍事件</h1>
      <div class="sidebar-subtitle">CENTRAL MARKET INCIDENT MAP</div>
    </div>

    <!-- Route toggles -->
    <div class="sidebar-section">
      <div class="sidebar-section-label">ROUTE LAYERS</div>
      <div id="route-toggles">
        <!-- JS generates -->
      </div>
    </div>

    <!-- Detail panel -->
    <div class="sidebar-detail" id="detail-panel">
      <div class="detail-empty">
        <div class="detail-empty-icon">⊕</div>
        <div class="detail-empty-text">地図上の要素を<br>クリックして詳細表示</div>
      </div>
    </div>

  </aside>

  <!-- ============================================================
       MAP VIEWPORT
       ============================================================ -->
  <div class="map-viewport" id="map-viewport">

    <!-- Grid overlay -->
    <div class="map-grid-overlay"></div>

    <!-- SVG Map container -->
    <div class="map-container">
      <svg class="map-svg" id="map-svg" xmlns="http://www.w3.org/2000/svg"
           viewBox="0 0 1000 1000" preserveAspectRatio="xMidYMid meet">
        <!-- JS renders all layers here -->
      </svg>
    </div>

    <!-- Zoom controls -->
    <div class="zoom-controls">
      <button class="zoom-btn" id="btn-zoom-in" title="ズームイン">+</button>
      <button class="zoom-btn" id="btn-zoom-out" title="ズームアウト">−</button>
      <button class="zoom-btn" id="btn-zoom-reset" title="リセット">⟳</button>
    </div>

    <!-- Coordinate display -->
    <div class="map-coords" id="map-coords">0, 0</div>

    <!-- Map legend -->
    <div class="map-legend">
      <div class="map-legend-title">MAP LEGEND</div>
      <div class="legend-row">
        <span class="legend-pin" style="background:#c9a961"></span>
        <span class="legend-label">スポット (POI)</span>
      </div>
      <div class="legend-row">
        <span class="legend-pin" style="background:#60a8e0"></span>
        <span class="legend-label">平時イベント</span>
      </div>
      <div class="legend-row">
        <span class="legend-pin" style="background:#e05050"></span>
        <span class="legend-label">攻撃イベント</span>
      </div>
      <div class="legend-row">
        <span class="legend-pin" style="background:#8b1a1a"></span>
        <span class="legend-label">死亡</span>
      </div>
      <div class="legend-row">
        <span class="legend-pin" style="background:#4a7fc1"></span>
        <span class="legend-label">FO介入</span>
      </div>
      <div class="legend-row">
        <span class="legend-pin" style="background:#c9a961"></span>
        <span class="legend-label">誓い</span>
      </div>
    </div>

  </div>

  <!-- ============================================================
       TIMELINE BAR (BOTTOM)
       ============================================================ -->
  <div class="timeline-bar">

    <div class="timeline-controls">
      <button class="tl-btn" id="btn-prev" title="前のイベント (←)">◀</button>
      <button class="tl-btn" id="btn-play" title="再生/停止 (Space)">▶</button>
      <button class="tl-btn" id="btn-next" title="次のイベント (→)">▶</button>
    </div>

    <div class="timeline-slider-wrap">
      <span class="timeline-label" id="tl-label">ALL</span>
      <input type="range" class="timeline-slider" id="tl-slider" min="0" max="10" value="0" step="1">
      <span class="timeline-event-name" id="tl-event-name">全イベント表示</span>
    </div>

  </div>

</div><!-- /.map-app -->

<script src="./map.js"></script>
</body>
</html>

==================================================
FILE: ./viewer/db/city/_local_staging_backup/grand-central_wip_2026-04-16_1811/archive/map_v2_pre_zero_overlay_2026-04-16/map.js
--------------------------------------------------
'use strict';

/* ============================================================
   CONSTANTS & CONFIG
   ============================================================ */

/** District visual config — shape polygons & colors */
const DISTRICT_CONFIG = {
  church_quarter: {
    color: 'rgba(120,80,160,0.18)',
    stroke: 'rgba(120,80,160,0.35)',
    poly: [
      [100, 100], [320, 100], [330, 140], [320, 300],
      [280, 310], [100, 290], [90, 200]
    ],
    labelOffset: { dx: 0, dy: -15 }
  },
  market_core: {
    color: 'rgba(200,120,60,0.18)',
    stroke: 'rgba(200,120,60,0.35)',
    poly: [
      [370, 380], [620, 370], [640, 420], [630, 580],
      [580, 600], [370, 590], [350, 520]
    ],
    labelOffset: { dx: 0, dy: -15 }
  },
  south_market_edge: {
    color: 'rgba(80,140,100,0.15)',
    stroke: 'rgba(80,140,100,0.30)',
    poly: [
      [370, 620], [620, 620], [640, 670], [630, 830],
      [560, 850], [370, 840], [350, 750]
    ],
    labelOffset: { dx: 0, dy: -10 }
  },
  alley_belt: {
    color: 'rgba(100,100,140,0.16)',
    stroke: 'rgba(100,100,140,0.32)',
    poly: [
      [660, 380], [870, 400], [880, 450], [870, 600],
      [820, 620], [660, 610], [650, 520]
    ],
    labelOffset: { dx: 0, dy: -10 }
  },
  fo_entry_axis: {
    color: 'rgba(60,120,200,0.16)',
    stroke: 'rgba(60,120,200,0.32)',
    poly: [
      [60, 720], [280, 710], [300, 760], [290, 920],
      [240, 940], [60, 930], [50, 840]
    ],
    labelOffset: { dx: 0, dy: -10 }
  },
  civic_square: {
    color: 'rgba(180,140,80,0.14)',
    stroke: 'rgba(180,140,80,0.30)',
    poly: [
      [700, 720], [940, 710], [960, 770], [950, 920],
      [900, 940], [700, 930], [690, 840]
    ],
    labelOffset: { dx: 0, dy: -10 }
  }
};

/** Route color map */
const ROUTE_COLORS = {
  rt_kernel_move:       '#e05050',
  rt_luca_move:         '#b080f0',
  rt_flamewing_invasion:'#ff6a33',
  rt_crowd_evacuation:  '#60d0a0',
  rt_jade_rescue:       '#60a8e0',
  rt_fo_suppression:    '#4a7fc1'
};

/** Pin color by time order — severity progression */
const PIN_COLORS = {
  1: '#60a8e0',   // 市場通常営業
  2: '#ff6a33',   // 炎翼セル配置
  3: '#e05050',   // 最初の爆発
  4: '#e05050',   // 召喚獣出現
  5: '#ff3030',   // 炎槍
  6: '#8b1a1a',   // カーネル死亡
  7: '#4a7fc1',   // FO到着
  8: '#60d0a0',   // 戦闘終了
  9: '#b080f0',   // 公開尋問
  10: '#c9a961'   // ジェイドの誓い
};

/* ============================================================
   STATE
   ============================================================ */
let mapData = null;
let currentPinIndex = 0;       // 0-based: which pins are "active" (0 = show all up to pin 1)
let isPlaying = false;
let playInterval = null;
let selectedItem = null;       // {type, id, data}
let routeVisibility = {};      // route_id -> boolean

// SVG pan/zoom
let viewBox = { x: 0, y: 0, w: 1000, h: 1000 };
let isDragging = false;
let dragStart = { x: 0, y: 0 };
let dragViewStart = { x: 0, y: 0 };

/* ============================================================
   DOM HELPERS
   ============================================================ */
const svgNS = 'http://www.w3.org/2000/svg';

function svgEl(tag, attrs) {
  const e = document.createElementNS(svgNS, tag);
  if (attrs) for (const [k, v] of Object.entries(attrs)) e.setAttribute(k, v);
  return e;
}

function htmlEl(tag, cls, text) {
  const e = document.createElement(tag);
  if (cls) e.className = cls;
  if (text != null) e.textContent = text;
  return e;
}

/* ============================================================
   FETCH DATA
   ============================================================ */
async function loadMapData() {
  const res = await fetch('./city_map.json');
  if (!res.ok) throw new Error('city_map.json の読み込みに失敗しました');
  return res.json();
}

/* ============================================================
   RENDER: DISTRICTS
   ============================================================ */
function renderDistricts(svg) {
  const g = svgEl('g', { id: 'layer-districts' });

  for (const dist of mapData.districts) {
    const cfg = DISTRICT_CONFIG[dist.district_id];
    if (!cfg) continue;

    const dg = svgEl('g', { class: 'district-group', 'data-district': dist.district_id });

    // Polygon shape
    const points = cfg.poly.map(p => p.join(',')).join(' ');
    const shape = svgEl('polygon', {
      class: 'district-shape',
      points,
      fill: cfg.color,
      stroke: cfg.stroke,
      'stroke-width': '1.5'
    });
    shape.addEventListener('click', () => selectItem('district', dist.district_id, dist));
    dg.appendChild(shape);

    // Center label
    const cx = cfg.poly.reduce((s, p) => s + p[0], 0) / cfg.poly.length + (cfg.labelOffset?.dx || 0);
    const cy = cfg.poly.reduce((s, p) => s + p[1], 0) / cfg.poly.length + (cfg.labelOffset?.dy || 0);

    const label = svgEl('text', {

==================================================
FILE: ./viewer/db/city/_local_staging_backup/grand-central_wip_2026-04-16_1811/city.css
--------------------------------------------------
@import url('https://fonts.googleapis.com/css2?family=Cormorant+Garamond:ital,wght@0,300;0,400;0,600;1,400&family=Noto+Serif+JP:wght@300;400;500&display=swap');

/* ============================================================
   CSS VARIABLES  （terror.css から継承）
   ============================================================ */
:root {
  --bg:           #060a11;
  --bg2:          #0a1020;
  --bg3:          #0e1628;
  --panel:        #0d1525;
  --panel2:       #111d35;

  --gold:         #c9a961;
  --gold2:        #e8c97a;
  --gold-dim:     #7a6035;
  --gold-glow:    rgba(201,169,97,0.15);

  --text:         #d8ccb4;
  --text2:        #7a6e58;
  --text3:        #4a4030;

  --border:       rgba(201,169,97,0.14);
  --border2:      rgba(201,169,97,0.30);
  --border3:      rgba(201,169,97,0.50);

  /* 区画カラー */
  --d-market:     #b8860b;   /* 中央市場区   */
  --d-east:       #3a6e5a;   /* 東住宅区     */
  --d-admin:      #2a4a7a;   /* 行政中枢区   */
  --d-port:       #4a4a6a;   /* 港湾物流区   */
  --d-church:     #7a3a6a;   /* 教会旧市街   */
  --d-officer:    #7a2a2a;   /* 士官予備区   */
}

/* ============================================================
   RESET & BASE
   ============================================================ */
*, *::before, *::after { margin: 0; padding: 0; box-sizing: border-box; }

html { scroll-behavior: smooth; }

body {
  background: var(--bg);
  color: var(--text);
  font-family: 'Noto Serif JP', serif;
  font-weight: 300;
  line-height: 1.8;
  min-height: 100vh;
  overflow-x: hidden;
}

/* 背景テクスチャ */
body::before {
  content: '';
  position: fixed; inset: 0;
  background:
    radial-gradient(ellipse 70% 40% at 50% 0%,   rgba(201,169,97,0.04) 0%, transparent 60%),
    radial-gradient(ellipse 60% 50% at 80% 100%,  rgba(20,40,90,0.12)  0%, transparent 60%),
    repeating-linear-gradient(
      45deg, transparent, transparent 100px,
      rgba(201,169,97,0.010) 100px, rgba(201,169,97,0.010) 101px
    );
  pointer-events: none;
  z-index: 0;
}

/* ============================================================
   SHELL
   ============================================================ */
.city-shell {
  position: relative;
  z-index: 1;
  max-width: 1200px;
  margin: 0 auto;
  padding: 0 24px 100px;
}

/* ============================================================
   ① BACK LINK
   ============================================================ */
.back-link {
  display: inline-flex;
  align-items: center;
  gap: 8px;
  font-family: 'Cormorant Garamond', serif;
  font-size: 11px;
  letter-spacing: 3px;
  color: var(--text2);
  text-decoration: none;
  padding: 20px 0;
  transition: color 0.2s;
}
.back-link:hover { color: var(--gold); }

/* ============================================================
   ② HEADER
   ============================================================ */
.city-header {
  padding: 52px 0 44px;
  border-bottom: 1px solid var(--border);
  margin-bottom: 60px;
  position: relative;
}

/* 金ラインアクセント */
.city-header::after {
  content: '';
  position: absolute;
  bottom: -1px; left: 0;
  width: 140px; height: 1px;
  background: var(--gold);
}

.city-archive-tag {
  font-family: 'Cormorant Garamond', serif;
  font-size: 9px;
  letter-spacing: 6px;
  color: var(--gold-dim);
  text-transform: uppercase;
  margin-bottom: 16px;
}

.city-title-ja {
  font-family: 'Noto Serif JP', serif;
  font-size: clamp(1.8rem, 5vw, 3rem);
  font-weight: 300;
  color: var(--text);
  letter-spacing: 0.12em;
  line-height: 1.3;
  margin-bottom: 8px;
}

.city-title-en {
  font-family: 'Cormorant Garamond', serif;
  font-size: clamp(0.65rem, 1.8vw, 0.85rem);
  letter-spacing: 5px;
  color: var(--gold);
  opacity: 0.65;
  text-transform: uppercase;
  margin-bottom: 28px;
}

.city-meta-row {
  display: flex;
  flex-wrap: wrap;
  gap: 10px;
}

.city-meta-chip {
  font-family: 'Cormorant Garamond', serif;
  font-size: 9px;
  letter-spacing: 3px;
  color: var(--text2);
  padding: 5px 14px;
  border: 1px solid var(--border);
  background: var(--panel);
  text-transform: uppercase;
}

/* ============================================================

==================================================
FILE: ./viewer/db/city/_local_staging_backup/grand-central_wip_2026-04-16_1811/events/zero-market/event-data.json
--------------------------------------------------
{
  "event_id": "zero_market_terror_v1",
  "timeline": [
    {
      "time_order": -30,
      "type": "normal",
      "district_ref": "central_market",
      "route_ref": "south_main_road",
      "pin_ref": "market_open"
    },
    {
      "time_order": 0,
      "type": "explosion",
      "district_ref": "central_market",
      "route_ref": "south_main_road",
      "pin_ref": "blast_origin"
    },
    {
      "time_order": 12,
      "type": "fo_entry",
      "district_ref": "central_market",
      "route_ref": "south_main_road",
      "pin_ref": "fo_entry_gate"
    }
  ]
}
==================================================
FILE: ./viewer/db/city/_local_staging_backup/grand-central_wip_2026-04-16_1811/events/zero-market/replay.js
--------------------------------------------------
"events": [
  {
    "event_id": "zero_market_terror_v1",
    "label": "ZERO 中央市場炎槍事件",
    "link": "../events/zero-market/event-data.json",
    "district_ref": "central_market"
  }
]
==================================================
FILE: ./viewer/db/city/_local_staging_backup/grand-central_wip_2026-04-16_1811/index.html
--------------------------------------------------
<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>グランセントラル都市区画図 | GRAND CENTRAL CITY DISTRICT MAP</title>
  <meta name="description" content="ZERO編〜本編の都市生活圏ベースマップ。中央市場テロ事件・ジェイド生活圏・士官学校導線に対応。">
  <link rel="stylesheet" href="./city.css">
</head>
<body>

<div class="city-shell">

  <!-- ============================================================
       BACK LINK
       ============================================================ -->
  <section id="sec-nav">
    <a class="back-link" href="../index.html">← INCIDENT RECORD</a>
  </section>

  <!-- ============================================================
       ① HEADER
       ============================================================ -->
  <section id="sec-header">
    <header class="city-header">
      <div class="city-archive-tag">City District Archive</div>
      <h1 class="city-title-ja">グランセントラル都市区画図</h1>
      <div class="city-title-en">Grand Central City District Map</div>
      <div class="city-meta-row">
        <span class="city-meta-chip">ZERO編 → 本編</span>
        <span class="city-meta-chip">World: Meritocracia</span>
        <span class="city-meta-chip">Base Map v1.0</span>
      </div>
    </header>
  </section>

  <!-- ============================================================
       ② MAP VIEWER
       ============================================================ -->
  <section id="sec-map" class="section-block">
    <div class="section-eyebrow">District Overview</div>
    <h2 class="section-title">都市全景図</h2>

    <div class="map-frame">
      <div class="map-frame-inner">

        <!-- ★ 画像本体 — ファイルを差し替えるだけで運用可 -->
        <img
          class="map-image"
          src="./grand-central-map.png"
          alt="グランセントラル都市区画図 — 中央市場区・行政中枢区・東住宅区・港湾物流区・教会旧市街・士官予備区"
          loading="eager"
        >

        <!-- POI追加用レイヤー（将来の拡張用 — 現時点は空） -->
        <div class="map-poi-layer" id="poi-layer" aria-hidden="true">
          <!--
            【POI追加手順】
            <button class="poi-pin" style="left:XX%; top:YY%;" title="スポット名">
              <span class="poi-label">スポット名</span>
            </button>
          -->
        </div>

      </div><!-- /.map-frame-inner -->
    </div><!-- /.map-frame -->
  </section>

  <!-- ============================================================
       ③ DISTRICT LEGEND
       ============================================================ -->
  <section id="sec-districts" class="section-block">
    <div class="section-eyebrow">District Index</div>
    <h2 class="section-title">区画一覧</h2>

    <div class="district-grid">

      <!-- 中央市場区 -->
      <article class="district-card" style="--d-color: #b8860b;">
        <div class="district-swatch"></div>
        <div class="district-body">
          <div class="district-name-ja">中央市場区</div>
          <div class="district-name-en">Central Market District</div>
          <p class="district-notes">
            都市の流通・商業の中核。中央市場・祝祭広場を擁し、
            平時は賑わいの象徴。ZERO編「中央市場炎槍事件」の主舞台。
          </p>
          <div class="district-tags">
            <span class="district-tag">MARKET</span>
            <span class="district-tag">INCIDENT ZONE</span>
          </div>
        </div>
      </article>

      <!-- 東住宅区 -->
      <article class="district-card" style="--d-color: #3a6e5a;">
        <div class="district-swatch"></div>
        <div class="district-body">
          <div class="district-name-ja">東住宅区</div>
          <div class="district-name-en">East Residential District</div>
          <p class="district-notes">
            市民の住宅地帯。ジェイド居住圏・パン屋通りを含む。
            本編では日常生活と士官学校通学の生活導線として機能。
          </p>
          <div class="district-tags">
            <span class="district-tag">RESIDENTIAL</span>
            <span class="district-tag">ジェイド生活圏</span>
          </div>
        </div>
      </article>

      <!-- 行政中枢区 -->
      <article class="district-card" style="--d-color: #2a4a7a;">
        <div class="district-swatch"></div>
        <div class="district-body">
          <div class="district-name-ja">行政中枢区</div>
          <div class="district-name-en">Administrative Core District</div>
          <p class="district-notes">
            中央庁舎・三兵団本部・審問庁を擁する権力の中枢。
            ZERO編後半の「公開尋問」導線と国家的幕引きの舞台。
          </p>
          <div class="district-tags">
            <span class="district-tag">GOVERNMENT</span>
            <span class="district-tag">FO本部</span>
          </div>
        </div>
      </article>

      <!-- 港湾物流区 -->
      <article class="district-card" style="--d-color: #4a4a6a;">
        <div class="district-swatch"></div>
        <div class="district-body">
          <div class="district-name-ja">港湾物流区</div>
          <div class="district-name-en">Harbor & Logistics District</div>
          <p class="district-notes">
            交易港・倉庫街・魔石流通センターを抱える物流拠点。
            炎翼セルの資材調達・潜伏ルートとの関連が示唆される。
          </p>
          <div class="district-tags">
            <span class="district-tag">HARBOR</span>
            <span class="district-tag">LOGISTICS</span>
          </div>
        </div>
      </article>

      <!-- 教会旧市街 -->
      <article class="district-card" style="--d-color: #7a3a6a;">
        <div class="district-swatch"></div>
        <div class="district-body">
          <div class="district-name-ja">教会旧市街</div>
          <div class="district-name-en">Old Church Quarter</div>
          <p class="district-notes">
            星導教会・地下礼拝路入口・鐘楼坂を含む旧市街地。
            炎翼セル上位組織の接続拠点・エリオス黒幕候補の活動圏。
          </p>
          <div class="district-tags">
            <span class="district-tag">CHURCH</span>
            <span class="district-tag">SHADOW OPS</span>
          </div>
        </div>

==================================================
FILE: ./viewer/db/city/_local_staging_backup/grand-central_wip_2026-04-16_1811/interactive/city-map.js
--------------------------------------------------
'use strict';

/* ============================================================
   CITY MAP — Grand Central Interactive Navigator
   Vanilla JS, no dependencies.
   ============================================================ */

/* ----------------------------------------------------------
   STATE
   ---------------------------------------------------------- */
const state = {
  data:         null,
  activeFilter: null,   // null = all
  activePoi:    null,   // poi id
  zoom:         1,
  panX:         0,
  panY:         0,
  dragging:     false,
  dragStartX:   0,
  dragStartY:   0,
  dragStartPanX:0,
  dragStartPanY:0,
  imgNaturalW:  0,
  imgNaturalH:  0,
};

const ZOOM_MIN  = 0.4;
const ZOOM_MAX  = 4.0;
const ZOOM_STEP = 0.18;

/* ----------------------------------------------------------
   DOM REFS
   ---------------------------------------------------------- */
const $ = id => document.getElementById(id);

let DOM = {};

/* ----------------------------------------------------------
   BOOT
   ---------------------------------------------------------- */
async function init() {
  DOM = {
    filterBar:    $('filter-bar'),
    poiList:      $('poi-list'),
    viewport:     $('map-viewport'),
    zoomRoot:     $('map-zoom-root'),
    imgWrap:      $('map-img-wrap'),
    mapImage:     $('map-image'),
    pinLayer:     $('poi-pin-layer'),
    detailPanel:  $('detail-panel'),
    detailBar:    $('detail-bar'),
    detailClose:  $('detail-close'),
    coords:       $('map-coords'),
    poiCount:     $('footer-poi-count'),
    sidebarToggle:$('sidebar-toggle'),
    sidebar:      $('sidebar'),
    backdrop:     $('sidebar-backdrop'),
  };

  try {
    const res  = await fetch('./map-data.json');
    if (!res.ok) throw new Error('map-data.json not found');
    state.data = await res.json();
  } catch (e) {
    console.error('[city-map]', e);
    return;
  }

  buildFilters();
  buildPoiList();

  // wait for image to load before placing pins
  if (DOM.mapImage.complete && DOM.mapImage.naturalWidth) {
    onImageLoaded();
  } else {
    DOM.mapImage.addEventListener('load', onImageLoaded, { once: true });
  }

  bindViewport();
  bindDetail();
  bindMobileSidebar();
  updateFooterCount();
}

/* ----------------------------------------------------------
   IMAGE LOADED → place pins & fit
   ---------------------------------------------------------- */
function onImageLoaded() {
  state.imgNaturalW = DOM.mapImage.naturalWidth  || DOM.mapImage.width;
  state.imgNaturalH = DOM.mapImage.naturalHeight || DOM.mapImage.height;

  // Set image rendered size to natural size (zoom handles scaling)
  DOM.mapImage.style.width  = state.imgNaturalW + 'px';
  DOM.mapImage.style.height = state.imgNaturalH + 'px';

  buildPins();
  fitToViewport();
}

/* fit image into viewport with some padding */
function fitToViewport() {
  const vw = DOM.viewport.clientWidth;
  const vh = DOM.viewport.clientHeight;
  const iw = state.imgNaturalW;
  const ih = state.imgNaturalH;
  if (!iw || !ih) return;

  const scaleX = (vw * 0.92) / iw;
  const scaleY = (vh * 0.92) / ih;
  const scale  = Math.min(scaleX, scaleY, 1);

  state.zoom = scale;
  // centre
  state.panX = (vw - iw * scale) / 2;
  state.panY = (vh - ih * scale) / 2;
  applyTransform();
}

/* ----------------------------------------------------------
   FILTER BAR
   ---------------------------------------------------------- */
function buildFilters() {
  const bar = DOM.filterBar;
  const filters = state.data.filters;

  // "ALL" button
  const allBtn = createFilterBtn('ALL', 'all', true);
  bar.appendChild(allBtn);

  filters.forEach(f => {
    const btn = createFilterBtn(f.label_ja, f.id, false);
    bar.appendChild(btn);
  });
}

function createFilterBtn(label, id, active) {
  const btn = document.createElement('button');
  btn.className = 'filter-btn' + (active ? ' active' : '');
  btn.textContent = label;
  btn.dataset.filter = id;
  btn.addEventListener('click', () => onFilterClick(id));
  return btn;
}

function onFilterClick(id) {
  state.activeFilter = (id === 'all') ? null : id;

  // update button states
  DOM.filterBar.querySelectorAll('.filter-btn').forEach(btn => {
    const isActive = (state.activeFilter === null && btn.dataset.filter === 'all')
                  || btn.dataset.filter === state.activeFilter;
    btn.classList.toggle('active', isActive);
  });

  updatePinVisibility();
  updatePoiListVisibility();
  updateFooterCount();
}

/* ----------------------------------------------------------

==================================================
FILE: ./viewer/db/city/_local_staging_backup/grand-central_wip_2026-04-16_1811/interactive/index.html
--------------------------------------------------
<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0, user-scalable=no">
  <title>グランセントラル インタラクティブ地図 | GRAND CENTRAL NAVIGATOR</title>
  <meta name="description" content="ZERO編〜本編 グランセントラル都市区画インタラクティブナビゲータ。POIクリック・ズーム・パン・フィルター対応。">
  <link rel="stylesheet" href="./interactive.css">
</head>
<body>

<div class="map-app">

  <!-- ============================================================
       TOP BAR
       ============================================================ -->
  <header class="topbar">
    <!-- モバイルサイドバートグル -->
    <button class="sidebar-toggle-btn" id="sidebar-toggle" aria-label="サイドバーを開く">☰</button>

    <a class="topbar-back" href="../index.html">← STATIC VIEW</a>

    <div class="topbar-title">
      グランセントラル
      <span>INTERACTIVE NAVIGATOR</span>
    </div>

    <div class="topbar-spacer"></div>

    <!-- フィルターバー -->
    <div class="filter-bar" id="filter-bar">
      <span class="filter-label">FILTER</span>
      <!-- JS が buildFilters() でボタンを生成 -->
    </div>
  </header>

  <!-- ============================================================
       SIDEBAR
       ============================================================ -->
  <aside class="sidebar" id="sidebar">
    <div class="sidebar-header">
      <div class="sidebar-archive-tag">City District Archive</div>
      <div class="sidebar-title">グランセントラル</div>
      <div class="sidebar-subtitle">Grand Central Navigator</div>
    </div>

    <!-- POIリスト -->
    <div class="poi-list-wrap">
      <div id="poi-list">
        <!-- JS が buildPoiList() で生成 -->
      </div>
    </div>
  </aside>

  <!-- ============================================================
       MAP VIEWPORT
       ============================================================ -->
  <div class="map-viewport" id="map-viewport">

    <!-- 座標表示 -->
    <div class="map-coords" id="map-coords">x:— y:—</div>

    <!-- ズームコントロール -->
    <div class="zoom-controls">
      <button class="zoom-btn" id="btn-zoom-in"  title="ズームイン (+)">+</button>
      <button class="zoom-btn" id="btn-zoom-out" title="ズームアウト (−)">−</button>
      <button class="zoom-btn" id="btn-zoom-reset" title="リセット (0)">⟳</button>
    </div>

    <!-- ズームルート（transform はここに適用） -->
    <div id="map-zoom-root" class="map-zoom-root">
      <div id="map-img-wrap" class="map-img-wrap">

        <!-- 地図画像 — 親フォルダの画像を参照 -->
        <img
          id="map-image"
          class="map-image"
          src="../grand-central-map.png"
          alt="グランセントラル都市区画図"
          draggable="false"
        >

        <!-- POIピンレイヤー (JS が pins を生成) -->
        <div id="poi-pin-layer" class="poi-pin-layer"></div>

      </div>
    </div>

    <!-- 詳細パネル -->
    <div class="detail-panel hidden" id="detail-panel">
      <div class="detail-bar" id="detail-bar"></div>
      <button class="detail-close" id="detail-close" aria-label="閉じる">✕</button>
      <div class="detail-body">
        <div class="detail-district" id="detail-district"></div>
        <div class="detail-name"    id="detail-name"></div>
        <div class="detail-name-en" id="detail-name-en"></div>
        <div class="detail-desc"    id="detail-desc"></div>
        <div id="detail-lore"></div>
      </div>
    </div>

  </div><!-- /.map-viewport -->

  <!-- ============================================================
       FOOTER
       ============================================================ -->
  <footer class="map-footer">
    <span class="footer-tag">MERITOCRACIA WORLD ARCHIVE</span>
    <span class="footer-poi-count" id="footer-poi-count">— POI</span>
    <span class="footer-spacer"></span>
    <span class="footer-hint">ホイール / ピンチでズーム　ドラッグでパン　[0] リセット</span>
  </footer>

</div><!-- /.map-app -->

<!-- モバイルサイドバー背景 -->
<div class="sidebar-backdrop" id="sidebar-backdrop"></div>

<script src="./city-map.js"></script>
</body>
</html>

==================================================
FILE: ./viewer/db/city/_local_staging_backup/grand-central_wip_2026-04-16_1811/interactive/interactive.css
--------------------------------------------------
@import url('https://fonts.googleapis.com/css2?family=Cormorant+Garamond:ital,wght@0,300;0,400;0,600;1,400&family=Noto+Serif+JP:wght@300;400;500&display=swap');

/* ============================================================
   CSS VARIABLES  （city.css / terror.css 継承）
   ============================================================ */
:root {
  --bg:           #060a11;
  --bg2:          #0a1020;
  --bg3:          #0e1628;
  --panel:        #0d1525;
  --panel2:       #111d35;
  --panel-glass:  rgba(13,21,37,0.92);

  --gold:         #c9a961;
  --gold2:        #e8c97a;
  --gold-dim:     #7a6035;
  --gold-glow:    rgba(201,169,97,0.15);
  --gold-glow2:   rgba(201,169,97,0.08);

  --text:         #d8ccb4;
  --text2:        #7a6e58;
  --text3:        #4a4030;

  --border:       rgba(201,169,97,0.14);
  --border2:      rgba(201,169,97,0.30);
  --border3:      rgba(201,169,97,0.50);

  --c-dead:       #8b1a1a;
  --c-dead-text:  #e05050;
  --c-fo:         #4a7fc1;
  --c-escape:     #b080f0;

  --sidebar-w:    320px;
  --topbar-h:     52px;
  --footer-h:     44px;
}

/* ============================================================
   RESET & BASE
   ============================================================ */
*, *::before, *::after { margin: 0; padding: 0; box-sizing: border-box; }
html, body { height: 100%; overflow: hidden; }

body {
  background: var(--bg);
  color: var(--text);
  font-family: 'Noto Serif JP', serif;
  font-weight: 300;
  user-select: none;
}

/* ============================================================
   APP SHELL
   ============================================================ */
.map-app {
  display: grid;
  grid-template-rows: var(--topbar-h) 1fr var(--footer-h);
  grid-template-columns: var(--sidebar-w) 1fr;
  grid-template-areas:
    "topbar  topbar"
    "sidebar viewport"
    "sidebar footer";
  height: 100vh;
  width: 100vw;
  overflow: hidden;
}

/* ============================================================
   TOP BAR
   ============================================================ */
.topbar {
  grid-area: topbar;
  display: flex;
  align-items: center;
  gap: 0;
  border-bottom: 1px solid var(--border);
  background: var(--panel);
  position: relative;
  z-index: 20;
  padding: 0 16px;
}

.topbar-back {
  font-family: 'Cormorant Garamond', serif;
  font-size: 10px;
  letter-spacing: 3px;
  color: var(--text2);
  text-decoration: none;
  padding: 0 16px 0 0;
  border-right: 1px solid var(--border);
  margin-right: 16px;
  transition: color 0.2s;
  white-space: nowrap;
}
.topbar-back:hover { color: var(--gold); }

.topbar-title {
  font-family: 'Cormorant Garamond', serif;
  font-size: 13px;
  letter-spacing: 4px;
  color: var(--gold);
  font-weight: 400;
  white-space: nowrap;
  flex-shrink: 0;
}
.topbar-title span {
  color: var(--text2);
  font-size: 10px;
  letter-spacing: 2px;
  margin-left: 10px;
}

.topbar-spacer { flex: 1; }

/* フィルターバー */
.filter-bar {
  display: flex;
  align-items: center;
  gap: 4px;
  flex-shrink: 0;
}

.filter-label {
  font-family: 'Cormorant Garamond', serif;
  font-size: 9px;
  letter-spacing: 3px;
  color: var(--text3);
  text-transform: uppercase;
  margin-right: 6px;
  white-space: nowrap;
}

.filter-btn {
  font-family: 'Cormorant Garamond', serif;
  font-size: 9px;
  letter-spacing: 2px;
  color: var(--text2);
  background: transparent;
  border: 1px solid var(--border);
  padding: 4px 10px;
  cursor: pointer;
  transition: all 0.18s;
  text-transform: uppercase;
  white-space: nowrap;
}
.filter-btn:hover {
  border-color: var(--border2);
  color: var(--text);
}
.filter-btn.active {
  background: var(--gold-glow2);
  border-color: var(--gold-dim);
  color: var(--gold);
}

/* モバイルトグル */
.sidebar-toggle-btn {
  display: none;
  background: none;
  border: 1px solid var(--border);

==================================================
FILE: ./viewer/db/city/_local_staging_backup/grand-central_wip_2026-04-16_1811/interactive/map-data.json
--------------------------------------------------
{
  "meta": {
    "map_id": "grand_central_interactive_v1",
    "title": "グランセントラル都市区画図",
    "title_en": "Grand Central City District Map",
    "image": "../grand-central-map.png",
    "version": "1.0"
  },
  "filters": [
    { "id": "zero",    "label": "ZERO編",    "label_ja": "ZERO編" },
    { "id": "main",    "label": "本編",      "label_ja": "本編" },
    { "id": "officer", "label": "士官学校",  "label_ja": "士官学校" },
    { "id": "church",  "label": "教会バイト","label_ja": "教会バイト" },
    { "id": "harbor",  "label": "港湾物流",  "label_ja": "港湾物流" }
  ],
  "pois": [
    {
      "id": "poi_central_market",
      "name": "中央市場",
      "name_en": "Central Market",
      "district": "中央市場区",
      "x_pct": 47.5,
      "y_pct": 24.0,
      "icon": "◈",
      "color": "#b8860b",
      "filters": ["zero", "main"],
      "description": "都市最大の商業・流通拠点。祝祭広場と隣接し、平時は市民の集う活気ある場所。ZERO編「中央市場炎槍事件」の主舞台となり、炎翼セルによる大規模テロ攻撃が発生した。",
      "lore_zero": "炎翼セル配置→初爆発→召喚獣出現→炎槍発射→カーネル死亡という一連のテロ事件が展開。ジェイドにとって「国家の嘘」と「心音覚醒」の起点となる転換点。",
      "lore_main": "本編ではテロ後の復興が進み、警備体制が強化された状態で登場。事件の痕跡が一部残る。",
      "lore_officer": "士官学校の演習ルート上に位置。市場警備との連携訓練が行われる。",
      "lore_church": null,
      "lore_harbor": null
    },
    {
      "id": "poi_festival_square",
      "name": "祝祭広場",
      "name_en": "Festival Square",
      "district": "中央市場区",
      "x_pct": 47.5,
      "y_pct": 34.5,
      "icon": "◉",
      "color": "#c9a961",
      "filters": ["zero", "main"],
      "description": "中央市場に隣接する市民広場。祭事・公式行事の場として機能する。ZERO編では炎翼セルの爆発波及エリアとなり、多数の市民が避難した。",
      "lore_zero": "初爆地点の延焼エリア。群衆避難ルートの起点。FOによる封鎖ラインが引かれた。",
      "lore_main": "定期市場と祭りが復活。テロ以前の賑わいを取り戻しつつある。",
      "lore_officer": null,
      "lore_church": null,
      "lore_harbor": null
    },
    {
      "id": "poi_star_church",
      "name": "星導教会",
      "name_en": "Star Guide Church",
      "district": "教会旧市街",
      "x_pct": 62.8,
      "y_pct": 55.0,
      "icon": "✦",
      "color": "#9a6aaa",
      "filters": ["zero", "church"],
      "description": "教会旧市街の中心的宗教施設。地下に礼拝堂・儀式広間・秘密通路を持つ。神父エリオスが管理し、炎翼セル上位組織との接続拠点として機能している疑いがある。",
      "lore_zero": "炎翼セル《地下儀式広間》が星導教会地下に存在。Scene30の締め「観測対象、反応あり」「監視継続」の示唆。エリオスが黒幕候補。",
      "lore_main": "表向きは通常運営。ルカのペンダントと七芒星の魔法石が回収伏線として継続する。",
      "lore_officer": null,
      "lore_church": "ルカが教会バイトとして勤務。地下礼拝路の存在を知る数少ない人物となる。",
      "lore_harbor": null
    },
    {
      "id": "poi_underground_entrance",
      "name": "地下礼拝路入口",
      "name_en": "Underground Chapel Entrance",
      "district": "教会旧市街",
      "x_pct": 65.5,
      "y_pct": 67.0,
      "icon": "▼",
      "color": "#7a3a6a",
      "filters": ["zero", "church"],
      "description": "教会旧市街の地下礼拝路への入口。表向きは廃れた礼拝施設への通路だが、炎翼セルの集合・展開拠点として利用されていた。",
      "lore_zero": "炎翼セル侵入軸の出発点。《地下儀式広間》→《ノクス潜伏路地》→《中央市場》という侵入ルートの起点。",
      "lore_main": null,
      "lore_officer": null,
      "lore_church": "ルカが誤って深部まで踏み込んでしまう。ペンダントの遺棄場所に近い。",
      "lore_harbor": null
    },
    {
      "id": "poi_jade_residence",
      "name": "ジェイド居住圏",
      "name_en": "Jade's Residential Area",
      "district": "東住宅区",
      "x_pct": 77.0,
      "y_pct": 41.0,
      "icon": "◆",
      "color": "#3a8e6a",
      "filters": ["zero", "main", "officer"],
      "description": "東住宅区内にあるジェイドの生活拠点。パン屋通りに近く、市場や士官学校への通学路が交差する。ZERO編からZERO編後を通じてジェイドの日常の軸となる場所。",
      "lore_zero": "テロ事件前後のジェイドの帰還地点。事件後の心理的変化・心音覚醒の余波を一人で処理する場。",
      "lore_main": "本編ジェイドの生活ベース。士官学校編の通学起点となる。",
      "lore_officer": "士官学校への最短ルートの出発点。朝の通学シーンの舞台。",
      "lore_church": null,
      "lore_harbor": null
    },
    {
      "id": "poi_bakery_street",
      "name": "パン屋通り",
      "name_en": "Baker's Street",
      "district": "東住宅区",
      "x_pct": 74.0,
      "y_pct": 31.0,
      "icon": "○",
      "color": "#c9a961",
      "filters": ["main", "officer"],
      "description": "東住宅区の商店街。ジェイドが日常的に通る生活導線。地元の人情と市井の空気が色濃く残る場所。",
      "lore_zero": null,
      "lore_main": "本編の日常描写の主舞台のひとつ。クララとの接点が生まれる場所。",
      "lore_officer": "士官学校通学ルートに含まれる。",
      "lore_church": null,
      "lore_harbor": null
    },
    {
      "id": "poi_east_gate",
      "name": "東門街道",
      "name_en": "East Gate Road",
      "district": "東住宅区",
      "x_pct": 80.5,
      "y_pct": 22.5,
      "icon": "→",
      "color": "#5a7a9a",
      "filters": ["main", "officer"],
      "description": "東住宅区の幹線街道。都市外部との主要アクセスルートであり、馬車停留宿が設置されている。",
      "lore_zero": null,
      "lore_main": "都市間移動の出入口として機能。",
      "lore_officer": "士官学校編で外部からの来訪者や訪問イベントに使用される導線。",
      "lore_church": null,
      "lore_harbor": null
    },
    {
      "id": "poi_central_office",
      "name": "中央庁舎",
      "name_en": "Central Government Hall",
      "district": "行政中枢区",
      "x_pct": 28.5,
      "y_pct": 41.0,
      "icon": "▣",
      "color": "#2a5a9a",
      "filters": ["zero", "main"],
      "description": "グランセントラル行政の本丸。三兵団本部と連携し、都市の法執行・政策決定を担う。ZERO編では炎槍事件の「迅速な解決」を演出した国家の意思決定拠点。",
      "lore_zero": "事件を早期収束させた国家の政治的意思がここから発令された。D-04《グラム》を実行犯として処刑予定とした決定もここで行われた疑いがある。",
      "lore_main": "本編では改革派と保守派の政治的対立の場として機能。",
      "lore_officer": null,
      "lore_church": null,
      "lore_harbor": null
    },
    {
      "id": "poi_three_corps_hq",
      "name": "三兵団本部",
      "name_en": "Three Corps HQ",
      "district": "行政中枢区",
      "x_pct": 27.0,
      "y_pct": 30.5,
      "icon": "◈",

==================================================
FILE: ./viewer/db/city/_local_staging_backup/grand-central_wip_2026-04-16_1811/map/city.css
--------------------------------------------------
@import url('https://fonts.googleapis.com/css2?family=Cormorant+Garamond:ital,wght@0,300;0,400;0,600;1,400&family=Noto+Serif+JP:wght@300;400;500&display=swap');

/* ============================================================
   CSS VARIABLES
   ============================================================ */
:root {
  --bg:           #060a11;
  --bg2:          #0a1020;
  --bg3:          #0e1628;
  --panel:        #0d1525;
  --panel2:       #111d35;

  --gold:         #c9a961;
  --gold2:        #e8c97a;
  --gold-dim:     #7a6035;
  --gold-glow:    rgba(201,169,97,0.15);

  --text:         #d8ccb4;
  --text2:        #7a6e58;
  --text3:        #4a4030;

  --border:       rgba(201,169,97,0.14);
  --border2:      rgba(201,169,97,0.3);

  /* ステータスカラー */
  --c-dead:       #8b1a1a;
  --c-dead-glow:  rgba(139,26,26,0.2);
  --c-dead-text:  #e05050;

  --c-captured:   #b8901a;
  --c-captured-glow: rgba(184,144,26,0.2);
  --c-captured-text: #e8c050;

  --c-escape:     #5a2d8a;
  --c-escape-glow: rgba(90,45,138,0.2);
  --c-escape-text: #b080f0;

  --c-civilian:   #1a4a7a;
  --c-civilian-glow: rgba(26,74,122,0.2);
  --c-civilian-text: #60a8e0;

  --c-unknown:    #3a3a4a;
  --c-unknown-glow: rgba(58,58,74,0.3);
  --c-unknown-text: #9090b0;

  --c-shadow:     #0a0a0a;
  --c-shadow-glow: rgba(201,169,97,0.08);
  --c-shadow-text: #c9a961;

  /* ハイライト種別 */
  --hl-death:     rgba(139,26,26,0.12);
  --hl-pivot:     rgba(60,100,180,0.1);
  --hl-deception: rgba(90,45,138,0.12);
  --hl-awakening: rgba(80,160,120,0.1);
  --hl-oath:      rgba(201,169,97,0.1);
}

/* ============================================================
   RESET & BASE
   ============================================================ */
*, *::before, *::after { margin: 0; padding: 0; box-sizing: border-box; }

html { scroll-behavior: smooth; }

body {
  background: var(--bg);
  color: var(--text);
  font-family: 'Noto Serif JP', serif;
  font-weight: 300;
  line-height: 1.8;
  min-height: 100vh;
  overflow-x: hidden;
}

body::before {
  content: '';
  position: fixed; inset: 0;
  background:
    radial-gradient(ellipse 80% 40% at 50% 0%, rgba(139,26,26,0.06) 0%, transparent 60%),
    radial-gradient(ellipse 60% 50% at 80% 100%, rgba(20,40,90,0.12) 0%, transparent 60%),
    repeating-linear-gradient(
      45deg, transparent, transparent 100px,
      rgba(201,169,97,0.012) 100px, rgba(201,169,97,0.012) 101px
    );
  pointer-events: none; z-index: 0;
}

/* ============================================================
   LAYOUT
   ============================================================ */
.terror-shell {
  position: relative; z-index: 1;
  max-width: 1040px;
  margin: 0 auto;
  padding: 0 20px 100px;
}

/* ============================================================
   BACK LINK
   ============================================================ */
.back-link {
  display: inline-flex;
  align-items: center;
  gap: 8px;
  font-family: 'Cormorant Garamond', serif;
  font-size: 11px;
  letter-spacing: 3px;
  color: var(--text2);
  text-decoration: none;
  padding: 20px 0;
  transition: color 0.2s;
}
.back-link:hover { color: var(--gold); }

/* ============================================================
   ① INCIDENT HEADER
   ============================================================ */
.incident-header {
  padding: 56px 0 48px;
  border-bottom: 1px solid var(--border);
  margin-bottom: 64px;
  position: relative;
}

.incident-header::after {
  content: '';
  position: absolute;
  bottom: -1px; left: 0;
  width: 120px; height: 1px;
  background: var(--gold);
}

.incident-tag {
  font-family: 'Cormorant Garamond', serif;
  font-size: 10px;
  letter-spacing: 5px;
  color: var(--c-dead-text);
  opacity: 0.7;
  margin-bottom: 14px;
  text-transform: uppercase;
}

.incident-title {
  font-family: 'Cormorant Garamond', serif;
  font-size: clamp(2.4rem, 7vw, 4.2rem);
  font-weight: 300;
  color: var(--text);
  letter-spacing: 0.08em;
  line-height: 1.2;
  margin-bottom: 6px;
}

.incident-sub {
  font-family: 'Cormorant Garamond', serif;
  font-size: 13px;
  letter-spacing: 4px;
  color: var(--gold);
  opacity: 0.6;
  margin-bottom: 36px;
}

==================================================
FILE: ./viewer/db/city/_local_staging_backup/grand-central_wip_2026-04-16_1811/map/city.js
--------------------------------------------------
'use strict';

/* ============================================================
   CONSTANTS
   ============================================================ */
const STATUS_CONFIG = {
  dead:     { label: '死亡',           dot: '#e05050' },
  captured: { label: '逮捕（影武者）', dot: '#e8c050' },
  escape:   { label: '逃亡・生存',     dot: '#b080f0' },
  civilian: { label: '生存（民間）',   dot: '#60a8e0' },
  unknown:  { label: '生死不明',       dot: '#9090b0' },
  shadow:   { label: '要監視・黒幕候補', dot: '#c9a961' },
};

const PHASE_ORDER = [
  '静寂と予兆',
  '英雄的選択と悲劇',
  '決別と覚醒の兆し',
  'FOによる制圧',
  '残酷な現実の確定',
  '政治的幕引き',
  '誓いへ',
];

const HL_BADGE_LABEL = {
  death:     '★ カーネル死亡',
  pivot:     '◆ 転換点',
  deception: '▲ 国家の欺瞞',
  awakening: '◉ 心音覚醒',
  oath:      '✦ 誓い',
};

const FORESHADOW_DATA = [
  {
    icon: '🔮',
    label: 'ABILITY',
    title: '心音システム初覚醒',
    desc: 'Scene14の異常な心音、Scene25の公開尋問での無自覚発火。「こいつじゃない」という確信がジェイドに生まれる起点。グローリアテストおよび夜化因子判定への直接の伏線。',
    connect: '→ グローリアテスト / 夜化因子 / 能力判定試験',
  },
  {
    icon: '📿',
    label: 'EVIDENCE',
    title: 'ペンダント証拠',
    desc: 'Scene27でカーネルの遺品として発見される七芒星の魔法石ペンダント。触れた瞬間に炎槍の記憶がフラッシュバック。教会バイト編でルカが持っていた物と同一。',
    connect: '→ 教会バイト編回収 / ルカ接点 / 炎翼セル証拠',
  },
  {
    icon: '👦',
    label: 'GUILT',
    title: 'ルカの罪悪感ライン',
    desc: 'Scene28でルカが黒煙を見つめ「ボクは悪くない」と自分に言い聞かせる。ペンダントが消えていることで全てを悟る。クララとは別の「生き残った罪」として作品を厚くする。',
    connect: '→ セラとの再接触 / 証言者候補 / 贖罪ライン',
  },
  {
    icon: '🏛',
    label: 'CONSPIRACY',
    title: '国家による真実隠蔽',
    desc: 'Scene24-26で国家は迅速な「解決」をアピール。D-04《グラム》が実行犯として処刑予定。本当の幹部《アーク》《ヴェル》は逃走、《ノクス》は自害を偽装している可能性がある。',
    connect: '→ グラム公開尋問 / ジェイドの孤独な違和感 / ZERO編エンジン',
  },
  {
    icon: '⛪',
    label: 'CHURCH',
    title: '教会地下黒幕継続',
    desc: 'Scene30の締めで「観測対象、反応あり」「監視継続」が示される。エリオスは神父として平常運用を続けるが、炎翼セル上位セルとの接続役を担う黒幕候補。',
    connect: '→ 炎翼セル上位組織 / エリオス / グローリアテスト編接続',
  },
  {
    icon: '🛡',
    label: 'FO',
    title: 'FOへの憧憬と不信の同居',
    desc: '聖剣で炎槍を止めたガーネットへの憧れ（FOへの憧憬）と、事件を早々に片付けた国家システムへの不信が、ジェイドの中に同時に生まれる。この矛盾がZERO編全体を駆動する。',
    connect: '→ FO入団動機 / 士官学校編 / 国家不信ライン',
  },
];

/* ============================================================
   UTILS
   ============================================================ */
function el(tag, cls, text) {
  const e = document.createElement(tag);
  if (cls) e.className = cls;
  if (text != null) e.textContent = text;
  return e;
}

function stagger(elements, base = 0, step = 40) {
  elements.forEach((e, i) => {
    e.style.animationDelay = `${base + i * step}ms`;
  });
}

/* ============================================================
   FETCH BOTH JSON
   ============================================================ */
async function loadAll() {
  const [storyRes, statusRes] = await Promise.all([
    fetch('./terror_story.json'),
    fetch('./terror_status.json'),
  ]);

  if (!storyRes.ok) throw new Error('terror_story.json の読み込みに失敗しました');
  if (!statusRes.ok) throw new Error('terror_status.json の読み込みに失敗しました');

  return {
    story:  await storyRes.json(),
    status: await statusRes.json(),
  };
}

/* ============================================================
   ① INCIDENT HEADER
   ============================================================ */
function renderHeader(story, status) {
  document.getElementById('incident-title').textContent = story.incident;
  document.getElementById('incident-sub').textContent   = story.subtitle;

  const members  = status.members;
  const dead     = members.filter(m => m.status === 'dead').length;
  const escape   = members.filter(m => m.status === 'escape' || m.status === 'unknown').length;
  const captured = members.filter(m => m.status === 'captured').length;

  document.getElementById('stat-dead').textContent     = dead;
  document.getElementById('stat-escape').textContent   = escape;
  document.getElementById('stat-captured').textContent = captured;
}

/* ============================================================
   ② STORY TIMELINE
   ============================================================ */
function renderTimeline(story) {
  const wrap = document.getElementById('timeline-wrap');
  const scenes = story.scenes;

  // フェーズでグループ化
  const phases = {};
  for (const sc of scenes) {
    if (!phases[sc.phase]) phases[sc.phase] = [];
    phases[sc.phase].push(sc);
  }

  const orderedPhases = PHASE_ORDER.filter(p => phases[p]);

  for (const phaseName of orderedPhases) {
    const group = phases[phaseName];

    // フェーズ区切り
    const divider = document.createElement('div');
    divider.className = 'phase-divider';
    divider.innerHTML = `
      <div class="phase-label">
        <span>${group[0].phase_en}</span>
        <span class="phase-label-ja">${phaseName}</span>
      </div>`;
    wrap.appendChild(divider);

    // シーンカード
    const cards = [];
    for (const sc of group) {

==================================================
FILE: ./viewer/db/city/_local_staging_backup/grand-central_wip_2026-04-16_1811/map/city_map.json
--------------------------------------------------
{
  "map_id": "grand_central_city_v2_final",

  "districts": [
    {
      "district_id": "market_core",
      "name": "中央市場区",
      "x": 520,
      "y": 220
    },
    {
      "district_id": "admin_core",
      "name": "行政中枢区",
      "x": 250,
      "y": 300
    },
    {
      "district_id": "east_residential",
      "name": "東住宅区",
      "x": 820,
      "y": 300
    },
    {
      "district_id": "port_logistics",
      "name": "港湾物流区",
      "x": 130,
      "y": 540
    },
    {
      "district_id": "church_oldtown",
      "name": "教会旧市街",
      "x": 760,
      "y": 650
    },
    {
      "district_id": "academy_outer",
      "name": "士官予備区",
      "x": 520,
      "y": 560
    }
  ],

  "locations": [
    {
      "location_id": "loc_central_market",
      "name": "中央市場",
      "district_id": "market_core",
      "x": 520,
      "y": 180
    },
    {
      "location_id": "loc_civic_square",
      "name": "祝祭広場",
      "district_id": "market_core",
      "x": 520,
      "y": 260
    },
    {
      "location_id": "loc_central_hall",
      "name": "中央庁舎",
      "district_id": "admin_core",
      "x": 250,
      "y": 360
    },
    {
      "location_id": "loc_fo_hq",
      "name": "三兵団本部",
      "district_id": "admin_core",
      "x": 220,
      "y": 260
    },
    {
      "location_id": "loc_jade_home",
      "name": "ジェイド居住圏",
      "district_id": "east_residential",
      "x": 860,
      "y": 420
    },
    {
      "location_id": "loc_east_street",
      "name": "東門街道",
      "district_id": "east_residential",
      "x": 820,
      "y": 220
    },
    {
      "location_id": "loc_trade_port",
      "name": "交易港",
      "district_id": "port_logistics",
      "x": 80,
      "y": 470
    },
    {
      "location_id": "loc_magic_flow",
      "name": "魔石流通センター",
      "district_id": "port_logistics",
      "x": 250,
      "y": 700
    },
    {
      "location_id": "loc_star_church",
      "name": "星導教会",
      "district_id": "church_oldtown",
      "x": 720,
      "y": 560
    },
    {
      "location_id": "loc_bell_slope",
      "name": "鐘楼坂",
      "district_id": "church_oldtown",
      "x": 900,
      "y": 560
    },
    {
      "location_id": "loc_underground_entry",
      "name": "地下礼拝路入口",
      "district_id": "church_oldtown",
      "x": 800,
      "y": 760
    },
    {
      "location_id": "loc_training_school",
      "name": "訓練校",
      "district_id": "academy_outer",
      "x": 460,
      "y": 720
    },
    {
      "location_id": "loc_academy_facility",
      "name": "士官学校関連施設",
      "district_id": "academy_outer",
      "x": 560,
      "y": 520
    }
  ],

  "routes": [
    {
      "route_id": "rt_market_to_east",
      "name": "中央市場街道",
      "points": [
        { "x": 520, "y": 320 },
        { "x": 650, "y": 330 },
        { "x": 760, "y": 300 }
      ]
    },
    {
      "route_id": "rt_market_to_admin",
      "name": "北礼拝路",
      "points": [
        { "x": 440, "y": 300 },
        { "x": 360, "y": 260 },
        { "x": 290, "y": 220 }
      ]
    },
    {
      "route_id": "rt_market_to_academy",
      "name": "士官街路",
      "points": [
        { "x": 540, "y": 380 },

==================================================
FILE: ./viewer/db/city/_local_staging_backup/grand-central_wip_2026-04-16_1811/map/city_status.json
--------------------------------------------------
{
  "incident": "中央市場炎槍事件",
  "cell": "炎翼セル",
  "members": [
    {
      "id": "C-01",
      "name": "アーク",
      "role": "セルリーダー",
      "role_en": "Cell Leader",
      "status": "escape",
      "status_label": "逃亡・生存",
      "public_info": "国家未把握",
      "future_hook": "上位セル接続担当。ZERO終盤黒幕導線。",
      "threat": "high"
    },
    {
      "id": "R-02",
      "name": "ノクス",
      "role": "儀式中枢",
      "role_en": "Ritual Core",
      "status": "unknown",
      "status_label": "生死不明（生存寄り）",
      "public_info": "死亡扱い",
      "future_hook": "影武者作戦の主導者候補。自害は偽装の可能性。",
      "threat": "high"
    },
    {
      "id": "R-03",
      "name": "セラ",
      "role": "儀式補助",
      "role_en": "Ritual Support",
      "status": "escape",
      "status_label": "逃亡・生存",
      "public_info": "行方不明",
      "future_hook": "ルカとの接点あり。後の証言者候補。迷いを持つ唯一の存在。",
      "threat": "medium"
    },
    {
      "id": "D-04",
      "name": "グラム",
      "role": "爆破担当",
      "role_en": "Demo Unit",
      "status": "captured",
      "status_label": "逮捕（影武者）",
      "public_info": "実行犯として報道・処刑予定",
      "future_hook": "公開尋問の替え玉本命。心音解析で「こいつじゃない」が確定する対象。",
      "threat": "low"
    },
    {
      "id": "D-05",
      "name": "イグナ",
      "role": "火炎拡大",
      "role_en": "Flame Unit",
      "status": "dead",
      "status_label": "死亡",
      "public_info": "戦死扱い",
      "future_hook": "炎翼セルの狂信性を象徴する存在。",
      "threat": "none"
    },
    {
      "id": "S-06",
      "name": "リド",
      "role": "市場監視",
      "role_en": "Market Scout",
      "status": "escape",
      "status_label": "逃亡・生存",
      "public_info": "未特定",
      "future_hook": "元一般人。再登場時に思想崩壊描写が可能な人物。",
      "threat": "medium"
    },
    {
      "id": "S-07",
      "name": "ヴェル",
      "role": "外周警戒",
      "role_en": "Perimeter Guard",
      "status": "escape",
      "status_label": "逃亡・生存",
      "public_info": "未特定",
      "future_hook": "残党ネットワーク維持役。逃走ルートを知る数少ない存在。",
      "threat": "medium"
    },
    {
      "id": "SUPPORT",
      "name": "ルカ",
      "role": "教会側被害者",
      "role_en": "Church Proxy",
      "status": "civilian",
      "status_label": "生存（民間）",
      "public_info": "無関係市民",
      "future_hook": "ペンダント伏線・贖罪ライン。知らずに利用された補助導線。",
      "threat": "none"
    },
    {
      "id": "CHURCH",
      "name": "エリオス",
      "role": "思想協力者",
      "role_en": "Church Collaborator",
      "status": "shadow",
      "status_label": "要監視・黒幕候補",
      "public_info": "神父として平常運用",
      "future_hook": "黒幕候補の中継点。教会地下への接続役。",
      "threat": "high"
    }
  ],
  "fo_involved": [
    {
      "id": "FO-LEAD",
      "name": "ガーネット・ヴァルグレイ",
      "role": "FO隊長",
      "role_en": "FO Commander",
      "action": "聖剣で炎槍を直接防御。路地裏でノクスの魔法陣を一撃破壊。",
      "note": "ジェイドに「人が神の槍を止めた」光景を刻んだ人物。"
    }
  ]
}

==================================================
FILE: ./viewer/db/city/_local_staging_backup/grand-central_wip_2026-04-16_1811/map/city_story.json
--------------------------------------------------
{
  "incident": "中央市場炎槍事件",
  "subtitle": "ZERO編プロローグ事件",
  "arc": "中編・後編",
  "scenes": [
    {
      "id": 8,
      "title": "上空の異変",
      "phase": "静寂と予兆",
      "phase_en": "SILENCE",
      "tags": ["異変", "天使の示唆", "伏線"],
      "highlight": false,
      "summary": "炎の粉が逆流し、煙が上へ吸い込まれる。ジェイドだけが違和感を覚える。空の裂け目に、翼に見える巨大な炎影——正体は明示しない。"
    },
    {
      "id": 9,
      "title": "第二波",
      "phase": "静寂と予兆",
      "phase_en": "SILENCE",
      "tags": ["召喚獣", "戦場化", "ZG限界"],
      "highlight": false,
      "summary": "炎翼セル第二手。召喚獣が屋台を薙ぎ倒し市場が完全に戦場化。中央警備隊だけでは抑えきれない。カーネルが子供の泣き声で足を止める。"
    },
    {
      "id": 10,
      "title": "カーネルの選択",
      "phase": "英雄的選択と悲劇",
      "phase_en": "SACRIFICE",
      "tags": ["人格的必然", "姉の決断", "利他"],
      "highlight": false,
      "summary": "「……先にクララを頼む」——倒壊しかけた屋台の陰に迷子の子供を見つけ、迷わず単身向かうカーネル。これが死亡の人格的必然性となる。"
    },
    {
      "id": 11,
      "title": "炎槍の予兆",
      "phase": "英雄的選択と悲劇",
      "phase_en": "SACRIFICE",
      "tags": ["音の消滅", "赤染め", "スローモーション"],
      "highlight": false,
      "summary": "空の裂け目が脈打ち、音が消える。市場全体の色が一瞬だけ赤に染まる。カーネルが子供を抱き上げた瞬間、空から一本の細く鋭い光——。"
    },
    {
      "id": 12,
      "title": "貫通",
      "phase": "英雄的選択と悲劇",
      "phase_en": "SACRIFICE",
      "tags": ["炎槍貫通", "カーネル重傷", "記憶の断片化"],
      "highlight": true,
      "highlight_type": "death",
      "summary": "炎槍がカーネルの胸部を貫く。光が胸を抜け、一拍遅れて血が滲む。子供だけは無傷。倒れる音——ジェイドの記憶はここで断片化する。"
    },
    {
      "id": 13,
      "title": "断末魔",
      "phase": "決別と覚醒の兆し",
      "phase_en": "AWAKENING",
      "tags": ["駆け寄る", "遺言候補", "クララの絶望"],
      "highlight": false,
      "summary": "「ジェイド……大丈夫」「強くなれ」——倒れたカーネルへ駆け寄るジェイド。呼吸がまだ浅く残っている。クララは自分を庇って倒れた現実に言葉を失う。"
    },
    {
      "id": 14,
      "title": "号泣",
      "phase": "決別と覚醒の兆し",
      "phase_en": "AWAKENING",
      "tags": ["音の遮断", "心音異常", "感情フリーズの前段"],
      "highlight": false,
      "summary": "市場の悲鳴は遠く、ジェイドの耳にはほぼ入らない。視界に映るのは姉の顔だけ。「なんで」「いやだ」が言葉にならない。心音が異常に跳ねる——能力覚醒の伏線。"
    },
    {
      "id": 15,
      "title": "黒鎧の介入",
      "phase": "決別と覚醒の兆し",
      "phase_en": "AWAKENING",
      "tags": ["FO登場", "聖剣防御", "神話感"],
      "highlight": true,
      "highlight_type": "pivot",
      "summary": "クララを狙う第二の炎槍——その瞬間、黒い影が割り込む。金属音。聖剣が炎槍を真正面から受け止める。振り返らず一言「下がれ」。ジェイドの目に「人が神の槍を止めた」光景が刻まれる。"
    },
    {
      "id": 16,
      "title": "制圧線",
      "phase": "FOによる制圧",
      "phase_en": "SUPPRESSION",
      "tags": ["FO展開", "避難誘導", "統率"],
      "highlight": false,
      "summary": "FO黒鎧部隊がジェイド周辺を防衛円で確保。隊長が即座に命令——北通路を避難路に指定、火炎術師を召集、残党索敵。ジェイドは初めて国家機関の統率を目撃する。"
    },
    {
      "id": 17,
      "title": "索敵と避難誘導",
      "phase": "FOによる制圧",
      "phase_en": "SUPPRESSION",
      "tags": ["部隊三分割", "儀式核追跡", "担架到着"],
      "highlight": false,
      "summary": "槍の落下角度から「儀式核は地上」と即断。部隊を索敵・避難・救護の三班に分割。後方医療班がようやく到着し、担架が見え始める。ジェイドだけ姉から離れられない。"
    },
    {
      "id": 18,
      "title": "路地裏の発見",
      "phase": "FOによる制圧",
      "phase_en": "SUPPRESSION",
      "tags": ["七芒星儀式痕", "炎翼セル発見", "ノクス"],
      "highlight": false,
      "summary": "市場南側の路地に七芒星の儀式痕。奥にノクスを中心とした黒ローブ複数が魔法陣を維持。セラが魔力供給で苦しみ、リドが外周警戒。「発見。炎翼セル残党」——FO突入。"
    },
    {
      "id": 19,
      "title": "路地裏戦",
      "phase": "FOによる制圧",
      "phase_en": "SUPPRESSION",
      "tags": ["近接戦", "聖剣vs神性炎", "魔法陣破壊"],
      "highlight": false,
      "summary": "イグナの火炎魔法で路地が燃える。ノクスが儀式を維持しながら後退、セラが一瞬迷いを見せる。隊長ガーネットがノクスへ一直線——一撃で魔法陣の核を破壊。上空の裂け目が崩壊し炎槍が停止。"
    },
    {
      "id": 20,
      "title": "制圧完了",
      "phase": "FOによる制圧",
      "phase_en": "SUPPRESSION",
      "tags": ["ノクス自害", "セラ逃走", "熱が冷える"],
      "highlight": false,
      "summary": "逃げ場なし——「秩序は戻る」ノクス自害、イグナ炎で自決。セラだけが生き残る。「中央市場、制圧完了」——事件の熱が一気に冷える。"
    },
    {
      "id": 21,
      "title": "救護区画",
      "phase": "残酷な現実の確定",
      "phase_en": "REALITY",
      "tags": ["死亡宣告", "まだ温かい", "現実の固定"],
      "highlight": true,
      "highlight_type": "death",
      "summary": "医療兵「……脈、なし。死亡確認」——第三者による宣告が現実を固定する。ジェイドの世界の音が、再び遠のく。"
    },
    {
      "id": 22,
      "title": "家族の再合流",
      "phase": "残酷な現実の確定",
      "phase_en": "REALITY",
      "tags": ["母の崩壊", "父の沈黙", "泣けないジェイド"],
      "highlight": false,
      "summary": "クララを抱きしめ、ジェイドを探し、布を掛けられたカーネルを見つける——探す順番が命。母の精神支柱がここで折れる。父は無言で立ち尽くす。ジェイドは泣けない。"
    },
    {
      "id": 23,
      "title": "残党掃討",
      "phase": "政治的幕引き",
      "phase_en": "DECEPTION",
      "tags": ["ヴェル逃走", "セラ行方不明", "替え玉残存"],
      "highlight": false,
      "summary": "FOが路地裏・地下通路で残党を狩る。S-07《ヴェル》逃走、R-03《セラ》行方不明——現場には替え玉だけ残る。事件が終わったと思うジェイド vs 終わっていないと知る読者、このズレ。"
    },
    {
      "id": 24,
      "title": "影武者逮捕",
      "phase": "政治的幕引き",
      "phase_en": "DECEPTION",
      "tags": ["国家アピール", "D-04グラム", "替え玉"],
      "highlight": true,
      "highlight_type": "deception",

==================================================
FILE: ./viewer/db/city/_local_staging_backup/grand-central_wip_2026-04-16_1811/map/index.html
--------------------------------------------------
<!DOCTYPE html>
<html lang="ja">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>中央市場炎槍事件 | Meritocracia Terror DB</title>
<link rel="stylesheet" href="./city.css">
</head>
<body>

<div class="terror-shell">

  <!-- ナビ -->
  <a class="back-link" href="../../index.html">← NATION ARCHIVE</a>
  <a class="back-link" href="./map.html" style="margin-left:24px; color:var(--gold-dim);">🗺 TACTICAL MAP →</a>

  <!-- ======================================================
       ① INCIDENT HEADER
       ====================================================== -->
  <section class="incident-header">
    <div class="incident-tag">ZERO編 ／ MERITOCRACIA INCIDENT RECORD</div>
    <h1 class="incident-title" id="incident-title">読み込み中...</h1>
    <p class="incident-sub" id="incident-sub"></p>

    <div class="incident-stats">
      <div class="stat-chip red">
        <span class="stat-label">死者</span>
        <span class="stat-val" id="stat-dead">—</span>
      </div>
      <div class="stat-chip purple">
        <span class="stat-label">逃亡・行方不明</span>
        <span class="stat-val" id="stat-escape">—</span>
      </div>
      <div class="stat-chip amber">
        <span class="stat-label">公式犯人（影武者）</span>
        <span class="stat-val" id="stat-captured">—</span>
      </div>
      <div class="stat-chip">
        <span class="stat-label">Scene</span>
        <span class="stat-val">08 – 30</span>
      </div>
    </div>
  </section>

  <!-- エラー表示 -->
  <div id="error-msg" style="display:none; color:#e05050; font-size:13px; margin-bottom:32px; padding:16px; border:1px solid rgba(139,26,26,0.3); background:rgba(139,26,26,0.08);"></div>

  <!-- ======================================================
       ② STORY TIMELINE VIEW
       ====================================================== -->
  <section class="section-block">
    <div class="section-eyebrow">02 — STORY TIMELINE</div>
    <h2 class="section-title">ストーリータイムライン</h2>
    <div class="timeline" id="timeline-wrap">
      <!-- JS が生成 -->
    </div>
  </section>

  <!-- ======================================================
       ③ CELL STATUS DB
       ====================================================== -->
  <section class="section-block">
    <div class="section-eyebrow">03 — CELL STATUS DATABASE</div>
    <h2 class="section-title">炎翼セル メンバーステータス</h2>

    <!-- 凡例 -->
    <div class="legend" id="status-legend"></div>

    <div class="status-grid" id="status-grid">
      <!-- JS が生成 -->
    </div>
  </section>

  <!-- ======================================================
       ④ LONG FORESHADOW PANEL
       ====================================================== -->
  <section class="section-block">
    <div class="section-eyebrow">04 — LONG FORESHADOW</div>
    <h2 class="section-title">長期伏線 考察DB</h2>

    <div class="foreshadow-grid" id="foreshadow-grid">
      <!-- JS が生成 -->
    </div>
  </section>

</div><!-- /.terror-shell -->

<script src="./city.js"></script>
</body>
</html>

==================================================
FILE: ./viewer/db/city/_local_staging_backup/grand-central_wip_2026-04-16_1811/map/map.css
--------------------------------------------------
@import url('https://fonts.googleapis.com/css2?family=Cormorant+Garamond:ital,wght@0,300;0,400;0,600;1,400&family=Noto+Serif+JP:wght@300;400;500&family=JetBrains+Mono:wght@300;400&display=swap');

/* ============================================================
   CSS VARIABLES — dark fantasy + London tube aesthetic
   ============================================================ */
:root {
  --bg:           #060a11;
  --bg2:          #0a1020;
  --bg3:          #0e1628;
  --panel:        #0d1525;
  --panel2:       #111d35;
  --panel-glass:  rgba(13,21,37,0.92);

  --gold:         #c9a961;
  --gold2:        #e8c97a;
  --gold-dim:     #7a6035;
  --gold-glow:    rgba(201,169,97,0.15);

  --text:         #d8ccb4;
  --text2:        #7a6e58;
  --text3:        #4a4030;

  --border:       rgba(201,169,97,0.14);
  --border2:      rgba(201,169,97,0.3);

  /* District palette — muted London-map tones */
  --dist-church:     rgba(120,80,160,0.18);
  --dist-church-s:   rgba(120,80,160,0.35);
  --dist-market:     rgba(200,120,60,0.18);
  --dist-market-s:   rgba(200,120,60,0.35);
  --dist-south:      rgba(80,140,100,0.15);
  --dist-south-s:    rgba(80,140,100,0.30);
  --dist-alley:      rgba(100,100,140,0.16);
  --dist-alley-s:    rgba(100,100,140,0.32);
  --dist-fo:         rgba(60,120,200,0.16);
  --dist-fo-s:       rgba(60,120,200,0.32);
  --dist-civic:      rgba(180,140,80,0.14);
  --dist-civic-s:    rgba(180,140,80,0.30);

  /* Route colors — Tube-line inspired */
  --rt-kernel:       #e05050;
  --rt-luca:         #b080f0;
  --rt-flamewing:    #ff6a33;
  --rt-crowd:        #60d0a0;
  --rt-jade:         #60a8e0;
  --rt-fo:           #4a7fc1;

  /* Pin severity */
  --pin-normal:      #60a8e0;
  --pin-deploy:      #ff6a33;
  --pin-explosion:   #e05050;
  --pin-death:       #8b1a1a;
  --pin-fo:          #4a7fc1;
  --pin-end:         #60d0a0;
  --pin-oath:        #c9a961;

  --sidebar-w:       360px;
  --timeline-h:      72px;
}

/* ============================================================
   RESET & BASE
   ============================================================ */
*, *::before, *::after { margin: 0; padding: 0; box-sizing: border-box; }
html { scroll-behavior: smooth; }

body {
  background: var(--bg);
  color: var(--text);
  font-family: 'Noto Serif JP', serif;
  font-weight: 300;
  line-height: 1.6;
  height: 100vh;
  overflow: hidden;
}

/* ============================================================
   MAIN LAYOUT — sidebar + map + bottom timeline
   ============================================================ */
.map-app {
  display: grid;
  grid-template-columns: var(--sidebar-w) 1fr;
  grid-template-rows: 1fr var(--timeline-h);
  height: 100vh;
  width: 100vw;
}

/* ============================================================
   SIDEBAR (LEFT PANE)
   ============================================================ */
.sidebar {
  grid-row: 1 / 3;
  background: var(--panel);
  border-right: 1px solid var(--border);
  display: flex;
  flex-direction: column;
  overflow: hidden;
  z-index: 10;
}

.sidebar-header {
  padding: 20px 20px 16px;
  border-bottom: 1px solid var(--border);
  flex-shrink: 0;
}

.sidebar-back {
  display: inline-flex;
  align-items: center;
  gap: 6px;
  font-family: 'Cormorant Garamond', serif;
  font-size: 10px;
  letter-spacing: 3px;
  color: var(--text2);
  text-decoration: none;
  transition: color 0.2s;
  margin-bottom: 14px;
}
.sidebar-back:hover { color: var(--gold); }

.sidebar-tag {
  font-family: 'Cormorant Garamond', serif;
  font-size: 9px;
  letter-spacing: 5px;
  color: rgba(224,80,80,0.7);
  text-transform: uppercase;
  margin-bottom: 6px;
}

.sidebar-title {
  font-family: 'Cormorant Garamond', serif;
  font-size: 1.4rem;
  font-weight: 300;
  color: var(--text);
  letter-spacing: 0.06em;
  line-height: 1.3;
  margin-bottom: 4px;
}

.sidebar-subtitle {
  font-family: 'Cormorant Garamond', serif;
  font-size: 11px;
  letter-spacing: 3px;
  color: var(--gold-dim);
}

/* Route toggles */
.sidebar-section {
  padding: 16px 20px;
  border-bottom: 1px solid var(--border);
  flex-shrink: 0;
}

.sidebar-section-label {
  font-family: 'Cormorant Garamond', serif;
  font-size: 9px;
  letter-spacing: 4px;
  color: var(--text2);
  text-transform: uppercase;
  margin-bottom: 10px;

==================================================
FILE: ./viewer/db/city/_local_staging_backup/grand-central_wip_2026-04-16_1811/map/map.html
--------------------------------------------------
<!DOCTYPE html>
<html lang="ja">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>中央市場テロ事件 地図 | Meritocracia Terror Map</title>
<meta name="description" content="ZERO編 中央市場炎槍事件の地図ビューア。ロンドン地下鉄路線図の読みやすさ + Googleマップの事件ピンUI + ダークファンタジー世界観を融合。">
<link rel="stylesheet" href="./map.css">
</head>
<body>

<!-- Mobile sidebar toggle -->
<button class="sidebar-toggle" id="sidebar-toggle" aria-label="メニュー">☰</button>

<!-- Mobile backdrop -->
<div class="sidebar-backdrop" id="sidebar-backdrop"></div>

<div class="map-app">

  <!-- ============================================================
       SIDEBAR (LEFT PANE)
       ============================================================ -->
  <aside class="sidebar" id="sidebar">

    <div class="sidebar-header">
      <a class="sidebar-back" href="./index.html">← INCIDENT RECORD</a>
      <div class="sidebar-tag">ZERO編 ／ TACTICAL MAP</div>
      <h1 class="sidebar-title">中央市場炎槍事件</h1>
      <div class="sidebar-subtitle">CENTRAL MARKET INCIDENT MAP</div>
    </div>

    <!-- Route toggles -->
    <div class="sidebar-section">
      <div class="sidebar-section-label">ROUTE LAYERS</div>
      <div id="route-toggles">
        <!-- JS generates -->
      </div>
    </div>

    <!-- Detail panel -->
    <div class="sidebar-detail" id="detail-panel">
      <div class="detail-empty">
        <div class="detail-empty-icon">⊕</div>
        <div class="detail-empty-text">地図上の要素を<br>クリックして詳細表示</div>
      </div>
    </div>

  </aside>

  <!-- ============================================================
       MAP VIEWPORT
       ============================================================ -->
  <div class="map-viewport" id="map-viewport">

    <!-- Grid overlay -->
    <div class="map-grid-overlay"></div>

    <!-- SVG Map container -->
    <div class="map-container">
      <svg class="map-svg" id="map-svg" xmlns="http://www.w3.org/2000/svg"
           viewBox="0 0 1000 1000" preserveAspectRatio="xMidYMid meet">
        <!-- JS renders all layers here -->
      </svg>
    </div>

    <!-- Zoom controls -->
    <div class="zoom-controls">
      <button class="zoom-btn" id="btn-zoom-in" title="ズームイン">+</button>
      <button class="zoom-btn" id="btn-zoom-out" title="ズームアウト">−</button>
      <button class="zoom-btn" id="btn-zoom-reset" title="リセット">⟳</button>
    </div>

    <!-- Coordinate display -->
    <div class="map-coords" id="map-coords">0, 0</div>

    <!-- Map legend -->
    <div class="map-legend">
      <div class="map-legend-title">MAP LEGEND</div>
      <div class="legend-row">
        <span class="legend-pin" style="background:#c9a961"></span>
        <span class="legend-label">スポット (POI)</span>
      </div>
      <div class="legend-row">
        <span class="legend-pin" style="background:#60a8e0"></span>
        <span class="legend-label">平時イベント</span>
      </div>
      <div class="legend-row">
        <span class="legend-pin" style="background:#e05050"></span>
        <span class="legend-label">攻撃イベント</span>
      </div>
      <div class="legend-row">
        <span class="legend-pin" style="background:#8b1a1a"></span>
        <span class="legend-label">死亡</span>
      </div>
      <div class="legend-row">
        <span class="legend-pin" style="background:#4a7fc1"></span>
        <span class="legend-label">FO介入</span>
      </div>
      <div class="legend-row">
        <span class="legend-pin" style="background:#c9a961"></span>
        <span class="legend-label">誓い</span>
      </div>
    </div>

  </div>

  <!-- ============================================================
       TIMELINE BAR (BOTTOM)
       ============================================================ -->
  <div class="timeline-bar">

    <div class="timeline-controls">
      <button class="tl-btn" id="btn-prev" title="前のイベント (←)">◀</button>
      <button class="tl-btn" id="btn-play" title="再生/停止 (Space)">▶</button>
      <button class="tl-btn" id="btn-next" title="次のイベント (→)">▶</button>
    </div>

    <div class="timeline-slider-wrap">
      <span class="timeline-label" id="tl-label">ALL</span>
      <input type="range" class="timeline-slider" id="tl-slider" min="0" max="10" value="0" step="1">
      <span class="timeline-event-name" id="tl-event-name">全イベント表示</span>
    </div>

  </div>

</div><!-- /.map-app -->

<script src="./map.js"></script>
</body>
</html>

==================================================
FILE: ./viewer/db/city/_local_staging_backup/grand-central_wip_2026-04-16_1811/map/map.js
--------------------------------------------------
'use strict';

/* ============================================================
   CONSTANTS & CONFIG
   ============================================================ */

/** District visual config — shape polygons & colors */
const DISTRICT_CONFIG = {
  church_quarter: {
    color: 'rgba(120,80,160,0.18)',
    stroke: 'rgba(120,80,160,0.35)',
    poly: [
      [100, 100], [320, 100], [330, 140], [320, 300],
      [280, 310], [100, 290], [90, 200]
    ],
    labelOffset: { dx: 0, dy: -15 }
  },
  market_core: {
    color: 'rgba(200,120,60,0.18)',
    stroke: 'rgba(200,120,60,0.35)',
    poly: [
      [370, 380], [620, 370], [640, 420], [630, 580],
      [580, 600], [370, 590], [350, 520]
    ],
    labelOffset: { dx: 0, dy: -15 }
  },
  south_market_edge: {
    color: 'rgba(80,140,100,0.15)',
    stroke: 'rgba(80,140,100,0.30)',
    poly: [
      [370, 620], [620, 620], [640, 670], [630, 830],
      [560, 850], [370, 840], [350, 750]
    ],
    labelOffset: { dx: 0, dy: -10 }
  },
  alley_belt: {
    color: 'rgba(100,100,140,0.16)',
    stroke: 'rgba(100,100,140,0.32)',
    poly: [
      [660, 380], [870, 400], [880, 450], [870, 600],
      [820, 620], [660, 610], [650, 520]
    ],
    labelOffset: { dx: 0, dy: -10 }
  },
  fo_entry_axis: {
    color: 'rgba(60,120,200,0.16)',
    stroke: 'rgba(60,120,200,0.32)',
    poly: [
      [60, 720], [280, 710], [300, 760], [290, 920],
      [240, 940], [60, 930], [50, 840]
    ],
    labelOffset: { dx: 0, dy: -10 }
  },
  civic_square: {
    color: 'rgba(180,140,80,0.14)',
    stroke: 'rgba(180,140,80,0.30)',
    poly: [
      [700, 720], [940, 710], [960, 770], [950, 920],
      [900, 940], [700, 930], [690, 840]
    ],
    labelOffset: { dx: 0, dy: -10 }
  }
};

/** Route color map */
const ROUTE_COLORS = {
  rt_kernel_move:       '#e05050',
  rt_luca_move:         '#b080f0',
  rt_flamewing_invasion:'#ff6a33',
  rt_crowd_evacuation:  '#60d0a0',
  rt_jade_rescue:       '#60a8e0',
  rt_fo_suppression:    '#4a7fc1'
};

/** Pin color by time order — severity progression */
const PIN_COLORS = {
  1: '#60a8e0',   // 市場通常営業
  2: '#ff6a33',   // 炎翼セル配置
  3: '#e05050',   // 最初の爆発
  4: '#e05050',   // 召喚獣出現
  5: '#ff3030',   // 炎槍
  6: '#8b1a1a',   // カーネル死亡
  7: '#4a7fc1',   // FO到着
  8: '#60d0a0',   // 戦闘終了
  9: '#b080f0',   // 公開尋問
  10: '#c9a961'   // ジェイドの誓い
};

/* ============================================================
   STATE
   ============================================================ */
let mapData = null;
let currentPinIndex = 0;       // 0-based: which pins are "active" (0 = show all up to pin 1)
let isPlaying = false;
let playInterval = null;
let selectedItem = null;       // {type, id, data}
let routeVisibility = {};      // route_id -> boolean

// SVG pan/zoom
let viewBox = { x: 0, y: 0, w: 1000, h: 1000 };
let isDragging = false;
let dragStart = { x: 0, y: 0 };
let dragViewStart = { x: 0, y: 0 };

/* ============================================================
   DOM HELPERS
   ============================================================ */
const svgNS = 'http://www.w3.org/2000/svg';

function svgEl(tag, attrs) {
  const e = document.createElementNS(svgNS, tag);
  if (attrs) for (const [k, v] of Object.entries(attrs)) e.setAttribute(k, v);
  return e;
}

function htmlEl(tag, cls, text) {
  const e = document.createElement(tag);
  if (cls) e.className = cls;
  if (text != null) e.textContent = text;
  return e;
}

/* ============================================================
   FETCH DATA
   ============================================================ */
async function loadMapData() {
  const res = await fetch('./city_map.json');
  if (!res.ok) throw new Error('city_map.json の読み込みに失敗しました');
  return res.json();
}

/* ============================================================
   RENDER: DISTRICTS
   ============================================================ */
function renderDistricts(svg) {
  const g = svgEl('g', { id: 'layer-districts' });

  for (const dist of mapData.districts) {
    const cfg = DISTRICT_CONFIG[dist.district_id];
    if (!cfg) continue;

    const dg = svgEl('g', { class: 'district-group', 'data-district': dist.district_id });

    // Polygon shape
    const points = cfg.poly.map(p => p.join(',')).join(' ');
    const shape = svgEl('polygon', {
      class: 'district-shape',
      points,
      fill: cfg.color,
      stroke: cfg.stroke,
      'stroke-width': '1.5'
    });
    shape.addEventListener('click', () => selectItem('district', dist.district_id, dist));
    dg.appendChild(shape);

    // Center label
    const cx = cfg.poly.reduce((s, p) => s + p[0], 0) / cfg.poly.length + (cfg.labelOffset?.dx || 0);
    const cy = cfg.poly.reduce((s, p) => s + p[1], 0) / cfg.poly.length + (cfg.labelOffset?.dy || 0);

    const label = svgEl('text', {

==================================================
FILE: ./viewer/db/city/_local_staging_backup/grand-central_wip_2026-04-16_1811/map_v1_archive_2026-04-16/city.css
--------------------------------------------------
@import url('https://fonts.googleapis.com/css2?family=Cormorant+Garamond:ital,wght@0,300;0,400;0,600;1,400&family=Noto+Serif+JP:wght@300;400;500&display=swap');

/* ============================================================
   CSS VARIABLES
   ============================================================ */
:root {
  --bg:           #060a11;
  --bg2:          #0a1020;
  --bg3:          #0e1628;
  --panel:        #0d1525;
  --panel2:       #111d35;

  --gold:         #c9a961;
  --gold2:        #e8c97a;
  --gold-dim:     #7a6035;
  --gold-glow:    rgba(201,169,97,0.15);

  --text:         #d8ccb4;
  --text2:        #7a6e58;
  --text3:        #4a4030;

  --border:       rgba(201,169,97,0.14);
  --border2:      rgba(201,169,97,0.3);

  /* ステータスカラー */
  --c-dead:       #8b1a1a;
  --c-dead-glow:  rgba(139,26,26,0.2);
  --c-dead-text:  #e05050;

  --c-captured:   #b8901a;
  --c-captured-glow: rgba(184,144,26,0.2);
  --c-captured-text: #e8c050;

  --c-escape:     #5a2d8a;
  --c-escape-glow: rgba(90,45,138,0.2);
  --c-escape-text: #b080f0;

  --c-civilian:   #1a4a7a;
  --c-civilian-glow: rgba(26,74,122,0.2);
  --c-civilian-text: #60a8e0;

  --c-unknown:    #3a3a4a;
  --c-unknown-glow: rgba(58,58,74,0.3);
  --c-unknown-text: #9090b0;

  --c-shadow:     #0a0a0a;
  --c-shadow-glow: rgba(201,169,97,0.08);
  --c-shadow-text: #c9a961;

  /* ハイライト種別 */
  --hl-death:     rgba(139,26,26,0.12);
  --hl-pivot:     rgba(60,100,180,0.1);
  --hl-deception: rgba(90,45,138,0.12);
  --hl-awakening: rgba(80,160,120,0.1);
  --hl-oath:      rgba(201,169,97,0.1);
}

/* ============================================================
   RESET & BASE
   ============================================================ */
*, *::before, *::after { margin: 0; padding: 0; box-sizing: border-box; }

html { scroll-behavior: smooth; }

body {
  background: var(--bg);
  color: var(--text);
  font-family: 'Noto Serif JP', serif;
  font-weight: 300;
  line-height: 1.8;
  min-height: 100vh;
  overflow-x: hidden;
}

body::before {
  content: '';
  position: fixed; inset: 0;
  background:
    radial-gradient(ellipse 80% 40% at 50% 0%, rgba(139,26,26,0.06) 0%, transparent 60%),
    radial-gradient(ellipse 60% 50% at 80% 100%, rgba(20,40,90,0.12) 0%, transparent 60%),
    repeating-linear-gradient(
      45deg, transparent, transparent 100px,
      rgba(201,169,97,0.012) 100px, rgba(201,169,97,0.012) 101px
    );
  pointer-events: none; z-index: 0;
}

/* ============================================================
   LAYOUT
   ============================================================ */
.terror-shell {
  position: relative; z-index: 1;
  max-width: 1040px;
  margin: 0 auto;
  padding: 0 20px 100px;
}

/* ============================================================
   BACK LINK
   ============================================================ */
.back-link {
  display: inline-flex;
  align-items: center;
  gap: 8px;
  font-family: 'Cormorant Garamond', serif;
  font-size: 11px;
  letter-spacing: 3px;
  color: var(--text2);
  text-decoration: none;
  padding: 20px 0;
  transition: color 0.2s;
}
.back-link:hover { color: var(--gold); }

/* ============================================================
   ① INCIDENT HEADER
   ============================================================ */
.incident-header {
  padding: 56px 0 48px;
  border-bottom: 1px solid var(--border);
  margin-bottom: 64px;
  position: relative;
}

.incident-header::after {
  content: '';
  position: absolute;
  bottom: -1px; left: 0;
  width: 120px; height: 1px;
  background: var(--gold);
}

.incident-tag {
  font-family: 'Cormorant Garamond', serif;
  font-size: 10px;
  letter-spacing: 5px;
  color: var(--c-dead-text);
  opacity: 0.7;
  margin-bottom: 14px;
  text-transform: uppercase;
}

.incident-title {
  font-family: 'Cormorant Garamond', serif;
  font-size: clamp(2.4rem, 7vw, 4.2rem);
  font-weight: 300;
  color: var(--text);
  letter-spacing: 0.08em;
  line-height: 1.2;
  margin-bottom: 6px;
}

.incident-sub {
  font-family: 'Cormorant Garamond', serif;
  font-size: 13px;
  letter-spacing: 4px;
  color: var(--gold);
  opacity: 0.6;
  margin-bottom: 36px;
}

==================================================
FILE: ./viewer/db/city/_local_staging_backup/grand-central_wip_2026-04-16_1811/map_v1_archive_2026-04-16/city.js
--------------------------------------------------
'use strict';

/* ============================================================
   CONSTANTS
   ============================================================ */
const STATUS_CONFIG = {
  dead:     { label: '死亡',           dot: '#e05050' },
  captured: { label: '逮捕（影武者）', dot: '#e8c050' },
  escape:   { label: '逃亡・生存',     dot: '#b080f0' },
  civilian: { label: '生存（民間）',   dot: '#60a8e0' },
  unknown:  { label: '生死不明',       dot: '#9090b0' },
  shadow:   { label: '要監視・黒幕候補', dot: '#c9a961' },
};

const PHASE_ORDER = [
  '静寂と予兆',
  '英雄的選択と悲劇',
  '決別と覚醒の兆し',
  'FOによる制圧',
  '残酷な現実の確定',
  '政治的幕引き',
  '誓いへ',
];

const HL_BADGE_LABEL = {
  death:     '★ カーネル死亡',
  pivot:     '◆ 転換点',
  deception: '▲ 国家の欺瞞',
  awakening: '◉ 心音覚醒',
  oath:      '✦ 誓い',
};

const FORESHADOW_DATA = [
  {
    icon: '🔮',
    label: 'ABILITY',
    title: '心音システム初覚醒',
    desc: 'Scene14の異常な心音、Scene25の公開尋問での無自覚発火。「こいつじゃない」という確信がジェイドに生まれる起点。グローリアテストおよび夜化因子判定への直接の伏線。',
    connect: '→ グローリアテスト / 夜化因子 / 能力判定試験',
  },
  {
    icon: '📿',
    label: 'EVIDENCE',
    title: 'ペンダント証拠',
    desc: 'Scene27でカーネルの遺品として発見される七芒星の魔法石ペンダント。触れた瞬間に炎槍の記憶がフラッシュバック。教会バイト編でルカが持っていた物と同一。',
    connect: '→ 教会バイト編回収 / ルカ接点 / 炎翼セル証拠',
  },
  {
    icon: '👦',
    label: 'GUILT',
    title: 'ルカの罪悪感ライン',
    desc: 'Scene28でルカが黒煙を見つめ「ボクは悪くない」と自分に言い聞かせる。ペンダントが消えていることで全てを悟る。クララとは別の「生き残った罪」として作品を厚くする。',
    connect: '→ セラとの再接触 / 証言者候補 / 贖罪ライン',
  },
  {
    icon: '🏛',
    label: 'CONSPIRACY',
    title: '国家による真実隠蔽',
    desc: 'Scene24-26で国家は迅速な「解決」をアピール。D-04《グラム》が実行犯として処刑予定。本当の幹部《アーク》《ヴェル》は逃走、《ノクス》は自害を偽装している可能性がある。',
    connect: '→ グラム公開尋問 / ジェイドの孤独な違和感 / ZERO編エンジン',
  },
  {
    icon: '⛪',
    label: 'CHURCH',
    title: '教会地下黒幕継続',
    desc: 'Scene30の締めで「観測対象、反応あり」「監視継続」が示される。エリオスは神父として平常運用を続けるが、炎翼セル上位セルとの接続役を担う黒幕候補。',
    connect: '→ 炎翼セル上位組織 / エリオス / グローリアテスト編接続',
  },
  {
    icon: '🛡',
    label: 'FO',
    title: 'FOへの憧憬と不信の同居',
    desc: '聖剣で炎槍を止めたガーネットへの憧れ（FOへの憧憬）と、事件を早々に片付けた国家システムへの不信が、ジェイドの中に同時に生まれる。この矛盾がZERO編全体を駆動する。',
    connect: '→ FO入団動機 / 士官学校編 / 国家不信ライン',
  },
];

/* ============================================================
   UTILS
   ============================================================ */
function el(tag, cls, text) {
  const e = document.createElement(tag);
  if (cls) e.className = cls;
  if (text != null) e.textContent = text;
  return e;
}

function stagger(elements, base = 0, step = 40) {
  elements.forEach((e, i) => {
    e.style.animationDelay = `${base + i * step}ms`;
  });
}

/* ============================================================
   FETCH BOTH JSON
   ============================================================ */
async function loadAll() {
  const [storyRes, statusRes] = await Promise.all([
    fetch('./terror_story.json'),
    fetch('./terror_status.json'),
  ]);

  if (!storyRes.ok) throw new Error('terror_story.json の読み込みに失敗しました');
  if (!statusRes.ok) throw new Error('terror_status.json の読み込みに失敗しました');

  return {
    story:  await storyRes.json(),
    status: await statusRes.json(),
  };
}

/* ============================================================
   ① INCIDENT HEADER
   ============================================================ */
function renderHeader(story, status) {
  document.getElementById('incident-title').textContent = story.incident;
  document.getElementById('incident-sub').textContent   = story.subtitle;

  const members  = status.members;
  const dead     = members.filter(m => m.status === 'dead').length;
  const escape   = members.filter(m => m.status === 'escape' || m.status === 'unknown').length;
  const captured = members.filter(m => m.status === 'captured').length;

  document.getElementById('stat-dead').textContent     = dead;
  document.getElementById('stat-escape').textContent   = escape;
  document.getElementById('stat-captured').textContent = captured;
}

/* ============================================================
   ② STORY TIMELINE
   ============================================================ */
function renderTimeline(story) {
  const wrap = document.getElementById('timeline-wrap');
  const scenes = story.scenes;

  // フェーズでグループ化
  const phases = {};
  for (const sc of scenes) {
    if (!phases[sc.phase]) phases[sc.phase] = [];
    phases[sc.phase].push(sc);
  }

  const orderedPhases = PHASE_ORDER.filter(p => phases[p]);

  for (const phaseName of orderedPhases) {
    const group = phases[phaseName];

    // フェーズ区切り
    const divider = document.createElement('div');
    divider.className = 'phase-divider';
    divider.innerHTML = `
      <div class="phase-label">
        <span>${group[0].phase_en}</span>
        <span class="phase-label-ja">${phaseName}</span>
      </div>`;
    wrap.appendChild(divider);

    // シーンカード
    const cards = [];
    for (const sc of group) {

==================================================
FILE: ./viewer/db/city/_local_staging_backup/grand-central_wip_2026-04-16_1811/map_v1_archive_2026-04-16/city_map.json
--------------------------------------------------
{
  "map_id": "grand_central_city_v1",

  "districts": [
    {
      "district_id": "market_core",
      "name": "中央市場区",
      "x": 520,
      "y": 220
    },
    {
      "district_id": "admin_core",
      "name": "行政中枢区",
      "x": 250,
      "y": 300
    },
    {
      "district_id": "east_residential",
      "name": "東住宅区",
      "x": 820,
      "y": 300
    },
    {
      "district_id": "port_logistics",
      "name": "港湾物流区",
      "x": 130,
      "y": 540
    },
    {
      "district_id": "church_oldtown",
      "name": "教会旧市街",
      "x": 760,
      "y": 650
    },
    {
      "district_id": "academy_outer",
      "name": "士官予備区",
      "x": 520,
      "y": 560
    }
  ],

  "locations": [
    {
      "location_id": "loc_central_market",
      "name": "中央市場",
      "district_id": "market_core",
      "x": 520,
      "y": 180
    },
    {
      "location_id": "loc_civic_square",
      "name": "祝祭広場",
      "district_id": "market_core",
      "x": 520,
      "y": 260
    },
    {
      "location_id": "loc_central_hall",
      "name": "中央庁舎",
      "district_id": "admin_core",
      "x": 250,
      "y": 360
    },
    {
      "location_id": "loc_fo_hq",
      "name": "三兵団本部",
      "district_id": "admin_core",
      "x": 220,
      "y": 260
    },
    {
      "location_id": "loc_jade_home",
      "name": "ジェイド居住圏",
      "district_id": "east_residential",
      "x": 860,
      "y": 420
    },
    {
      "location_id": "loc_east_street",
      "name": "東門街道",
      "district_id": "east_residential",
      "x": 820,
      "y": 220
    },
    {
      "location_id": "loc_trade_port",
      "name": "交易港",
      "district_id": "port_logistics",
      "x": 80,
      "y": 470
    },
    {
      "location_id": "loc_magic_flow",
      "name": "魔石流通センター",
      "district_id": "port_logistics",
      "x": 250,
      "y": 700
    },
    {
      "location_id": "loc_star_church",
      "name": "星導教会",
      "district_id": "church_oldtown",
      "x": 720,
      "y": 560
    },
    {
      "location_id": "loc_bell_slope",
      "name": "鐘楼坂",
      "district_id": "church_oldtown",
      "x": 900,
      "y": 560
    },
    {
      "location_id": "loc_underground_entry",
      "name": "地下礼拝路入口",
      "district_id": "church_oldtown",
      "x": 800,
      "y": 760
    },
    {
      "location_id": "loc_training_school",
      "name": "訓練校",
      "district_id": "academy_outer",
      "x": 460,
      "y": 720
    },
    {
      "location_id": "loc_academy_facility",
      "name": "士官学校関連施設",
      "district_id": "academy_outer",
      "x": 560,
      "y": 520
    }
  ],

  "routes": [
    {
      "route_id": "rt_market_to_east",
      "name": "中央市場街道",
      "points": [
        { "x": 520, "y": 320 },
        { "x": 650, "y": 330 },
        { "x": 760, "y": 300 }
      ]
    },
    {
      "route_id": "rt_market_to_admin",
      "name": "北礼拝路",
      "points": [
        { "x": 440, "y": 300 },
        { "x": 360, "y": 260 },
        { "x": 290, "y": 220 }
      ]
    },
    {
      "route_id": "rt_market_to_academy",
      "name": "士官街路",
      "points": [
        { "x": 540, "y": 380 },

==================================================
FILE: ./viewer/db/city/_local_staging_backup/grand-central_wip_2026-04-16_1811/map_v1_archive_2026-04-16/city_status.json
--------------------------------------------------
{
  "incident": "中央市場炎槍事件",
  "cell": "炎翼セル",
  "members": [
    {
      "id": "C-01",
      "name": "アーク",
      "role": "セルリーダー",
      "role_en": "Cell Leader",
      "status": "escape",
      "status_label": "逃亡・生存",
      "public_info": "国家未把握",
      "future_hook": "上位セル接続担当。ZERO終盤黒幕導線。",
      "threat": "high"
    },
    {
      "id": "R-02",
      "name": "ノクス",
      "role": "儀式中枢",
      "role_en": "Ritual Core",
      "status": "unknown",
      "status_label": "生死不明（生存寄り）",
      "public_info": "死亡扱い",
      "future_hook": "影武者作戦の主導者候補。自害は偽装の可能性。",
      "threat": "high"
    },
    {
      "id": "R-03",
      "name": "セラ",
      "role": "儀式補助",
      "role_en": "Ritual Support",
      "status": "escape",
      "status_label": "逃亡・生存",
      "public_info": "行方不明",
      "future_hook": "ルカとの接点あり。後の証言者候補。迷いを持つ唯一の存在。",
      "threat": "medium"
    },
    {
      "id": "D-04",
      "name": "グラム",
      "role": "爆破担当",
      "role_en": "Demo Unit",
      "status": "captured",
      "status_label": "逮捕（影武者）",
      "public_info": "実行犯として報道・処刑予定",
      "future_hook": "公開尋問の替え玉本命。心音解析で「こいつじゃない」が確定する対象。",
      "threat": "low"
    },
    {
      "id": "D-05",
      "name": "イグナ",
      "role": "火炎拡大",
      "role_en": "Flame Unit",
      "status": "dead",
      "status_label": "死亡",
      "public_info": "戦死扱い",
      "future_hook": "炎翼セルの狂信性を象徴する存在。",
      "threat": "none"
    },
    {
      "id": "S-06",
      "name": "リド",
      "role": "市場監視",
      "role_en": "Market Scout",
      "status": "escape",
      "status_label": "逃亡・生存",
      "public_info": "未特定",
      "future_hook": "元一般人。再登場時に思想崩壊描写が可能な人物。",
      "threat": "medium"
    },
    {
      "id": "S-07",
      "name": "ヴェル",
      "role": "外周警戒",
      "role_en": "Perimeter Guard",
      "status": "escape",
      "status_label": "逃亡・生存",
      "public_info": "未特定",
      "future_hook": "残党ネットワーク維持役。逃走ルートを知る数少ない存在。",
      "threat": "medium"
    },
    {
      "id": "SUPPORT",
      "name": "ルカ",
      "role": "教会側被害者",
      "role_en": "Church Proxy",
      "status": "civilian",
      "status_label": "生存（民間）",
      "public_info": "無関係市民",
      "future_hook": "ペンダント伏線・贖罪ライン。知らずに利用された補助導線。",
      "threat": "none"
    },
    {
      "id": "CHURCH",
      "name": "エリオス",
      "role": "思想協力者",
      "role_en": "Church Collaborator",
      "status": "shadow",
      "status_label": "要監視・黒幕候補",
      "public_info": "神父として平常運用",
      "future_hook": "黒幕候補の中継点。教会地下への接続役。",
      "threat": "high"
    }
  ],
  "fo_involved": [
    {
      "id": "FO-LEAD",
      "name": "ガーネット・ヴァルグレイ",
      "role": "FO隊長",
      "role_en": "FO Commander",
      "action": "聖剣で炎槍を直接防御。路地裏でノクスの魔法陣を一撃破壊。",
      "note": "ジェイドに「人が神の槍を止めた」光景を刻んだ人物。"
    }
  ]
}

==================================================
FILE: ./viewer/db/city/_local_staging_backup/grand-central_wip_2026-04-16_1811/map_v1_archive_2026-04-16/city_story.json
--------------------------------------------------
{
  "incident": "中央市場炎槍事件",
  "subtitle": "ZERO編プロローグ事件",
  "arc": "中編・後編",
  "scenes": [
    {
      "id": 8,
      "title": "上空の異変",
      "phase": "静寂と予兆",
      "phase_en": "SILENCE",
      "tags": ["異変", "天使の示唆", "伏線"],
      "highlight": false,
      "summary": "炎の粉が逆流し、煙が上へ吸い込まれる。ジェイドだけが違和感を覚える。空の裂け目に、翼に見える巨大な炎影——正体は明示しない。"
    },
    {
      "id": 9,
      "title": "第二波",
      "phase": "静寂と予兆",
      "phase_en": "SILENCE",
      "tags": ["召喚獣", "戦場化", "ZG限界"],
      "highlight": false,
      "summary": "炎翼セル第二手。召喚獣が屋台を薙ぎ倒し市場が完全に戦場化。中央警備隊だけでは抑えきれない。カーネルが子供の泣き声で足を止める。"
    },
    {
      "id": 10,
      "title": "カーネルの選択",
      "phase": "英雄的選択と悲劇",
      "phase_en": "SACRIFICE",
      "tags": ["人格的必然", "姉の決断", "利他"],
      "highlight": false,
      "summary": "「……先にクララを頼む」——倒壊しかけた屋台の陰に迷子の子供を見つけ、迷わず単身向かうカーネル。これが死亡の人格的必然性となる。"
    },
    {
      "id": 11,
      "title": "炎槍の予兆",
      "phase": "英雄的選択と悲劇",
      "phase_en": "SACRIFICE",
      "tags": ["音の消滅", "赤染め", "スローモーション"],
      "highlight": false,
      "summary": "空の裂け目が脈打ち、音が消える。市場全体の色が一瞬だけ赤に染まる。カーネルが子供を抱き上げた瞬間、空から一本の細く鋭い光——。"
    },
    {
      "id": 12,
      "title": "貫通",
      "phase": "英雄的選択と悲劇",
      "phase_en": "SACRIFICE",
      "tags": ["炎槍貫通", "カーネル重傷", "記憶の断片化"],
      "highlight": true,
      "highlight_type": "death",
      "summary": "炎槍がカーネルの胸部を貫く。光が胸を抜け、一拍遅れて血が滲む。子供だけは無傷。倒れる音——ジェイドの記憶はここで断片化する。"
    },
    {
      "id": 13,
      "title": "断末魔",
      "phase": "決別と覚醒の兆し",
      "phase_en": "AWAKENING",
      "tags": ["駆け寄る", "遺言候補", "クララの絶望"],
      "highlight": false,
      "summary": "「ジェイド……大丈夫」「強くなれ」——倒れたカーネルへ駆け寄るジェイド。呼吸がまだ浅く残っている。クララは自分を庇って倒れた現実に言葉を失う。"
    },
    {
      "id": 14,
      "title": "号泣",
      "phase": "決別と覚醒の兆し",
      "phase_en": "AWAKENING",
      "tags": ["音の遮断", "心音異常", "感情フリーズの前段"],
      "highlight": false,
      "summary": "市場の悲鳴は遠く、ジェイドの耳にはほぼ入らない。視界に映るのは姉の顔だけ。「なんで」「いやだ」が言葉にならない。心音が異常に跳ねる——能力覚醒の伏線。"
    },
    {
      "id": 15,
      "title": "黒鎧の介入",
      "phase": "決別と覚醒の兆し",
      "phase_en": "AWAKENING",
      "tags": ["FO登場", "聖剣防御", "神話感"],
      "highlight": true,
      "highlight_type": "pivot",
      "summary": "クララを狙う第二の炎槍——その瞬間、黒い影が割り込む。金属音。聖剣が炎槍を真正面から受け止める。振り返らず一言「下がれ」。ジェイドの目に「人が神の槍を止めた」光景が刻まれる。"
    },
    {
      "id": 16,
      "title": "制圧線",
      "phase": "FOによる制圧",
      "phase_en": "SUPPRESSION",
      "tags": ["FO展開", "避難誘導", "統率"],
      "highlight": false,
      "summary": "FO黒鎧部隊がジェイド周辺を防衛円で確保。隊長が即座に命令——北通路を避難路に指定、火炎術師を召集、残党索敵。ジェイドは初めて国家機関の統率を目撃する。"
    },
    {
      "id": 17,
      "title": "索敵と避難誘導",
      "phase": "FOによる制圧",
      "phase_en": "SUPPRESSION",
      "tags": ["部隊三分割", "儀式核追跡", "担架到着"],
      "highlight": false,
      "summary": "槍の落下角度から「儀式核は地上」と即断。部隊を索敵・避難・救護の三班に分割。後方医療班がようやく到着し、担架が見え始める。ジェイドだけ姉から離れられない。"
    },
    {
      "id": 18,
      "title": "路地裏の発見",
      "phase": "FOによる制圧",
      "phase_en": "SUPPRESSION",
      "tags": ["七芒星儀式痕", "炎翼セル発見", "ノクス"],
      "highlight": false,
      "summary": "市場南側の路地に七芒星の儀式痕。奥にノクスを中心とした黒ローブ複数が魔法陣を維持。セラが魔力供給で苦しみ、リドが外周警戒。「発見。炎翼セル残党」——FO突入。"
    },
    {
      "id": 19,
      "title": "路地裏戦",
      "phase": "FOによる制圧",
      "phase_en": "SUPPRESSION",
      "tags": ["近接戦", "聖剣vs神性炎", "魔法陣破壊"],
      "highlight": false,
      "summary": "イグナの火炎魔法で路地が燃える。ノクスが儀式を維持しながら後退、セラが一瞬迷いを見せる。隊長ガーネットがノクスへ一直線——一撃で魔法陣の核を破壊。上空の裂け目が崩壊し炎槍が停止。"
    },
    {
      "id": 20,
      "title": "制圧完了",
      "phase": "FOによる制圧",
      "phase_en": "SUPPRESSION",
      "tags": ["ノクス自害", "セラ逃走", "熱が冷える"],
      "highlight": false,
      "summary": "逃げ場なし——「秩序は戻る」ノクス自害、イグナ炎で自決。セラだけが生き残る。「中央市場、制圧完了」——事件の熱が一気に冷える。"
    },
    {
      "id": 21,
      "title": "救護区画",
      "phase": "残酷な現実の確定",
      "phase_en": "REALITY",
      "tags": ["死亡宣告", "まだ温かい", "現実の固定"],
      "highlight": true,
      "highlight_type": "death",
      "summary": "医療兵「……脈、なし。死亡確認」——第三者による宣告が現実を固定する。ジェイドの世界の音が、再び遠のく。"
    },
    {
      "id": 22,
      "title": "家族の再合流",
      "phase": "残酷な現実の確定",
      "phase_en": "REALITY",
      "tags": ["母の崩壊", "父の沈黙", "泣けないジェイド"],
      "highlight": false,
      "summary": "クララを抱きしめ、ジェイドを探し、布を掛けられたカーネルを見つける——探す順番が命。母の精神支柱がここで折れる。父は無言で立ち尽くす。ジェイドは泣けない。"
    },
    {
      "id": 23,
      "title": "残党掃討",
      "phase": "政治的幕引き",
      "phase_en": "DECEPTION",
      "tags": ["ヴェル逃走", "セラ行方不明", "替え玉残存"],
      "highlight": false,
      "summary": "FOが路地裏・地下通路で残党を狩る。S-07《ヴェル》逃走、R-03《セラ》行方不明——現場には替え玉だけ残る。事件が終わったと思うジェイド vs 終わっていないと知る読者、このズレ。"
    },
    {
      "id": 24,
      "title": "影武者逮捕",
      "phase": "政治的幕引き",
      "phase_en": "DECEPTION",
      "tags": ["国家アピール", "D-04グラム", "替え玉"],
      "highlight": true,
      "highlight_type": "deception",

==================================================
FILE: ./viewer/db/city/_local_staging_backup/grand-central_wip_2026-04-16_1811/map_v1_archive_2026-04-16/index.html
--------------------------------------------------
<!DOCTYPE html>
<html lang="ja">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>中央市場炎槍事件 | Meritocracia Terror DB</title>
<link rel="stylesheet" href="./city.css">
</head>
<body>

<div class="terror-shell">

  <!-- ナビ -->
  <a class="back-link" href="../../index.html">← NATION ARCHIVE</a>
  <a class="back-link" href="./map.html" style="margin-left:24px; color:var(--gold-dim);">🗺 TACTICAL MAP →</a>

  <!-- ======================================================
       ① INCIDENT HEADER
       ====================================================== -->
  <section class="incident-header">
    <div class="incident-tag">ZERO編 ／ MERITOCRACIA INCIDENT RECORD</div>
    <h1 class="incident-title" id="incident-title">読み込み中...</h1>
    <p class="incident-sub" id="incident-sub"></p>

    <div class="incident-stats">
      <div class="stat-chip red">
        <span class="stat-label">死者</span>
        <span class="stat-val" id="stat-dead">—</span>
      </div>
      <div class="stat-chip purple">
        <span class="stat-label">逃亡・行方不明</span>
        <span class="stat-val" id="stat-escape">—</span>
      </div>
      <div class="stat-chip amber">
        <span class="stat-label">公式犯人（影武者）</span>
        <span class="stat-val" id="stat-captured">—</span>
      </div>
      <div class="stat-chip">
        <span class="stat-label">Scene</span>
        <span class="stat-val">08 – 30</span>
      </div>
    </div>
  </section>

  <!-- エラー表示 -->
  <div id="error-msg" style="display:none; color:#e05050; font-size:13px; margin-bottom:32px; padding:16px; border:1px solid rgba(139,26,26,0.3); background:rgba(139,26,26,0.08);"></div>

  <!-- ======================================================
       ② STORY TIMELINE VIEW
       ====================================================== -->
  <section class="section-block">
    <div class="section-eyebrow">02 — STORY TIMELINE</div>
    <h2 class="section-title">ストーリータイムライン</h2>
    <div class="timeline" id="timeline-wrap">
      <!-- JS が生成 -->
    </div>
  </section>

  <!-- ======================================================
       ③ CELL STATUS DB
       ====================================================== -->
  <section class="section-block">
    <div class="section-eyebrow">03 — CELL STATUS DATABASE</div>
    <h2 class="section-title">炎翼セル メンバーステータス</h2>

    <!-- 凡例 -->
    <div class="legend" id="status-legend"></div>

    <div class="status-grid" id="status-grid">
      <!-- JS が生成 -->
    </div>
  </section>

  <!-- ======================================================
       ④ LONG FORESHADOW PANEL
       ====================================================== -->
  <section class="section-block">
    <div class="section-eyebrow">04 — LONG FORESHADOW</div>
    <h2 class="section-title">長期伏線 考察DB</h2>

    <div class="foreshadow-grid" id="foreshadow-grid">
      <!-- JS が生成 -->
    </div>
  </section>

</div><!-- /.terror-shell -->

<script src="./city.js"></script>
</body>
</html>

==================================================
FILE: ./viewer/db/city/_local_staging_backup/grand-central_wip_2026-04-16_1811/map_v1_archive_2026-04-16/map.css
--------------------------------------------------
@import url('https://fonts.googleapis.com/css2?family=Cormorant+Garamond:ital,wght@0,300;0,400;0,600;1,400&family=Noto+Serif+JP:wght@300;400;500&family=JetBrains+Mono:wght@300;400&display=swap');

/* ============================================================
   CSS VARIABLES — dark fantasy + London tube aesthetic
   ============================================================ */
:root {
  --bg:           #060a11;
  --bg2:          #0a1020;
  --bg3:          #0e1628;
  --panel:        #0d1525;
  --panel2:       #111d35;
  --panel-glass:  rgba(13,21,37,0.92);

  --gold:         #c9a961;
  --gold2:        #e8c97a;
  --gold-dim:     #7a6035;
  --gold-glow:    rgba(201,169,97,0.15);

  --text:         #d8ccb4;
  --text2:        #7a6e58;
  --text3:        #4a4030;

  --border:       rgba(201,169,97,0.14);
  --border2:      rgba(201,169,97,0.3);

  /* District palette — muted London-map tones */
  --dist-church:     rgba(120,80,160,0.18);
  --dist-church-s:   rgba(120,80,160,0.35);
  --dist-market:     rgba(200,120,60,0.18);
  --dist-market-s:   rgba(200,120,60,0.35);
  --dist-south:      rgba(80,140,100,0.15);
  --dist-south-s:    rgba(80,140,100,0.30);
  --dist-alley:      rgba(100,100,140,0.16);
  --dist-alley-s:    rgba(100,100,140,0.32);
  --dist-fo:         rgba(60,120,200,0.16);
  --dist-fo-s:       rgba(60,120,200,0.32);
  --dist-civic:      rgba(180,140,80,0.14);
  --dist-civic-s:    rgba(180,140,80,0.30);

  /* Route colors — Tube-line inspired */
  --rt-kernel:       #e05050;
  --rt-luca:         #b080f0;
  --rt-flamewing:    #ff6a33;
  --rt-crowd:        #60d0a0;
  --rt-jade:         #60a8e0;
  --rt-fo:           #4a7fc1;

  /* Pin severity */
  --pin-normal:      #60a8e0;
  --pin-deploy:      #ff6a33;
  --pin-explosion:   #e05050;
  --pin-death:       #8b1a1a;
  --pin-fo:          #4a7fc1;
  --pin-end:         #60d0a0;
  --pin-oath:        #c9a961;

  --sidebar-w:       360px;
  --timeline-h:      72px;
}

/* ============================================================
   RESET & BASE
   ============================================================ */
*, *::before, *::after { margin: 0; padding: 0; box-sizing: border-box; }
html { scroll-behavior: smooth; }

body {
  background: var(--bg);
  color: var(--text);
  font-family: 'Noto Serif JP', serif;
  font-weight: 300;
  line-height: 1.6;
  height: 100vh;
  overflow: hidden;
}

/* ============================================================
   MAIN LAYOUT — sidebar + map + bottom timeline
   ============================================================ */
.map-app {
  display: grid;
  grid-template-columns: var(--sidebar-w) 1fr;
  grid-template-rows: 1fr var(--timeline-h);
  height: 100vh;
  width: 100vw;
}

/* ============================================================
   SIDEBAR (LEFT PANE)
   ============================================================ */
.sidebar {
  grid-row: 1 / 3;
  background: var(--panel);
  border-right: 1px solid var(--border);
  display: flex;
  flex-direction: column;
  overflow: hidden;
  z-index: 10;
}

.sidebar-header {
  padding: 20px 20px 16px;
  border-bottom: 1px solid var(--border);
  flex-shrink: 0;
}

.sidebar-back {
  display: inline-flex;
  align-items: center;
  gap: 6px;
  font-family: 'Cormorant Garamond', serif;
  font-size: 10px;
  letter-spacing: 3px;
  color: var(--text2);
  text-decoration: none;
  transition: color 0.2s;
  margin-bottom: 14px;
}
.sidebar-back:hover { color: var(--gold); }

.sidebar-tag {
  font-family: 'Cormorant Garamond', serif;
  font-size: 9px;
  letter-spacing: 5px;
  color: rgba(224,80,80,0.7);
  text-transform: uppercase;
  margin-bottom: 6px;
}

.sidebar-title {
  font-family: 'Cormorant Garamond', serif;
  font-size: 1.4rem;
  font-weight: 300;
  color: var(--text);
  letter-spacing: 0.06em;
  line-height: 1.3;
  margin-bottom: 4px;
}

.sidebar-subtitle {
  font-family: 'Cormorant Garamond', serif;
  font-size: 11px;
  letter-spacing: 3px;
  color: var(--gold-dim);
}

/* Route toggles */
.sidebar-section {
  padding: 16px 20px;
  border-bottom: 1px solid var(--border);
  flex-shrink: 0;
}

.sidebar-section-label {
  font-family: 'Cormorant Garamond', serif;
  font-size: 9px;
  letter-spacing: 4px;
  color: var(--text2);
  text-transform: uppercase;
  margin-bottom: 10px;

==================================================
FILE: ./viewer/db/city/_local_staging_backup/grand-central_wip_2026-04-16_1811/map_v1_archive_2026-04-16/map.html
--------------------------------------------------
<!DOCTYPE html>
<html lang="ja">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>中央市場テロ事件 地図 | Meritocracia Terror Map</title>
<meta name="description" content="ZERO編 中央市場炎槍事件の地図ビューア。ロンドン地下鉄路線図の読みやすさ + Googleマップの事件ピンUI + ダークファンタジー世界観を融合。">
<link rel="stylesheet" href="./map.css">
</head>
<body>

<!-- Mobile sidebar toggle -->
<button class="sidebar-toggle" id="sidebar-toggle" aria-label="メニュー">☰</button>

<!-- Mobile backdrop -->
<div class="sidebar-backdrop" id="sidebar-backdrop"></div>

<div class="map-app">

  <!-- ============================================================
       SIDEBAR (LEFT PANE)
       ============================================================ -->
  <aside class="sidebar" id="sidebar">

    <div class="sidebar-header">
      <a class="sidebar-back" href="./index.html">← INCIDENT RECORD</a>
      <div class="sidebar-tag">ZERO編 ／ TACTICAL MAP</div>
      <h1 class="sidebar-title">中央市場炎槍事件</h1>
      <div class="sidebar-subtitle">CENTRAL MARKET INCIDENT MAP</div>
    </div>

    <!-- Route toggles -->
    <div class="sidebar-section">
      <div class="sidebar-section-label">ROUTE LAYERS</div>
      <div id="route-toggles">
        <!-- JS generates -->
      </div>
    </div>

    <!-- Detail panel -->
    <div class="sidebar-detail" id="detail-panel">
      <div class="detail-empty">
        <div class="detail-empty-icon">⊕</div>
        <div class="detail-empty-text">地図上の要素を<br>クリックして詳細表示</div>
      </div>
    </div>

  </aside>

  <!-- ============================================================
       MAP VIEWPORT
       ============================================================ -->
  <div class="map-viewport" id="map-viewport">

    <!-- Grid overlay -->
    <div class="map-grid-overlay"></div>

    <!-- SVG Map container -->
    <div class="map-container">
      <svg class="map-svg" id="map-svg" xmlns="http://www.w3.org/2000/svg"
           viewBox="0 0 1000 1000" preserveAspectRatio="xMidYMid meet">
        <!-- JS renders all layers here -->
      </svg>
    </div>

    <!-- Zoom controls -->
    <div class="zoom-controls">
      <button class="zoom-btn" id="btn-zoom-in" title="ズームイン">+</button>
      <button class="zoom-btn" id="btn-zoom-out" title="ズームアウト">−</button>
      <button class="zoom-btn" id="btn-zoom-reset" title="リセット">⟳</button>
    </div>

    <!-- Coordinate display -->
    <div class="map-coords" id="map-coords">0, 0</div>

    <!-- Map legend -->
    <div class="map-legend">
      <div class="map-legend-title">MAP LEGEND</div>
      <div class="legend-row">
        <span class="legend-pin" style="background:#c9a961"></span>
        <span class="legend-label">スポット (POI)</span>
      </div>
      <div class="legend-row">
        <span class="legend-pin" style="background:#60a8e0"></span>
        <span class="legend-label">平時イベント</span>
      </div>
      <div class="legend-row">
        <span class="legend-pin" style="background:#e05050"></span>
        <span class="legend-label">攻撃イベント</span>
      </div>
      <div class="legend-row">
        <span class="legend-pin" style="background:#8b1a1a"></span>
        <span class="legend-label">死亡</span>
      </div>
      <div class="legend-row">
        <span class="legend-pin" style="background:#4a7fc1"></span>
        <span class="legend-label">FO介入</span>
      </div>
      <div class="legend-row">
        <span class="legend-pin" style="background:#c9a961"></span>
        <span class="legend-label">誓い</span>
      </div>
    </div>

  </div>

  <!-- ============================================================
       TIMELINE BAR (BOTTOM)
       ============================================================ -->
  <div class="timeline-bar">

    <div class="timeline-controls">
      <button class="tl-btn" id="btn-prev" title="前のイベント (←)">◀</button>
      <button class="tl-btn" id="btn-play" title="再生/停止 (Space)">▶</button>
      <button class="tl-btn" id="btn-next" title="次のイベント (→)">▶</button>
    </div>

    <div class="timeline-slider-wrap">
      <span class="timeline-label" id="tl-label">ALL</span>
      <input type="range" class="timeline-slider" id="tl-slider" min="0" max="10" value="0" step="1">
      <span class="timeline-event-name" id="tl-event-name">全イベント表示</span>
    </div>

  </div>

</div><!-- /.map-app -->

<script src="./map.js"></script>
</body>
</html>

==================================================
FILE: ./viewer/db/city/_local_staging_backup/grand-central_wip_2026-04-16_1811/map_v1_archive_2026-04-16/map.js
--------------------------------------------------
'use strict';

/* ============================================================
   CONSTANTS & CONFIG
   ============================================================ */

/** District visual config — shape polygons & colors */
const DISTRICT_CONFIG = {
  church_quarter: {
    color: 'rgba(120,80,160,0.18)',
    stroke: 'rgba(120,80,160,0.35)',
    poly: [
      [100, 100], [320, 100], [330, 140], [320, 300],
      [280, 310], [100, 290], [90, 200]
    ],
    labelOffset: { dx: 0, dy: -15 }
  },
  market_core: {
    color: 'rgba(200,120,60,0.18)',
    stroke: 'rgba(200,120,60,0.35)',
    poly: [
      [370, 380], [620, 370], [640, 420], [630, 580],
      [580, 600], [370, 590], [350, 520]
    ],
    labelOffset: { dx: 0, dy: -15 }
  },
  south_market_edge: {
    color: 'rgba(80,140,100,0.15)',
    stroke: 'rgba(80,140,100,0.30)',
    poly: [
      [370, 620], [620, 620], [640, 670], [630, 830],
      [560, 850], [370, 840], [350, 750]
    ],
    labelOffset: { dx: 0, dy: -10 }
  },
  alley_belt: {
    color: 'rgba(100,100,140,0.16)',
    stroke: 'rgba(100,100,140,0.32)',
    poly: [
      [660, 380], [870, 400], [880, 450], [870, 600],
      [820, 620], [660, 610], [650, 520]
    ],
    labelOffset: { dx: 0, dy: -10 }
  },
  fo_entry_axis: {
    color: 'rgba(60,120,200,0.16)',
    stroke: 'rgba(60,120,200,0.32)',
    poly: [
      [60, 720], [280, 710], [300, 760], [290, 920],
      [240, 940], [60, 930], [50, 840]
    ],
    labelOffset: { dx: 0, dy: -10 }
  },
  civic_square: {
    color: 'rgba(180,140,80,0.14)',
    stroke: 'rgba(180,140,80,0.30)',
    poly: [
      [700, 720], [940, 710], [960, 770], [950, 920],
      [900, 940], [700, 930], [690, 840]
    ],
    labelOffset: { dx: 0, dy: -10 }
  }
};

/** Route color map */
const ROUTE_COLORS = {
  rt_kernel_move:       '#e05050',
  rt_luca_move:         '#b080f0',
  rt_flamewing_invasion:'#ff6a33',
  rt_crowd_evacuation:  '#60d0a0',
  rt_jade_rescue:       '#60a8e0',
  rt_fo_suppression:    '#4a7fc1'
};

/** Pin color by time order — severity progression */
const PIN_COLORS = {
  1: '#60a8e0',   // 市場通常営業
  2: '#ff6a33',   // 炎翼セル配置
  3: '#e05050',   // 最初の爆発
  4: '#e05050',   // 召喚獣出現
  5: '#ff3030',   // 炎槍
  6: '#8b1a1a',   // カーネル死亡
  7: '#4a7fc1',   // FO到着
  8: '#60d0a0',   // 戦闘終了
  9: '#b080f0',   // 公開尋問
  10: '#c9a961'   // ジェイドの誓い
};

/* ============================================================
   STATE
   ============================================================ */
let mapData = null;
let currentPinIndex = 0;       // 0-based: which pins are "active" (0 = show all up to pin 1)
let isPlaying = false;
let playInterval = null;
let selectedItem = null;       // {type, id, data}
let routeVisibility = {};      // route_id -> boolean

// SVG pan/zoom
let viewBox = { x: 0, y: 0, w: 1000, h: 1000 };
let isDragging = false;
let dragStart = { x: 0, y: 0 };
let dragViewStart = { x: 0, y: 0 };

/* ============================================================
   DOM HELPERS
   ============================================================ */
const svgNS = 'http://www.w3.org/2000/svg';

function svgEl(tag, attrs) {
  const e = document.createElementNS(svgNS, tag);
  if (attrs) for (const [k, v] of Object.entries(attrs)) e.setAttribute(k, v);
  return e;
}

function htmlEl(tag, cls, text) {
  const e = document.createElement(tag);
  if (cls) e.className = cls;
  if (text != null) e.textContent = text;
  return e;
}

/* ============================================================
   FETCH DATA
   ============================================================ */
async function loadMapData() {
  const res = await fetch('./city_map.json');
  if (!res.ok) throw new Error('city_map.json の読み込みに失敗しました');
  return res.json();
}

/* ============================================================
   RENDER: DISTRICTS
   ============================================================ */
function renderDistricts(svg) {
  const g = svgEl('g', { id: 'layer-districts' });

  for (const dist of mapData.districts) {
    const cfg = DISTRICT_CONFIG[dist.district_id];
    if (!cfg) continue;

    const dg = svgEl('g', { class: 'district-group', 'data-district': dist.district_id });

    // Polygon shape
    const points = cfg.poly.map(p => p.join(',')).join(' ');
    const shape = svgEl('polygon', {
      class: 'district-shape',
      points,
      fill: cfg.color,
      stroke: cfg.stroke,
      'stroke-width': '1.5'
    });
    shape.addEventListener('click', () => selectItem('district', dist.district_id, dist));
    dg.appendChild(shape);

    // Center label
    const cx = cfg.poly.reduce((s, p) => s + p[0], 0) / cfg.poly.length + (cfg.labelOffset?.dx || 0);
    const cy = cfg.poly.reduce((s, p) => s + p[1], 0) / cfg.poly.length + (cfg.labelOffset?.dy || 0);

    const label = svgEl('text', {

==================================================
FILE: ./viewer/db/city/_local_staging_backup/grand-central_wip_2026-04-16_1811/v2/index.html
--------------------------------------------------
<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width,initial-scale=1.0">
  <title>Grand Central Map v2</title>
  <link rel="stylesheet" href="./v2.css">
</head>
<body>
  <header class="header">
    <h1>グランセントラル都市マップ v2</h1>
    <p>ドラッグで都市全体 → 士官学校詳細へ移動</p>
  </header>

  <main class="map-scroll">
    <section class="map-panel">
      <h2>都市全体</h2>
      <img src="../grand-central-map.png" alt="都市全体図">
    </section>

    <section class="map-panel">
      <h2>士官学校詳細</h2>
      <img src="../academy/academy-map.png" alt="士官学校詳細">
    </section>
  </main>
</body>
</html>
==================================================
FILE: ./viewer/db/city/_local_staging_backup/grand-central_wip_2026-04-16_1811/v2/v2.css
--------------------------------------------------
body {
  margin: 0;
  background: #07101c;
  color: #fff;
  font-family: sans-serif;
}

.header {
  padding: 20px;
  border-bottom: 1px solid #333;
  background: #0b1420;
}

.header h1 {
  margin: 0;
  color: #d4af37;
}

.map-scroll {
  display: flex;
  overflow-x: auto;
  gap: 40px;
  padding: 30px;
  scroll-snap-type: x mandatory;
}

.map-panel {
  min-width: 90vw;
  flex-shrink: 0;
  scroll-snap-align: start;
}

.map-panel h2 {
  margin-bottom: 16px;
  color: #d4af37;
}

.map-panel img {
  width: 100%;
  border-radius: 12px;
  border: 1px solid #333;
}
==================================================
FILE: ./viewer/db/city/_local_staging_backup/grand-central_wip_2026-04-16_1811/v3/index.html
--------------------------------------------------
<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0, user-scalable=no">
  <title>グランセントラル 連続地図 v3 | GRAND CENTRAL SEAMLESS MAP</title>
  <meta name="description" content="グランセントラル都市全体図＋士官学校詳細の連続インタラクティブマップ v3。">
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Noto+Serif+JP:wght@400;600;700&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="./v3.css">
</head>
<body>

<!-- ══════════════════════════════════════════════════════════
     TOP BAR
     ══════════════════════════════════════════════════════════ -->
<header class="topbar">
  <a class="topbar-back" href="../index.html">← VIEWER HUB</a>
  <div class="topbar-sep"></div>

  <div class="topbar-title">
    グランセントラル
    <span>SEAMLESS NAVIGATOR v3</span>
  </div>

  <div class="topbar-spacer"></div>

  <!-- フィルターバー -->
  <div class="filter-bar" id="filter-bar">
    <span class="filter-label">FILTER</span>
    <!-- JS buildFilters() -->
  </div>

  <div class="topbar-sep"></div>

  <!-- Map mode tabs -->
  <div class="mode-tabs">
    <button class="mode-tab active" data-mode="combined">連続</button>
    <button class="mode-tab" data-mode="gc">都市</button>
    <button class="mode-tab" data-mode="ac">学校</button>
  </div>

  <div class="topbar-sep"></div>

  <!-- Minimap toggle -->
  <button class="minimap-toggle" id="minimap-toggle">ミニマップ</button>

  <!-- Zoom controls -->
  <div class="zoom-controls">
    <button class="zoom-btn" id="btn-zoom-in"    title="ズームイン (+)">+</button>
    <button class="zoom-btn" id="btn-zoom-out"   title="ズームアウト (−)">−</button>
    <button class="zoom-btn" id="btn-zoom-reset" title="全体表示 (0)">⟳</button>
  </div>
</header>


<!-- ══════════════════════════════════════════════════════════
     MAP VIEWPORT
     ══════════════════════════════════════════════════════════ -->
<div class="map-viewport" id="map-viewport">

  <!-- World root — all map content lives here, transform applied here -->
  <div id="world">

    <!-- GC map image -->
    <img id="layer-gc" class="map-layer"
         src="../grand-central-map.png"
         alt="グランセントラル都市区画図"
         draggable="false">

    <!-- Academy map image -->
    <img id="layer-ac" class="map-layer"
         src="../academy/academy-map.png"
         alt="士官学校詳細図"
         draggable="false">

    <!-- Seam blending overlay (JS paints gradient here) -->
    <div id="seam-overlay"></div>

    <!-- POI pins -->
    <div id="poi-pin-layer" class="poi-pin-layer"></div>

  </div><!-- /#world -->

  <!-- Coordinate display -->
  <div class="map-coords" id="map-coords">x:— y:—</div>

</div><!-- /.map-viewport -->


<!-- ══════════════════════════════════════════════════════════
     DETAIL PANEL
     ══════════════════════════════════════════════════════════ -->
<div class="detail-panel hidden" id="detail-panel">
  <div class="detail-bar" id="detail-bar"></div>
  <button class="detail-close" id="detail-close" aria-label="閉じる">✕</button>
  <div class="detail-body">
    <div class="detail-district" id="detail-district"></div>
    <div class="detail-name"     id="detail-name"></div>
    <div class="detail-name-en"  id="detail-name-en"></div>
    <div class="detail-desc"     id="detail-desc"></div>
    <div id="detail-lore"></div>
  </div>
</div>


<!-- ══════════════════════════════════════════════════════════
     MINIMAP
     ══════════════════════════════════════════════════════════ -->
<div id="minimap-wrap">
  <canvas id="minimap-canvas"></canvas>
  <div id="minimap-viewport-rect"></div>
</div>


<!-- ══════════════════════════════════════════════════════════
     FOOTER
     ══════════════════════════════════════════════════════════ -->
<footer class="map-footer">
  <span class="footer-tag">MERITOCRACIA WORLD ARCHIVE</span>
  <span class="footer-poi-count" id="footer-poi-count">— POI</span>
  <span class="footer-spacer"></span>
  <span class="footer-hint">ホイール / ピンチでズーム　ドラッグでパン　[0]全体表示　[←→↑↓]移動</span>
</footer>

<!-- Scroll hint (auto-fades) -->
<div class="scroll-hint">← ドラッグして士官学校エリアへ →</div>

<script src="./v3.js"></script>
</body>
</html>

==================================================
FILE: ./viewer/db/city/_local_staging_backup/grand-central_wip_2026-04-16_1811/v3/v3.css
--------------------------------------------------
/* ============================================================
   GRAND CENTRAL NAVIGATOR v3 — Seamless Dual-Map Viewer
   Aesthetic: Dark Cartographic / Fantasy Atlas
   ============================================================ */

:root {
  --bg:          #0b0e16;
  --chrome:      #12161f;
  --border:      #2a3040;
  --gold:        #c9a961;
  --gold-dim:    #8a7040;
  --gold-bright: #f0d080;
  --text:        #d4c9a8;
  --text-dim:    #7a7060;
  --red:         #9a3a3a;
  --blue:        #2a5a9a;
  --green:       #3a7a5a;
  --panel-bg:    rgba(11,14,22,0.92);
  --seam-w:      60px;   /* blending seam width */
  --topbar-h:    48px;
  --footer-h:    36px;
}

*, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }

html, body {
  width: 100%; height: 100%; overflow: hidden;
  background: var(--bg);
  color: var(--text);
  font-family: 'Noto Serif JP', 'Georgia', serif;
  -webkit-font-smoothing: antialiased;
}

/* ── TOPBAR ─────────────────────────────────────────────── */
.topbar {
  position: fixed; top: 0; left: 0; right: 0;
  height: var(--topbar-h);
  background: var(--chrome);
  border-bottom: 1px solid var(--border);
  display: flex; align-items: center; gap: 12px;
  padding: 0 16px;
  z-index: 100;
  box-shadow: 0 2px 16px rgba(0,0,0,0.5);
}

.topbar-back {
  color: var(--gold-dim);
  text-decoration: none;
  font-size: 11px;
  letter-spacing: .08em;
  white-space: nowrap;
  transition: color .2s;
}
.topbar-back:hover { color: var(--gold); }

.topbar-sep {
  width: 1px; height: 20px;
  background: var(--border);
  flex-shrink: 0;
}

.topbar-title {
  font-size: 13px;
  font-weight: 600;
  letter-spacing: .12em;
  color: var(--gold);
  white-space: nowrap;
}
.topbar-title span {
  font-size: 9px;
  color: var(--text-dim);
  letter-spacing: .18em;
  display: block;
  margin-top: 1px;
}

.topbar-spacer { flex: 1; }

/* map-mode toggle */
.mode-tabs {
  display: flex; gap: 2px;
  background: rgba(0,0,0,0.3);
  border: 1px solid var(--border);
  border-radius: 4px;
  padding: 2px;
}
.mode-tab {
  background: none; border: none;
  color: var(--text-dim);
  font-size: 10px; letter-spacing: .1em;
  padding: 4px 10px;
  border-radius: 3px;
  cursor: pointer;
  transition: all .15s;
  white-space: nowrap;
}
.mode-tab.active {
  background: var(--gold-dim);
  color: var(--bg);
}
.mode-tab:hover:not(.active) { color: var(--text); }

/* minimap toggle */
.minimap-toggle {
  background: none; border: 1px solid var(--border);
  color: var(--text-dim); font-size: 10px;
  padding: 4px 8px; border-radius: 3px;
  cursor: pointer; transition: all .15s;
  letter-spacing: .08em;
}
.minimap-toggle:hover { border-color: var(--gold-dim); color: var(--gold-dim); }

/* zoom controls */
.zoom-controls {
  display: flex; gap: 2px;
}
.zoom-btn {
  background: none; border: 1px solid var(--border);
  color: var(--text-dim); width: 28px; height: 28px;
  border-radius: 3px; cursor: pointer;
  font-size: 14px; line-height: 1;
  transition: all .15s;
  display: flex; align-items: center; justify-content: center;
}
.zoom-btn:hover { border-color: var(--gold-dim); color: var(--gold); }

/* ── VIEWPORT ────────────────────────────────────────────── */
.map-viewport {
  position: fixed;
  top: var(--topbar-h);
  bottom: var(--footer-h);
  left: 0; right: 0;
  overflow: hidden;
  cursor: grab;
  background: var(--bg);
  /* Subtle noise texture */
  background-image:
    radial-gradient(ellipse at 20% 80%, rgba(42,90,154,0.06) 0%, transparent 60%),
    radial-gradient(ellipse at 80% 20%, rgba(201,169,97,0.04) 0%, transparent 50%);
}
.map-viewport.dragging { cursor: grabbing; }

/* ── CANVAS WORLD ────────────────────────────────────────── */
#world {
  position: absolute;
  /* size set by JS */
  transform-origin: 0 0;
  will-change: transform;
}

/* ── MAP LAYERS ──────────────────────────────────────────── */
.map-layer {
  position: absolute;
  top: 0;
  image-rendering: crisp-edges;
  display: block;
}

/* Grand Central — left map */
#layer-gc {

==================================================
FILE: ./viewer/db/city/_local_staging_backup/grand-central_wip_2026-04-16_1811/v3/v3.js
--------------------------------------------------
/**
 * GRAND CENTRAL NAVIGATOR v3
 * Seamless dual-map viewer
 * grand-central-map.png (1408×768) + academy-map.png (1380×752)
 */

'use strict';

// ─── CONFIG ────────────────────────────────────────────────────────────────

const CFG = {
  gc: { src: '../grand-central-map.png', w: 1408, h: 768 },
  ac: { src: '../academy-map.png',        w: 1380, h: 752 },

  // Where academy left-edge anchors on the combined world X axis
  // We overlap slightly so the seam at the 士官予備区 feels continuous.
  // Visually the 士官予備区 is roughly x=40–55% on GC map.
  // We want academy to "grow out" of that zone toward the right.
  // Overlap amount: 120px (the two images share ~120px of visual territory)
  overlap: 130,

  // Vertical offset: academy map is slightly taller/shorter than GC.
  // Center academy vertically relative to GC height.
  get acOffsetY() {
    return Math.round((CFG.gc.h - CFG.ac.h) / 2);
  },

  // World dimensions
  get worldW() { return CFG.gc.w + CFG.ac.w - CFG.overlap; },
  get worldH() { return Math.max(CFG.gc.h, CFG.ac.h); },

  // Seam X position (right edge of GC / left edge of AC in world coords)
  get seamX()   { return CFG.gc.w - CFG.overlap; },

  // Zoom
  minZoom: 0.35,
  maxZoom: 2.5,
  zoomStep: 0.18,
  initialZoom: null, // computed on init
};

// Hotspot data — all coords in WORLD-PIXEL space
// GC POIs use the x_pct/y_pct from map-data.json → converted to px
// Academy POIs are defined separately in world coords
const ACADEMY_POIS = [
  {
    id: 'ac_main_building',
    name: '本校舎',
    name_en: 'Main Academy Building',
    district: '士官学校',
    icon: '◈', color: '#9a3a3a',
    wx_pct: null, wy_pct: null,   // set below
    ac_x_pct: 49, ac_y_pct: 14,  // relative to academy image
    filters: ['officer'],
    description: '士官学校の中心棟。入学式・全校集会・重要行事が行われる。高い尖塔と石造りの外観が特徴。',
    lore_officer: 'ジェイドの入学式の舞台。グローリアテスト結果の発表もここで行われる。',
  },
  {
    id: 'ac_central_plaza',
    name: '中央広場',
    name_en: 'Central Plaza',
    district: '士官学校',
    icon: '◉', color: '#c9a961',
    ac_x_pct: 44, ac_y_pct: 46,
    filters: ['officer'],
    description: '本校舎正面の大広場。朝礼・訓練集合・自由時間の場として機能する。',
    lore_officer: '生徒同士の交流の起点。イベントフラグが多数存在する場所。',
  },
  {
    id: 'ac_faculty_building',
    name: '教官棟',
    name_en: 'Faculty Building',
    district: '士官学校',
    icon: '▣', color: '#2a5a9a',
    ac_x_pct: 20, ac_y_pct: 22,
    filters: ['officer'],
    description: 'ユミナら教官の執務室・会議室が集まる棟。許可なき立入禁止。',
    lore_officer: 'ユミナとの面談シーン・成績通知の舞台候補。',
  },
  {
    id: 'ac_dormitory',
    name: '学生寮',
    name_en: 'Student Dormitory',
    district: '士官学校',
    icon: '□', color: '#4a6a5a',
    ac_x_pct: 14, ac_y_pct: 38,
    filters: ['officer'],
    description: '学生の宿舎区画。複数棟から成り、性別・学年で分かれている。',
    lore_officer: '夜の自由時間・秘密の交流・夜化因子発覚前夜の描写の主舞台候補。',
  },
  {
    id: 'ac_front_gate',
    name: '士官学校正門',
    name_en: 'Academy Main Gate',
    district: '士官学校',
    icon: '→', color: '#8a7040',
    ac_x_pct: 64, ac_y_pct: 46,
    filters: ['officer', 'main'],
    description: 'グランセントラル都市部から東門街道経由でアクセスできる正門。通学生・来訪者の主要入口。',
    lore_officer: 'ジェイドが初登校する際に通過する門。都市側との接続点。',
    lore_main: '本編でのFO訓練許可証の受取場所候補。',
  },
  {
    id: 'ac_training_ground_inner',
    name: '訓練校（詳細）',
    name_en: 'Inner Training Ground',
    district: '士官予備区',
    icon: '△', color: '#7a2a2a',
    ac_x_pct: 34, ac_y_pct: 78,
    filters: ['officer'],
    description: '実戦演習・魔法戦闘訓練が行われる内部演習場。アカデミーマップ上の詳細位置。',
    lore_officer: 'グローリアテストの試験広場。心音能力の意図せぬ発動が起こりうる現場。',
  },
  {
    id: 'ac_star_church_near',
    name: '星導教会（近傍）',
    name_en: 'Star Guide Church (nearby)',
    district: '教会旧市街',
    icon: '✦', color: '#9a6aaa',
    ac_x_pct: 62, ac_y_pct: 68,
    filters: ['church'],
    description: '士官学校エリアから見た星導教会の方角。裏門からアクセス可能な旧市街側。',
    lore_church: 'ルカが勤務する教会への近道ルート。士官学校生が密かに抜ける裏路地。',
  },
];

// ─── STATE ─────────────────────────────────────────────────────────────────

const S = {
  zoom: 1,
  panX: 0,
  panY: 0,
  dragging: false,
  lastMX: 0, lastMY: 0,
  activeFilters: new Set(['zero','main','officer','church','harbor']),
  mapMode: 'combined', // 'combined' | 'gc' | 'ac'
  minimapVisible: true,
  allPois: [],   // merged GC + AC pois in world-px coords
};

// ─── DOM REFS ───────────────────────────────────────────────────────────────

const $ = id => document.getElementById(id);
let vp, world, layerGC, layerAC,
    pinLayer, coordsEl, footerCount,
    detailPanel, detailBar, detailClose,
    detailDistrict, detailName, detailNameEn, detailDesc, detailLore,
    filterBar, minimapWrap, minimapCanvas, minimapCtx, minimapRect;

// ─── INIT ───────────────────────────────────────────────────────────────────

window.addEventListener('DOMContentLoaded', () => {
  // cache DOM
  vp            = $('map-viewport');
  world         = $('world');
  layerGC       = $('layer-gc');
  layerAC       = $('layer-ac');
  pinLayer      = $('poi-pin-layer');
  coordsEl      = $('map-coords');
  footerCount   = $('footer-poi-count');

==================================================
FILE: ./viewer/db/city/grand-central/city.css
--------------------------------------------------
@import url('https://fonts.googleapis.com/css2?family=Cormorant+Garamond:ital,wght@0,300;0,400;0,600;1,400&family=Noto+Serif+JP:wght@300;400;500&display=swap');

/* ============================================================
   CSS VARIABLES  （terror.css から継承）
   ============================================================ */
:root {
  --bg:           #060a11;
  --bg2:          #0a1020;
  --bg3:          #0e1628;
  --panel:        #0d1525;
  --panel2:       #111d35;

  --gold:         #c9a961;
  --gold2:        #e8c97a;
  --gold-dim:     #7a6035;
  --gold-glow:    rgba(201,169,97,0.15);

  --text:         #d8ccb4;
  --text2:        #7a6e58;
  --text3:        #4a4030;

  --border:       rgba(201,169,97,0.14);
  --border2:      rgba(201,169,97,0.30);
  --border3:      rgba(201,169,97,0.50);

  /* 区画カラー */
  --d-market:     #b8860b;   /* 中央市場区   */
  --d-east:       #3a6e5a;   /* 東住宅区     */
  --d-admin:      #2a4a7a;   /* 行政中枢区   */
  --d-port:       #4a4a6a;   /* 港湾物流区   */
  --d-church:     #7a3a6a;   /* 教会旧市街   */
  --d-officer:    #7a2a2a;   /* 士官予備区   */
}

/* ============================================================
   RESET & BASE
   ============================================================ */
*, *::before, *::after { margin: 0; padding: 0; box-sizing: border-box; }

html { scroll-behavior: smooth; }

body {
  background: var(--bg);
  color: var(--text);
  font-family: 'Noto Serif JP', serif;
  font-weight: 300;
  line-height: 1.8;
  min-height: 100vh;
  overflow-x: hidden;
}

/* 背景テクスチャ */
body::before {
  content: '';
  position: fixed; inset: 0;
  background:
    radial-gradient(ellipse 70% 40% at 50% 0%,   rgba(201,169,97,0.04) 0%, transparent 60%),
    radial-gradient(ellipse 60% 50% at 80% 100%,  rgba(20,40,90,0.12)  0%, transparent 60%),
    repeating-linear-gradient(
      45deg, transparent, transparent 100px,
      rgba(201,169,97,0.010) 100px, rgba(201,169,97,0.010) 101px
    );
  pointer-events: none;
  z-index: 0;
}

/* ============================================================
   SHELL
   ============================================================ */
.city-shell {
  position: relative;
  z-index: 1;
  max-width: 1200px;
  margin: 0 auto;
  padding: 0 24px 100px;
}

/* ============================================================
   ① BACK LINK
   ============================================================ */
.back-link {
  display: inline-flex;
  align-items: center;
  gap: 8px;
  font-family: 'Cormorant Garamond', serif;
  font-size: 11px;
  letter-spacing: 3px;
  color: var(--text2);
  text-decoration: none;
  padding: 20px 0;
  transition: color 0.2s;
}
.back-link:hover { color: var(--gold); }

/* ============================================================
   ② HEADER
   ============================================================ */
.city-header {
  padding: 52px 0 44px;
  border-bottom: 1px solid var(--border);
  margin-bottom: 60px;
  position: relative;
}

/* 金ラインアクセント */
.city-header::after {
  content: '';
  position: absolute;
  bottom: -1px; left: 0;
  width: 140px; height: 1px;
  background: var(--gold);
}

.city-archive-tag {
  font-family: 'Cormorant Garamond', serif;
  font-size: 9px;
  letter-spacing: 6px;
  color: var(--gold-dim);
  text-transform: uppercase;
  margin-bottom: 16px;
}

.city-title-ja {
  font-family: 'Noto Serif JP', serif;
  font-size: clamp(1.8rem, 5vw, 3rem);
  font-weight: 300;
  color: var(--text);
  letter-spacing: 0.12em;
  line-height: 1.3;
  margin-bottom: 8px;
}

.city-title-en {
  font-family: 'Cormorant Garamond', serif;
  font-size: clamp(0.65rem, 1.8vw, 0.85rem);
  letter-spacing: 5px;
  color: var(--gold);
  opacity: 0.65;
  text-transform: uppercase;
  margin-bottom: 28px;
}

.city-meta-row {
  display: flex;
  flex-wrap: wrap;
  gap: 10px;
}

.city-meta-chip {
  font-family: 'Cormorant Garamond', serif;
  font-size: 9px;
  letter-spacing: 3px;
  color: var(--text2);
  padding: 5px 14px;
  border: 1px solid var(--border);
  background: var(--panel);
  text-transform: uppercase;
}

/* ============================================================

==================================================
FILE: ./viewer/db/city/grand-central/index.html
--------------------------------------------------
<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>グランセントラル都市区画図 | GRAND CENTRAL CITY DISTRICT MAP</title>
  <meta name="description" content="ZERO編〜本編の都市生活圏ベースマップ。中央市場テロ事件・ジェイド生活圏・士官学校導線に対応。">
  <link rel="stylesheet" href="./city.css">
</head>
<body>

<div class="city-shell">

  <!-- ============================================================
       BACK LINK
       ============================================================ -->
  <section id="sec-nav">
    <a class="back-link" href="../index.html">← INCIDENT RECORD</a>
  </section>

  <!-- ============================================================
       ① HEADER
       ============================================================ -->
  <section id="sec-header">
    <header class="city-header">
      <div class="city-archive-tag">City District Archive</div>
      <h1 class="city-title-ja">グランセントラル都市区画図</h1>
      <div class="city-title-en">Grand Central City District Map</div>
      <div class="city-meta-row">
        <span class="city-meta-chip">ZERO編 → 本編</span>
        <span class="city-meta-chip">World: Meritocracia</span>
        <span class="city-meta-chip">Base Map v1.0</span>
      </div>
    </header>
  </section>

  <!-- ============================================================
       ② MAP VIEWER
       ============================================================ -->
  <section id="sec-map" class="section-block">
    <div class="section-eyebrow">District Overview</div>
    <h2 class="section-title">都市全景図</h2>

    <div class="map-frame">
      <div class="map-frame-inner">

        <!-- ★ 画像本体 — ファイルを差し替えるだけで運用可 -->
        <img
          class="map-image"
          src="./grand-central-map.png"
          alt="グランセントラル都市区画図 — 中央市場区・行政中枢区・東住宅区・港湾物流区・教会旧市街・士官予備区"
          loading="eager"
        >

        <!-- POI追加用レイヤー（将来の拡張用 — 現時点は空） -->
        <div class="map-poi-layer" id="poi-layer" aria-hidden="true">
          <!--
            【POI追加手順】
            <button class="poi-pin" style="left:XX%; top:YY%;" title="スポット名">
              <span class="poi-label">スポット名</span>
            </button>
          -->
        </div>

      </div><!-- /.map-frame-inner -->
    </div><!-- /.map-frame -->
  </section>

  <!-- ============================================================
       ③ DISTRICT LEGEND
       ============================================================ -->
  <section id="sec-districts" class="section-block">
    <div class="section-eyebrow">District Index</div>
    <h2 class="section-title">区画一覧</h2>

    <div class="district-grid">

      <!-- 中央市場区 -->
      <article class="district-card" style="--d-color: #b8860b;">
        <div class="district-swatch"></div>
        <div class="district-body">
          <div class="district-name-ja">中央市場区</div>
          <div class="district-name-en">Central Market District</div>
          <p class="district-notes">
            都市の流通・商業の中核。中央市場・祝祭広場を擁し、
            平時は賑わいの象徴。ZERO編「中央市場炎槍事件」の主舞台。
          </p>
          <div class="district-tags">
            <span class="district-tag">MARKET</span>
            <span class="district-tag">INCIDENT ZONE</span>
          </div>
        </div>
      </article>

      <!-- 東住宅区 -->
      <article class="district-card" style="--d-color: #3a6e5a;">
        <div class="district-swatch"></div>
        <div class="district-body">
          <div class="district-name-ja">東住宅区</div>
          <div class="district-name-en">East Residential District</div>
          <p class="district-notes">
            市民の住宅地帯。ジェイド居住圏・パン屋通りを含む。
            本編では日常生活と士官学校通学の生活導線として機能。
          </p>
          <div class="district-tags">
            <span class="district-tag">RESIDENTIAL</span>
            <span class="district-tag">ジェイド生活圏</span>
          </div>
        </div>
      </article>

      <!-- 行政中枢区 -->
      <article class="district-card" style="--d-color: #2a4a7a;">
        <div class="district-swatch"></div>
        <div class="district-body">
          <div class="district-name-ja">行政中枢区</div>
          <div class="district-name-en">Administrative Core District</div>
          <p class="district-notes">
            中央庁舎・三兵団本部・審問庁を擁する権力の中枢。
            ZERO編後半の「公開尋問」導線と国家的幕引きの舞台。
          </p>
          <div class="district-tags">
            <span class="district-tag">GOVERNMENT</span>
            <span class="district-tag">FO本部</span>
          </div>
        </div>
      </article>

      <!-- 港湾物流区 -->
      <article class="district-card" style="--d-color: #4a4a6a;">
        <div class="district-swatch"></div>
        <div class="district-body">
          <div class="district-name-ja">港湾物流区</div>
          <div class="district-name-en">Harbor & Logistics District</div>
          <p class="district-notes">
            交易港・倉庫街・魔石流通センターを抱える物流拠点。
            炎翼セルの資材調達・潜伏ルートとの関連が示唆される。
          </p>
          <div class="district-tags">
            <span class="district-tag">HARBOR</span>
            <span class="district-tag">LOGISTICS</span>
          </div>
        </div>
      </article>

      <!-- 教会旧市街 -->
      <article class="district-card" style="--d-color: #7a3a6a;">
        <div class="district-swatch"></div>
        <div class="district-body">
          <div class="district-name-ja">教会旧市街</div>
          <div class="district-name-en">Old Church Quarter</div>
          <p class="district-notes">
            星導教会・地下礼拝路入口・鐘楼坂を含む旧市街地。
            炎翼セル上位組織の接続拠点・エリオス黒幕候補の活動圏。
          </p>
          <div class="district-tags">
            <span class="district-tag">CHURCH</span>
            <span class="district-tag">SHADOW OPS</span>
          </div>
        </div>

==================================================
FILE: ./viewer/db/city/index.html
--------------------------------------------------
<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>メリトクラシア国 地域一覧</title>
  <style>
    body {
      background: #0b1220;
      color: #e5d0a2;
      font-family: sans-serif;
      padding: 40px;
    }
    h1 {
      margin-bottom: 24px;
    }
    .card-grid {
      display: grid;
      gap: 16px;
      max-width: 700px;
    }
    .card {
      border: 1px solid #8a6a2f;
      padding: 20px;
      border-radius: 8px;
      background: #10192b;
      transition: 0.2s;
    }
    .card:hover {
      background: #162238;
    }
    a {
      text-decoration: none;
      color: inherit;
    }
    h2 {
      margin: 0 0 8px;
    }
    p {
      margin: 0;
      color: #b8a47d;
    }
  </style>
</head>
<body>
  <h1>🏙 メリトクラシア国 地域一覧</h1>

  <div class="card-grid">
    <a href="./grand-central/">
      <div class="card">
        <h2>🏛 グランセントラル都市区画図</h2>
        <p>本番運用版 / Incident DB / Tactical Map</p>
      </div>
    </a>

    <a href="./_local_staging_backup/grand-central_interactive_v1/">
      <div class="card">
        <h2>🗺 Grand Central Interactive v1</h2>
        <p>Gemini 1枚地図完全再現 / POI詳細 / Zoom対応</p>
      </div>
    </a>
    
 <a href="./_local_staging_backup/academy_interactive_v1/">
  <div class="card">
    <h2>🎓 Academy Interactive v1</h2>
    <p>士官学校詳細図 / 校舎POI / 日常導線 / ZERO地下接続</p>
  </div>
</a>

  </div>
</body>
</html>
==================================================
FILE: ./viewer/db/mythology/index.html
--------------------------------------------------
<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="UTF-8">
  <title>Mythology | Meritocracia</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      background: #0f1115;
      color: #e5e7eb;
      margin: 0;
      padding: 40px;
      line-height: 1.6;
    }
    h1, h2, h3 {
      color: #60a5fa;
    }
    .section {
      margin-bottom: 40px;
      padding: 20px;
      border-left: 3px solid #60a5fa;
      background: #1a1d24;
    }
    .node {
      margin-left: 20px;
    }
    .arrow {
      color: #9ca3af;
      margin: 10px 0;
    }
  </style>
</head>

<body>

<h1>🌌 Mythology Tree</h1>
<p>メリトクラシア 神話構造（v1.0）</p>

<div class="section">
  <h2>■ 原初層</h2>
  <div class="node">カオス：絶対的選別存在</div>
</div>

<div class="section">
  <h2>■ 天界層</h2>
  <div class="node">メタトロン：選別・最適化</div>
  <div class="node">ルシファー：反逆・強者選別</div>
</div>

<div class="section">
  <h2>■ 分岐点</h2>
  <div class="node">メタトロン崩壊</div>
  <div class="node">原因：正しさが人を救わない</div>
</div>

<div class="section">
  <h2>■ 分裂構造</h2>
  <div class="node">① 本体：封印</div>
  <div class="node">② 機能：心音システム</div>
  <div class="node">③ 意志：ジェイドへ転生</div>
</div>

<div class="section">
  <h2>■ 堕天使層</h2>
  <div class="node">ベルフェゴール：観測者</div>
</div>

<div class="section">
  <h2>■ 人間世界</h2>
  <div class="node">メリトクラシア：能力選別国家</div>
  <div class="node">グローリアテスト：地上の選別装置</div>
</div>

<div class="section">
  <h2>■ 主人公</h2>
  <div class="node">ジェイド：選別を拒否する存在</div>
</div>

<div class="section">
  <h2>■ 最終構造</h2>
  <div class="node">カオス：信仰選別</div>
  <div class="node">ルシファー：強者選別</div>
  <div class="node">国家：能力選別</div>
  <div class="arrow">↓</div>
  <div class="node">ジェイド：選別の拒否</div>
</div>

</body>
</html>

==================================================
FILE: ./viewer/db/nation/citizen-petition-system/index.html
--------------------------------------------------
<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>貴族陳情制度 | Meritocracia DB</title>
  <link rel="stylesheet" href="style.css">
</head>
<body>
  <main class="container">
    <h1>🏛 貴族陳情制度</h1>
    <p class="lead">
      平民は国家へ直接不満や要望を届けず、担当貴族を通して中央へ上申する制度。
    </p>

    <section>
      <h2>制度概要</h2>
      <p>
        メリトクラシアにおける民意の流れを整理する正式制度。
        発言権には責任と資格が伴い、街区ごとの担当貴族が窓口となる。
      </p>
    </section>

    <section>
      <h2>士官学校編での活用</h2>
      <p>
        学費、商業規制、治安要請、抗議活動などを
        暴徒化ではなく制度的な政治行動として描写できる。
      </p>
    </section>

    <section>
      <h2>国家テーマ接続</h2>
      <p>
        「声を上げる資格は責任によって得る」──
        実力主義国家メリトクラシアを象徴する制度。
      </p>
    </section>
<a href="./citizen-petition-system/">
  <div class="card">
    <small>POLITICAL SYSTEM</small>
    <h2>貴族陳情制度</h2>
    <p>平民の声を担当貴族経由で中央へ届ける正式制度</p>
  </div>
</a>

  </main>
</body>
</html>

==================================================
FILE: ./viewer/db/nation/citizen-petition-system/index_backup_2026-04-15_1657.html
--------------------------------------------------
<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>貴族陳情制度 | Meritocracia DB</title>
  <link rel="stylesheet" href="style.css">
</head>
<body>
  <main class="container">
    <h1>🏛 貴族陳情制度</h1>
    <p class="lead">
      平民は国家へ直接不満や要望を届けず、担当貴族を通して中央へ上申する制度。
    </p>

    <section>
      <h2>制度概要</h2>
      <p>
        メリトクラシアにおける民意の流れを整理する正式制度。
        発言権には責任と資格が伴い、街区ごとの担当貴族が窓口となる。
      </p>
    </section>

    <section>
      <h2>士官学校編での活用</h2>
      <p>
        学費、商業規制、治安要請、抗議活動などを
        暴徒化ではなく制度的な政治行動として描写できる。
      </p>
    </section>

    <section>
      <h2>国家テーマ接続</h2>
      <p>
        「声を上げる資格は責任によって得る」──
        実力主義国家メリトクラシアを象徴する制度。
      </p>
    </section>
  </main>
</body>
</html>

==================================================
FILE: ./viewer/db/nation/citizen-petition-system/style.css
--------------------------------------------------
body {
  margin: 0;
  min-height: 100vh;
  color: #e8e8e8;
  font-family: sans-serif;
  line-height: 1.8;
  background:
    radial-gradient(circle at top right, rgba(25, 60, 120, 0.25), transparent 40%),
    radial-gradient(circle at bottom left, rgba(10, 40, 90, 0.35), transparent 45%),
    linear-gradient(135deg, #07111f 0%, #09182a 40%, #0b1f36 100%);
}

.container {
  max-width: 900px;
  margin: 48px auto;
  padding: 40px 28px;
  background: rgba(8, 18, 36, 0.55);
  border: 1px solid rgba(212, 175, 55, 0.15);
  border-radius: 16px;
  backdrop-filter: blur(6px);
  box-shadow: 0 10px 30px rgba(0, 0, 0, 0.25);
}

h1, h2 {
  color: #d6b36a;
  letter-spacing: 0.04em;
}

.lead {
  font-size: 1.1rem;
  color: #d7dbe4;
}

section {
  margin-top: 32px;
  padding-top: 12px;
  border-top: 1px solid rgba(214, 179, 106, 0.12);
}

==================================================
FILE: ./viewer/db/nation/government/central_government_phase2.html
--------------------------------------------------
<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>中央政府組織図 Phase2 | Nation Archive</title>
  <link href="https://fonts.googleapis.com/css2?family=Cormorant+Garamond:wght@400;600&family=Noto+Serif+JP:wght@300;400;500&display=swap" rel="stylesheet">
  <style>
    :root {
      --bg: #070d18;
      --panel: #0d1526;
      --panel2: #111e35;
      --border: rgba(201,169,97,0.2);
      --border-bright: rgba(201,169,97,0.5);
      --gold: #c9a961;
      --gold-dim: #8a7040;
      --text: #f4efe2;
      --muted: #a59a7a;
      --dim: #5a5040;
      --zg: #4a7fc1;
      --fo: #c14a4a;
      --va: #7a4ac1;
      --lotus: #c9a961;
    }
    * { margin:0; padding:0; box-sizing:border-box; }
    body { background:var(--bg); color:var(--text); font-family:'Noto Serif JP',serif; min-height:100vh; line-height:1.8; }
    body::before {
      content:''; position:fixed; inset:0;
      background:
        radial-gradient(ellipse 70% 50% at 50% 0%, rgba(13,30,60,0.8) 0%, transparent 60%),
        repeating-linear-gradient(45deg, transparent, transparent 80px, rgba(201,169,97,0.015) 80px, rgba(201,169,97,0.015) 81px);
      pointer-events:none; z-index:0;
    }
    .shell { position:relative; z-index:1; max-width:900px; margin:0 auto; padding:56px 24px 100px; }
    .back { display:inline-flex; align-items:center; gap:8px; font-family:'Cormorant Garamond',serif; font-size:11px; letter-spacing:3px; color:var(--muted); text-decoration:none; margin-bottom:40px; transition:color 0.2s; }
    .back:hover{color:var(--gold)}
    .doc-header { border-bottom:1px solid var(--border); padding-bottom:28px; margin-bottom:56px; }
    .doc-sub { font-family:'Cormorant Garamond',serif; font-size:11px; letter-spacing:5px; color:var(--gold); opacity:0.7; margin-bottom:12px; }
    .doc-title { font-family:'Cormorant Garamond',serif; font-size:clamp(2rem,6vw,3.2rem); font-weight:600; color:var(--gold); letter-spacing:0.06em; margin-bottom:10px; }
    .doc-meta { font-size:12px; color:var(--dim); letter-spacing:2px; }
    .doc-lead { margin-top:14px; font-size:14px; color:var(--muted); line-height:2; border-left:2px solid var(--border-bright); padding-left:16px; }
    .section { margin-bottom:64px; animation:fadein 0.6s ease both; }
    @keyframes fadein{from{opacity:0;transform:translateY(14px)}to{opacity:1;transform:translateY(0)}}
    .section:nth-child(2){animation-delay:.05s}.section:nth-child(3){animation-delay:.1s}
    .section:nth-child(4){animation-delay:.15s}.section:nth-child(5){animation-delay:.2s}
    .section:nth-child(6){animation-delay:.25s}.section:nth-child(7){animation-delay:.3s}
    .section:nth-child(8){animation-delay:.35s}.section:nth-child(9){animation-delay:.4s}
    .section:nth-child(10){animation-delay:.45s}
    .section-label { font-family:'Cormorant Garamond',serif; font-size:10px; letter-spacing:4px; color:var(--gold-dim); text-transform:uppercase; margin-bottom:6px; }
    .section-title { font-family:'Cormorant Garamond',serif; font-size:1.5rem; font-weight:600; color:var(--text); border-bottom:1px solid var(--border); padding-bottom:10px; margin-bottom:24px; }
    .body-text { font-size:14px; color:var(--muted); line-height:2.1; margin-bottom:16px; }
    .body-text strong{color:var(--text);font-weight:500}

    /* ── 組織図 ── */
    .org-chart {
      background:var(--panel); border:1px solid var(--border-bright);
      padding:32px; position:relative; overflow:hidden;
    }
    .org-chart::before { content:''; position:absolute; top:0; left:0; right:0; height:2px; background:linear-gradient(to right, var(--gold), transparent); }
    .org-top {
      text-align:center; margin-bottom:32px;
      padding-bottom:24px; border-bottom:1px solid var(--border);
    }
    .org-top-label { font-size:10px; letter-spacing:4px; color:var(--gold-dim); margin-bottom:8px; font-family:'Cormorant Garamond',serif; }
    .org-top-name { font-family:'Cormorant Garamond',serif; font-size:2rem; font-weight:600; color:var(--gold); }
    .org-top-role { font-size:12px; color:var(--muted); letter-spacing:2px; margin-top:4px; }
    .org-assistants { display:flex; gap:16px; justify-content:center; margin-bottom:24px; }
    .org-assist-card { padding:10px 18px; border:1px solid var(--border); background:rgba(7,13,24,0.6); font-size:12px; color:var(--muted); text-align:center; }
    .org-assist-card .label { font-size:10px; color:var(--gold-dim); letter-spacing:2px; margin-bottom:4px; font-family:'Cormorant Garamond',serif; }
    .org-grid { display:grid; grid-template-columns:repeat(auto-fill,minmax(180px,1fr)); gap:10px; }
    .org-node { padding:12px 16px; border:1px solid var(--border); background:rgba(7,13,24,0.5); }
    .org-node-label { font-size:9px; letter-spacing:2px; color:var(--gold-dim); margin-bottom:4px; font-family:'Cormorant Garamond',serif; }
    .org-node-name { font-size:12px; color:var(--text); }

    /* ── 兵団責任者 ── */
    .corps-leader-cards { display:grid; grid-template-columns:repeat(auto-fit,minmax(260px,1fr)); gap:20px; margin-top:8px; }
    .corps-leader { background:var(--panel); border:1px solid var(--border); padding:22px; position:relative; overflow:hidden; transition:border-color 0.25s; }
    .corps-leader::before { content:''; position:absolute; top:0; left:0; width:100%; height:3px; }
    .corps-leader.zg::before{background:var(--zg)} .corps-leader.fo::before{background:var(--fo)} .corps-leader.va::before{background:var(--va)}
    .corps-leader.zg:hover{border-color:rgba(74,127,193,0.4)} .corps-leader.fo:hover{border-color:rgba(193,74,74,0.4)} .corps-leader.va:hover{border-color:rgba(122,74,193,0.4)}
    .cl-corps { font-family:'Cormorant Garamond',serif; font-size:11px; letter-spacing:3px; margin-bottom:6px; }
    .cl-corps.zg{color:var(--zg)} .cl-corps.fo{color:var(--fo)} .cl-corps.va{color:var(--va)}
    .cl-name { font-family:'Cormorant Garamond',serif; font-size:20px; color:var(--text); margin-bottom:4px; }
    .cl-role { font-size:11px; color:var(--gold-dim); letter-spacing:1px; }
    .cl-tbd { color:var(--dim); font-style:italic; }

    /* ── 4層テンプレ ── */
    .layer-flow { display:flex; flex-direction:column; gap:0; margin-top:8px; }
    .layer-item { display:flex; align-items:stretch; border:1px solid var(--border); margin-bottom:-1px; background:var(--panel); transition:background 0.2s; }
    .layer-item:hover{background:var(--panel2)}
    .layer-num { width:40px; display:flex; align-items:center; justify-content:center; font-family:'Cormorant Garamond',serif; font-size:16px; color:var(--gold-dim); border-right:1px solid var(--border); flex-shrink:0; }
    .layer-body { padding:14px 20px; flex:1; }
    .layer-role { font-family:'Cormorant Garamond',serif; font-size:14px; color:var(--gold); margin-bottom:4px; }
    .layer-desc { font-size:12px; color:var(--muted); }

    /* ── 人物カード（副官・若手・現場） ── */
    .person-cards { display:grid; grid-template-columns:repeat(auto-fit,minmax(260px,1fr)); gap:20px; margin-top:8px; }
    .person-card { background:var(--panel); border:1px solid var(--border); padding:22px; transition:border-color 0.25s; }
    .person-card:hover{border-color:var(--border-bright)}
    .pc-corps { font-family:'Cormorant Garamond',serif; font-size:10px; letter-spacing:3px; margin-bottom:6px; }
    .pc-corps.zg{color:var(--zg)} .pc-corps.fo{color:var(--fo)} .pc-corps.va{color:var(--va)}
    .pc-tag { font-size:9px; letter-spacing:2px; color:var(--dim); margin-bottom:8px; font-family:'Cormorant Garamond',serif; }
    .pc-name { font-family:'Cormorant Garamond',serif; font-size:18px; color:var(--text); margin-bottom:4px; }
    .pc-role { font-size:11px; color:var(--gold-dim); letter-spacing:1px; margin-bottom:12px; }
    .pc-funcs { display:flex; flex-wrap:wrap; gap:5px; }
    .pc-func { font-size:10px; padding:2px 8px; border:1px solid var(--border); color:var(--muted); }

    /* ── 本編接続マップ ── */
    .connection-grid { display:grid; grid-template-columns:1fr; gap:10px; margin-top:8px; }
    .conn-row { display:flex; align-items:flex-start; gap:16px; padding:12px 16px; background:var(--panel); border:1px solid var(--border); }
    .conn-event { font-family:'Cormorant Garamond',serif; font-size:13px; color:var(--gold); min-width:150px; flex-shrink:0; }
    .conn-members { font-size:12px; color:var(--muted); line-height:1.8; }

    /* ── TODOリスト ── */
    .todo-block { margin-bottom:24px; }
    .todo-priority { font-family:'Cormorant Garamond',serif; font-size:11px; letter-spacing:3px; color:var(--gold); margin-bottom:10px; }
    .todo-list { list-style:none; }
    .todo-list li { font-size:13px; color:var(--muted); padding:7px 0; border-bottom:1px solid rgba(201,169,97,0.07); display:flex; align-items:center; gap:10px; }
    .todo-list li::before { content:'▷'; color:var(--gold-dim); font-size:10px; flex-shrink:0; }

    /* ── 結論ボックス ── */
    .conclusion-box { background:var(--panel); border:1px solid var(--border-bright); padding:28px; text-align:center; position:relative; }
    .conclusion-box::before { content:''; position:absolute; top:0; left:0; right:0; height:2px; background:linear-gradient(to right,var(--zg),var(--fo),var(--va),var(--gold)); }
    .conclusion-text { font-family:'Cormorant Garamond',serif; font-size:1.1rem; font-style:italic; color:var(--text); line-height:2; }
    .conclusion-text strong { color:var(--gold); }

    @media(max-width:600px){.corps-leader-cards,.person-cards{grid-template-columns:1fr}.org-grid{grid-template-columns:1fr 1fr}.conn-row{flex-direction:column}}
  </style>
</head>
<body>
<div class="shell">
  <a class="back" href="./index.html">← 国家機関一覧表</a>

  <header class="doc-header">
    <p class="doc-sub">GOVERNMENT DOCUMENT — 2026-04-02</p>
    <h1 class="doc-title">中央政府組織図 Phase2</h1>
    <p class="doc-meta">統合元：中央政府組織図ドラフト / 三兵団人物レイヤー設計ログ　｜　決定権者：Lancer</p>
    <p class="doc-lead">Dukeロータスを頂点とした中央政府の見取り図と三兵団の人物運用レイヤーを一本化。制度図 + 人物4層 + 本編接続を備えた国家ドラマDB Phase2 へ到達。</p>
  </header>

  <!-- 1. 結論 -->
  <section class="section">
    <p class="section-label">Section 01</p>
    <h2 class="section-title">本日の結論 — 中央政府 + 三兵団人物運用の統合</h2>
    <p class="body-text">本日の成果は、前日の三兵団制度設計をさらに拡張し、<strong>Dukeロータスを頂点とした中央政府の見取り図と、三兵団の人物運用レイヤーを一本化したこと</strong>である。</p>
    <p class="body-text">これにより、国家機関一覧は単なる制度図を超え、<strong>政治劇・士官学校編・市場テロ・宗教セル捜査まで接続可能な国家ドラマDB</strong>へ進化した。</p>
  </section>

  <!-- 2. 組織図 -->
  <section class="section">
    <p class="section-label">Section 02</p>
    <h2 class="section-title">国家中央政府 正式組織図 — 統合版</h2>
    <div class="org-chart">
      <div class="org-top">
        <div class="org-top-label">国家最高中枢</div>
        <div class="org-top-name">Duke ロータス</div>
        <div class="org-top-role">国家元首 / 最高統治責任者</div>
      </div>
      <div class="org-assistants">
        <div class="org-assist-card"><div class="label">首席補佐官</div>（未定）</div>

==================================================
FILE: ./viewer/db/nation/government/military_system_design_20260401.html
--------------------------------------------------
<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>三兵団制度設計 | Nation Archive</title>
  <link href="https://fonts.googleapis.com/css2?family=Cormorant+Garamond:wght@400;600&family=Noto+Serif+JP:wght@300;400;500&display=swap" rel="stylesheet">
  <style>
    :root {
      --bg: #070d18;
      --panel: #0d1526;
      --panel2: #111e35;
      --border: rgba(201,169,97,0.2);
      --border-bright: rgba(201,169,97,0.5);
      --gold: #c9a961;
      --gold-dim: #8a7040;
      --text: #f4efe2;
      --muted: #a59a7a;
      --dim: #5a5040;
      --zg: #4a7fc1;
      --fo: #c14a4a;
      --va: #7a4ac1;
    }
    * { margin:0; padding:0; box-sizing:border-box; }
    body {
      background: var(--bg);
      color: var(--text);
      font-family: 'Noto Serif JP', serif;
      min-height: 100vh; line-height: 1.8;
    }
    body::before {
      content:''; position:fixed; inset:0;
      background:
        radial-gradient(ellipse 70% 50% at 50% 0%, rgba(13,30,60,0.8) 0%, transparent 60%),
        repeating-linear-gradient(45deg, transparent, transparent 80px, rgba(201,169,97,0.015) 80px, rgba(201,169,97,0.015) 81px);
      pointer-events:none; z-index:0;
    }
    .shell { position:relative; z-index:1; max-width:900px; margin:0 auto; padding:56px 24px 100px; }
    .back {
      display:inline-flex; align-items:center; gap:8px;
      font-family:'Cormorant Garamond',serif; font-size:11px; letter-spacing:3px;
      color:var(--muted); text-decoration:none; margin-bottom:40px; transition:color 0.2s;
    }
    .back:hover { color:var(--gold); }
    .doc-header { border-bottom:1px solid var(--border); padding-bottom:28px; margin-bottom:56px; }
    .doc-sub { font-family:'Cormorant Garamond',serif; font-size:11px; letter-spacing:5px; color:var(--gold); opacity:0.7; margin-bottom:12px; }
    .doc-title { font-family:'Cormorant Garamond',serif; font-size:clamp(2rem,6vw,3.2rem); font-weight:600; color:var(--gold); letter-spacing:0.06em; margin-bottom:10px; }
    .doc-meta { font-size:12px; color:var(--dim); letter-spacing:2px; }
    .doc-lead { margin-top:14px; font-size:14px; color:var(--muted); line-height:2; border-left:2px solid var(--border-bright); padding-left:16px; }
    .section { margin-bottom:64px; animation:fadein 0.6s ease both; }
    @keyframes fadein { from{opacity:0;transform:translateY(14px)} to{opacity:1;transform:translateY(0)} }
    .section:nth-child(2){animation-delay:.05s} .section:nth-child(3){animation-delay:.1s}
    .section:nth-child(4){animation-delay:.15s} .section:nth-child(5){animation-delay:.2s}
    .section:nth-child(6){animation-delay:.25s} .section:nth-child(7){animation-delay:.3s}
    .section:nth-child(8){animation-delay:.35s}
    .section-label { font-family:'Cormorant Garamond',serif; font-size:10px; letter-spacing:4px; color:var(--gold-dim); text-transform:uppercase; margin-bottom:6px; }
    .section-title { font-family:'Cormorant Garamond',serif; font-size:1.5rem; font-weight:600; color:var(--text); border-bottom:1px solid var(--border); padding-bottom:10px; margin-bottom:24px; }
    .body-text { font-size:14px; color:var(--muted); line-height:2.1; margin-bottom:16px; }
    .body-text strong { color:var(--text); font-weight:500; }
    .axis-row { display:flex; gap:16px; flex-wrap:wrap; margin:20px 0; }
    .axis-badge { display:flex; align-items:center; gap:10px; padding:10px 20px; border:1px solid var(--border); background:rgba(13,21,38,0.8); font-family:'Cormorant Garamond',serif; font-size:15px; letter-spacing:2px; }
    .axis-badge .code { font-size:18px; font-weight:600; }
    .axis-badge .eq { color:var(--dim); }
    .axis-badge .val { color:var(--gold); }
    .axis-badge.zg .code{color:var(--zg)} .axis-badge.fo .code{color:var(--fo)} .axis-badge.va .code{color:var(--va)}
    .corps-cards { display:grid; grid-template-columns:repeat(auto-fit,minmax(260px,1fr)); gap:20px; margin-top:8px; }
    .corps-card { background:var(--panel); border:1px solid var(--border); padding:24px; position:relative; overflow:hidden; transition:border-color 0.25s,box-shadow 0.25s; }
    .corps-card::before { content:''; position:absolute; top:0; left:0; width:3px; height:100%; }
    .corps-card.zg::before{background:var(--zg)} .corps-card.fo::before{background:var(--fo)} .corps-card.va::before{background:var(--va)}
    .corps-card.zg:hover{border-color:rgba(74,127,193,0.5);box-shadow:0 0 20px rgba(74,127,193,0.1)}
    .corps-card.fo:hover{border-color:rgba(193,74,74,0.5);box-shadow:0 0 20px rgba(193,74,74,0.1)}
    .corps-card.va:hover{border-color:rgba(122,74,193,0.5);box-shadow:0 0 20px rgba(122,74,193,0.1)}
    .corps-name { font-family:'Cormorant Garamond',serif; font-size:20px; font-weight:600; margin-bottom:4px; }
    .corps-card.zg .corps-name{color:var(--zg)} .corps-card.fo .corps-name{color:var(--fo)} .corps-card.va .corps-name{color:var(--va)}
    .corps-full { font-size:11px; color:var(--dim); letter-spacing:2px; margin-bottom:14px; }
    .corps-desc { font-size:12px; color:var(--muted); margin-bottom:16px; line-height:1.8; }
    .tree-box { background:rgba(7,13,24,0.8); border:1px solid var(--border); padding:16px 20px; margin:12px 0; font-family:'Cormorant Garamond',serif; font-size:13px; letter-spacing:1px; }
    .tree-box .tree-line{color:var(--dim)} .tree-box .tree-node{color:var(--text)} .tree-box .tree-leaf{color:var(--muted);font-size:12px}
    .use-tags { display:flex; flex-wrap:wrap; gap:6px; margin-top:10px; }
    .use-tag { font-size:10px; letter-spacing:1px; padding:3px 10px; border:1px solid var(--border); color:var(--muted); }
    .rank-grid { display:grid; grid-template-columns:1fr 1fr 1fr 1fr; gap:1px; background:var(--border); border:1px solid var(--border); margin-top:16px; overflow:hidden; }
    .rank-header { background:var(--panel2); padding:10px 14px; font-family:'Cormorant Garamond',serif; font-size:11px; letter-spacing:3px; color:var(--gold); }
    .rank-cell { background:var(--panel); padding:10px 14px; font-size:12px; color:var(--muted); line-height:1.8; }
    .person-cards { display:grid; grid-template-columns:repeat(auto-fit,minmax(260px,1fr)); gap:20px; margin-top:8px; }
    .person-card { background:var(--panel); border:1px solid var(--border); padding:22px; transition:border-color 0.25s; }
    .person-card:hover{border-color:var(--border-bright)}
    .person-corps { font-size:10px; letter-spacing:3px; margin-bottom:8px; font-family:'Cormorant Garamond',serif; }
    .person-corps.zg{color:var(--zg)} .person-corps.fo{color:var(--fo)} .person-corps.va{color:var(--va)}
    .person-name { font-family:'Cormorant Garamond',serif; font-size:20px; color:var(--text); margin-bottom:4px; }
    .person-role { font-size:11px; color:var(--gold-dim); letter-spacing:1px; margin-bottom:12px; }
    .person-thought { font-size:12px; color:var(--muted); line-height:1.9; margin-bottom:12px; padding-left:10px; border-left:2px solid var(--border); }
    .person-thought .thought-tag { display:block; font-family:'Cormorant Garamond',serif; font-size:13px; color:var(--gold); font-style:italic; margin-bottom:6px; }
    .person-quote { font-family:'Cormorant Garamond',serif; font-style:italic; font-size:13px; color:var(--muted); border-top:1px solid var(--border); padding-top:12px; margin-top:8px; opacity:0.8; }
    .drama-box { background:var(--panel); border:1px solid var(--border-bright); padding:28px; position:relative; overflow:hidden; }
    .drama-box::before { content:''; position:absolute; top:0; left:0; right:0; height:2px; background:linear-gradient(to right,var(--zg),var(--fo),var(--va),var(--gold)); }
    .drama-line { display:flex; align-items:flex-start; gap:14px; padding:10px 0; border-bottom:1px solid var(--border); }
    .drama-line:last-child{border-bottom:none}
    .drama-who { font-family:'Cormorant Garamond',serif; font-size:13px; min-width:140px; }
    .drama-who.zg{color:var(--zg)} .drama-who.fo{color:var(--fo)} .drama-who.va{color:var(--va)} .drama-who.lotus{color:var(--gold)}
    .drama-action { font-size:13px; color:var(--muted); }
    .axis-list { display:grid; grid-template-columns:repeat(auto-fill,minmax(200px,1fr)); gap:10px; margin-top:8px; }
    .axis-item { padding:10px 16px; border:1px solid var(--border); background:var(--panel); font-size:12px; color:var(--muted); letter-spacing:1px; display:flex; align-items:center; gap:8px; }
    .axis-item::before{content:'◆';color:var(--gold-dim);font-size:8px}
    .todo-section{margin-top:8px}
    .priority-label { font-family:'Cormorant Garamond',serif; font-size:11px; letter-spacing:3px; color:var(--gold); margin:16px 0 8px; }
    .todo-list{list-style:none}
    .todo-list li { font-size:13px; color:var(--muted); padding:7px 0; border-bottom:1px solid rgba(201,169,97,0.07); display:flex; align-items:center; gap:10px; }
    .todo-list li::before{content:'▷';color:var(--gold-dim);font-size:10px;flex-shrink:0}
    @media(max-width:600px){.rank-grid{grid-template-columns:1fr 1fr}.corps-cards,.person-cards{grid-template-columns:1fr}}
  </style>
</head>
<body>
<div class="shell">
  <a class="back" href="./index.html">← 国家機関一覧表</a>

  <header class="doc-header">
    <p class="doc-sub">MILITARY DOCUMENT — 2026-04-01</p>
    <h1 class="doc-title">三兵団制度設計</h1>
    <p class="doc-meta">統合元：Gemini比較設計ログ / 国家機関一覧表ルームログ　｜　決定権者：Lancer</p>
    <p class="doc-lead">三兵団を増やさず内部機能の分化で国家の厚みを出す方針を確定。ZG・FO・VAの3軸と共通階層テンプレを正式採用した統合議事録。</p>
  </header>

  <section class="section">
    <p class="section-label">Section 01</p>
    <h2 class="section-title">本日の結論 — 重複統合後の正式コア</h2>
    <p class="body-text">本日の議論で確定した中核は、<strong>三兵団を増やさず内部機能の分化で国家の厚みを出す</strong>方針である。主人公ジェイドはこの3思想すべてと衝突・共鳴しながら成長する。</p>
    <div class="axis-row">
      <div class="axis-badge zg"><span class="code">ZG</span><span class="eq">=</span><span class="val">秩序</span></div>
      <div class="axis-badge fo"><span class="code">FO</span><span class="eq">=</span><span class="val">武力</span></div>
      <div class="axis-badge va"><span class="code">VA</span><span class="eq">=</span><span class="val">真実</span></div>
    </div>
  </section>

  <section class="section">
    <p class="section-label">Section 02</p>
    <h2 class="section-title">三兵団の正式内部構造 — 統合確定</h2>
    <p class="body-text">内部構造は読者理解とゲームUIを両立するため、以下の共通階層で統一する。</p>
    <div class="tree-box">
      <div><span class="tree-node">兵団</span></div>
      <div><span class="tree-line">└ </span><span class="tree-node">局</span></div>
      <div><span class="tree-line">　└ </span><span class="tree-node">連隊</span></div>
      <div><span class="tree-line">　　└ </span><span class="tree-node">班</span></div>
      <div><span class="tree-line">　　　└ </span><span class="tree-leaf">個人</span></div>
    </div>
    <p class="body-text" style="margin-top:20px;">各兵団の内部機能は以下の<strong>3層テンプレ</strong>で設計する。これを全兵団の黄金テンプレとして採用。</p>
    <div class="tree-box">
      <div><span class="tree-node">象徴層</span></div>
      <div><span class="tree-node">実務層</span></div>
      <div><span class="tree-node">特殊層</span></div>
    </div>
  </section>

  <section class="section">
    <p class="section-label">Section 03</p>
    <h2 class="section-title">三兵団正式テンプレ — 重複除去版</h2>
    <div class="corps-cards">
      <div class="corps-card zg">
        <div class="corps-name">ZG</div>
        <div class="corps-full">Zentral Garde</div>
        <div class="corps-desc">国家秩序と制度運営の中枢。</div>

==================================================
FILE: ./viewer/db/nation/index.html
--------------------------------------------------
<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Meritocracia Nation Archive</title>
  <link rel="stylesheet" href="nation.css">
</head>
<body>
  <main class="nation-shell">
    <header class="nation-header">
      <p class="sub">MERITOCRACIA CENTRAL ARCHIVE</p>
      <h1>国家制度アーカイブ</h1>
      <p class="lead">
        国家基礎・統治制度・軍事・階級制度を管理する正史閲覧ポータル
      </p>
    </header>

    <!-- 国家基礎 -->
    <section class="nation-category">
      <h2>🏛 国家基礎</h2>
      <div class="nation-grid">
        <a href="./overview/" class="nation-card">
          <span class="badge">OVERVIEW</span>
          <h2>国家概要</h2>
          <p>国名・人口・首都・政体・建国年の基本プロフィール</p>
        </a>
      </div>
    </section>

    <!-- 統治制度 -->
    <section class="nation-category">
      <h2>⚖ 統治制度</h2>
      <div class="nation-grid">
        <a href="./government/central_government_phase2.html" class="nation-card">
          <span class="badge">GOVERNMENT</span>
          <h2>中央政府組織図</h2>
          <p>中央政府 + 三兵団人物4層の統合運用DB</p>
        </a>

        <a href="./citizen-petition-system/" class="nation-card">
          <span class="badge">POLITICAL</span>
          <h2>貴族陳情制度</h2>
          <p>平民の声を担当貴族経由で中央へ届ける正式制度</p>
        </a>
      </div>
    </section>

    <!-- 軍事・治安 -->
    <section class="nation-category">
      <h2>⚔ 軍事・治安</h2>
      <div class="nation-grid">
        <a href="./三兵団/" class="nation-card">
          <span class="badge">MILITARY</span>
          <h2>三兵団</h2>
          <p>ZG / FO / VA の統治思想と兵団構造</p>
        </a>

        <a href="./government/military_system_design_20260401.html" class="nation-card">
          <span class="badge">MILITARY DOC</span>
          <h2>三兵団制度設計</h2>
          <p>ZG / FO / VA の統治思想と政治劇テンプレ</p>
        </a>
      </div>
    </section>

    <!-- 身分・教育 -->
    <section class="nation-category">
      <h2>🎓 身分・教育</h2>
      <div class="nation-grid">
        <a href="./階級制度/" class="nation-card">
          <span class="badge">RANK SYSTEM</span>
          <h2>階級制度</h2>
          <p>爵位・士官学校・昇格ルートの閲覧</p>
        </a>
      </div>
    </section>

    <!-- 外交・周辺国家 -->
<section class="nation-category">
  <h2>🌍 旧宗主国・周辺国家</h2>
  <div class="nation-grid">
    <a href="../world/old-union/" class="nation-card">
      <span class="badge">OLD UNION</span>
      <h2>旧サンギス＝レグナ連合王国</h2>
      <p>旧宗主国4構成国家と独立の火種を閲覧</p>
    </a>
  </div>
</section>

  </main>
</body>
</html>
==================================================
FILE: ./viewer/db/nation/nation.css
--------------------------------------------------
:root {
  --bg: #070d18;
  --panel: #0d1526;
  --border: rgba(201, 169, 97, 0.25);
  --gold: #c9a961;
  --text: #f4efe2;
  --muted: #a59a7a;
}

body {
  margin: 0;
  background: radial-gradient(circle at top, #0c1830 0%, #070d18 60%);
  color: var(--text);
  font-family: "Noto Serif JP", serif;
}

.nation-shell {
  max-width: 1200px;
  margin: 0 auto;
  padding: 64px 24px;
}

.nation-header {
  margin-bottom: 48px;
  border-bottom: 1px solid var(--border);
  padding-bottom: 24px;
}

.sub {
  color: var(--gold);
  letter-spacing: 0.18em;
  font-size: 13px;
}

h1 {
  font-size: 48px;
  margin: 10px 0;
  color: var(--gold);
}

.lead {
  color: var(--muted);
  line-height: 1.8;
}

.nation-grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
  gap: 24px;
}

.nation-card {
  display: block;
  text-decoration: none;
  color: inherit;
  background: rgba(13, 21, 38, 0.95);
  border: 1px solid var(--border);
  border-radius: 16px;
  padding: 28px;
  transition: 0.25s ease;
}

.nation-card:hover {
  transform: translateY(-3px);
  border-color: var(--gold);
  box-shadow: 0 0 18px rgba(201, 169, 97, 0.15);
}

.badge {
  display: inline-block;
  color: var(--gold);
  font-size: 12px;
  margin-bottom: 10px;
  letter-spacing: 0.12em;
}

.nation-card h2 {
  margin: 0 0 12px;
  color: var(--text);
}

.nation-card p {
  color: var(--muted);
  line-height: 1.7;
}
==================================================
FILE: ./viewer/db/nation/overview/index.html
--------------------------------------------------
<!DOCTYPE html>
<html lang="ja">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>国家プロフィール｜メリトクラシア連邦公爵領</title>
    <style>
        :root {
            --royal-blue: #1a2a44;
            --gold: #c4a77d;
            --paper: #fdfdfb;
            --ink: #2d3436;
        }

        body {
            background-color: #e2e2e2;
            color: var(--ink);
            font-family: 'Times New Roman', serif, 'Hiragino Mincho ProN', 'MS Mincho';
            margin: 0;
            padding: 40px 20px;
            line-height: 1.8;
        }

        .document {
            max-width: 800px;
            margin: 0 auto;
            background: var(--paper);
            padding: 60px 80px;
            box-shadow: 0 4px 20px rgba(0,0,0,0.15);
            border: 1px solid #d1d1d1;
            position: relative;
        }

        /* 装飾的な枠線 */
        .document::before {
            content: "";
            position: absolute;
            top: 20px; bottom: 20px; left: 20px; right: 20px;
            border: 1px solid #e0d8c3;
            pointer-events: none;
        }

        header {
            text-align: center;
            border-bottom: 2px solid var(--gold);
            margin-bottom: 50px;
            padding-bottom: 20px;
        }

        .embassy-seal {
            font-size: 12px;
            letter-spacing: 0.3em;
            color: var(--gold);
            text-transform: uppercase;
            margin-bottom: 10px;
        }

        h1 {
            font-size: 32px;
            color: var(--royal-blue);
            margin: 0;
            letter-spacing: 0.1em;
        }

        .motto {
            font-style: italic;
            font-size: 18px;
            color: var(--royal-blue);
            margin-top: 15px;
            font-weight: bold;
        }

        h2 {
            font-size: 20px;
            color: var(--royal-blue);
            border-left: 4px solid var(--gold);
            padding-left: 15px;
            margin-top: 40px;
            border-bottom: 1px solid #eee;
        }

        .info-grid {
            display: grid;
            grid-template-columns: 140px 1fr;
            gap: 15px;
            margin: 20px 0;
        }

        .label {
            font-weight: bold;
            color: var(--gold);
        }

        .content-box {
            background: rgba(196, 167, 125, 0.05);
            padding: 20px;
            border-radius: 4px;
            margin: 15px 0;
        }

        ul {
            padding-left: 20px;
        }

        li {
            margin-bottom: 10px;
        }

        footer {
            margin-top: 60px;
            text-align: right;
            font-size: 14px;
            color: #777;
            border-top: 1px solid #eee;
            padding-top: 20px;
        }

        .watermark {
            position: absolute;
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%) rotate(-30deg);
            font-size: 80px;
            color: rgba(196, 167, 125, 0.03);
            white-space: nowrap;
            pointer-events: none;
            z-index: 0;
        }
    </style>
</head>
<body>

<div class="document">
    <div class="watermark">MERITOCRACIA</div>

    <header>
        <div class="embassy-seal">National Archive of Meritocracia</div>
        <h1>国家プロフィール：メリトクラシア</h1>
        <div class="motto">"Meritum Omnia Vincit"<br>(功績はすべてに勝る)</div>
    </header>

    <section>
        <h2>I. 基本統計</h2>
        <div class="info-grid">
            <div class="label">国家名称</div>
            <div>メリトクラシア（Meritocracia）</div>

            <div class="label">建国</div>
            <div>建国 8年目（ZERO編時点。独立から間もない新興国家）</div>

            <div class="label">首都</div>
            <div>グランセントラル（Grand Central）</div>

            <div class="label">推定人口</div>
            <div>約820万人（前産業革命英国ベースの規模）</div>

            <div class="label">国土面積</div>
            <div>約26万 km²（英国本島相当の島国・準島国規模）</div>
        </div>
    </section>

==================================================
FILE: ./viewer/db/nation/sanginaria/index.backup_2026-04-19_191844.html
--------------------------------------------------
<!DOCTYPE html>
<html lang="ja">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>国家プロフィール｜メリトクラシア連邦公爵領</title>
    <style>
        :root {
            --royal-blue: #1a2a44;
            --gold: #c4a77d;
            --paper: #fdfdfb;
            --ink: #2d3436;
        }

        body {
            background-color: #e2e2e2;
            color: var(--ink);
            font-family: 'Times New Roman', serif, 'Hiragino Mincho ProN', 'MS Mincho';
            margin: 0;
            padding: 40px 20px;
            line-height: 1.8;
        }

        .document {
            max-width: 800px;
            margin: 0 auto;
            background: var(--paper);
            padding: 60px 80px;
            box-shadow: 0 4px 20px rgba(0,0,0,0.15);
            border: 1px solid #d1d1d1;
            position: relative;
        }

        /* 装飾的な枠線 */
        .document::before {
            content: "";
            position: absolute;
            top: 20px; bottom: 20px; left: 20px; right: 20px;
            border: 1px solid #e0d8c3;
            pointer-events: none;
        }

        header {
            text-align: center;
            border-bottom: 2px solid var(--gold);
            margin-bottom: 50px;
            padding-bottom: 20px;
        }

        .embassy-seal {
            font-size: 12px;
            letter-spacing: 0.3em;
            color: var(--gold);
            text-transform: uppercase;
            margin-bottom: 10px;
        }

        h1 {
            font-size: 32px;
            color: var(--royal-blue);
            margin: 0;
            letter-spacing: 0.1em;
        }

        .motto {
            font-style: italic;
            font-size: 18px;
            color: var(--royal-blue);
            margin-top: 15px;
            font-weight: bold;
        }

        h2 {
            font-size: 20px;
            color: var(--royal-blue);
            border-left: 4px solid var(--gold);
            padding-left: 15px;
            margin-top: 40px;
            border-bottom: 1px solid #eee;
        }

        .info-grid {
            display: grid;
            grid-template-columns: 140px 1fr;
            gap: 15px;
            margin: 20px 0;
        }

        .label {
            font-weight: bold;
            color: var(--gold);
        }

        .content-box {
            background: rgba(196, 167, 125, 0.05);
            padding: 20px;
            border-radius: 4px;
            margin: 15px 0;
        }

        ul {
            padding-left: 20px;
        }

        li {
            margin-bottom: 10px;
        }

        footer {
            margin-top: 60px;
            text-align: right;
            font-size: 14px;
            color: #777;
            border-top: 1px solid #eee;
            padding-top: 20px;
        }

        .watermark {
            position: absolute;
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%) rotate(-30deg);
            font-size: 80px;
            color: rgba(196, 167, 125, 0.03);
            white-space: nowrap;
            pointer-events: none;
            z-index: 0;
        }
    </style>
</head>
<body>

<div class="document">
    <div class="watermark">MERITOCRACIA</div>

    <header>
        <div class="embassy-seal">National Archive of Meritocracia</div>
        <h1>国家プロフィール：メリトクラシア</h1>
        <div class="motto">"Meritum Omnia Vincit"<br>(功績はすべてに勝る)</div>
    </header>

    <section>
        <h2>I. 基本統計</h2>
        <div class="info-grid">
            <div class="label">国家名称</div>
            <div>メリトクラシア（Meritocracia）</div>

            <div class="label">建国</div>
            <div>建国 8年目（ZERO編時点。独立から間もない新興国家）</div>

            <div class="label">首都</div>
            <div>グランセントラル（Grand Central）</div>

            <div class="label">推定人口</div>
            <div>約820万人（前産業革命英国ベースの規模）</div>

            <div class="label">国土面積</div>
            <div>約26万 km²（英国本島相当の島国・準島国規模）</div>
        </div>
    </section>

==================================================
FILE: ./viewer/db/nation/sanginaria/index.html
--------------------------------------------------
<!DOCTYPE html>
<html lang="ja">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>サンギナリア王国 | 国家プロフィール</title>
    <link rel="stylesheet" href="../nation.css">
</head>
<body>
    <main class="nation-profile">
        <header class="profile-header">
            <h1 class="nation-name">サンギナリア王国</h1>
            <p class="nation-en-name">Sanginaria Kingdom</p>
        </header>

        <section class="profile-section" id="overview">
            <h2>1. 国家概要</h2>
            <div class="content-block">
                <p>サンギナリア王国は、旧サンギス＝レグナ連合王国時代から続く最古参の王権国家である。独立後も旧連合正統の継承者を自任しており、周辺諸国の中でも特に伝統と格式を重んじる。</p>
                <div class="info-card">
                    <ul class="stats-list">
                        <li><strong>推定人口：</strong> 約210万人</li>
                        <li><strong>国土面積：</strong> 約6.5万 km²</li>
                        <li><strong>政体：</strong> 聖統王権制および貴族審議院
                            <p class="sub-text">王を中心とする伝統的な王権国家。表向きは貴族・聖職者・地方領主による審議院を有するが、実質的には王家と上位血統貴族が統治の実権を握っている。</p>
                        </li>
                        <li><strong>時代区分：</strong> 前産業革命期（儀礼王権・神授秩序時代）
                            <p class="sub-text">経済合理性よりも血統、祭礼、神意を最優先する。制度の近代化は近隣諸国に比べ遅れているが、政治儀礼と統治演出は極めて高度に洗練されている。</p>
                        </li>
                    </ul>
                </div>
            </div>
        </section>

        <section class="profile-section" id="ideology">
            <h2>2. 建国理念と国家思想</h2>
            <div class="content-block">
                <p>国家の根幹には「人は皆平等ではなく、継ぐべき秩序には序列がある」という強固な思想が存在する。</p>
                <dl class="definition-list">
                    <dt>血統正統主義</dt>
                    <dd>統治資格は生まれと系譜に宿るという考え。</dd>
                    <dt>神授秩序思想</dt>
                    <dd>国家秩序は人間が作るものではなく、天（神）から与えられる不可侵のものとする。</dd>
                    <dt>継承国家意識</dt>
                    <dd>自らこそが旧連合の本流であると自負し、独立国家メリトクラシアを秩序の“簒奪者”と見なす傾向が強い。</dd>
                </dl>
            </div>
        </section>

        <section class="profile-section" id="dual-nature">
            <h2>3. 表の顔と裏の実態</h2>
            <div class="content-block">
                <p>サンギナリアは、対外的には極めて上品で洗練された文化国家として振る舞うが、その深層には異様な統治構造を抱えている。</p>
                <div class="dual-card-container">
                    <div class="side-card front">
                        <h3>表の顔</h3>
                        <p>白亜の聖堂と古城が並ぶ美しい景観を持ち、騎士道、名誉、忠誠を重んじる。芸術や宗教建築の水準は極めて高く、旧秩序の守護者として穏健な態度を示す。</p>
                    </div>
                    <div class="side-card back">
                        <h3>裏の実態</h3>
                        <p>単なる宗教国家ではなく、神性技術を国家秩序維持のために転用・制度化した「魂加工国家」としての側面を持つ。高適合魂の選別、階級の固定、異端の排除を徹底し、神性を統治の基盤として組み込んでいる。</p>
                    </div>
                </div>
            </div>
        </section>

        <section class="profile-section" id="divine-tech">
            <h2>4. 神性技術ルール</h2>
            <div class="content-block">
                <p>サンギナリアが保持する神性技術は、万能な奇跡ではない。その運用には厳格なルールと制約が存在する。</p>
                <ul class="standard-list">
                    <li><strong>限定的な干渉：</strong> 神そのものを使役・支配・召喚することは不可能である。また、天使本体を完全な形で地上運用する技術も存在しない。</li>
                    <li><strong>観測と加工：</strong> 扱える対象は、神や天使そのものではなく、神性の欠片、残滓、反応、および「高適合魂」に限られる。サンギナリアの技術は、これらを観測・抽出し、国家秩序へ再利用するためのものである。</li>
                    <li><strong>技術の性質：</strong> 死者の蘇生ではなく、魂という資源を再構成・再利用する「加工技術」として定義される。</li>
                </ul>
            </div>
        </section>

        <section class="profile-section" id="soul-processing">
            <h2>5. 魂加工国家としての性質</h2>
            <div class="content-block">
                <p>サンギナリアにおいて、魂は宗教的な救済対象ではなく、秩序を維持するための「資源」である。</p>
                <ul class="standard-list">
                    <li><strong>魂の格差：</strong> 「魂にも格がある」と考え、優れた魂は国家の秩序に奉仕すべきであるという思想を持つ。死は終わりではなく、魂の再配置の機会とされる。</li>
                    <li><strong>神性人格上書き：</strong> 統治中枢の一部には、人間人格に神性由来の思考傾向や秩序志向を上書きする技術が適用されている。これは本物の天使が宿るのではなく、神性に基づく人格パターンを定着させるものである。</li>
                </ul>
            </div>
        </section>

        <section class="profile-section" id="meritocracia-relation">
            <h2>6. 対メリトクラシア認識</h2>
            <div class="content-block">
                <p>サンギナリアは、メリトクラシアを「本来あるべき秩序を否定した危険国家」として思想的に敵視している。</p>
                <ul class="standard-list">
                    <li><strong>思想的侮辱：</strong> 血統ではなく功績で人を評価し、平民に上昇機会を与えるメリトクラシアの制度は、サンギナリアの神授秩序を根本から否定するものである。</li>
                    <li><strong>対立軸：</strong> 「努力・功績・意志」を掲げるメリトクラシアに対し、サンギナリアは「継承・選別・神授秩序」の側に立つ。この対立は、単なる外交問題を超えた長期的な思想闘争となっている。</li>
                </ul>
            </div>
        </section>

        <section class="profile-section" id="incident-market">
            <h2>7. 中央市場テロ事件との接続</h2>
            <div class="content-block">
                <p>メリトクラシアで発生した中央市場テロ事件は、表向きの宗教テロという側面に加え、複数の階層を持つ非公式工作であった。</p>
                <div class="info-card">
                    <h4>三層構造の目的</h4>
                    <ul class="standard-list">
                        <li>メリトクラシア社会への攻撃と動揺誘発。</li>
                        <li>高適合魂の選別と回収。</li>
                        <li>サンギナリア内部の権力闘争および将来的な王権簒奪の準備。</li>
                    </ul>
                    <p class="note-text">※事件の性質：サンギナリア王国が公式に命じたものではなく、過激派セルを利用・誘導した裏の資源確保作戦として位置づけられる。</p>
                </div>
            </div>
        </section>

        <section class="profile-section" id="incident-kernel">
            <h2>8. カーネル魂回収との接続</h2>
            <div class="content-block">
                <p>カーネル・レオンハルトの死は、この魂回収作戦における重要な成果の一つであった。</p>
                <ul class="standard-list">
                    <li><strong>高神性適合：</strong> カーネルは前世が下級天使だった魂を持つ高神性適合個体であり、その親和性や気配は周囲に自然な影響を与えていた。</li>
                    <li><strong>選別と認定：</strong> 教会での活動中に神父エリオスがその神性反応を察知。上位ルートへの報告を経て、サンギナリア上層部により正式な回収対象として認定された。</li>
                    <li><strong>死の再解釈：</strong> 彼女の死は偶然の悲劇ではなく、価値ある魂を国家秩序へ組み込もうとするサンギナリアの思想に巻き込まれた、計画的な回収案件である。</li>
                </ul>
            </div>
        </section>

        <section class="profile-section" id="internal-actors">
            <h2>9. サンギナリア内部の関与ライン</h2>
            <div class="content-block">
                <div class="actor-grid">
                    <div class="actor-card">
                        <strong>政務中枢の黒幕（グロースカンツラー）</strong>
                        <p>事件の真の首謀者。国王の知らぬところで暗部組織を私的に掌握し、工作と回収を主導した実質的な権力者。</p>
                    </div>
                    <div class="actor-card">
                        <strong>サンギナリア上層</strong>
                        <p>エリオスの報告を受け、魂回収の命令を下した意思決定層。</p>
                    </div>
                    <div class="actor-card">
                        <strong>神父エリオス</strong>
                        <p>高適合個体の発見者・報告者。現場の主犯ではなく、情報を上位へ流す内通協力者および誘導役。</p>
                    </div>
                    <div class="actor-card">
                        <strong>炎翼セル</strong>
                        <p>サンギナリア出身者を含む実行部隊。思想的には宗教浄化を信じて動いているが、実際には背後の実力者に利用されている。</p>
                    </div>
                </div>
            </div>
        </section>

        <section class="profile-section footer-notes" id="pending-items">
            <h2>10. 現時点の保留事項・未定事項</h2>
            <div class="content-block">
                <ul class="pending-list">
                    <li>首都名称： 「ルクス＝サングリア」などの案があるが、最終確定ではない。</li>
                    <li>中枢組織の正式名称： 「聖上書き議会（The Overwritten）」などの案をベースに、品位や重厚感を再検討中。</li>
                    <li>上位層の詳細： 「熾天使の代行者」「神性評議の十二席」といった、王家に連なる上位神性宿主の人数、役職、具体的な構成。</li>
                    <li>個人名： 首謀者である「グロースカンツラー」の本名、および王族の各詳細。</li>

==================================================
FILE: ./viewer/db/nation/sanginaria/sanginaria-map/memo/📘 灰冠修道院 設定メモ（固定用）.md
--------------------------------------------------
📘 灰冠修道院 設定メモ（固定用）
1. 施設名

灰冠修道院（かいかんしゅうどういん）

サンギナリア王国東部森林外縁に存在する、旧巡礼修道院。
表向きは病者・巡礼者・孤児を受け入れる慈善施設であり、裏では均衡解放戦線・炎翼セルの前線拠点兼中継施設として機能している。

2. 立地
所在： サンギナリア王都ルクス＝サングリア北東
地形： 東部森林外縁の丘陵地帯
周辺環境：
森へ続く小道あり
巡礼路から少し外れた静かな立地
王都から近すぎず遠すぎない中継地点
外見上は地方の古びた修道院として自然に存在できる
3. 表向きの役割

灰冠修道院は、表向きには以下の役割を持つ。

巡礼者の休息所
病者や負傷者の施療施設
孤児や行き場のない者を受け入れる保護施設
地方住民に開かれた小規模宗教施設

このため、外部から見れば「老朽化したが善良な宗教施設」にしか見えない。

4. 裏向きの役割

灰冠修道院の実態は、炎翼セルの前線拠点兼中継施設 である。

主な裏機能は以下。

炎翼セル構成員の潜伏・集合
儀式準備のための中継作業
祭具・爆薬・儀式石・封書などの保管
上層命令の受領・伝達
協力者との密談
高適合魂に関する一次搬送・報告
緊急時の地下退避および森林側への離脱

つまり、灰冠修道院は「現場実務を回す宗教フロント」である。

5. 施設の本質

灰冠修道院は単なる悪のアジトではない。
サンギナリア王国そのものが持つ、

表の聖性
裏の禁忌
慈悲と選別
救済と支配

を象徴する縮図である。

この施設では、表では祈りと保護が行われ、
裏では儀式準備・選別・密談・搬送が進行する。

6. 地上構造

地上部は修道院として自然に機能する構造を持つ。

主な地上区画
正門・前庭
巡礼者が出入りする正式な入口。石畳と小規模な前庭を持つ。
礼拝堂棟
修道院の中心。祈祷・説教・儀式を表向きに執り行う。
施療院・宿泊棟
病者・巡礼者の受け入れ施設。内部では協力者の滞在にも使える。
回廊付き中庭
平穏で宗教施設らしい印象を与える空間。外見上の“善良さ”を補強する。
墓地
旧修道士や寄進者の墓が並ぶ。地下納骨堂の存在を自然に隠す役割も持つ。
鐘楼
宗教施設としての象徴であり、外周監視にも転用可能。
菜園・小農地
自給用かつ生活感を演出する区画。修道院の日常性を支える。
裏門
森林側へ抜ける小道に繋がる。非常時の離脱路として重要。
7. 地下構造

地下は修道院本来の埋葬・保管空間を転用した秘密区画である。

主な地下区画
旧納骨堂
元々は埋葬施設。石棺や壁龕が残り、聖なる死の空気を持つ。
儀式準備室
赤蝋、儀式陣、七芒星紋章などが置かれた前処理区画。
本格召喚ではなく、準備・補助・確認を行う部屋。
物資保管庫
祭具に偽装した装備、封書、儀式石、禁書、爆薬などを保管。
密談室
指揮官や協力者が会談する小部屋。命令系統と現場の接点。
隠し通路
森林側または丘陵側へ抜ける脱出路。人員・小型物資の搬送が可能。
8. 炎翼セルとの対応

灰冠修道院は炎翼セル各員の役割とも整合する。

C-01《アーク》
地下密談室を主な指揮拠点とする。全体命令と最終判断を担う。
R-02《ノクス》
儀式準備室の中心担当。天使召喚前段階の管理役。
R-03《セラ》
儀式補助担当。儀式準備室で補助・魔力供給・揺らぎを担う。
D-04《グラム》
物資保管庫を中心に爆薬・器材の準備を担当。
D-05《イグナ》
火炎系破壊工作の準備と出撃支援を担当。
S-06《リド》
巡礼者・周辺人流・街道監視を担当。
S-07《ヴェル》
裏門・森林側離脱路の管理、逃走補助を担当。
9. 物語上の役割

灰冠修道院は、物語において以下の役割を持つ。

炎翼セルの実務拠点
サンギナリアの表裏二重構造の象徴
王都中枢と禁域施設を繋ぐ前線ハブ
宗教施設の善性を隠れ蓑にした工作拠点
後の暴露や捜索で極めて重要となる“静かな異常地点”
10. 現段階での固定結論

灰冠修道院は、
「見た目は善良な地方修道院」
でありながら、
「炎翼セルが潜伏し、儀式準備・密談・物資管理・脱出を行える前線拠点」
として成立している。

サンギナリアにおける宗教と禁忌、救済と支配の両面を象徴する施設として正式採用する。
==================================================
FILE: ./viewer/db/nation/sanginaria/sanginaria-map/memo/📘 灰冠修道院を起点にしたテロ当日動線案.md
--------------------------------------------------
📘 灰冠修道院を起点にしたテロ当日動線案
1. 基本方針

中央市場テロ事件において、灰冠修道院は実行部隊の集合地点であると同時に、
物資中継・儀式準備・退避導線の起点として機能した。

この事件は単発の突撃ではなく、

修道院での準備
市街地側への分散移動
市場での複合型攻撃
失敗時の逃走／証拠隠滅
回収物の搬送

まで含めた一連の作戦として成立している。

2. 事件前夜の修道院内配置

前夜の灰冠修道院は、地上では平常運営を装いながら、地下で最終準備が進んでいた。

地上
正門・前庭：通常の修道院として静穏を維持
礼拝堂棟：夜間祈祷を装った人員統制
宿泊棟：協力者・偽装巡礼者を滞在させる
墓地・鐘楼：外部警戒と導線確認
地下
儀式準備室：天使契約関連の最終確認
物資保管庫：爆薬・儀式石・通信文・外套の配布
密談室：アークと上位命令系統の最終確認
隠し通路：緊急時の退路確認
3. 炎翼セル各員の当日役割
C-01《アーク》
修道院地下密談室で最終命令を出す
全体指揮
市場到着後は現地統制よりも作戦成否判断を優先
R-02《ノクス》
儀式準備室の責任者
天使分霊・神性干渉関連の前処理を担う
現地儀式担当の中核
R-03《セラ》
ノクスの補助
儀式道具・魔力供給・封印補助
心理的に揺らぎを見せる可能性あり
D-04《グラム》
爆薬・器材の搬出担当
物資保管庫から必要装備を小分けで運ぶ
市場側では破壊工作の主担当
D-05《イグナ》
火炎・混乱拡大担当
市場での二次被害誘発
召喚獣や火災による視界阻害・恐怖演出も補助
S-06《リド》
巡礼者や街道の監視
王都方面の人流確認
市場近辺では一般群衆への擬態役も可能
S-07《ヴェル》
修道院裏門・森林小道・撤退導線の管理
逃走補助
必要なら書類・痕跡の処理役
4. 当日朝：修道院からの出発

事件当日、炎翼セルは一斉移動しない。
目立つためである。

出発方法
巡礼者
荷運び人
聖職関係者
雑役
市場へ向かう一般商人風

などに偽装し、時間差で分散出発する。

修道院内の流れ
地下物資保管庫から必要装備を取り出す
宿泊棟や礼拝堂周辺で外見を整える
正門または裏門から、別々の時刻で出発
王都方向へ合流せず、途中で複数経路に分かれる

この方式により、灰冠修道院そのものは平常運営に見える。

5. 修道院から王都圏への移動

灰冠修道院は王都ルクス＝サングリア北東の東部森林外縁にあるため、
王都圏への導線は以下の二系統に分けるのが自然。

ルートA：表街道ルート
巡礼路や王都近郊街道を利用
一般人への偽装に向く
リドが人流監視を兼ねる
露骨な荷物は運べない
ルートB：森林縁の裏道ルート
裏門から小道へ出る
森林縁や丘陵地の脇道を通る
爆薬や儀式道具の搬送に向く
ヴェルが退路としても確認

この二重導線にすることで、
一方が止められても全体作戦が崩れにくい。

6. 市場侵入の前段階

炎翼セルは市場へ入る前に、王都外縁または市場周辺で再編成を行う。

前段階の整理
グラム：爆薬配置地点へ先行
イグナ：火炎・混乱拡大位置へ移動
ノクス／セラ：儀式成立に必要な地点へ潜入
リド：市場内外の警戒
ヴェル：外周・退路確認
アーク：全体のタイミングを合わせる

ここで重要なのは、全員が同じ場所に集まらないこと。
細胞型テロ組織として自然な構造になる。

7. 市場での攻撃順序

既存設定に合わせるなら、攻撃は複合型で進行する。

段階1：爆破
市場南側または周辺で最初の爆発
グラム主導
群衆パニック誘発
人流と視線を乱す
段階2：召喚獣解放または混乱拡大
イグナまたは補助役が混乱を拡大
火災・恐怖・逃走を誘発
市民被害を一気に広げる
段階3：儀式成立
ノクス／セラが準備してきた神性儀式を完成
天使分霊または炎槍系現象の発生
事件の象徴段階

この時点で、事件は単なる爆破ではなく
宗教儀式テロとして成立する。

8. カーネル死亡との接続

修道院側で事前に準備された儀式・物資・人員が、結果として市場の惨事を形成し、
カーネルの死へ繋がる。

重要なのは、灰冠修道院は直接の戦闘現場ではないが、
事件の裏側では明確に起点施設であること。

つまり、

灰冠修道院で準備されたものが、市場で人を殺した

という因果を持たせる。

9. 事件後の撤退導線

攻撃後、炎翼セルは全員が修道院へ戻るわけではない。
ここが重要。

撤退パターン
A. 現地死亡・殉教
一部実行犯はその場で死亡
影武者や捨て駒も含む
B. 王都内潜伏

==================================================
FILE: ./viewer/db/nation/アーカイブ/index.html
--------------------------------------------------
<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Meritocracia Nation Archive</title>
  <link rel="stylesheet" href="nation.css">
</head>
<body>
  <main class="nation-shell">
    <header class="nation-header">
      <p class="sub">MERITOCRACIA CENTRAL ARCHIVE</p>
      <h1>国家制度アーカイブ</h1>
      <p class="lead">
        中央政府、三兵団、階級制度を管理する正史閲覧ポータル
      </p>
    </header>

    <section class="nation-grid">
      <a href="./三兵団/" class="nation-card">
        <span class="badge">MILITARY</span>
        <h2>三兵団</h2>
        <p>ZG / FO / VA の統治思想と兵団構造</p>
      </a>

      <a href="./government/central_government_phase2.html" class="nation-card">
        <span class="badge">GOVERNMENT</span>
        <h2>中央政府組織図 Phase2</h2>
        <p>中央政府 + 三兵団人物4層の統合運用DB</p>
      </a>

      <a href="./government/military_system_design_20260401.html" class="nation-card">
        <span class="badge">MILITARY DOC</span>
        <h2>三兵団制度設計</h2>
        <p>ZG / FO / VA の統治思想と政治劇テンプレ</p>
      </a>

      <a href="./階級制度/" class="nation-card">
        <span class="badge">RANK SYSTEM</span>
        <h2>階級制度</h2>
        <p>爵位・士官学校・昇格ルートの閲覧</p>
      </a>
      <a href="./citizen-petition-system/" class="nation-card">
        <span class="badge">POLITICAL SYSTEM</span>
        <h2>貴族陳情制度</h2>
        <p>平民の声を担当貴族経由で中央へ届ける正式制度</p>
      </a>

    </section>
  </main>
</body>
</html>
==================================================
FILE: ./viewer/db/nation/三兵団/index.html
--------------------------------------------------
<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>三兵団 | Nation Archive</title>
  <link rel="stylesheet" href="../nation.css">
</head>
<body>
  <main class="nation-shell">
    <header class="nation-header">
      <p class="sub">MILITARY ARCHIVE</p>
      <h1>三兵団</h1>
      <p class="lead">ZG / FO / VA の統治思想と兵団構造資料</p>
    </header>

    <section class="nation-grid">
      <a href="./三兵団_ドイツ語・ラテン語確定.md" class="nation-card">
        <span class="badge">DOCUMENT</span>
        <h2>ドイツ語・ラテン語確定</h2>
        <p>兵団名称・語源・正式命名資料</p>
      </a>
    </section>
  </main>
</body>
</html>

==================================================
FILE: ./viewer/db/nation/三兵団/三兵団_ドイツ語・ラテン語確定.md
--------------------------------------------------
📋 議事録：メリトクラシア 三兵団ドイツ語/ラテン語ネーミング確定
作成日： 2025年12月27日
決定権者： Lancer
参加AI： Claude

🎯 確定事項
三兵団の正式名称（確定版）
日本語ドイツ語/ラテン語読み略称中央警備隊Zentral Gardeツェントラル・ガルデZG前線騎士団FrontordenフロントオルデンFO監察特務隊Vigil Arcanaウィギル・アルカナVA

🏛 各兵団の詳細設定
①中央警備隊（Zentral Garde / ZG）
言語： ドイツ語
語源：

Zentral = 中央の
Garde = 親衛隊・精鋭警備

役割：

内務・治安維持
国家試験運営
中枢警備
国家中枢直属

ニュアンス：

威厳がある
エリート感
腐敗の温床にもなりうる
権力の象徴

所属キャラクター：

ユミナ
ライナルト家

特徴：

シンプルで言いやすい
ゲームUIで使いやすい
ボイスでも自然


②前線騎士団（Frontorden / FO）
言語： ドイツ語
語源：

Front = 前線
Orden = 騎士団・勲章団

役割：

対外戦争
テロリスト弾圧
最前線での戦闘

ニュアンス：

泥臭い
死亡率高い
実力主義
英雄も出るが墓も増える
名誉はあるが宗教色は薄い

関連設定：

ジェイドの憧れ先
叩き上げの実力者が多い

特徴：

短くて力強い
「騎士団」の格式を保ちつつ簡潔


③監察特務隊（Vigil Arcana / VA）
言語： ラテン語
語源：

Vigil = 監視者
Arcana = 秘密の

意味： 秘密の監視者
役割：

国境・魔境の監視
未確認脅威への対応
極秘任務
国家直轄・記録に残らない仕事

ニュアンス：

完全に官僚・影の組織
軍とも警備とも違う
闇組織感
国家の裏側

関連設定：

天使
審問庁
国家の闇
後半の重要な伏線に関与

特徴：

ラテン語の格調高さ
神秘的・不気味な響き
「秘密」を象徴する名前


🎨 ネーミング方針（確認）
採用基準
✅ 実在ドイツ語/ラテン語の語彙のみ使用
✅ 直訳しすぎず「組織名として自然」
✅ 口に出して読める
✅ ゲームUI・ボイスで破綻しない
✅ 硬すぎず、格式もある
言語バランス

ドイツ語：2（中央警備隊、前線騎士団）
ラテン語：1（監察特務隊）

意図：

ドイツ語 = 軍事・実力主義の雰囲気
ラテン語 = 古代ローマ風の格式・神秘性
監察特務隊だけラテン語 = 「異質な存在」を強調


📝 略称の使用場面
ZG / FO / VA は以下で使用：

内部文書
無線通信
兵士同士の会話
ゲームUI（簡潔表示）
セーブデータ


⚔️ 進撃の巨人との差別化（確認済み）
❌ 調査兵団／憲兵団の焼き直し感 → なし
✅ 国家官僚感 → 強い
✅ 群像劇・汚職・対立 → 描ける
✅ ゲームの進路選択 → 明確
==================================================
FILE: ./viewer/db/nation/国家機関一覧表/# 🏛 統合議事録｜国家機関一覧・中央政府組織図 Phase2.md
--------------------------------------------------
# 🏛 統合議事録｜国家機関一覧・中央政府組織図 Phase2（2026-04-02統合版）

作成日: 2026-04-02
統合元: 中央政府組織図ドラフト / 三兵団人物レイヤー設計ログ
決定権者: Lancer

---

## 1. 本日の結論（中央政府 + 三兵団人物運用の統合）

本日の成果は、前日の三兵団制度設計をさらに拡張し、**Dukeロータスを頂点とした中央政府の見取り図と、三兵団の人物運用レイヤーを一本化したこと**である。

これにより、国家機関一覧は単なる制度図を超え、**政治劇・士官学校編・市場テロ・宗教セル捜査まで接続可能な国家ドラマDB**へ進化した。

---

## 2. 国家中央政府 正式組織図（統合版）

### 国家最高中枢

* **Duke ロータス**

  * 国家元首 / 最高統治責任者
* 首席補佐官：未定
* 内政長官：未定

### 国家最高会議（中央政府）

* 三兵団統合司令部
* 審問庁中央局
* 士官学校統括院
* 宗教監督局
* 外交諜報院
* 財務復興省
* 転移門資源庁

この構図を「日本の内閣府モデル」に相当する国家統治フレームとして正式採用。

---

## 3. 三兵団 責任者レイヤー（正式更新）

### FO｜Frontorden（前線騎士団）

* 責任者：**ガーネット・ヴァルグレイ**
* 2番手：副団長（未定）

### ZG｜Zero Guard

* 責任者：未定
* 2番手：未定

### VA｜Vigil Administration

* 責任者：未定
* 2番手：未定

---

## 4. 三兵団 人物4層レイヤー（本日の正式コア）

本日、三兵団の人物運用を以下の4層で統一した。

```text
トップ → 国家思想
副官 → 実務責任者
若手 → 次世代象徴
現場代表 → 本編主力
```

これを今後の兵団キャラ設計テンプレとして固定する。

---

## 5. 三兵団 副官（優先度S-1正式採用）

### ZG副官｜制度の刃

* **セレス・オブシディアン**
* 役職：首席監察補佐
* 機能：国家試験不正 / 士官学校監査 / 貴族腐敗調査

### FO副官｜戦場の現実

* **ヴァルク・ルベライト**
* 役職：第一戦線副官
* 機能：市場テロ初動 / 都市封鎖 / 若手騎士教育

### VA副官｜真相への案内人

* **ミラ・アメジスト**
* 役職：深層観測副官
* 機能：炎翼セル残党 / 堕天使案件 / 心音解析ログ統合

---

## 6. 三兵団 若手エース（優先度S-2正式採用）

### ZG若手エース

* **ヴィオラ＝スティルネン**
* 制度エリート / 国家試験執行局推薦候補

### FO若手エース

* **キール**
* 都市防衛連隊 若手特進枠

### VA若手エース

* **フィーネ**
* 異端宗教追跡室 見習い観測官

この3名を次世代の思想継承者として固定。

---

## 7. 三兵団 現場代表（優先度S-3正式採用）

### ZG現場代表

* **ユミナ**
* 所属：貴族監察班
* 役職：監察主任
* 用途：試験不正 / 貴族汚職 / 学校編事件

### FO現場代表

* **ライナルト**
* 所属：都市防衛連隊
* 役職：中隊長
* 用途：市場テロ / 都市封鎖 / 革命戦

### VA現場代表

* **レイジ**
* 所属：異端宗教追跡室
* 役職：特務主任
* 用途：炎翼セル / 地下儀式 / ベルフェゴール伏線

---

## 8. 士官学校・審問庁との本編接続

今回の人物4層により、本編主要フェーズとの接続先が明確化した。

* 士官学校編 → ヴィオラ / ユミナ / セレス
* 市場テロ → ライナルト / ヴァルク / ミラ
* 心音解析 → フィーネ / レイジ / ミラ
* 宗教セル → VAライン総動員

これにより、国家制度と本編事件が完全接続された。

---

## 9. 次フェーズ（Priority A）

次に進めるべきは以下。

### A-1 中央政府 顔役キャラ埋め

==================================================
FILE: ./viewer/db/nation/国家機関一覧表/# 📘 統合議事録｜国家機関一覧・三兵団制度設計（2026-04-01統合版）.md
--------------------------------------------------
# 📘 統合議事録｜国家機関一覧・三兵団制度設計（2026-04-01統合版）

作成日: 2026-04-01
統合元: Gemini比較設計ログ / 国家機関一覧表ルームログ
決定権者: Lancer

---

## 1. 本日の結論（重複統合後の正式コア）

本日の議論で確定した中核は、**三兵団を増やさず内部機能の分化で国家の厚みを出す**方針である。

基本思想は以下の3軸に集約される。

* **ZG = 秩序**
* **FO = 武力**
* **VA = 真実**

主人公ジェイドはこの3思想すべてと衝突・共鳴しながら成長する。

---

## 2. 三兵団の正式内部構造（統合確定）

内部構造は読者理解とゲームUIを両立するため、以下の共通階層で統一する。

```text
兵団
└ 局
   └ 連隊
      └ 班
         └ 個人
```

さらに各兵団の内部機能は、以下の**3層テンプレ**で設計する。

```text
象徴層
実務層
特殊層
```

この3層を全兵団の黄金テンプレとして採用。

---

## 3. 三兵団正式テンプレ（重複除去版）

### Zentral Garde（ZG）

国家秩序と制度運営の中枢。

```text
ZG
├ 象徴：中央儀礼近衛隊
├ 実務：国家試験執行局
└ 特殊：貴族監察班
```

用途:

* 国家中枢警備
* グローリアテスト運営
* 士官学校接続
* 貴族腐敗監視

---

### Frontorden（FO）

国家の盾と剣。

```text
FO
├ 象徴：英雄騎士中隊
├ 実務：都市防衛連隊
└ 特殊：対宗教テロ・対天使分霊制圧班
```

用途:

* 中央市場テロ出動
* 都市封鎖
* 魔物討伐
* 戦争前線

---

### Vigil Arcana（VA）

国家の裏側・異常監視・真相追跡。

```text
VA
├ 象徴：封印執行官
├ 実務：異端宗教追跡室
└ 特殊：堕天使・吸血鬼監視班
```

用途:

* 炎翼セル残党捜査
* 真犯人追跡
* ベルフェゴール案件
* 世界構造暴露

---

## 4. 階級・役職呼称（正式統合）

階層そのものは共通化しつつ、兵団ごとに語感を変えて空気を演出する。

### 共通階層

```text
兵団長
局長
連隊長
班長
隊員
```

### ZG呼称

```text
総監
局長
警備連隊長
監察主任
警備官
```

### FO呼称

```text
総騎士長
戦線局長
連隊長
中隊長
騎士
```

### VA呼称

```text
首席監察官
深層局長
執行官
特務主任
観測官
```

---

## 5. 三兵団トップ人物像（正式採用候補）

### ZGトップ【総監】

* 思想: **秩序こそ慈悲**
* 冷徹な完璧主義者

==================================================
FILE: ./viewer/db/nation/階級制度/index.html
--------------------------------------------------
<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>階級制度 | Nation Archive</title>
  <link rel="stylesheet" href="../nation.css">
</head>
<body>
  <main class="nation-shell">
    <header class="nation-header">
      <p class="sub">RANK SYSTEM ARCHIVE</p>
      <h1>階級制度</h1>
      <p class="lead">
        爵位・士官学校・昇格制度・特例ルートの制度資料
      </p>
    </header>

    <section class="nation-grid">
      <a href="./階級制度_概要.md" class="nation-card">
        <span class="badge">RANK</span>
        <h2>階級制度 概要</h2>
        <p>国家階級・昇格・推薦・功績制度の基礎資料</p>
      </a>
    </section>
  </main>
</body>
</html>

==================================================
FILE: ./viewer/db/nation/階級制度/階級制度_概要.md
--------------------------------------------------
# メリトクラシア階級制度

## 全9階級一覧

1. セルウス（Servus）
2. パリア（Paria）
3. ウンフェーイグ（Unfähig）
4. フェーイグ（Fähig）
5. バロン（Baron）
6. グラーフ（Graf）
7. マルクグラーフ（Markgraf）
8. ヘルツォーク（Herzog）
9. グロースヘルツォーク（Großherzog）

## 補足

- 実力主義による昇格制度
- 世襲制の有無
- 奴隷所有権との関係



==================================================
FILE: ./viewer/db/terror/index.html
--------------------------------------------------
<!DOCTYPE html>
<html lang="ja">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>中央市場炎槍事件 | Meritocracia Terror DB</title>
<link rel="stylesheet" href="./terror.css">
</head>
<body>

<div class="terror-shell">

  <!-- ナビ -->
  <a class="back-link" href="../../index.html">← NATION ARCHIVE</a>
  <a class="back-link" href="./map.html" style="margin-left:24px; color:var(--gold-dim);">🗺 TACTICAL MAP →</a>

  <!-- ======================================================
       ① INCIDENT HEADER
       ====================================================== -->
  <section class="incident-header">
    <div class="incident-tag">ZERO編 ／ MERITOCRACIA INCIDENT RECORD</div>
    <h1 class="incident-title" id="incident-title">読み込み中...</h1>
    <p class="incident-sub" id="incident-sub"></p>

    <div class="incident-stats">
      <div class="stat-chip red">
        <span class="stat-label">死者</span>
        <span class="stat-val" id="stat-dead">—</span>
      </div>
      <div class="stat-chip purple">
        <span class="stat-label">逃亡・行方不明</span>
        <span class="stat-val" id="stat-escape">—</span>
      </div>
      <div class="stat-chip amber">
        <span class="stat-label">公式犯人（影武者）</span>
        <span class="stat-val" id="stat-captured">—</span>
      </div>
      <div class="stat-chip">
        <span class="stat-label">Scene</span>
        <span class="stat-val">08 – 30</span>
      </div>
    </div>
  </section>

  <!-- エラー表示 -->
  <div id="error-msg" style="display:none; color:#e05050; font-size:13px; margin-bottom:32px; padding:16px; border:1px solid rgba(139,26,26,0.3); background:rgba(139,26,26,0.08);"></div>

  <!-- ======================================================
       ② STORY TIMELINE VIEW
       ====================================================== -->
  <section class="section-block">
    <div class="section-eyebrow">02 — STORY TIMELINE</div>
    <h2 class="section-title">ストーリータイムライン</h2>
    <div class="timeline" id="timeline-wrap">
      <!-- JS が生成 -->
    </div>
  </section>

  <!-- ======================================================
       ③ CELL STATUS DB
       ====================================================== -->
  <section class="section-block">
    <div class="section-eyebrow">03 — CELL STATUS DATABASE</div>
    <h2 class="section-title">炎翼セル メンバーステータス</h2>

    <!-- 凡例 -->
    <div class="legend" id="status-legend"></div>

    <div class="status-grid" id="status-grid">
      <!-- JS が生成 -->
    </div>
  </section>

  <!-- ======================================================
       ④ LONG FORESHADOW PANEL
       ====================================================== -->
  <section class="section-block">
    <div class="section-eyebrow">04 — LONG FORESHADOW</div>
    <h2 class="section-title">長期伏線 考察DB</h2>

    <div class="foreshadow-grid" id="foreshadow-grid">
      <!-- JS が生成 -->
    </div>
  </section>

</div><!-- /.terror-shell -->

<script src="./terror.js"></script>
</body>
</html>

==================================================
FILE: ./viewer/db/terror/map-data.json
--------------------------------------------------
{
  "map_id": "zero_central_market_incident_v1",
  "districts": [
    {
      "district_id": "church_quarter",
      "name": "星導教会区画",
      "x": 200,
      "y": 200
    },
    {
      "district_id": "market_core",
      "name": "市場中心部",
      "x": 500,
      "y": 500
    },
    {
      "district_id": "south_market_edge",
      "name": "市場南縁部",
      "x": 500,
      "y": 750
    },
    {
      "district_id": "alley_belt",
      "name": "路地裏地帯",
      "x": 750,
      "y": 500
    },
    {
      "district_id": "fo_entry_axis",
      "name": "FO侵入軸",
      "x": 200,
      "y": 800
    },
    {
      "district_id": "civic_square",
      "name": "市民広場",
      "x": 800,
      "y": 800
    }
  ],
  "locations": [
    {
      "location_id": "loc_star_church",
      "name": "星導教会",
      "district_id": "church_quarter",
      "x": 200,
      "y": 200
    },
    {
      "location_id": "loc_chapel",
      "name": "礼拝堂",
      "district_id": "church_quarter",
      "x": 220,
      "y": 210
    },
    {
      "location_id": "loc_underground_stairs",
      "name": "地下階段",
      "district_id": "church_quarter",
      "x": 210,
      "y": 230
    },
    {
      "location_id": "loc_ritual_hall",
      "name": "地下儀式広間",
      "district_id": "church_quarter",
      "x": 190,
      "y": 250
    },
    {
      "location_id": "loc_central_market",
      "name": "中央市場",
      "district_id": "market_core",
      "x": 500,
      "y": 500
    },
    {
      "location_id": "loc_claras_mill",
      "name": "クララの粉屋",
      "district_id": "market_core",
      "x": 555,
      "y": 455
    },
    {
      "location_id": "loc_first_explosion",
      "name": "初爆地点",
      "district_id": "market_core",
      "x": 440,
      "y": 535
    },
    {
      "location_id": "loc_kernel_death",
      "name": "カーネル死亡地点",
      "district_id": "market_core",
      "x": 545,
      "y": 555
    },
    {
      "location_id": "loc_nox_hideout",
      "name": "ノクス潜伏路地",
      "district_id": "alley_belt",
      "x": 720,
      "y": 520
    },
    {
      "location_id": "loc_fo_entry",
      "name": "FO侵入口",
      "district_id": "fo_entry_axis",
      "x": 150,
      "y": 850
    },
    {
      "location_id": "loc_public_interrogation",
      "name": "公開尋問広場",
      "district_id": "civic_square",
      "x": 850,
      "y": 850
    },
    {
      "location_id": "loc_jade_oath",
      "name": "ジェイド誓い地点",
      "district_id": "market_core",
      "x": 600,
      "y": 500
    }
  ],
  "routes": [
    {
      "route_id": "rt_kernel_move",
      "name": "カーネル移動",
      "points": [
        { "x": 500, "y": 700 },
        { "x": 500, "y": 500 },
        { "x": 555, "y": 455 },
        { "x": 545, "y": 555 }
      ]
    },
    {
      "route_id": "rt_luca_move",
      "name": "ルカ移動",
      "points": [
        { "x": 200, "y": 200 },
        { "x": 220, "y": 210 }
      ]
    },
    {
      "route_id": "rt_flamewing_invasion",
      "name": "炎翼セル侵入",
      "points": [
        { "x": 190, "y": 250 },
        { "x": 720, "y": 520 },
        { "x": 440, "y": 535 }
      ]
    },
    {
      "route_id": "rt_crowd_evacuation",
      "name": "群衆避難",
      "points": [
        { "x": 500, "y": 500 },
        { "x": 500, "y": 200 },

==================================================
FILE: ./viewer/db/terror/map.css
--------------------------------------------------
@import url('https://fonts.googleapis.com/css2?family=Cormorant+Garamond:ital,wght@0,300;0,400;0,600;1,400&family=Noto+Serif+JP:wght@300;400;500&family=JetBrains+Mono:wght@300;400&display=swap');

/* ============================================================
   CSS VARIABLES — dark fantasy + London tube aesthetic
   ============================================================ */
:root {
  --bg:           #060a11;
  --bg2:          #0a1020;
  --bg3:          #0e1628;
  --panel:        #0d1525;
  --panel2:       #111d35;
  --panel-glass:  rgba(13,21,37,0.92);

  --gold:         #c9a961;
  --gold2:        #e8c97a;
  --gold-dim:     #7a6035;
  --gold-glow:    rgba(201,169,97,0.15);

  --text:         #d8ccb4;
  --text2:        #7a6e58;
  --text3:        #4a4030;

  --border:       rgba(201,169,97,0.14);
  --border2:      rgba(201,169,97,0.3);

  /* District palette — muted London-map tones */
  --dist-church:     rgba(120,80,160,0.18);
  --dist-church-s:   rgba(120,80,160,0.35);
  --dist-market:     rgba(200,120,60,0.18);
  --dist-market-s:   rgba(200,120,60,0.35);
  --dist-south:      rgba(80,140,100,0.15);
  --dist-south-s:    rgba(80,140,100,0.30);
  --dist-alley:      rgba(100,100,140,0.16);
  --dist-alley-s:    rgba(100,100,140,0.32);
  --dist-fo:         rgba(60,120,200,0.16);
  --dist-fo-s:       rgba(60,120,200,0.32);
  --dist-civic:      rgba(180,140,80,0.14);
  --dist-civic-s:    rgba(180,140,80,0.30);

  /* Route colors — Tube-line inspired */
  --rt-kernel:       #e05050;
  --rt-luca:         #b080f0;
  --rt-flamewing:    #ff6a33;
  --rt-crowd:        #60d0a0;
  --rt-jade:         #60a8e0;
  --rt-fo:           #4a7fc1;

  /* Pin severity */
  --pin-normal:      #60a8e0;
  --pin-deploy:      #ff6a33;
  --pin-explosion:   #e05050;
  --pin-death:       #8b1a1a;
  --pin-fo:          #4a7fc1;
  --pin-end:         #60d0a0;
  --pin-oath:        #c9a961;

  --sidebar-w:       360px;
  --timeline-h:      72px;
}

/* ============================================================
   RESET & BASE
   ============================================================ */
*, *::before, *::after { margin: 0; padding: 0; box-sizing: border-box; }
html { scroll-behavior: smooth; }

body {
  background: var(--bg);
  color: var(--text);
  font-family: 'Noto Serif JP', serif;
  font-weight: 300;
  line-height: 1.6;
  height: 100vh;
  overflow: hidden;
}

/* ============================================================
   MAIN LAYOUT — sidebar + map + bottom timeline
   ============================================================ */
.map-app {
  display: grid;
  grid-template-columns: var(--sidebar-w) 1fr;
  grid-template-rows: 1fr var(--timeline-h);
  height: 100vh;
  width: 100vw;
}

/* ============================================================
   SIDEBAR (LEFT PANE)
   ============================================================ */
.sidebar {
  grid-row: 1 / 3;
  background: var(--panel);
  border-right: 1px solid var(--border);
  display: flex;
  flex-direction: column;
  overflow: hidden;
  z-index: 10;
}

.sidebar-header {
  padding: 20px 20px 16px;
  border-bottom: 1px solid var(--border);
  flex-shrink: 0;
}

.sidebar-back {
  display: inline-flex;
  align-items: center;
  gap: 6px;
  font-family: 'Cormorant Garamond', serif;
  font-size: 10px;
  letter-spacing: 3px;
  color: var(--text2);
  text-decoration: none;
  transition: color 0.2s;
  margin-bottom: 14px;
}
.sidebar-back:hover { color: var(--gold); }

.sidebar-tag {
  font-family: 'Cormorant Garamond', serif;
  font-size: 9px;
  letter-spacing: 5px;
  color: rgba(224,80,80,0.7);
  text-transform: uppercase;
  margin-bottom: 6px;
}

.sidebar-title {
  font-family: 'Cormorant Garamond', serif;
  font-size: 1.4rem;
  font-weight: 300;
  color: var(--text);
  letter-spacing: 0.06em;
  line-height: 1.3;
  margin-bottom: 4px;
}

.sidebar-subtitle {
  font-family: 'Cormorant Garamond', serif;
  font-size: 11px;
  letter-spacing: 3px;
  color: var(--gold-dim);
}

/* Route toggles */
.sidebar-section {
  padding: 16px 20px;
  border-bottom: 1px solid var(--border);
  flex-shrink: 0;
}

.sidebar-section-label {
  font-family: 'Cormorant Garamond', serif;
  font-size: 9px;
  letter-spacing: 4px;
  color: var(--text2);
  text-transform: uppercase;
  margin-bottom: 10px;

==================================================
FILE: ./viewer/db/terror/map.html
--------------------------------------------------
<!DOCTYPE html>
<html lang="ja">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>中央市場テロ事件 地図 | Meritocracia Terror Map</title>
<meta name="description" content="ZERO編 中央市場炎槍事件の地図ビューア。ロンドン地下鉄路線図の読みやすさ + Googleマップの事件ピンUI + ダークファンタジー世界観を融合。">
<link rel="stylesheet" href="./map.css">
</head>
<body>

<!-- Mobile sidebar toggle -->
<button class="sidebar-toggle" id="sidebar-toggle" aria-label="メニュー">☰</button>

<!-- Mobile backdrop -->
<div class="sidebar-backdrop" id="sidebar-backdrop"></div>

<div class="map-app">

  <!-- ============================================================
       SIDEBAR (LEFT PANE)
       ============================================================ -->
  <aside class="sidebar" id="sidebar">

    <div class="sidebar-header">
      <a class="sidebar-back" href="./index.html">← INCIDENT RECORD</a>
      <div class="sidebar-tag">ZERO編 ／ TACTICAL MAP</div>
      <h1 class="sidebar-title">中央市場炎槍事件</h1>
      <div class="sidebar-subtitle">CENTRAL MARKET INCIDENT MAP</div>
    </div>

    <!-- Route toggles -->
    <div class="sidebar-section">
      <div class="sidebar-section-label">ROUTE LAYERS</div>
      <div id="route-toggles">
        <!-- JS generates -->
      </div>
    </div>

    <!-- Detail panel -->
    <div class="sidebar-detail" id="detail-panel">
      <div class="detail-empty">
        <div class="detail-empty-icon">⊕</div>
        <div class="detail-empty-text">地図上の要素を<br>クリックして詳細表示</div>
      </div>
    </div>

  </aside>

  <!-- ============================================================
       MAP VIEWPORT
       ============================================================ -->
  <div class="map-viewport" id="map-viewport">

    <!-- Grid overlay -->
    <div class="map-grid-overlay"></div>

    <!-- SVG Map container -->
    <div class="map-container">
      <svg class="map-svg" id="map-svg" xmlns="http://www.w3.org/2000/svg"
           viewBox="0 0 1000 1000" preserveAspectRatio="xMidYMid meet">
        <!-- JS renders all layers here -->
      </svg>
    </div>

    <!-- Zoom controls -->
    <div class="zoom-controls">
      <button class="zoom-btn" id="btn-zoom-in" title="ズームイン">+</button>
      <button class="zoom-btn" id="btn-zoom-out" title="ズームアウト">−</button>
      <button class="zoom-btn" id="btn-zoom-reset" title="リセット">⟳</button>
    </div>

    <!-- Coordinate display -->
    <div class="map-coords" id="map-coords">0, 0</div>

    <!-- Map legend -->
    <div class="map-legend">
      <div class="map-legend-title">MAP LEGEND</div>
      <div class="legend-row">
        <span class="legend-pin" style="background:#c9a961"></span>
        <span class="legend-label">スポット (POI)</span>
      </div>
      <div class="legend-row">
        <span class="legend-pin" style="background:#60a8e0"></span>
        <span class="legend-label">平時イベント</span>
      </div>
      <div class="legend-row">
        <span class="legend-pin" style="background:#e05050"></span>
        <span class="legend-label">攻撃イベント</span>
      </div>
      <div class="legend-row">
        <span class="legend-pin" style="background:#8b1a1a"></span>
        <span class="legend-label">死亡</span>
      </div>
      <div class="legend-row">
        <span class="legend-pin" style="background:#4a7fc1"></span>
        <span class="legend-label">FO介入</span>
      </div>
      <div class="legend-row">
        <span class="legend-pin" style="background:#c9a961"></span>
        <span class="legend-label">誓い</span>
      </div>
    </div>

  </div>

  <!-- ============================================================
       TIMELINE BAR (BOTTOM)
       ============================================================ -->
  <div class="timeline-bar">

    <div class="timeline-controls">
      <button class="tl-btn" id="btn-prev" title="前のイベント (←)">◀</button>
      <button class="tl-btn" id="btn-play" title="再生/停止 (Space)">▶</button>
      <button class="tl-btn" id="btn-next" title="次のイベント (→)">▶</button>
    </div>

    <div class="timeline-slider-wrap">
      <span class="timeline-label" id="tl-label">ALL</span>
      <input type="range" class="timeline-slider" id="tl-slider" min="0" max="10" value="0" step="1">
      <span class="timeline-event-name" id="tl-event-name">全イベント表示</span>
    </div>

  </div>

</div><!-- /.map-app -->

<script src="./map.js"></script>
</body>
</html>

==================================================
FILE: ./viewer/db/terror/map.js
--------------------------------------------------
'use strict';

/* ============================================================
   CONSTANTS & CONFIG
   ============================================================ */

/** District visual config — shape polygons & colors */
const DISTRICT_CONFIG = {
  church_quarter: {
    color: 'rgba(120,80,160,0.18)',
    stroke: 'rgba(120,80,160,0.35)',
    poly: [
      [100, 100], [320, 100], [330, 140], [320, 300],
      [280, 310], [100, 290], [90, 200]
    ],
    labelOffset: { dx: 0, dy: -15 }
  },
  market_core: {
    color: 'rgba(200,120,60,0.18)',
    stroke: 'rgba(200,120,60,0.35)',
    poly: [
      [370, 380], [620, 370], [640, 420], [630, 580],
      [580, 600], [370, 590], [350, 520]
    ],
    labelOffset: { dx: 0, dy: -15 }
  },
  south_market_edge: {
    color: 'rgba(80,140,100,0.15)',
    stroke: 'rgba(80,140,100,0.30)',
    poly: [
      [370, 620], [620, 620], [640, 670], [630, 830],
      [560, 850], [370, 840], [350, 750]
    ],
    labelOffset: { dx: 0, dy: -10 }
  },
  alley_belt: {
    color: 'rgba(100,100,140,0.16)',
    stroke: 'rgba(100,100,140,0.32)',
    poly: [
      [660, 380], [870, 400], [880, 450], [870, 600],
      [820, 620], [660, 610], [650, 520]
    ],
    labelOffset: { dx: 0, dy: -10 }
  },
  fo_entry_axis: {
    color: 'rgba(60,120,200,0.16)',
    stroke: 'rgba(60,120,200,0.32)',
    poly: [
      [60, 720], [280, 710], [300, 760], [290, 920],
      [240, 940], [60, 930], [50, 840]
    ],
    labelOffset: { dx: 0, dy: -10 }
  },
  civic_square: {
    color: 'rgba(180,140,80,0.14)',
    stroke: 'rgba(180,140,80,0.30)',
    poly: [
      [700, 720], [940, 710], [960, 770], [950, 920],
      [900, 940], [700, 930], [690, 840]
    ],
    labelOffset: { dx: 0, dy: -10 }
  }
};

/** Route color map */
const ROUTE_COLORS = {
  rt_kernel_move:       '#e05050',
  rt_luca_move:         '#b080f0',
  rt_flamewing_invasion:'#ff6a33',
  rt_crowd_evacuation:  '#60d0a0',
  rt_jade_rescue:       '#60a8e0',
  rt_fo_suppression:    '#4a7fc1'
};

/** Pin color by time order — severity progression */
const PIN_COLORS = {
  1: '#60a8e0',   // 市場通常営業
  2: '#ff6a33',   // 炎翼セル配置
  3: '#e05050',   // 最初の爆発
  4: '#e05050',   // 召喚獣出現
  5: '#ff3030',   // 炎槍
  6: '#8b1a1a',   // カーネル死亡
  7: '#4a7fc1',   // FO到着
  8: '#60d0a0',   // 戦闘終了
  9: '#b080f0',   // 公開尋問
  10: '#c9a961'   // ジェイドの誓い
};

/* ============================================================
   STATE
   ============================================================ */
let mapData = null;
let currentPinIndex = 0;       // 0-based: which pins are "active" (0 = show all up to pin 1)
let isPlaying = false;
let playInterval = null;
let selectedItem = null;       // {type, id, data}
let routeVisibility = {};      // route_id -> boolean

// SVG pan/zoom
let viewBox = { x: 0, y: 0, w: 1000, h: 1000 };
let isDragging = false;
let dragStart = { x: 0, y: 0 };
let dragViewStart = { x: 0, y: 0 };

/* ============================================================
   DOM HELPERS
   ============================================================ */
const svgNS = 'http://www.w3.org/2000/svg';

function svgEl(tag, attrs) {
  const e = document.createElementNS(svgNS, tag);
  if (attrs) for (const [k, v] of Object.entries(attrs)) e.setAttribute(k, v);
  return e;
}

function htmlEl(tag, cls, text) {
  const e = document.createElement(tag);
  if (cls) e.className = cls;
  if (text != null) e.textContent = text;
  return e;
}

/* ============================================================
   FETCH DATA
   ============================================================ */
async function loadMapData() {
  const res = await fetch('./map-data.json');
  if (!res.ok) throw new Error('map-data.json の読み込みに失敗しました');
  return res.json();
}

/* ============================================================
   RENDER: DISTRICTS
   ============================================================ */
function renderDistricts(svg) {
  const g = svgEl('g', { id: 'layer-districts' });

  for (const dist of mapData.districts) {
    const cfg = DISTRICT_CONFIG[dist.district_id];
    if (!cfg) continue;

    const dg = svgEl('g', { class: 'district-group', 'data-district': dist.district_id });

    // Polygon shape
    const points = cfg.poly.map(p => p.join(',')).join(' ');
    const shape = svgEl('polygon', {
      class: 'district-shape',
      points,
      fill: cfg.color,
      stroke: cfg.stroke,
      'stroke-width': '1.5'
    });
    shape.addEventListener('click', () => selectItem('district', dist.district_id, dist));
    dg.appendChild(shape);

    // Center label
    const cx = cfg.poly.reduce((s, p) => s + p[0], 0) / cfg.poly.length + (cfg.labelOffset?.dx || 0);
    const cy = cfg.poly.reduce((s, p) => s + p[1], 0) / cfg.poly.length + (cfg.labelOffset?.dy || 0);

    const label = svgEl('text', {

==================================================
FILE: ./viewer/db/terror/terror.css
--------------------------------------------------
@import url('https://fonts.googleapis.com/css2?family=Cormorant+Garamond:ital,wght@0,300;0,400;0,600;1,400&family=Noto+Serif+JP:wght@300;400;500&display=swap');

/* ============================================================
   CSS VARIABLES
   ============================================================ */
:root {
  --bg:           #060a11;
  --bg2:          #0a1020;
  --bg3:          #0e1628;
  --panel:        #0d1525;
  --panel2:       #111d35;

  --gold:         #c9a961;
  --gold2:        #e8c97a;
  --gold-dim:     #7a6035;
  --gold-glow:    rgba(201,169,97,0.15);

  --text:         #d8ccb4;
  --text2:        #7a6e58;
  --text3:        #4a4030;

  --border:       rgba(201,169,97,0.14);
  --border2:      rgba(201,169,97,0.3);

  /* ステータスカラー */
  --c-dead:       #8b1a1a;
  --c-dead-glow:  rgba(139,26,26,0.2);
  --c-dead-text:  #e05050;

  --c-captured:   #b8901a;
  --c-captured-glow: rgba(184,144,26,0.2);
  --c-captured-text: #e8c050;

  --c-escape:     #5a2d8a;
  --c-escape-glow: rgba(90,45,138,0.2);
  --c-escape-text: #b080f0;

  --c-civilian:   #1a4a7a;
  --c-civilian-glow: rgba(26,74,122,0.2);
  --c-civilian-text: #60a8e0;

  --c-unknown:    #3a3a4a;
  --c-unknown-glow: rgba(58,58,74,0.3);
  --c-unknown-text: #9090b0;

  --c-shadow:     #0a0a0a;
  --c-shadow-glow: rgba(201,169,97,0.08);
  --c-shadow-text: #c9a961;

  /* ハイライト種別 */
  --hl-death:     rgba(139,26,26,0.12);
  --hl-pivot:     rgba(60,100,180,0.1);
  --hl-deception: rgba(90,45,138,0.12);
  --hl-awakening: rgba(80,160,120,0.1);
  --hl-oath:      rgba(201,169,97,0.1);
}

/* ============================================================
   RESET & BASE
   ============================================================ */
*, *::before, *::after { margin: 0; padding: 0; box-sizing: border-box; }

html { scroll-behavior: smooth; }

body {
  background: var(--bg);
  color: var(--text);
  font-family: 'Noto Serif JP', serif;
  font-weight: 300;
  line-height: 1.8;
  min-height: 100vh;
  overflow-x: hidden;
}

body::before {
  content: '';
  position: fixed; inset: 0;
  background:
    radial-gradient(ellipse 80% 40% at 50% 0%, rgba(139,26,26,0.06) 0%, transparent 60%),
    radial-gradient(ellipse 60% 50% at 80% 100%, rgba(20,40,90,0.12) 0%, transparent 60%),
    repeating-linear-gradient(
      45deg, transparent, transparent 100px,
      rgba(201,169,97,0.012) 100px, rgba(201,169,97,0.012) 101px
    );
  pointer-events: none; z-index: 0;
}

/* ============================================================
   LAYOUT
   ============================================================ */
.terror-shell {
  position: relative; z-index: 1;
  max-width: 1040px;
  margin: 0 auto;
  padding: 0 20px 100px;
}

/* ============================================================
   BACK LINK
   ============================================================ */
.back-link {
  display: inline-flex;
  align-items: center;
  gap: 8px;
  font-family: 'Cormorant Garamond', serif;
  font-size: 11px;
  letter-spacing: 3px;
  color: var(--text2);
  text-decoration: none;
  padding: 20px 0;
  transition: color 0.2s;
}
.back-link:hover { color: var(--gold); }

/* ============================================================
   ① INCIDENT HEADER
   ============================================================ */
.incident-header {
  padding: 56px 0 48px;
  border-bottom: 1px solid var(--border);
  margin-bottom: 64px;
  position: relative;
}

.incident-header::after {
  content: '';
  position: absolute;
  bottom: -1px; left: 0;
  width: 120px; height: 1px;
  background: var(--gold);
}

.incident-tag {
  font-family: 'Cormorant Garamond', serif;
  font-size: 10px;
  letter-spacing: 5px;
  color: var(--c-dead-text);
  opacity: 0.7;
  margin-bottom: 14px;
  text-transform: uppercase;
}

.incident-title {
  font-family: 'Cormorant Garamond', serif;
  font-size: clamp(2.4rem, 7vw, 4.2rem);
  font-weight: 300;
  color: var(--text);
  letter-spacing: 0.08em;
  line-height: 1.2;
  margin-bottom: 6px;
}

.incident-sub {
  font-family: 'Cormorant Garamond', serif;
  font-size: 13px;
  letter-spacing: 4px;
  color: var(--gold);
  opacity: 0.6;
  margin-bottom: 36px;
}

==================================================
FILE: ./viewer/db/terror/terror.js
--------------------------------------------------
'use strict';

/* ============================================================
   CONSTANTS
   ============================================================ */
const STATUS_CONFIG = {
  dead:     { label: '死亡',           dot: '#e05050' },
  captured: { label: '逮捕（影武者）', dot: '#e8c050' },
  escape:   { label: '逃亡・生存',     dot: '#b080f0' },
  civilian: { label: '生存（民間）',   dot: '#60a8e0' },
  unknown:  { label: '生死不明',       dot: '#9090b0' },
  shadow:   { label: '要監視・黒幕候補', dot: '#c9a961' },
};

const PHASE_ORDER = [
  '静寂と予兆',
  '英雄的選択と悲劇',
  '決別と覚醒の兆し',
  'FOによる制圧',
  '残酷な現実の確定',
  '政治的幕引き',
  '誓いへ',
];

const HL_BADGE_LABEL = {
  death:     '★ カーネル死亡',
  pivot:     '◆ 転換点',
  deception: '▲ 国家の欺瞞',
  awakening: '◉ 心音覚醒',
  oath:      '✦ 誓い',
};

const FORESHADOW_DATA = [
  {
    icon: '🔮',
    label: 'ABILITY',
    title: '心音システム初覚醒',
    desc: 'Scene14の異常な心音、Scene25の公開尋問での無自覚発火。「こいつじゃない」という確信がジェイドに生まれる起点。グローリアテストおよび夜化因子判定への直接の伏線。',
    connect: '→ グローリアテスト / 夜化因子 / 能力判定試験',
  },
  {
    icon: '📿',
    label: 'EVIDENCE',
    title: 'ペンダント証拠',
    desc: 'Scene27でカーネルの遺品として発見される七芒星の魔法石ペンダント。触れた瞬間に炎槍の記憶がフラッシュバック。教会バイト編でルカが持っていた物と同一。',
    connect: '→ 教会バイト編回収 / ルカ接点 / 炎翼セル証拠',
  },
  {
    icon: '👦',
    label: 'GUILT',
    title: 'ルカの罪悪感ライン',
    desc: 'Scene28でルカが黒煙を見つめ「ボクは悪くない」と自分に言い聞かせる。ペンダントが消えていることで全てを悟る。クララとは別の「生き残った罪」として作品を厚くする。',
    connect: '→ セラとの再接触 / 証言者候補 / 贖罪ライン',
  },
  {
    icon: '🏛',
    label: 'CONSPIRACY',
    title: '国家による真実隠蔽',
    desc: 'Scene24-26で国家は迅速な「解決」をアピール。D-04《グラム》が実行犯として処刑予定。本当の幹部《アーク》《ヴェル》は逃走、《ノクス》は自害を偽装している可能性がある。',
    connect: '→ グラム公開尋問 / ジェイドの孤独な違和感 / ZERO編エンジン',
  },
  {
    icon: '⛪',
    label: 'CHURCH',
    title: '教会地下黒幕継続',
    desc: 'Scene30の締めで「観測対象、反応あり」「監視継続」が示される。エリオスは神父として平常運用を続けるが、炎翼セル上位セルとの接続役を担う黒幕候補。',
    connect: '→ 炎翼セル上位組織 / エリオス / グローリアテスト編接続',
  },
  {
    icon: '🛡',
    label: 'FO',
    title: 'FOへの憧憬と不信の同居',
    desc: '聖剣で炎槍を止めたガーネットへの憧れ（FOへの憧憬）と、事件を早々に片付けた国家システムへの不信が、ジェイドの中に同時に生まれる。この矛盾がZERO編全体を駆動する。',
    connect: '→ FO入団動機 / 士官学校編 / 国家不信ライン',
  },
];

/* ============================================================
   UTILS
   ============================================================ */
function el(tag, cls, text) {
  const e = document.createElement(tag);
  if (cls) e.className = cls;
  if (text != null) e.textContent = text;
  return e;
}

function stagger(elements, base = 0, step = 40) {
  elements.forEach((e, i) => {
    e.style.animationDelay = `${base + i * step}ms`;
  });
}

/* ============================================================
   FETCH BOTH JSON
   ============================================================ */
async function loadAll() {
  const [storyRes, statusRes] = await Promise.all([
    fetch('./terror_story.json'),
    fetch('./terror_status.json'),
  ]);

  if (!storyRes.ok) throw new Error('terror_story.json の読み込みに失敗しました');
  if (!statusRes.ok) throw new Error('terror_status.json の読み込みに失敗しました');

  return {
    story:  await storyRes.json(),
    status: await statusRes.json(),
  };
}

/* ============================================================
   ① INCIDENT HEADER
   ============================================================ */
function renderHeader(story, status) {
  document.getElementById('incident-title').textContent = story.incident;
  document.getElementById('incident-sub').textContent   = story.subtitle;

  const members  = status.members;
  const dead     = members.filter(m => m.status === 'dead').length;
  const escape   = members.filter(m => m.status === 'escape' || m.status === 'unknown').length;
  const captured = members.filter(m => m.status === 'captured').length;

  document.getElementById('stat-dead').textContent     = dead;
  document.getElementById('stat-escape').textContent   = escape;
  document.getElementById('stat-captured').textContent = captured;
}

/* ============================================================
   ② STORY TIMELINE
   ============================================================ */
function renderTimeline(story) {
  const wrap = document.getElementById('timeline-wrap');
  const scenes = story.scenes;

  // フェーズでグループ化
  const phases = {};
  for (const sc of scenes) {
    if (!phases[sc.phase]) phases[sc.phase] = [];
    phases[sc.phase].push(sc);
  }

  const orderedPhases = PHASE_ORDER.filter(p => phases[p]);

  for (const phaseName of orderedPhases) {
    const group = phases[phaseName];

    // フェーズ区切り
    const divider = document.createElement('div');
    divider.className = 'phase-divider';
    divider.innerHTML = `
      <div class="phase-label">
        <span>${group[0].phase_en}</span>
        <span class="phase-label-ja">${phaseName}</span>
      </div>`;
    wrap.appendChild(divider);

    // シーンカード
    const cards = [];
    for (const sc of group) {

==================================================
FILE: ./viewer/db/terror/terror_status.json
--------------------------------------------------
{
  "incident": "中央市場炎槍事件",
  "cell": "炎翼セル",
  "members": [
    {
      "id": "C-01",
      "name": "アーク",
      "role": "セルリーダー",
      "role_en": "Cell Leader",
      "status": "escape",
      "status_label": "逃亡・生存",
      "public_info": "国家未把握",
      "future_hook": "上位セル接続担当。ZERO終盤黒幕導線。",
      "threat": "high"
    },
    {
      "id": "R-02",
      "name": "ノクス",
      "role": "儀式中枢",
      "role_en": "Ritual Core",
      "status": "unknown",
      "status_label": "生死不明（生存寄り）",
      "public_info": "死亡扱い",
      "future_hook": "影武者作戦の主導者候補。自害は偽装の可能性。",
      "threat": "high"
    },
    {
      "id": "R-03",
      "name": "セラ",
      "role": "儀式補助",
      "role_en": "Ritual Support",
      "status": "escape",
      "status_label": "逃亡・生存",
      "public_info": "行方不明",
      "future_hook": "ルカとの接点あり。後の証言者候補。迷いを持つ唯一の存在。",
      "threat": "medium"
    },
    {
      "id": "D-04",
      "name": "グラム",
      "role": "爆破担当",
      "role_en": "Demo Unit",
      "status": "captured",
      "status_label": "逮捕（影武者）",
      "public_info": "実行犯として報道・処刑予定",
      "future_hook": "公開尋問の替え玉本命。心音解析で「こいつじゃない」が確定する対象。",
      "threat": "low"
    },
    {
      "id": "D-05",
      "name": "イグナ",
      "role": "火炎拡大",
      "role_en": "Flame Unit",
      "status": "dead",
      "status_label": "死亡",
      "public_info": "戦死扱い",
      "future_hook": "炎翼セルの狂信性を象徴する存在。",
      "threat": "none"
    },
    {
      "id": "S-06",
      "name": "リド",
      "role": "市場監視",
      "role_en": "Market Scout",
      "status": "escape",
      "status_label": "逃亡・生存",
      "public_info": "未特定",
      "future_hook": "元一般人。再登場時に思想崩壊描写が可能な人物。",
      "threat": "medium"
    },
    {
      "id": "S-07",
      "name": "ヴェル",
      "role": "外周警戒",
      "role_en": "Perimeter Guard",
      "status": "escape",
      "status_label": "逃亡・生存",
      "public_info": "未特定",
      "future_hook": "残党ネットワーク維持役。逃走ルートを知る数少ない存在。",
      "threat": "medium"
    },
    {
      "id": "SUPPORT",
      "name": "ルカ",
      "role": "教会側被害者",
      "role_en": "Church Proxy",
      "status": "civilian",
      "status_label": "生存（民間）",
      "public_info": "無関係市民",
      "future_hook": "ペンダント伏線・贖罪ライン。知らずに利用された補助導線。",
      "threat": "none"
    },
    {
      "id": "CHURCH",
      "name": "エリオス",
      "role": "思想協力者",
      "role_en": "Church Collaborator",
      "status": "shadow",
      "status_label": "要監視・黒幕候補",
      "public_info": "神父として平常運用",
      "future_hook": "黒幕候補の中継点。教会地下への接続役。",
      "threat": "high"
    }
  ],
  "fo_involved": [
    {
      "id": "FO-LEAD",
      "name": "ガーネット・ヴァルグレイ",
      "role": "FO隊長",
      "role_en": "FO Commander",
      "action": "聖剣で炎槍を直接防御。路地裏でノクスの魔法陣を一撃破壊。",
      "note": "ジェイドに「人が神の槍を止めた」光景を刻んだ人物。"
    }
  ]
}

==================================================
FILE: ./viewer/db/terror/terror_story.json
--------------------------------------------------
{
  "incident": "中央市場炎槍事件",
  "subtitle": "ZERO編プロローグ事件",
  "arc": "中編・後編",
  "scenes": [
    {
      "id": 8,
      "title": "上空の異変",
      "phase": "静寂と予兆",
      "phase_en": "SILENCE",
      "tags": ["異変", "天使の示唆", "伏線"],
      "highlight": false,
      "summary": "炎の粉が逆流し、煙が上へ吸い込まれる。ジェイドだけが違和感を覚える。空の裂け目に、翼に見える巨大な炎影——正体は明示しない。"
    },
    {
      "id": 9,
      "title": "第二波",
      "phase": "静寂と予兆",
      "phase_en": "SILENCE",
      "tags": ["召喚獣", "戦場化", "ZG限界"],
      "highlight": false,
      "summary": "炎翼セル第二手。召喚獣が屋台を薙ぎ倒し市場が完全に戦場化。中央警備隊だけでは抑えきれない。カーネルが子供の泣き声で足を止める。"
    },
    {
      "id": 10,
      "title": "カーネルの選択",
      "phase": "英雄的選択と悲劇",
      "phase_en": "SACRIFICE",
      "tags": ["人格的必然", "姉の決断", "利他"],
      "highlight": false,
      "summary": "「……先にクララを頼む」——倒壊しかけた屋台の陰に迷子の子供を見つけ、迷わず単身向かうカーネル。これが死亡の人格的必然性となる。"
    },
    {
      "id": 11,
      "title": "炎槍の予兆",
      "phase": "英雄的選択と悲劇",
      "phase_en": "SACRIFICE",
      "tags": ["音の消滅", "赤染め", "スローモーション"],
      "highlight": false,
      "summary": "空の裂け目が脈打ち、音が消える。市場全体の色が一瞬だけ赤に染まる。カーネルが子供を抱き上げた瞬間、空から一本の細く鋭い光——。"
    },
    {
      "id": 12,
      "title": "貫通",
      "phase": "英雄的選択と悲劇",
      "phase_en": "SACRIFICE",
      "tags": ["炎槍貫通", "カーネル重傷", "記憶の断片化"],
      "highlight": true,
      "highlight_type": "death",
      "summary": "炎槍がカーネルの胸部を貫く。光が胸を抜け、一拍遅れて血が滲む。子供だけは無傷。倒れる音——ジェイドの記憶はここで断片化する。"
    },
    {
      "id": 13,
      "title": "断末魔",
      "phase": "決別と覚醒の兆し",
      "phase_en": "AWAKENING",
      "tags": ["駆け寄る", "遺言候補", "クララの絶望"],
      "highlight": false,
      "summary": "「ジェイド……大丈夫」「強くなれ」——倒れたカーネルへ駆け寄るジェイド。呼吸がまだ浅く残っている。クララは自分を庇って倒れた現実に言葉を失う。"
    },
    {
      "id": 14,
      "title": "号泣",
      "phase": "決別と覚醒の兆し",
      "phase_en": "AWAKENING",
      "tags": ["音の遮断", "心音異常", "感情フリーズの前段"],
      "highlight": false,
      "summary": "市場の悲鳴は遠く、ジェイドの耳にはほぼ入らない。視界に映るのは姉の顔だけ。「なんで」「いやだ」が言葉にならない。心音が異常に跳ねる——能力覚醒の伏線。"
    },
    {
      "id": 15,
      "title": "黒鎧の介入",
      "phase": "決別と覚醒の兆し",
      "phase_en": "AWAKENING",
      "tags": ["FO登場", "聖剣防御", "神話感"],
      "highlight": true,
      "highlight_type": "pivot",
      "summary": "クララを狙う第二の炎槍——その瞬間、黒い影が割り込む。金属音。聖剣が炎槍を真正面から受け止める。振り返らず一言「下がれ」。ジェイドの目に「人が神の槍を止めた」光景が刻まれる。"
    },
    {
      "id": 16,
      "title": "制圧線",
      "phase": "FOによる制圧",
      "phase_en": "SUPPRESSION",
      "tags": ["FO展開", "避難誘導", "統率"],
      "highlight": false,
      "summary": "FO黒鎧部隊がジェイド周辺を防衛円で確保。隊長が即座に命令——北通路を避難路に指定、火炎術師を召集、残党索敵。ジェイドは初めて国家機関の統率を目撃する。"
    },
    {
      "id": 17,
      "title": "索敵と避難誘導",
      "phase": "FOによる制圧",
      "phase_en": "SUPPRESSION",
      "tags": ["部隊三分割", "儀式核追跡", "担架到着"],
      "highlight": false,
      "summary": "槍の落下角度から「儀式核は地上」と即断。部隊を索敵・避難・救護の三班に分割。後方医療班がようやく到着し、担架が見え始める。ジェイドだけ姉から離れられない。"
    },
    {
      "id": 18,
      "title": "路地裏の発見",
      "phase": "FOによる制圧",
      "phase_en": "SUPPRESSION",
      "tags": ["七芒星儀式痕", "炎翼セル発見", "ノクス"],
      "highlight": false,
      "summary": "市場南側の路地に七芒星の儀式痕。奥にノクスを中心とした黒ローブ複数が魔法陣を維持。セラが魔力供給で苦しみ、リドが外周警戒。「発見。炎翼セル残党」——FO突入。"
    },
    {
      "id": 19,
      "title": "路地裏戦",
      "phase": "FOによる制圧",
      "phase_en": "SUPPRESSION",
      "tags": ["近接戦", "聖剣vs神性炎", "魔法陣破壊"],
      "highlight": false,
      "summary": "イグナの火炎魔法で路地が燃える。ノクスが儀式を維持しながら後退、セラが一瞬迷いを見せる。隊長ガーネットがノクスへ一直線——一撃で魔法陣の核を破壊。上空の裂け目が崩壊し炎槍が停止。"
    },
    {
      "id": 20,
      "title": "制圧完了",
      "phase": "FOによる制圧",
      "phase_en": "SUPPRESSION",
      "tags": ["ノクス自害", "セラ逃走", "熱が冷える"],
      "highlight": false,
      "summary": "逃げ場なし——「秩序は戻る」ノクス自害、イグナ炎で自決。セラだけが生き残る。「中央市場、制圧完了」——事件の熱が一気に冷える。"
    },
    {
      "id": 21,
      "title": "救護区画",
      "phase": "残酷な現実の確定",
      "phase_en": "REALITY",
      "tags": ["死亡宣告", "まだ温かい", "現実の固定"],
      "highlight": true,
      "highlight_type": "death",
      "summary": "医療兵「……脈、なし。死亡確認」——第三者による宣告が現実を固定する。ジェイドの世界の音が、再び遠のく。"
    },
    {
      "id": 22,
      "title": "家族の再合流",
      "phase": "残酷な現実の確定",
      "phase_en": "REALITY",
      "tags": ["母の崩壊", "父の沈黙", "泣けないジェイド"],
      "highlight": false,
      "summary": "クララを抱きしめ、ジェイドを探し、布を掛けられたカーネルを見つける——探す順番が命。母の精神支柱がここで折れる。父は無言で立ち尽くす。ジェイドは泣けない。"
    },
    {
      "id": 23,
      "title": "残党掃討",
      "phase": "政治的幕引き",
      "phase_en": "DECEPTION",
      "tags": ["ヴェル逃走", "セラ行方不明", "替え玉残存"],
      "highlight": false,
      "summary": "FOが路地裏・地下通路で残党を狩る。S-07《ヴェル》逃走、R-03《セラ》行方不明——現場には替え玉だけ残る。事件が終わったと思うジェイド vs 終わっていないと知る読者、このズレ。"
    },
    {
      "id": 24,
      "title": "影武者逮捕",
      "phase": "政治的幕引き",
      "phase_en": "DECEPTION",
      "tags": ["国家アピール", "D-04グラム", "替え玉"],
      "highlight": true,
      "highlight_type": "deception",

==================================================
FILE: ./viewer/db/world/_prototype/index.html
--------------------------------------------------
<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Meritocracia World Terrain Prototype</title>
  <link rel="stylesheet" href="./world.css" />
</head>
<body>
  <main class="world-shell">
    <section class="map-panel">
      <div class="map-stage" id="map-stage">
        <div class="zoom-controls">
          <button id="zoom-in" type="button">＋</button>
          <button id="zoom-out" type="button">－</button>
          <button id="zoom-reset" type="button">Reset</button>
        </div>

        <div class="map-viewport" id="map-viewport">
          <img
            src="./assets/world-terrain-map.png"
            alt="Meritocracia World Terrain Prototype Map"
            class="terrain-map"
            id="terrain-map"
          />
          <div class="poi-layer" id="poi-layer"></div>
        </div>
      </div>
    </section>

    <aside class="side-panel">
      <div class="side-inner">
        <p class="panel-kicker">WORLD MAP</p>
        <h1 class="panel-title">Terrain Prototype v1</h1>
        <p class="panel-desc" id="panel-desc">
          地形ベース画像に HTML overlay で POI を後載せする試作ワールドハブ。
        </p>

        <div class="edit-tools" id="edit-tools" hidden>
          <p class="selected-label">EDIT MODE</p>
          <div class="tool-row">
            <button id="export-btn" class="tool-btn" type="button">座標を書き出す</button>
            <button id="reset-btn" class="tool-btn" type="button">位置を元に戻す</button>
          </div>
          <textarea id="export-output" class="export-output" readonly placeholder="ここに座標JSONが出ます"></textarea>
        </div>

        <div class="selected-card">
          <p class="selected-label">SELECTED POI</p>
          <h2 id="selected-name">未選択</h2>
          <p id="selected-text">
            地図上のポイントをクリックすると、ここに説明が表示されます。
          </p>
          <a id="selected-link" class="action-link disabled" href="#" target="_self" aria-disabled="true">
            遷移先なし
          </a>
        </div>

        <div class="quick-links">
          <p class="selected-label">QUICK LINKS</p>
          <div id="quick-link-list" class="quick-link-list"></div>
        </div>
      </div>
    </aside>
  </main>

  <script src="./world.js"></script>
</body>
</html>
==================================================
FILE: ./viewer/db/world/_prototype/world-data.json
--------------------------------------------------
{
  "map_id": "meritocracia_world_terrain_v1",
  "title": "Meritocracia World Terrain Prototype",
  "description": "Gemini地形画像を Terrain Base として採用し、POI を後載せする world hub 試作版。",
  "pois": [
    {
      "id": "brighton",
      "label": "首都ブライトン",
      "x": 53,
      "y": 58,
      "text": "南部平野と中央交通結節の中核。国家運営・政治・制度の中心拠点。",
      "link": "../nation/"
    },
    {
      "id": "grand_central",
      "label": "Grand Central",
      "x": 60,
      "y": 51,
      "text": "本編導線の中核都市。市場・行政・都市事件・各種DB接続の主要ハブ。",
      "link": "../city/grand-central/map/"
    },
    {
      "id": "academy",
      "label": "士官学校",
      "x": 63,
      "y": 56,
      "text": "ジェイドたちの成長舞台。学園編・制度教育・人物相関の主要接続点。",
      "link": "../../story/academy/"
    },
    {
      "id": "inquisition",
      "label": "審問庁",
      "x": 56,
      "y": 48,
      "text": "監視・調査・異常観測を担う国家機関。ZERO事件以降の違和感と深く関わる。",
      "link": "../nation/"
    }
  ],
  "quick_links": [
    {
      "label": "NATION DB",
      "link": "../nation/"
    },
    {
      "label": "GRAND CENTRAL MAP",
      "link": "../city/grand-central/map/"
    },
    {
      "label": "ACADEMY HUB",
      "link": "../../story/academy/"
    },
    {
      "label": "STORY HUB",
      "link": "../../story/"
    }
  ]
}
==================================================
FILE: ./viewer/db/world/_prototype/world.css
--------------------------------------------------
:root {
  --bg: #07111f;
  --panel: rgba(8, 16, 28, 0.88);
  --panel-strong: rgba(10, 20, 34, 0.94);
  --line: rgba(198, 166, 106, 0.45);
  --gold: #c6a66a;
  --gold-strong: #e2c68d;
  --text: #f3ead8;
  --muted: #cfc2ab;
  --shadow: rgba(0, 0, 0, 0.35);
}

* {
  box-sizing: border-box;
}

html,
body {
  margin: 0;
  min-height: 100%;
  background:
    radial-gradient(circle at top, #0d2340 0%, #07111f 55%, #040a13 100%);
  color: var(--text);
  font-family:
    "Noto Serif JP",
    "Hiragino Mincho ProN",
    "Yu Mincho",
    serif;
}

body {
  padding: 20px;
}

.world-shell {
  display: grid;
  grid-template-columns: minmax(0, 1fr) 320px;
  gap: 18px;
  align-items: stretch;
  min-height: calc(100vh - 40px);
}

.map-panel,
.side-panel {
  min-width: 0;
}

.map-stage {
  position: relative;
  width: 100%;
  border: 1px solid var(--line);
  border-radius: 18px;
  overflow: hidden;
  background: rgba(4, 10, 19, 0.9);
  box-shadow: 0 18px 40px var(--shadow);
  user-select: none;
}

.terrain-map {
  display: block;
  width: 100%;
  height: auto;
}

.poi-layer {
  position: absolute;
  inset: 0;
}

.poi {
  position: absolute;
  transform: translate(-50%, -50%);
  display: inline-flex;
  align-items: center;
  gap: 8px;
  border: 1px solid rgba(226, 198, 141, 0.7);
  background: rgba(11, 23, 38, 0.84);
  color: var(--text);
  padding: 7px 10px;
  border-radius: 999px;
  cursor: pointer;
  box-shadow: 0 8px 18px rgba(0, 0, 0, 0.25);
  transition:
    transform 0.15s ease,
    background 0.15s ease,
    border-color 0.15s ease,
    box-shadow 0.15s ease;
  backdrop-filter: blur(3px);
}

.poi:hover,
.poi.active {
  transform: translate(-50%, -50%) scale(1.04);
  background: rgba(26, 42, 66, 0.94);
  border-color: var(--gold-strong);
  box-shadow: 0 0 0 1px rgba(226, 198, 141, 0.25), 0 12px 24px rgba(0, 0, 0, 0.32);
}

.poi.editable {
  cursor: grab;
}

.poi.dragging {
  cursor: grabbing;
  z-index: 10;
}

.poi-dot {
  width: 10px;
  height: 10px;
  border-radius: 999px;
  background: var(--gold-strong);
  box-shadow: 0 0 10px rgba(226, 198, 141, 0.8);
  flex: 0 0 auto;
}

.poi-label {
  font-size: 13px;
  line-height: 1;
  white-space: nowrap;
}

.side-panel {
  border: 1px solid var(--line);
  border-radius: 18px;
  background: linear-gradient(180deg, var(--panel) 0%, var(--panel-strong) 100%);
  box-shadow: 0 18px 40px var(--shadow);
}

.side-inner {
  padding: 22px 18px;
  display: flex;
  flex-direction: column;
  gap: 18px;
}

.panel-kicker,
.selected-label {
  margin: 0;
  font-size: 11px;
  letter-spacing: 0.18em;
  color: var(--gold);
}

.panel-title {
  margin: 0;
  font-size: 28px;
  line-height: 1.15;
  color: var(--gold-strong);
}

.panel-desc,
#selected-text {
  margin: 0;
  color: var(--muted);
  line-height: 1.7;
  font-size: 14px;
}

.selected-card,

==================================================
FILE: ./viewer/db/world/_prototype/world.js
--------------------------------------------------
document.addEventListener("DOMContentLoaded", async () => {
  const poiLayer = document.getElementById("poi-layer");
  const mapStage = document.getElementById("map-stage");
  const mapViewport = document.getElementById("map-viewport");
  const selectedName = document.getElementById("selected-name");
  const selectedText = document.getElementById("selected-text");
  const selectedLink = document.getElementById("selected-link");
  const quickLinkList = document.getElementById("quick-link-list");
  const editTools = document.getElementById("edit-tools");
  const exportBtn = document.getElementById("export-btn");
  const resetBtn = document.getElementById("reset-btn");
  const exportOutput = document.getElementById("export-output");

  const zoomInBtn = document.getElementById("zoom-in");
  const zoomOutBtn = document.getElementById("zoom-out");
  const zoomResetBtn = document.getElementById("zoom-reset");

  const urlParams = new URLSearchParams(window.location.search);
  const isEditMode = urlParams.get("edit") === "1";

  let originalPois = [];
  let currentPois = [];
  let activePoiId = null;

  let zoomLevel = 1;
  let panX = 0;
  let panY = 0;

  const MIN_ZOOM = 1;
  const MAX_ZOOM = 4;
  const ZOOM_STEP = 0.1;

  if (isEditMode && editTools) {
    editTools.hidden = false;
  }

  try {
    const response = await fetch("./world-data.json");
    if (!response.ok) {
      throw new Error("world-data.json の読み込みに失敗しました");
    }

    const data = await response.json();
    originalPois = (data.pois || []).map((poi) => ({ ...poi }));
    currentPois = (data.pois || []).map((poi) => ({ ...poi }));

    renderPois();
    renderQuickLinks(data.quick_links || []);
    updateViewportTransform();
  } catch (error) {
    console.error("[world.js]", error);
    selectedName.textContent = "読み込み失敗";
    selectedText.textContent = "world-data.json の読込に失敗しました。ファイル配置やJSON構文を確認してください。";
    selectedLink.textContent = "遷移先なし";
    selectedLink.classList.add("disabled");
  }

  zoomInBtn?.addEventListener("click", () => {
    zoomLevel = clamp(zoomLevel + ZOOM_STEP, MIN_ZOOM, MAX_ZOOM);
    updateViewportTransform();
  });

  zoomOutBtn?.addEventListener("click", () => {
    zoomLevel = clamp(zoomLevel - ZOOM_STEP, MIN_ZOOM, MAX_ZOOM);
    if (zoomLevel === 1) {
      panX = 0;
      panY = 0;
    }
    updateViewportTransform();
  });

  zoomResetBtn?.addEventListener("click", () => {
    zoomLevel = 1;
    panX = 0;
    panY = 0;
    updateViewportTransform();
  });

  mapStage?.addEventListener("wheel", (event) => {
    event.preventDefault();

    const direction = event.deltaY > 0 ? -1 : 1;
    zoomLevel = clamp(zoomLevel + direction * ZOOM_STEP, MIN_ZOOM, MAX_ZOOM);

    if (zoomLevel === 1) {
      panX = 0;
      panY = 0;
    }

    updateViewportTransform();
  }, { passive: false });

  enableMapPan();

  exportBtn?.addEventListener("click", () => {
    const exportData = currentPois.map((poi) => ({
      id: poi.id,
      x: round2(poi.x),
      y: round2(poi.y)
    }));
    exportOutput.value = JSON.stringify(exportData, null, 2);
    exportOutput.focus();
    exportOutput.select();
  });

  resetBtn?.addEventListener("click", () => {
    currentPois = originalPois.map((poi) => ({ ...poi }));
    renderPois();
    exportOutput.value = "";
  });

  function renderPois() {
    poiLayer.innerHTML = "";

    currentPois.forEach((poi, index) => {
      const button = document.createElement("button");
      button.type = "button";
      button.className = "poi";
      if (isEditMode) {
        button.classList.add("editable");
      }

      button.style.left = `${poi.x}%`;
      button.style.top = `${poi.y}%`;
      button.dataset.id = poi.id;

      button.innerHTML = `
        <span class="poi-dot"></span>
        <span class="poi-label">${poi.label}</span>
      `;

      button.addEventListener("click", () => {
        setActivePoi(poi.id);
      });

      if (isEditMode) {
        enablePoiDrag(button, poi.id);
      }

      poiLayer.appendChild(button);

      if (activePoiId === poi.id) {
        button.classList.add("active");
      }

      if (!activePoiId && index === 0) {
        activePoiId = poi.id;
        button.classList.add("active");
        updateSelected(poi);
      }
    });

    if (activePoiId) {
      const activePoi = currentPois.find((poi) => poi.id === activePoiId);
      if (activePoi) {
        updateSelected(activePoi);
      }
    }
  }


==================================================
FILE: ./viewer/db/world/_prototype_v1_2_zoomtest/index.html
--------------------------------------------------
<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Meritocracia World Terrain Prototype</title>
  <link rel="stylesheet" href="./world.css" />
</head>
<body>
  <main class="world-shell">
    <section class="map-panel">
      <div class="map-stage" id="map-stage">
        <div class="zoom-controls">
          <button id="zoom-in" type="button">＋</button>
          <button id="zoom-out" type="button">－</button>
          <button id="zoom-reset" type="button">Reset</button>
          <input id="zoom-input" type="number" min="1" max="100" step="0.1" value="1" />
          <button id="zoom-apply" type="button">Apply</button>
        </div>

        <div class="map-viewport" id="map-viewport">
          <img
            src="./assets/world-terrain-map.png"
            alt="Meritocracia World Terrain Prototype Map"
            class="terrain-map"
            id="terrain-map"
          />
          <div class="poi-layer" id="poi-layer"></div>
        </div>
      </div>
    </section>

    <aside class="side-panel">
      <div class="side-inner">
        <p class="panel-kicker">WORLD MAP</p>
        <h1 class="panel-title">Terrain Prototype v1</h1>
        <p class="panel-desc" id="panel-desc">
          地形ベース画像に HTML overlay で POI を後載せする試作ワールドハブ。
        </p>

        <div class="edit-tools" id="edit-tools" hidden>
          <p class="selected-label">EDIT MODE</p>
          <div class="tool-row">
            <button id="export-btn" class="tool-btn" type="button">座標を書き出す</button>
            <button id="reset-btn" class="tool-btn" type="button">位置を元に戻す</button>
          </div>
          <textarea id="export-output" class="export-output" readonly placeholder="ここに座標JSONが出ます"></textarea>
        </div>

        <div class="poi-size-tools">
          <p class="selected-label">POI SIZE</p>
          <div class="poi-size-row">
            <input id="poi-scale-input" type="number" min="0.2" max="3" step="0.1" value="1" />
            <button id="poi-scale-apply" class="tool-btn" type="button">Apply</button>
            <button id="poi-scale-reset" class="tool-btn" type="button">Reset</button>
          </div>
        </div>

        <div class="selected-card">
          <p class="selected-label">SELECTED POI</p>
          <h2 id="selected-name">未選択</h2>
          <p id="selected-text">
            地図上のポイントをクリックすると、ここに説明が表示されます。
          </p>
          <a id="selected-link" class="action-link disabled" href="#" target="_self" aria-disabled="true">
            遷移先なし
          </a>
        </div>

        <div class="quick-links">
          <p class="selected-label">QUICK LINKS</p>
          <div id="quick-link-list" class="quick-link-list"></div>
        </div>
      </div>
    </aside>
  </main>

  <script src="./world.js"></script>
</body>
</html>
==================================================
FILE: ./viewer/db/world/_prototype_v1_2_zoomtest/world-data.json
--------------------------------------------------
{
  "map_id": "meritocracia_world_terrain_v1",
  "title": "Meritocracia World Terrain Prototype",
  "description": "Gemini地形画像を Terrain Base として採用し、POI を後載せする world hub 試作版。",
  "pois": [
    {
      "id": "brighton",
      "label": "首都ブライトン",
      "x": 53,
      "y": 58,
      "text": "南部平野と中央交通結節の中核。国家運営・政治・制度の中心拠点。",
      "link": "../nation/"
    },
    {
      "id": "grand_central",
      "label": "Grand Central",
      "x": 60,
      "y": 51,
      "text": "本編導線の中核都市。市場・行政・都市事件・各種DB接続の主要ハブ。",
      "link": "../city/grand-central/map/"
    },
    {
      "id": "academy",
      "label": "士官学校",
      "x": 63,
      "y": 56,
      "text": "ジェイドたちの成長舞台。学園編・制度教育・人物相関の主要接続点。",
      "link": "../../story/academy/"
    },
    {
      "id": "inquisition",
      "label": "審問庁",
      "x": 56,
      "y": 48,
      "text": "監視・調査・異常観測を担う国家機関。ZERO事件以降の違和感と深く関わる。",
      "link": "../nation/"
    }
  ],
  "quick_links": [
    {
      "label": "NATION DB",
      "link": "../nation/"
    },
    {
      "label": "GRAND CENTRAL MAP",
      "link": "../city/grand-central/map/"
    },
    {
      "label": "ACADEMY HUB",
      "link": "../../story/academy/"
    },
    {
      "label": "STORY HUB",
      "link": "../../story/"
    }
  ]
}
==================================================
FILE: ./viewer/db/world/_prototype_v1_2_zoomtest/world.css
--------------------------------------------------
:root {
  --bg: #07111f;
  --panel: rgba(8, 16, 28, 0.88);
  --panel-strong: rgba(10, 20, 34, 0.94);
  --line: rgba(198, 166, 106, 0.45);
  --gold: #c6a66a;
  --gold-strong: #e2c68d;
  --text: #f3ead8;
  --muted: #cfc2ab;
  --shadow: rgba(0, 0, 0, 0.35);
}

* {
  box-sizing: border-box;
}

html,
body {
  margin: 0;
  min-height: 100%;
  background:
    radial-gradient(circle at top, #0d2340 0%, #07111f 55%, #040a13 100%);
  color: var(--text);
  font-family:
    "Noto Serif JP",
    "Hiragino Mincho ProN",
    "Yu Mincho",
    serif;
}

body {
  padding: 20px;
}

.world-shell {
  display: grid;
  grid-template-columns: minmax(0, 1fr) 320px;
  gap: 18px;
  align-items: stretch;
  min-height: calc(100vh - 40px);
}

.map-panel,
.side-panel {
  min-width: 0;
}

.map-stage {
  position: relative;
  width: 100%;
  border: 1px solid var(--line);
  border-radius: 18px;
  overflow: hidden;
  background: rgba(4, 10, 19, 0.9);
  box-shadow: 0 18px 40px var(--shadow);
  user-select: none;
}

.terrain-map {
  display: block;
  width: 100%;
  height: auto;
}

.poi-layer {
  position: absolute;
  inset: 0;
}

.poi {
  position: absolute;
  transform: translate(-50%, -50%);
  display: inline-flex;
  align-items: center;
  gap: 6px;
  border: 1px solid rgba(226, 198, 141, 0.55);
  background: rgba(11, 23, 38, 0.72);
  color: var(--text);
  padding: 4px 7px;
  border-radius: 999px;
  cursor: pointer;
  box-shadow: 0 6px 12px rgba(0, 0, 0, 0.22);
  transition:
    transform 0.15s ease,
    background 0.15s ease,
    border-color 0.15s ease,
    box-shadow 0.15s ease;
  backdrop-filter: blur(2px);
}

.poi:hover,
.poi.active {
  background: rgba(26, 42, 66, 0.86);
  border-color: var(--gold-strong);
  box-shadow: 0 0 0 1px rgba(226, 198, 141, 0.18), 0 8px 16px rgba(0, 0, 0, 0.28);
}

.poi.editable {
  cursor: grab;
}

.poi.dragging {
  cursor: grabbing;
  z-index: 10;
}

.poi-dot {
  width: 7px;
  height: 7px;
  border-radius: 999px;
  background: var(--gold-strong);
  box-shadow: 0 0 6px rgba(226, 198, 141, 0.75);
  flex: 0 0 auto;
}

.poi-label {
  font-size: 11px;
  line-height: 1;
  white-space: nowrap;
  letter-spacing: 0.01em;
}

.side-panel {
  border: 1px solid var(--line);
  border-radius: 18px;
  background: linear-gradient(180deg, var(--panel) 0%, var(--panel-strong) 100%);
  box-shadow: 0 18px 40px var(--shadow);
}

.side-inner {
  padding: 22px 18px;
  display: flex;
  flex-direction: column;
  gap: 18px;
}

.panel-kicker,
.selected-label {
  margin: 0;
  font-size: 11px;
  letter-spacing: 0.18em;
  color: var(--gold);
}

.panel-title {
  margin: 0;
  font-size: 28px;
  line-height: 1.15;
  color: var(--gold-strong);
}

.panel-desc,
#selected-text {
  margin: 0;
  color: var(--muted);
  line-height: 1.7;
  font-size: 14px;
}

.selected-card,

==================================================
FILE: ./viewer/db/world/_prototype_v1_2_zoomtest/world.js
--------------------------------------------------
document.addEventListener("DOMContentLoaded", async () => {
  const poiLayer = document.getElementById("poi-layer");
  const mapStage = document.getElementById("map-stage");
  const mapViewport = document.getElementById("map-viewport");
  const selectedName = document.getElementById("selected-name");
  const selectedText = document.getElementById("selected-text");
  const selectedLink = document.getElementById("selected-link");
  const quickLinkList = document.getElementById("quick-link-list");
  const editTools = document.getElementById("edit-tools");
  const exportBtn = document.getElementById("export-btn");
  const resetBtn = document.getElementById("reset-btn");
  const exportOutput = document.getElementById("export-output");
  const poiScaleInput = document.getElementById("poi-scale-input");
  const poiScaleApplyBtn = document.getElementById("poi-scale-apply");
  const poiScaleResetBtn = document.getElementById("poi-scale-reset");

  const zoomInBtn = document.getElementById("zoom-in");
  const zoomOutBtn = document.getElementById("zoom-out");
  const zoomResetBtn = document.getElementById("zoom-reset");
  const zoomInput = document.getElementById("zoom-input");
  const zoomApplyBtn = document.getElementById("zoom-apply");

  const urlParams = new URLSearchParams(window.location.search);
  const isEditMode = urlParams.get("edit") === "1";

  let originalPois = [];
  let currentPois = [];
  let activePoiId = null;

  let zoomLevel = 1;
  let panX = 0;
  let panY = 0;

  const MIN_ZOOM = 1;
  const MAX_ZOOM = 100;
  const ZOOM_STEP = 1;

  if (isEditMode && editTools) {
    editTools.hidden = false;
  }

  try {
    const response = await fetch("./world-data.json");
    if (!response.ok) {
      throw new Error("world-data.json の読み込みに失敗しました");
    }

    const data = await response.json();
    originalPois = (data.pois || []).map((poi) => ({
      ...poi,
      scale: typeof poi.scale === "number" ? poi.scale : 1
    }));
    currentPois = (data.pois || []).map((poi) => ({
      ...poi,
      scale: typeof poi.scale === "number" ? poi.scale : 1
    }));

    renderPois();
    renderQuickLinks(data.quick_links || []);
    updateViewportTransform();
  } catch (error) {
    console.error("[world.js]", error);
    selectedName.textContent = "読み込み失敗";
    selectedText.textContent = "world-data.json の読込に失敗しました。ファイル配置やJSON構文を確認してください。";
    selectedLink.textContent = "遷移先なし";
    selectedLink.classList.add("disabled");
  }

  zoomInBtn?.addEventListener("click", () => {
    zoomLevel = clamp(zoomLevel + ZOOM_STEP, MIN_ZOOM, MAX_ZOOM);
    updateViewportTransform();
  });

  zoomOutBtn?.addEventListener("click", () => {
    zoomLevel = clamp(zoomLevel - ZOOM_STEP, MIN_ZOOM, MAX_ZOOM);
    if (zoomLevel === 1) {
      panX = 0;
      panY = 0;
    }
    updateViewportTransform();
  });

  zoomResetBtn?.addEventListener("click", () => {
    zoomLevel = 1;
    panX = 0;
    panY = 0;
    updateViewportTransform();
  });

  zoomApplyBtn?.addEventListener("click", () => {
    applyZoomInput();
  });

  zoomInput?.addEventListener("keydown", (event) => {
    if (event.key === "Enter") {
      applyZoomInput();
    }
  });

  mapStage?.addEventListener("wheel", (event) => {
    event.preventDefault();

    const direction = event.deltaY > 0 ? -1 : 1;
    zoomLevel = clamp(zoomLevel + direction * ZOOM_STEP, MIN_ZOOM, MAX_ZOOM);

    if (zoomLevel === 1) {
      panX = 0;
      panY = 0;
    }

    updateViewportTransform();
  }, { passive: false });

  enableMapPan();

  exportBtn?.addEventListener("click", () => {
    const exportData = currentPois.map((poi) => ({
      id: poi.id,
      x: round2(poi.x),
      y: round2(poi.y),
      scale: round2(typeof poi.scale === "number" ? poi.scale : 1)
    }));
    exportOutput.value = JSON.stringify(exportData, null, 2);
    exportOutput.focus();
    exportOutput.select();
  });

  resetBtn?.addEventListener("click", () => {
    currentPois = originalPois.map((poi) => ({ ...poi }));
    renderPois();
    exportOutput.value = "";
  });

  poiScaleApplyBtn?.addEventListener("click", () => {
    applySelectedPoiScale();
  });

  poiScaleInput?.addEventListener("keydown", (event) => {
    if (event.key === "Enter") {
      applySelectedPoiScale();
    }
  });

  poiScaleResetBtn?.addEventListener("click", () => {
    resetSelectedPoiScale();
  });

  function renderPois() {
    poiLayer.innerHTML = "";

    currentPois.forEach((poi, index) => {
      const button = document.createElement("button");
      button.type = "button";
      button.className = "poi";
      if (isEditMode) {
        button.classList.add("editable");
      }

      button.style.left = `${poi.x}%`;
      button.style.top = `${poi.y}%`;

==================================================
FILE: ./viewer/db/world/archive/world_terrain_prototype_v1_1_2026-04-16/index.html
--------------------------------------------------
<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Meritocracia World Terrain Prototype</title>
  <link rel="stylesheet" href="./world.css" />
</head>
<body>
  <main class="world-shell">
    <section class="map-panel">
      <div class="map-stage" id="map-stage">
        <div class="zoom-controls">
          <button id="zoom-in" type="button">＋</button>
          <button id="zoom-out" type="button">－</button>
          <button id="zoom-reset" type="button">Reset</button>
        </div>

        <div class="map-viewport" id="map-viewport">
          <img
            src="./assets/world-terrain-map.png"
            alt="Meritocracia World Terrain Prototype Map"
            class="terrain-map"
            id="terrain-map"
          />
          <div class="poi-layer" id="poi-layer"></div>
        </div>
      </div>
    </section>

    <aside class="side-panel">
      <div class="side-inner">
        <p class="panel-kicker">WORLD MAP</p>
        <h1 class="panel-title">Terrain Prototype v1</h1>
        <p class="panel-desc" id="panel-desc">
          地形ベース画像に HTML overlay で POI を後載せする試作ワールドハブ。
        </p>

        <div class="edit-tools" id="edit-tools" hidden>
          <p class="selected-label">EDIT MODE</p>
          <div class="tool-row">
            <button id="export-btn" class="tool-btn" type="button">座標を書き出す</button>
            <button id="reset-btn" class="tool-btn" type="button">位置を元に戻す</button>
          </div>
          <textarea id="export-output" class="export-output" readonly placeholder="ここに座標JSONが出ます"></textarea>
        </div>

        <div class="selected-card">
          <p class="selected-label">SELECTED POI</p>
          <h2 id="selected-name">未選択</h2>
          <p id="selected-text">
            地図上のポイントをクリックすると、ここに説明が表示されます。
          </p>
          <a id="selected-link" class="action-link disabled" href="#" target="_self" aria-disabled="true">
            遷移先なし
          </a>
        </div>

        <div class="quick-links">
          <p class="selected-label">QUICK LINKS</p>
          <div id="quick-link-list" class="quick-link-list"></div>
        </div>
      </div>
    </aside>
  </main>

  <script src="./world.js"></script>
</body>
</html>
==================================================
FILE: ./viewer/db/world/archive/world_terrain_prototype_v1_1_2026-04-16/world-data.json
--------------------------------------------------
{
  "map_id": "meritocracia_world_terrain_v1",
  "title": "Meritocracia World Terrain Prototype",
  "description": "Gemini地形画像を Terrain Base として採用し、POI を後載せする world hub 試作版。",
  "pois": [
    {
      "id": "brighton",
      "label": "首都ブライトン",
      "x": 53,
      "y": 58,
      "text": "南部平野と中央交通結節の中核。国家運営・政治・制度の中心拠点。",
      "link": "../nation/"
    },
    {
      "id": "grand_central",
      "label": "Grand Central",
      "x": 60,
      "y": 51,
      "text": "本編導線の中核都市。市場・行政・都市事件・各種DB接続の主要ハブ。",
      "link": "../city/grand-central/map/"
    },
    {
      "id": "academy",
      "label": "士官学校",
      "x": 63,
      "y": 56,
      "text": "ジェイドたちの成長舞台。学園編・制度教育・人物相関の主要接続点。",
      "link": "../../story/academy/"
    },
    {
      "id": "inquisition",
      "label": "審問庁",
      "x": 56,
      "y": 48,
      "text": "監視・調査・異常観測を担う国家機関。ZERO事件以降の違和感と深く関わる。",
      "link": "../nation/"
    }
  ],
  "quick_links": [
    {
      "label": "NATION DB",
      "link": "../nation/"
    },
    {
      "label": "GRAND CENTRAL MAP",
      "link": "../city/grand-central/map/"
    },
    {
      "label": "ACADEMY HUB",
      "link": "../../story/academy/"
    },
    {
      "label": "STORY HUB",
      "link": "../../story/"
    }
  ]
}
==================================================
FILE: ./viewer/db/world/archive/world_terrain_prototype_v1_1_2026-04-16/world.css
--------------------------------------------------
:root {
  --bg: #07111f;
  --panel: rgba(8, 16, 28, 0.88);
  --panel-strong: rgba(10, 20, 34, 0.94);
  --line: rgba(198, 166, 106, 0.45);
  --gold: #c6a66a;
  --gold-strong: #e2c68d;
  --text: #f3ead8;
  --muted: #cfc2ab;
  --shadow: rgba(0, 0, 0, 0.35);
}

* {
  box-sizing: border-box;
}

html,
body {
  margin: 0;
  min-height: 100%;
  background:
    radial-gradient(circle at top, #0d2340 0%, #07111f 55%, #040a13 100%);
  color: var(--text);
  font-family:
    "Noto Serif JP",
    "Hiragino Mincho ProN",
    "Yu Mincho",
    serif;
}

body {
  padding: 20px;
}

.world-shell {
  display: grid;
  grid-template-columns: minmax(0, 1fr) 320px;
  gap: 18px;
  align-items: stretch;
  min-height: calc(100vh - 40px);
}

.map-panel,
.side-panel {
  min-width: 0;
}

.map-stage {
  position: relative;
  width: 100%;
  border: 1px solid var(--line);
  border-radius: 18px;
  overflow: hidden;
  background: rgba(4, 10, 19, 0.9);
  box-shadow: 0 18px 40px var(--shadow);
  user-select: none;
}

.terrain-map {
  display: block;
  width: 100%;
  height: auto;
}

.poi-layer {
  position: absolute;
  inset: 0;
}

.poi {
  position: absolute;
  transform: translate(-50%, -50%);
  display: inline-flex;
  align-items: center;
  gap: 8px;
  border: 1px solid rgba(226, 198, 141, 0.7);
  background: rgba(11, 23, 38, 0.84);
  color: var(--text);
  padding: 7px 10px;
  border-radius: 999px;
  cursor: pointer;
  box-shadow: 0 8px 18px rgba(0, 0, 0, 0.25);
  transition:
    transform 0.15s ease,
    background 0.15s ease,
    border-color 0.15s ease,
    box-shadow 0.15s ease;
  backdrop-filter: blur(3px);
}

.poi:hover,
.poi.active {
  transform: translate(-50%, -50%) scale(1.04);
  background: rgba(26, 42, 66, 0.94);
  border-color: var(--gold-strong);
  box-shadow: 0 0 0 1px rgba(226, 198, 141, 0.25), 0 12px 24px rgba(0, 0, 0, 0.32);
}

.poi.editable {
  cursor: grab;
}

.poi.dragging {
  cursor: grabbing;
  z-index: 10;
}

.poi-dot {
  width: 10px;
  height: 10px;
  border-radius: 999px;
  background: var(--gold-strong);
  box-shadow: 0 0 10px rgba(226, 198, 141, 0.8);
  flex: 0 0 auto;
}

.poi-label {
  font-size: 13px;
  line-height: 1;
  white-space: nowrap;
}

.side-panel {
  border: 1px solid var(--line);
  border-radius: 18px;
  background: linear-gradient(180deg, var(--panel) 0%, var(--panel-strong) 100%);
  box-shadow: 0 18px 40px var(--shadow);
}

.side-inner {
  padding: 22px 18px;
  display: flex;
  flex-direction: column;
  gap: 18px;
}

.panel-kicker,
.selected-label {
  margin: 0;
  font-size: 11px;
  letter-spacing: 0.18em;
  color: var(--gold);
}

.panel-title {
  margin: 0;
  font-size: 28px;
  line-height: 1.15;
  color: var(--gold-strong);
}

.panel-desc,
#selected-text {
  margin: 0;
  color: var(--muted);
  line-height: 1.7;
  font-size: 14px;
}

.selected-card,

==================================================
FILE: ./viewer/db/world/archive/world_terrain_prototype_v1_1_2026-04-16/world.js
--------------------------------------------------
document.addEventListener("DOMContentLoaded", async () => {
  const poiLayer = document.getElementById("poi-layer");
  const mapStage = document.getElementById("map-stage");
  const mapViewport = document.getElementById("map-viewport");
  const selectedName = document.getElementById("selected-name");
  const selectedText = document.getElementById("selected-text");
  const selectedLink = document.getElementById("selected-link");
  const quickLinkList = document.getElementById("quick-link-list");
  const editTools = document.getElementById("edit-tools");
  const exportBtn = document.getElementById("export-btn");
  const resetBtn = document.getElementById("reset-btn");
  const exportOutput = document.getElementById("export-output");

  const zoomInBtn = document.getElementById("zoom-in");
  const zoomOutBtn = document.getElementById("zoom-out");
  const zoomResetBtn = document.getElementById("zoom-reset");

  const urlParams = new URLSearchParams(window.location.search);
  const isEditMode = urlParams.get("edit") === "1";

  let originalPois = [];
  let currentPois = [];
  let activePoiId = null;

  let zoomLevel = 1;
  let panX = 0;
  let panY = 0;

  const MIN_ZOOM = 1;
  const MAX_ZOOM = 4;
  const ZOOM_STEP = 0.1;

  if (isEditMode && editTools) {
    editTools.hidden = false;
  }

  try {
    const response = await fetch("./world-data.json");
    if (!response.ok) {
      throw new Error("world-data.json の読み込みに失敗しました");
    }

    const data = await response.json();
    originalPois = (data.pois || []).map((poi) => ({ ...poi }));
    currentPois = (data.pois || []).map((poi) => ({ ...poi }));

    renderPois();
    renderQuickLinks(data.quick_links || []);
    updateViewportTransform();
  } catch (error) {
    console.error("[world.js]", error);
    selectedName.textContent = "読み込み失敗";
    selectedText.textContent = "world-data.json の読込に失敗しました。ファイル配置やJSON構文を確認してください。";
    selectedLink.textContent = "遷移先なし";
    selectedLink.classList.add("disabled");
  }

  zoomInBtn?.addEventListener("click", () => {
    zoomLevel = clamp(zoomLevel + ZOOM_STEP, MIN_ZOOM, MAX_ZOOM);
    updateViewportTransform();
  });

  zoomOutBtn?.addEventListener("click", () => {
    zoomLevel = clamp(zoomLevel - ZOOM_STEP, MIN_ZOOM, MAX_ZOOM);
    if (zoomLevel === 1) {
      panX = 0;
      panY = 0;
    }
    updateViewportTransform();
  });

  zoomResetBtn?.addEventListener("click", () => {
    zoomLevel = 1;
    panX = 0;
    panY = 0;
    updateViewportTransform();
  });

  mapStage?.addEventListener("wheel", (event) => {
    event.preventDefault();

    const direction = event.deltaY > 0 ? -1 : 1;
    zoomLevel = clamp(zoomLevel + direction * ZOOM_STEP, MIN_ZOOM, MAX_ZOOM);

    if (zoomLevel === 1) {
      panX = 0;
      panY = 0;
    }

    updateViewportTransform();
  }, { passive: false });

  enableMapPan();

  exportBtn?.addEventListener("click", () => {
    const exportData = currentPois.map((poi) => ({
      id: poi.id,
      x: round2(poi.x),
      y: round2(poi.y)
    }));
    exportOutput.value = JSON.stringify(exportData, null, 2);
    exportOutput.focus();
    exportOutput.select();
  });

  resetBtn?.addEventListener("click", () => {
    currentPois = originalPois.map((poi) => ({ ...poi }));
    renderPois();
    exportOutput.value = "";
  });

  function renderPois() {
    poiLayer.innerHTML = "";

    currentPois.forEach((poi, index) => {
      const button = document.createElement("button");
      button.type = "button";
      button.className = "poi";
      if (isEditMode) {
        button.classList.add("editable");
      }

      button.style.left = `${poi.x}%`;
      button.style.top = `${poi.y}%`;
      button.dataset.id = poi.id;

      button.innerHTML = `
        <span class="poi-dot"></span>
        <span class="poi-label">${poi.label}</span>
      `;

      button.addEventListener("click", () => {
        setActivePoi(poi.id);
      });

      if (isEditMode) {
        enablePoiDrag(button, poi.id);
      }

      poiLayer.appendChild(button);

      if (activePoiId === poi.id) {
        button.classList.add("active");
      }

      if (!activePoiId && index === 0) {
        activePoiId = poi.id;
        button.classList.add("active");
        updateSelected(poi);
      }
    });

    if (activePoiId) {
      const activePoi = currentPois.find((poi) => poi.id === activePoiId);
      if (activePoi) {
        updateSelected(activePoi);
      }
    }
  }


==================================================
FILE: ./viewer/db/world/archive/world_terrain_prototype_v1_2026-04-16/index.html
--------------------------------------------------
<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Meritocracia World Terrain Prototype</title>
  <link rel="stylesheet" href="./world.css" />
</head>
<body>
  <main class="world-shell">
    <section class="map-panel">
      <div class="map-stage" id="map-stage">
        <img
          src="./assets/world-terrain-map.png"
          alt="Meritocracia World Terrain Prototype Map"
          class="terrain-map"
          id="terrain-map"
        />
        <div class="poi-layer" id="poi-layer"></div>
      </div>
    </section>

    <aside class="side-panel">
      <div class="side-inner">
        <p class="panel-kicker">WORLD MAP</p>
        <h1 class="panel-title">Terrain Prototype v1</h1>
        <p class="panel-desc" id="panel-desc">
          地形ベース画像に HTML overlay で POI を後載せする試作ワールドハブ。
        </p>

        <div class="edit-tools" id="edit-tools" hidden>
          <p class="selected-label">EDIT MODE</p>
          <div class="tool-row">
            <button id="export-btn" class="tool-btn" type="button">座標を書き出す</button>
            <button id="reset-btn" class="tool-btn" type="button">位置を元に戻す</button>
          </div>
          <textarea id="export-output" class="export-output" readonly placeholder="ここに座標JSONが出ます"></textarea>
        </div>

        <div class="selected-card">
          <p class="selected-label">SELECTED POI</p>
          <h2 id="selected-name">未選択</h2>
          <p id="selected-text">
            地図上のポイントをクリックすると、ここに説明が表示されます。
          </p>
          <a id="selected-link" class="action-link disabled" href="#" target="_self" aria-disabled="true">
            遷移先なし
          </a>
        </div>

        <div class="quick-links">
          <p class="selected-label">QUICK LINKS</p>
          <div id="quick-link-list" class="quick-link-list"></div>
        </div>
      </div>
    </aside>
  </main>

  <script src="./world.js"></script>
</body>
</html>
==================================================
FILE: ./viewer/db/world/archive/world_terrain_prototype_v1_2026-04-16/world-data.json
--------------------------------------------------
{
  "map_id": "meritocracia_world_terrain_v1",
  "title": "Meritocracia World Terrain Prototype",
  "description": "Gemini地形画像を Terrain Base として採用し、POI を後載せする world hub 試作版。",
  "pois": [
    {
      "id": "brighton",
      "label": "首都ブライトン",
      "x": 53,
      "y": 58,
      "text": "南部平野と中央交通結節の中核。国家運営・政治・制度の中心拠点。",
      "link": "../nation/"
    },
    {
      "id": "grand_central",
      "label": "Grand Central",
      "x": 60,
      "y": 51,
      "text": "本編導線の中核都市。市場・行政・都市事件・各種DB接続の主要ハブ。",
      "link": "../city/grand-central/map/"
    },
    {
      "id": "academy",
      "label": "士官学校",
      "x": 63,
      "y": 56,
      "text": "ジェイドたちの成長舞台。学園編・制度教育・人物相関の主要接続点。",
      "link": "../../story/academy/"
    },
    {
      "id": "inquisition",
      "label": "審問庁",
      "x": 56,
      "y": 48,
      "text": "監視・調査・異常観測を担う国家機関。ZERO事件以降の違和感と深く関わる。",
      "link": "../nation/"
    }
  ],
  "quick_links": [
    {
      "label": "NATION DB",
      "link": "../nation/"
    },
    {
      "label": "GRAND CENTRAL MAP",
      "link": "../city/grand-central/map/"
    },
    {
      "label": "ACADEMY HUB",
      "link": "../../story/academy/"
    },
    {
      "label": "STORY HUB",
      "link": "../../story/"
    }
  ]
}
==================================================
FILE: ./viewer/db/world/archive/world_terrain_prototype_v1_2026-04-16/world.css
--------------------------------------------------
:root {
  --bg: #07111f;
  --panel: rgba(8, 16, 28, 0.88);
  --panel-strong: rgba(10, 20, 34, 0.94);
  --line: rgba(198, 166, 106, 0.45);
  --gold: #c6a66a;
  --gold-strong: #e2c68d;
  --text: #f3ead8;
  --muted: #cfc2ab;
  --shadow: rgba(0, 0, 0, 0.35);
}

* {
  box-sizing: border-box;
}

html,
body {
  margin: 0;
  min-height: 100%;
  background:
    radial-gradient(circle at top, #0d2340 0%, #07111f 55%, #040a13 100%);
  color: var(--text);
  font-family:
    "Noto Serif JP",
    "Hiragino Mincho ProN",
    "Yu Mincho",
    serif;
}

body {
  padding: 20px;
}

.world-shell {
  display: grid;
  grid-template-columns: minmax(0, 1fr) 320px;
  gap: 18px;
  align-items: stretch;
  min-height: calc(100vh - 40px);
}

.map-panel,
.side-panel {
  min-width: 0;
}

.map-stage {
  position: relative;
  width: 100%;
  border: 1px solid var(--line);
  border-radius: 18px;
  overflow: hidden;
  background: rgba(4, 10, 19, 0.9);
  box-shadow: 0 18px 40px var(--shadow);
  user-select: none;
}

.terrain-map {
  display: block;
  width: 100%;
  height: auto;
}

.poi-layer {
  position: absolute;
  inset: 0;
}

.poi {
  position: absolute;
  transform: translate(-50%, -50%);
  display: inline-flex;
  align-items: center;
  gap: 8px;
  border: 1px solid rgba(226, 198, 141, 0.7);
  background: rgba(11, 23, 38, 0.84);
  color: var(--text);
  padding: 7px 10px;
  border-radius: 999px;
  cursor: pointer;
  box-shadow: 0 8px 18px rgba(0, 0, 0, 0.25);
  transition:
    transform 0.15s ease,
    background 0.15s ease,
    border-color 0.15s ease,
    box-shadow 0.15s ease;
  backdrop-filter: blur(3px);
}

.poi:hover,
.poi.active {
  transform: translate(-50%, -50%) scale(1.04);
  background: rgba(26, 42, 66, 0.94);
  border-color: var(--gold-strong);
  box-shadow: 0 0 0 1px rgba(226, 198, 141, 0.25), 0 12px 24px rgba(0, 0, 0, 0.32);
}

.poi.editable {
  cursor: grab;
}

.poi.dragging {
  cursor: grabbing;
  z-index: 10;
}

.poi-dot {
  width: 10px;
  height: 10px;
  border-radius: 999px;
  background: var(--gold-strong);
  box-shadow: 0 0 10px rgba(226, 198, 141, 0.8);
  flex: 0 0 auto;
}

.poi-label {
  font-size: 13px;
  line-height: 1;
  white-space: nowrap;
}

.side-panel {
  border: 1px solid var(--line);
  border-radius: 18px;
  background: linear-gradient(180deg, var(--panel) 0%, var(--panel-strong) 100%);
  box-shadow: 0 18px 40px var(--shadow);
}

.side-inner {
  padding: 22px 18px;
  display: flex;
  flex-direction: column;
  gap: 18px;
}

.panel-kicker,
.selected-label {
  margin: 0;
  font-size: 11px;
  letter-spacing: 0.18em;
  color: var(--gold);
}

.panel-title {
  margin: 0;
  font-size: 28px;
  line-height: 1.15;
  color: var(--gold-strong);
}

.panel-desc,
#selected-text {
  margin: 0;
  color: var(--muted);
  line-height: 1.7;
  font-size: 14px;
}

.selected-card,

==================================================
FILE: ./viewer/db/world/archive/world_terrain_prototype_v1_2026-04-16/world.js
--------------------------------------------------
document.addEventListener("DOMContentLoaded", async () => {
  const poiLayer = document.getElementById("poi-layer");
  const mapStage = document.getElementById("map-stage");
  const selectedName = document.getElementById("selected-name");
  const selectedText = document.getElementById("selected-text");
  const selectedLink = document.getElementById("selected-link");
  const quickLinkList = document.getElementById("quick-link-list");
  const editTools = document.getElementById("edit-tools");
  const exportBtn = document.getElementById("export-btn");
  const resetBtn = document.getElementById("reset-btn");
  const exportOutput = document.getElementById("export-output");

  const urlParams = new URLSearchParams(window.location.search);
  const isEditMode = urlParams.get("edit") === "1";

  let originalPois = [];
  let currentPois = [];
  let activePoiId = null;

  if (isEditMode && editTools) {
    editTools.hidden = false;
  }

  try {
    const response = await fetch("./world-data.json");
    if (!response.ok) {
      throw new Error("world-data.json の読み込みに失敗しました");
    }

    const data = await response.json();
    originalPois = (data.pois || []).map((poi) => ({ ...poi }));
    currentPois = (data.pois || []).map((poi) => ({ ...poi }));

    renderPois();
    renderQuickLinks(data.quick_links || []);
  } catch (error) {
    console.error("[world.js]", error);
    selectedName.textContent = "読み込み失敗";
    selectedText.textContent = "world-data.json の読込に失敗しました。ファイル配置やJSON構文を確認してください。";
    selectedLink.textContent = "遷移先なし";
    selectedLink.classList.add("disabled");
  }

  exportBtn?.addEventListener("click", () => {
    const exportData = currentPois.map((poi) => ({
      id: poi.id,
      x: round2(poi.x),
      y: round2(poi.y)
    }));
    exportOutput.value = JSON.stringify(exportData, null, 2);
    exportOutput.focus();
    exportOutput.select();
  });

  resetBtn?.addEventListener("click", () => {
    currentPois = originalPois.map((poi) => ({ ...poi }));
    renderPois();
    exportOutput.value = "";
  });

  function renderPois() {
    poiLayer.innerHTML = "";

    currentPois.forEach((poi, index) => {
      const button = document.createElement("button");
      button.type = "button";
      button.className = "poi";
      if (isEditMode) {
        button.classList.add("editable");
      }

      button.style.left = `${poi.x}%`;
      button.style.top = `${poi.y}%`;
      button.dataset.id = poi.id;

      button.innerHTML = `
        <span class="poi-dot"></span>
        <span class="poi-label">${poi.label}</span>
      `;

      button.addEventListener("click", () => {
        setActivePoi(poi.id);
      });

      if (isEditMode) {
        enableDrag(button, poi.id);
      }

      poiLayer.appendChild(button);

      if (activePoiId === poi.id) {
        button.classList.add("active");
      }

      if (!activePoiId && index === 0) {
        activePoiId = poi.id;
        button.classList.add("active");
        updateSelected(poi);
      }
    });

    if (activePoiId) {
      const activePoi = currentPois.find((poi) => poi.id === activePoiId);
      if (activePoi) {
        updateSelected(activePoi);
      }
    }
  }

  function renderQuickLinks(links) {
    quickLinkList.innerHTML = "";

    links.forEach((item) => {
      const anchor = document.createElement("a");
      anchor.className = "quick-link";
      anchor.href = item.link;
      anchor.textContent = item.label;
      quickLinkList.appendChild(anchor);
    });
  }

  function setActivePoi(poiId) {
    activePoiId = poiId;
    document.querySelectorAll(".poi").forEach((el) => {
      el.classList.toggle("active", el.dataset.id === poiId);
    });

    const poi = currentPois.find((item) => item.id === poiId);
    if (poi) {
      updateSelected(poi);
    }
  }

  function updateSelected(poi) {
    selectedName.textContent = poi.label;
    const coordText = isEditMode
      ? `\n\n現在座標: x=${round2(poi.x)} / y=${round2(poi.y)}`
      : "";

    selectedText.textContent = `${poi.text || "説明は未設定です。"}${coordText}`;

    if (poi.link) {
      selectedLink.href = poi.link;
      selectedLink.textContent = "この拠点へ移動";
      selectedLink.classList.remove("disabled");
      selectedLink.removeAttribute("aria-disabled");
    } else {
      selectedLink.href = "#";
      selectedLink.textContent = "遷移先なし";
      selectedLink.classList.add("disabled");
      selectedLink.setAttribute("aria-disabled", "true");
    }
  }

  function enableDrag(element, poiId) {
    let isDragging = false;
    let moved = false;

    element.addEventListener("pointerdown", (event) => {
      if (!isEditMode) return;

==================================================
FILE: ./viewer/db/world/archive/world_terrain_prototype_v1_2_2026-04-16/index.html
--------------------------------------------------
<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Meritocracia World Terrain Prototype</title>
  <link rel="stylesheet" href="./world.css" />
</head>
<body>
  <main class="world-shell">
    <section class="map-panel">
      <div class="map-stage" id="map-stage">
        <div class="zoom-controls">
          <button id="zoom-in" type="button">＋</button>
          <button id="zoom-out" type="button">－</button>
          <button id="zoom-reset" type="button">Reset</button>
        </div>

        <div class="map-viewport" id="map-viewport">
          <img
            src="./assets/world-terrain-map.png"
            alt="Meritocracia World Terrain Prototype Map"
            class="terrain-map"
            id="terrain-map"
          />
          <div class="poi-layer" id="poi-layer"></div>
        </div>
      </div>
    </section>

    <aside class="side-panel">
      <div class="side-inner">
        <p class="panel-kicker">WORLD MAP</p>
        <h1 class="panel-title">Terrain Prototype v1</h1>
        <p class="panel-desc" id="panel-desc">
          地形ベース画像に HTML overlay で POI を後載せする試作ワールドハブ。
        </p>

        <div class="edit-tools" id="edit-tools" hidden>
          <p class="selected-label">EDIT MODE</p>
          <div class="tool-row">
            <button id="export-btn" class="tool-btn" type="button">座標を書き出す</button>
            <button id="reset-btn" class="tool-btn" type="button">位置を元に戻す</button>
          </div>
          <textarea id="export-output" class="export-output" readonly placeholder="ここに座標JSONが出ます"></textarea>
        </div>

        <div class="selected-card">
          <p class="selected-label">SELECTED POI</p>
          <h2 id="selected-name">未選択</h2>
          <p id="selected-text">
            地図上のポイントをクリックすると、ここに説明が表示されます。
          </p>
          <a id="selected-link" class="action-link disabled" href="#" target="_self" aria-disabled="true">
            遷移先なし
          </a>
        </div>

        <div class="quick-links">
          <p class="selected-label">QUICK LINKS</p>
          <div id="quick-link-list" class="quick-link-list"></div>
        </div>
      </div>
    </aside>
  </main>

  <script src="./world.js"></script>
</body>
</html>
==================================================
FILE: ./viewer/db/world/archive/world_terrain_prototype_v1_2_2026-04-16/world-data.json
--------------------------------------------------
{
  "map_id": "meritocracia_world_terrain_v1",
  "title": "Meritocracia World Terrain Prototype",
  "description": "Gemini地形画像を Terrain Base として採用し、POI を後載せする world hub 試作版。",
  "pois": [
    {
      "id": "brighton",
      "label": "首都ブライトン",
      "x": 53,
      "y": 58,
      "text": "南部平野と中央交通結節の中核。国家運営・政治・制度の中心拠点。",
      "link": "../nation/"
    },
    {
      "id": "grand_central",
      "label": "Grand Central",
      "x": 60,
      "y": 51,
      "text": "本編導線の中核都市。市場・行政・都市事件・各種DB接続の主要ハブ。",
      "link": "../city/grand-central/map/"
    },
    {
      "id": "academy",
      "label": "士官学校",
      "x": 63,
      "y": 56,
      "text": "ジェイドたちの成長舞台。学園編・制度教育・人物相関の主要接続点。",
      "link": "../../story/academy/"
    },
    {
      "id": "inquisition",
      "label": "審問庁",
      "x": 56,
      "y": 48,
      "text": "監視・調査・異常観測を担う国家機関。ZERO事件以降の違和感と深く関わる。",
      "link": "../nation/"
    }
  ],
  "quick_links": [
    {
      "label": "NATION DB",
      "link": "../nation/"
    },
    {
      "label": "GRAND CENTRAL MAP",
      "link": "../city/grand-central/map/"
    },
    {
      "label": "ACADEMY HUB",
      "link": "../../story/academy/"
    },
    {
      "label": "STORY HUB",
      "link": "../../story/"
    }
  ]
}
==================================================
FILE: ./viewer/db/world/archive/world_terrain_prototype_v1_2_2026-04-16/world.css
--------------------------------------------------
:root {
  --bg: #07111f;
  --panel: rgba(8, 16, 28, 0.88);
  --panel-strong: rgba(10, 20, 34, 0.94);
  --line: rgba(198, 166, 106, 0.45);
  --gold: #c6a66a;
  --gold-strong: #e2c68d;
  --text: #f3ead8;
  --muted: #cfc2ab;
  --shadow: rgba(0, 0, 0, 0.35);
}

* {
  box-sizing: border-box;
}

html,
body {
  margin: 0;
  min-height: 100%;
  background:
    radial-gradient(circle at top, #0d2340 0%, #07111f 55%, #040a13 100%);
  color: var(--text);
  font-family:
    "Noto Serif JP",
    "Hiragino Mincho ProN",
    "Yu Mincho",
    serif;
}

body {
  padding: 20px;
}

.world-shell {
  display: grid;
  grid-template-columns: minmax(0, 1fr) 320px;
  gap: 18px;
  align-items: stretch;
  min-height: calc(100vh - 40px);
}

.map-panel,
.side-panel {
  min-width: 0;
}

.map-stage {
  position: relative;
  width: 100%;
  border: 1px solid var(--line);
  border-radius: 18px;
  overflow: hidden;
  background: rgba(4, 10, 19, 0.9);
  box-shadow: 0 18px 40px var(--shadow);
  user-select: none;
}

.terrain-map {
  display: block;
  width: 100%;
  height: auto;
}

.poi-layer {
  position: absolute;
  inset: 0;
}

.poi {
  position: absolute;
  transform: translate(-50%, -50%);
  display: inline-flex;
  align-items: center;
  gap: 8px;
  border: 1px solid rgba(226, 198, 141, 0.7);
  background: rgba(11, 23, 38, 0.84);
  color: var(--text);
  padding: 7px 10px;
  border-radius: 999px;
  cursor: pointer;
  box-shadow: 0 8px 18px rgba(0, 0, 0, 0.25);
  transition:
    transform 0.15s ease,
    background 0.15s ease,
    border-color 0.15s ease,
    box-shadow 0.15s ease;
  backdrop-filter: blur(3px);
}

.poi:hover,
.poi.active {
  transform: translate(-50%, -50%) scale(1.04);
  background: rgba(26, 42, 66, 0.94);
  border-color: var(--gold-strong);
  box-shadow: 0 0 0 1px rgba(226, 198, 141, 0.25), 0 12px 24px rgba(0, 0, 0, 0.32);
}

.poi.editable {
  cursor: grab;
}

.poi.dragging {
  cursor: grabbing;
  z-index: 10;
}

.poi-dot {
  width: 10px;
  height: 10px;
  border-radius: 999px;
  background: var(--gold-strong);
  box-shadow: 0 0 10px rgba(226, 198, 141, 0.8);
  flex: 0 0 auto;
}

.poi-label {
  font-size: 13px;
  line-height: 1;
  white-space: nowrap;
}

.side-panel {
  border: 1px solid var(--line);
  border-radius: 18px;
  background: linear-gradient(180deg, var(--panel) 0%, var(--panel-strong) 100%);
  box-shadow: 0 18px 40px var(--shadow);
}

.side-inner {
  padding: 22px 18px;
  display: flex;
  flex-direction: column;
  gap: 18px;
}

.panel-kicker,
.selected-label {
  margin: 0;
  font-size: 11px;
  letter-spacing: 0.18em;
  color: var(--gold);
}

.panel-title {
  margin: 0;
  font-size: 28px;
  line-height: 1.15;
  color: var(--gold-strong);
}

.panel-desc,
#selected-text {
  margin: 0;
  color: var(--muted);
  line-height: 1.7;
  font-size: 14px;
}

.selected-card,

==================================================
FILE: ./viewer/db/world/archive/world_terrain_prototype_v1_2_2026-04-16/world.js
--------------------------------------------------
document.addEventListener("DOMContentLoaded", async () => {
  const poiLayer = document.getElementById("poi-layer");
  const mapStage = document.getElementById("map-stage");
  const mapViewport = document.getElementById("map-viewport");
  const selectedName = document.getElementById("selected-name");
  const selectedText = document.getElementById("selected-text");
  const selectedLink = document.getElementById("selected-link");
  const quickLinkList = document.getElementById("quick-link-list");
  const editTools = document.getElementById("edit-tools");
  const exportBtn = document.getElementById("export-btn");
  const resetBtn = document.getElementById("reset-btn");
  const exportOutput = document.getElementById("export-output");

  const zoomInBtn = document.getElementById("zoom-in");
  const zoomOutBtn = document.getElementById("zoom-out");
  const zoomResetBtn = document.getElementById("zoom-reset");

  const urlParams = new URLSearchParams(window.location.search);
  const isEditMode = urlParams.get("edit") === "1";

  let originalPois = [];
  let currentPois = [];
  let activePoiId = null;

  let zoomLevel = 1;
  let panX = 0;
  let panY = 0;

  const MIN_ZOOM = 1;
  const MAX_ZOOM = 100;
  const ZOOM_STEP = 1;

  if (isEditMode && editTools) {
    editTools.hidden = false;
  }

  try {
    const response = await fetch("./world-data.json");
    if (!response.ok) {
      throw new Error("world-data.json の読み込みに失敗しました");
    }

    const data = await response.json();
    originalPois = (data.pois || []).map((poi) => ({ ...poi }));
    currentPois = (data.pois || []).map((poi) => ({ ...poi }));

    renderPois();
    renderQuickLinks(data.quick_links || []);
    updateViewportTransform();
  } catch (error) {
    console.error("[world.js]", error);
    selectedName.textContent = "読み込み失敗";
    selectedText.textContent = "world-data.json の読込に失敗しました。ファイル配置やJSON構文を確認してください。";
    selectedLink.textContent = "遷移先なし";
    selectedLink.classList.add("disabled");
  }

  zoomInBtn?.addEventListener("click", () => {
    zoomLevel = clamp(zoomLevel + ZOOM_STEP, MIN_ZOOM, MAX_ZOOM);
    updateViewportTransform();
  });

  zoomOutBtn?.addEventListener("click", () => {
    zoomLevel = clamp(zoomLevel - ZOOM_STEP, MIN_ZOOM, MAX_ZOOM);
    if (zoomLevel === 1) {
      panX = 0;
      panY = 0;
    }
    updateViewportTransform();
  });

  zoomResetBtn?.addEventListener("click", () => {
    zoomLevel = 1;
    panX = 0;
    panY = 0;
    updateViewportTransform();
  });

  mapStage?.addEventListener("wheel", (event) => {
    event.preventDefault();

    const direction = event.deltaY > 0 ? -1 : 1;
    zoomLevel = clamp(zoomLevel + direction * ZOOM_STEP, MIN_ZOOM, MAX_ZOOM);

    if (zoomLevel === 1) {
      panX = 0;
      panY = 0;
    }

    updateViewportTransform();
  }, { passive: false });

  enableMapPan();

  exportBtn?.addEventListener("click", () => {
    const exportData = currentPois.map((poi) => ({
      id: poi.id,
      x: round2(poi.x),
      y: round2(poi.y)
    }));
    exportOutput.value = JSON.stringify(exportData, null, 2);
    exportOutput.focus();
    exportOutput.select();
  });

  resetBtn?.addEventListener("click", () => {
    currentPois = originalPois.map((poi) => ({ ...poi }));
    renderPois();
    exportOutput.value = "";
  });

  function renderPois() {
    poiLayer.innerHTML = "";

    currentPois.forEach((poi, index) => {
      const button = document.createElement("button");
      button.type = "button";
      button.className = "poi";
      if (isEditMode) {
        button.classList.add("editable");
      }

      button.style.left = `${poi.x}%`;
      button.style.top = `${poi.y}%`;
      button.dataset.id = poi.id;

      button.innerHTML = `
        <span class="poi-dot"></span>
        <span class="poi-label">${poi.label}</span>
      `;

      button.addEventListener("click", () => {
        setActivePoi(poi.id);
      });

      if (isEditMode) {
        enablePoiDrag(button, poi.id);
      }

      poiLayer.appendChild(button);

      if (activePoiId === poi.id) {
        button.classList.add("active");
      }

      if (!activePoiId && index === 0) {
        activePoiId = poi.id;
        button.classList.add("active");
        updateSelected(poi);
      }
    });

    if (activePoiId) {
      const activePoi = currentPois.find((poi) => poi.id === activePoiId);
      if (activePoi) {
        updateSelected(activePoi);
      }
    }
  }


==================================================
FILE: ./viewer/db/world/index.html
--------------------------------------------------
<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>世界国家・地域アーカイブ｜Meritocracia Viewer</title>
  <style>
    :root {
      --bg: #070b14;
      --panel: #0d1422;
      --panel-2: #0a1120;
      --line: rgba(212, 175, 55, 0.22);
      --line-strong: rgba(212, 175, 55, 0.42);
      --text: #e8dcc0;
      --muted: #a8966c;
      --accent: #d4af37;
      --hover: rgba(212, 175, 55, 0.08);
      --shadow: 0 12px 32px rgba(0, 0, 0, 0.35);
    }

    * {
      box-sizing: border-box;
    }

    body {
      margin: 0;
      font-family: "Noto Serif JP", "Yu Mincho", serif;
      background:
        radial-gradient(circle at top, rgba(212,175,55,0.08), transparent 28%),
        linear-gradient(180deg, #08101d 0%, #050911 100%);
      color: var(--text);
      min-height: 100vh;
    }

    .shell {
      max-width: 1320px;
      margin: 0 auto;
      padding: 56px 28px 80px;
    }

    .hero {
      border: 1px solid var(--line-strong);
      background: linear-gradient(180deg, rgba(14,20,34,0.94), rgba(8,13,24,0.94));
      border-radius: 24px;
      padding: 32px 32px 28px;
      box-shadow: var(--shadow);
      margin-bottom: 36px;
    }

    .eyebrow {
      color: var(--muted);
      letter-spacing: 0.24em;
      font-size: 0.78rem;
      text-transform: uppercase;
      margin-bottom: 14px;
    }

    h1 {
      margin: 0 0 14px;
      font-size: clamp(2rem, 3vw, 3rem);
      letter-spacing: 0.06em;
      font-weight: 600;
    }

    .lead {
      margin: 0;
      max-width: 920px;
      color: #cdbb93;
      line-height: 1.9;
      font-size: 0.98rem;
    }

    .section-title {
      margin: 0 0 14px;
      color: var(--accent);
      letter-spacing: 0.14em;
      font-size: 0.9rem;
      text-transform: uppercase;
    }

    .grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
      gap: 22px;
    }

    .card {
      display: block;
      text-decoration: none;
      color: inherit;
      background: linear-gradient(180deg, rgba(13,20,34,0.96), rgba(8,14,26,0.96));
      border: 1px solid var(--line);
      border-radius: 22px;
      padding: 24px 22px 22px;
      min-height: 220px;
      transition: transform 0.18s ease, border-color 0.18s ease, background 0.18s ease;
      box-shadow: var(--shadow);
    }

    .card:hover {
      transform: translateY(-4px);
      border-color: var(--line-strong);
      background: linear-gradient(180deg, rgba(18,27,45,0.98), rgba(11,18,31,0.98));
    }

    .card-tag {
      display: inline-block;
      color: var(--accent);
      font-size: 0.72rem;
      letter-spacing: 0.14em;
      margin-bottom: 14px;
      text-transform: uppercase;
    }

    .card h2 {
      margin: 0 0 10px;
      font-size: 1.35rem;
      line-height: 1.45;
      font-weight: 600;
    }

    .card p {
      margin: 0;
      color: #c7b792;
      line-height: 1.85;
      font-size: 0.95rem;
    }

    .subgrid {
      margin-top: 42px;
      display: grid;
      grid-template-columns: 1.2fr 0.8fr;
      gap: 22px;
    }

    .panel {
      background: linear-gradient(180deg, rgba(10,17,32,0.96), rgba(7,12,21,0.96));
      border: 1px solid var(--line);
      border-radius: 22px;
      padding: 24px;
      box-shadow: var(--shadow);
    }

    .list {
      margin: 0;
      padding-left: 1.2rem;
      color: #cdbb93;
      line-height: 1.95;
    }

    .small {
      color: #a8966c;
      font-size: 0.92rem;
      line-height: 1.8;
      margin-top: 10px;
    }

    .back {
      display: inline-flex;
      align-items: center;

==================================================
FILE: ./viewer/db/world/old-union/index.html
--------------------------------------------------
<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>旧サンギス＝レグナ連合王国｜外交アーカイブ</title>
  <style>
  body {
    margin: 0;
    font-family: "Noto Sans JP", sans-serif;
    background: #0f1115;
    color: #e8e8e8;
    line-height: 1.7;
  }

  .shell {
    max-width: 1200px;
    margin: 0 auto;
    padding: 48px 24px;
  }

  header {
    margin-bottom: 48px;
  }

  .sub {
    color: #9aa4b2;
    font-size: 0.9rem;
    letter-spacing: 0.08em;
  }

  h1 {
    margin: 8px 0;
    font-size: 2rem;
  }

  .lead {
    color: #b8c0cc;
    max-width: 900px;
  }

  .grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(260px, 1fr));
    gap: 24px;
    margin-top: 32px;
  }

  .card {
    background: #181c22;
    border: 1px solid #2b313a;
    border-radius: 16px;
    padding: 24px;
    box-shadow: 0 4px 18px rgba(0,0,0,0.25);
  }

  .badge {
    display: inline-block;
    font-size: 0.75rem;
    color: #d4af37;
    margin-bottom: 12px;
    letter-spacing: 0.08em;
  }

  h2 {
    margin-top: 0;
    font-size: 1.2rem;
  }

  .note {
    margin-top: 48px;
    padding: 20px;
    background: #16181d;
    border-left: 4px solid #d4af37;
    color: #c9d1d9;
  }

  .grid a.card {
    display: block;
    color: inherit;
    text-decoration: none;
  }

  .grid a.card:hover {
    color: inherit;
    text-decoration: none;
  }
</style>
</head>
<body>
  <main class="shell">
    <header>
      <p class="sub">WORLD ARCHIVE / OLD UNION</p>
      <h1>旧サンギス＝レグナ連合王国</h1>
      <p class="lead">
        メリトクラシア独立以前の旧連合4構成国。
        現在も外交・思想・宗教圧の基盤として影響を残している。
      </p>
    </header>

   <section class="grid">
  <div class="card">
    <span class="badge">LABOR / PRACTICAL</span>
    <h2>オペラティア公国</h2>
    <p>旧連合の実務・労働国家。後に独立し、メリトクラシア共和国へ改称。</p>
  </div>

  <a class="card" href="../../nation/sanginaria/">
    <span class="badge">BLOOD / LEGITIMACY</span>
    <h2>サンギナリア王国</h2>
    <p>血統と正統性を重視する旧連合の中枢国家。</p>
  </a>

  <div class="card">
    <span class="badge">MILITARY / ORDER</span>
    <h2>ヴォルガード公国</h2>
    <p>軍事・秩序・命令系統を担う強権国家。</p>
  </div>

  <div class="card">
    <span class="badge">RELIGION / JUDGEMENT</span>
    <h2>ユーディカ聖教国</h2>
    <p>宗教・法・裁き・天界思想を担う精神的支柱。</p>
  </div>
</section>

    <section class="note">
      ※ 構造理解モデルとして英国連合王国を参考にしているが、
      各構成国を現実国家へ厳密に1対1対応させる設計は採用していない。
      思想的役割を優先して整理している。
    </section>
  </main>
</body>
</html>
